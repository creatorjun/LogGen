GET

POST

PUT

  DELETE

GET

PUT

  DELETE

PUT

PUT

IPv4 인증 포탈 정책 설정 REST API 문서

https://{url}/api/po

urlurl: required (string)
장비 IP:Port

IPv4 인증 포탈 정책 설정

/auth-portal/4/policies

/auth-portal/4/policies/{pk}

인증 포탈 정책 설정 적용/취소

/command/auth-portal-4-policies/apply

/command/auth-portal-4-policies/cancel

IPv4 인증 포탈 정책 설정

GET

/auth-portal/4/policies

인증 포탈 정책 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증 포탈 정책 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: array of PortalGetPolicy

Items: PortalGetPolicy
Items

name
name: required (string - minLength: 1 - maxLength: 31)
정책 이름

descdesc: (string - maxLength: 128)
설명

seqseq: (integer - default: 0)
순번(추가 시 기본값 0으로 설정되며 최상단으로 추가됨)
이동	기능에서	사용되며	지정된	순번으로	이동합니다.

useuse: required (one of 0, 1)
사용 여부
사용	안함:	0,	사용:	1

action
action: required (one of 1, 2, 3)
인증 포털 설정
인증	프로파일:	1,	URL	리다이렉션:	2,	예외:	3
ap_type  속성이 디바이스(2) 일 경우는 URL 리다이렉션과 예외만 설정 가능

auth_prof_id: required (integer)
auth_prof_id
인증 프로파일 ID
action  속성이 1일 경우 설정

[인증 프로파일 목록 조회]
GET	{{url}}/api/sm/auth/profiles

redir_url: required (string - maxLength: 256)
redir_url
URL 리다이렉션 주소
action  속성이 2일 경우 설정

ap_type  속성이 디바이스(2) 일 경우 통합 클라이언트 다운로드 URL을 기본값으로 설정 가능
GET	{{url}}/api/po/auth-portal/client/download-url

ap_type: required (integer)
ap_type
타입 구분
사용자:	1,	디바이스:	2

SupportedVersion:	2.0.0

auth_portal_rule_id
auth_portal_rule_id: required (integer)
인증 포털 정책 ID

auth_prof_name
auth_prof_name: required (string)
인증 프로파일 이름

srcsrc: required (array of 주소 객체)
출발지 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID (ANY:	0)

ip_ver
ip_ver: required (integer)
IP 버전
IPv4:	1,	IPv6:	2

name
name: required (string)
객체 이름 (ANY:	Any)

zone
zone: required (integer)
Zone
(내부망:	1,	외부망:	2,	DMZ:	3)

addr_obj_type
addr_obj_type: required (integer)
주소 객체 타입 (ANY:	0)
호스트객체:	1,	네트워크	객체(넷마스크):	2,	네트워크	객체(범위):	3

excpt: required (integer)
excpt
예외 객체 여부
예외	객체	아님:	0,	예외	객체:	1

dstdst: required (array of 주소 객체)
목적지 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID (ANY:	0)

ip_ver
ip_ver: required (integer)
IP 버전
IPv4:	1,	IPv6:	2

name
name: required (string)
객체 이름 (ANY:	Any)

zone
zone: required (integer)
Zone
(내부망:	1,	외부망:	2,	DMZ:	3)

addr_obj_type
addr_obj_type: required (integer)
주소 객체 타입 (ANY:	0)
호스트객체:	1,	네트워크	객체(넷마스크):	2,	네트워크	객체(범위):	3

excpt: required (integer)
excpt
예외 객체 여부
예외	객체	아님:	0,	예외	객체:	1

srvsrv: required (array of 서비스 객체)
서비스 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 서비스 객체
Items

srv_obj_id
srv_obj_id: required (integer)
서비스 객체 ID

srv_obj_type
srv_obj_type: required (integer)
서비스 객체 종류
서비스	객체:	1,	서비스	그룹	객체:	2

name
name: required (string)
서비스 명

descdesc: required (string)
객체 설명

pre_def
pre_def: required (integer)
기본 정의 프로토콜 여부
1:기본	정의,	2:	사용자	정의

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"auth_portal_rule_id":	1,
				"fw_vers":	1,
				"use":	1,
				"seq":	1,
				"name":	"인증	포탈	이름",
				"desc":	"인증	포탈	설명",
				"ap_type":	1,
				"action":	2,
				"auth_prof_id":	null,
				"auth_prof_name":	null,
				"redir_url":	"http://example.com",
				"src":	[{
						"addr_obj_id":	1,
						"addr_obj_type":	1,
						"ip_ver":	1,
						"excpt":	0,
						"name":	"호스트객체	이름",
						"zone":	3,
						"desc":	"호스트객체	설명"
				}],
				"dst":	[{
						"addr_obj_id":	0,
						"addr_obj_type":	0,
						"ip_ver":	3,
						"excpt":	0,
						"name":	"Any",
						"zone":	0,
						"desc":	""
				}],
				"srv":	[{
						"srv_obj_id":	49,
						"srv_obj_type":	1,
						"name":	"HTTP",
						"desc":	"",
						"pre_def":	1
				}]
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/auth-portal/4/policies

인증 포탈 정책를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 31)
정책 이름

descdesc: (string - maxLength: 128)
설명

seqseq: (integer - default: 0)
순번(추가 시 기본값 0으로 설정되며 최상단으로 추가됨)
이동	기능에서	사용되며	지정된	순번으로	이동합니다.

useuse: required (one of 0, 1)
사용 여부
사용	안함:	0,	사용:	1

action
action: required (one of 1, 2, 3)
인증 포털 설정
인증	프로파일:	1,	URL	리다이렉션:	2,	예외:	3
ap_type  속성이 디바이스(2) 일 경우는 URL 리다이렉션과 예외만 설정 가능

auth_prof_id: required (integer)
auth_prof_id
인증 프로파일 ID
action  속성이 1일 경우 설정

[인증 프로파일 목록 조회]
GET	{{url}}/api/sm/auth/profiles

redir_url
redir_url: required (string - maxLength: 256)
URL 리다이렉션 주소
action  속성이 2일 경우 설정

ap_type  속성이 디바이스(2) 일 경우 통합 클라이언트 다운로드 URL을 기본값으로 설정 가능
GET	{{url}}/api/po/auth-portal/client/download-url

ap_type
ap_type: required (integer)
타입 구분
사용자:	1,	디바이스:	2

SupportedVersion:	2.0.0

srcsrc: required (array of SetAddrObject)
출발지 객체 ID 목록

Items: SetAddrObject
Items

idid: required (integer)
주소 객체 ID (ANY:	0)

excpt
excpt: required (one of 0, 1)
예외 객체 여부
(예외	객체	아님:	0,	예외:	1)

dstdst: required (array of SetAddrObject)
목적지 객체 ID 목록

Items: SetAddrObject
Items

idid: required (integer)
주소 객체 ID (ANY:	0)

excpt
excpt: required (one of 0, 1)
예외 객체 여부
(예외	객체	아님:	0,	예외:	1)

srvsrv: required (array of integer)

서비스 객체 ID 목록

Example:
Example

{
		"use":	1,
		"name":	"New	인증	포탈	이름",
		"desc":	"New	인증	포탈	설명",
		"src":	[
				{"id":	1,	"excpt":	0}
		],
		"dst":	[
				{"id":	0,	"excpt":	0}
		],
		"srv":	[49],
		"ap_type":	2,
		"action":	2,
		"auth_prof_id":	null,
		"redir_url":	"http://example.com"
}

Response

HTTP status code 200

인증 포탈 정책 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 31)
정책 이름

descdesc: (string - maxLength: 128)
설명

seqseq: (integer - default: 0)
순번(추가 시 기본값 0으로 설정되며 최상단으로 추가됨)
이동	기능에서	사용되며	지정된	순번으로	이동합니다.

useuse: required (one of 0, 1)
사용 여부
사용	안함:	0,	사용:	1

action
action: required (one of 1, 2, 3)
인증 포털 설정
인증	프로파일:	1,	URL	리다이렉션:	2,	예외:	3
ap_type  속성이 디바이스(2) 일 경우는 URL 리다이렉션과 예외만 설정 가능

auth_prof_id
auth_prof_id: required (integer)
인증 프로파일 ID
action  속성이 1일 경우 설정

[인증 프로파일 목록 조회]
GET	{{url}}/api/sm/auth/profiles

redir_url
redir_url: required (string - maxLength: 256)
URL 리다이렉션 주소
action  속성이 2일 경우 설정

ap_type  속성이 디바이스(2) 일 경우 통합 클라이언트 다운로드 URL을 기본값으로 설정 가능
GET	{{url}}/api/po/auth-portal/client/download-url

ap_type
ap_type: required (integer)
타입 구분
사용자:	1,	디바이스:	2

SupportedVersion:	2.0.0

auth_portal_rule_id
auth_portal_rule_id: required (integer)
인증 포털 정책 ID

auth_prof_name: required (string)
auth_prof_name
인증 프로파일 이름

srcsrc: required (array of 주소 객체)
출발지 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID (ANY:	0)

ip_ver
ip_ver: required (integer)
IP 버전
IPv4:	1,	IPv6:	2

name
name: required (string)
객체 이름 (ANY:	Any)

zone
zone: required (integer)
Zone
(내부망:	1,	외부망:	2,	DMZ:	3)

addr_obj_type
addr_obj_type: required (integer)
주소 객체 타입 (ANY:	0)
호스트객체:	1,	네트워크	객체(넷마스크):	2,	네트워크	객체(범위):	3

excpt: required (integer)
excpt

예외 객체 여부
예외	객체	아님:	0,	예외	객체:	1

dstdst: required (array of 주소 객체)
목적지 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID (ANY:	0)

ip_ver
ip_ver: required (integer)
IP 버전
IPv4:	1,	IPv6:	2

name
name: required (string)
객체 이름 (ANY:	Any)

zone
zone: required (integer)
Zone
(내부망:	1,	외부망:	2,	DMZ:	3)

addr_obj_type
addr_obj_type: required (integer)
주소 객체 타입 (ANY:	0)
호스트객체:	1,	네트워크	객체(넷마스크):	2,	네트워크	객체(범위):	3

excpt
excpt: required (integer)
예외 객체 여부
예외	객체	아님:	0,	예외	객체:	1

srvsrv: required (array of 서비스 객체)
서비스 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 서비스 객체
Items

srv_obj_id: required (integer)
srv_obj_id
서비스 객체 ID

srv_obj_type
srv_obj_type: required (integer)
서비스 객체 종류
서비스	객체:	1,	서비스	그룹	객체:	2

name
name: required (string)
서비스 명

descdesc: required (string)
객체 설명

pre_def
pre_def: required (integer)
기본 정의 프로토콜 여부
1:기본	정의,	2:	사용자	정의

Example:
Example

{
		"code":	"ok",
		"result":	{
				"auth_portal_rule_id":	2,
				"fw_vers":	1,
				"use":	1,
				"seq":	1,
				"name":	"New	인증	포탈	이름",
				"desc":	"New	인증	포탈	설명",
				"ap_type":	2,
				"action":	2,
				"auth_prof_id":	null,
				"auth_prof_name":	null,
				"redir_url":	"http://example.com",
				"src":	[{
						"addr_obj_id":	1,
						"addr_obj_type":	1,
						"ip_ver":	1,
						"excpt":	0,
						"name":	"호스트객체	이름",
						"zone":	3,
						"desc":	"호스트객체	설명"
				}],
				"dst":	[{
						"addr_obj_id":	0,
						"addr_obj_type":	0,
						"ip_ver":	3,
						"excpt":	0,
						"name":	"Any",
						"zone":	0,
						"desc":	""
				}],
				"srv":	[{
						"srv_obj_id":	49,
						"srv_obj_type":	1,
						"name":	"HTTP",
						"desc":	"",
						"pre_def":	1
				}]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/auth-portal/4/policies

인증 포탈 정책 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
수정될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"auth_portal_rule_id":	1,	"use":	0},
				{"auth_portal_rule_id":	2,	"use":	0}
		]
}

Response

HTTP status code 200

인증 포탈 정책 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/auth-portal/4/policies

인증 포탈 정책 목록을 (일괄)삭제한다.

Request

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of object)
items
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"auth_portal_rule_id":	1},
				{"auth_portal_rule_id":	2}
		]
}

Response

HTTP status code 200

인증 포탈 정책 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth-portal/4/policies/{pk}

인증 포탈 정책을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
인증 포탈 정책 ID (auth_portal_rule_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증 포탈 정책 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name: required (string - minLength: 1 - maxLength: 31)
name

정책 이름

descdesc: (string - maxLength: 128)
설명

seqseq: (integer - default: 0)
순번(추가 시 기본값 0으로 설정되며 최상단으로 추가됨)
이동	기능에서	사용되며	지정된	순번으로	이동합니다.

useuse: required (one of 0, 1)
사용 여부
사용	안함:	0,	사용:	1

action
action: required (one of 1, 2, 3)
인증 포털 설정
인증	프로파일:	1,	URL	리다이렉션:	2,	예외:	3
ap_type  속성이 디바이스(2) 일 경우는 URL 리다이렉션과 예외만 설정 가능

auth_prof_id: required (integer)
auth_prof_id
인증 프로파일 ID
action  속성이 1일 경우 설정

[인증 프로파일 목록 조회]
GET	{{url}}/api/sm/auth/profiles

redir_url
redir_url: required (string - maxLength: 256)
URL 리다이렉션 주소
action  속성이 2일 경우 설정

ap_type  속성이 디바이스(2) 일 경우 통합 클라이언트 다운로드 URL을 기본값으로 설정 가능
GET	{{url}}/api/po/auth-portal/client/download-url

ap_type
ap_type: required (integer)
타입 구분
사용자:	1,	디바이스:	2

SupportedVersion:	2.0.0

auth_portal_rule_id
auth_portal_rule_id: required (integer)
인증 포털 정책 ID

auth_prof_name
auth_prof_name: required (string)
인증 프로파일 이름

srcsrc: required (array of 주소 객체)
출발지 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 주소 객체
Items

addr_obj_id: required (integer)
addr_obj_id
객체 ID (ANY:	0)

ip_ver
ip_ver: required (integer)
IP 버전
IPv4:	1,	IPv6:	2

name
name: required (string)
객체 이름 (ANY:	Any)

zone
zone: required (integer)
Zone
(내부망:	1,	외부망:	2,	DMZ:	3)

addr_obj_type
addr_obj_type: required (integer)
주소 객체 타입 (ANY:	0)
호스트객체:	1,	네트워크	객체(넷마스크):	2,	네트워크	객체(범위):	3

excpt
excpt: required (integer)
예외 객체 여부
예외	객체	아님:	0,	예외	객체:	1

dstdst: required (array of 주소 객체)
목적지 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 주소 객체
Items

addr_obj_id: required (integer)
addr_obj_id
객체 ID (ANY:	0)

ip_ver
ip_ver: required (integer)
IP 버전
IPv4:	1,	IPv6:	2

name: required (string)
name
객체 이름 (ANY:	Any)

zone
zone: required (integer)
Zone
(내부망:	1,	외부망:	2,	DMZ:	3)

addr_obj_type
addr_obj_type: required (integer)
주소 객체 타입 (ANY:	0)
호스트객체:	1,	네트워크	객체(넷마스크):	2,	네트워크	객체(범위):	3

excpt
excpt: required (integer)
예외 객체 여부
예외	객체	아님:	0,	예외	객체:	1

srvsrv: required (array of 서비스 객체)
서비스 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 서비스 객체
Items

srv_obj_id: required (integer)
srv_obj_id
서비스 객체 ID

srv_obj_type
srv_obj_type: required (integer)
서비스 객체 종류
서비스	객체:	1,	서비스	그룹	객체:	2

name: required (string)
name

서비스 명

descdesc: required (string)
객체 설명

pre_def: required (integer)
pre_def
기본 정의 프로토콜 여부
1:기본	정의,	2:	사용자	정의

Example:
Example

{
		"code":	"ok",
		"result":	{
				"auth_portal_rule_id":	2,
				"fw_vers":	1,
				"use":	1,
				"seq":	1,
				"name":	"인증	포탈	이름",
				"desc":	"인증	포탈	설명",
				"ap_type":	2,
				"action":	2,
				"auth_prof_id":	null,
				"auth_prof_name":	null,
				"redir_url":	"http://example.com",
				"src":	[{
						"addr_obj_id":	1,
						"addr_obj_type":	1,
						"ip_ver":	1,
						"excpt":	0,
						"name":	"호스트객체	이름",
						"zone":	3,
						"desc":	"호스트객체	설명"
				}],
				"dst":	[{
						"addr_obj_id":	0,
						"addr_obj_type":	0,
						"ip_ver":	3,
						"excpt":	0,
						"name":	"Any",
						"zone":	0,
						"desc":	""
				}],
				"srv":	[{
						"srv_obj_id":	49,
						"srv_obj_type":	1,
						"name":	"HTTP",
						"desc":	"",
						"pre_def":	1
				}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/auth-portal/4/policies/{pk}

인증 포탈 정책을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
인증 포탈 정책 ID (auth_portal_rule_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 31)
정책 이름

descdesc: (string - maxLength: 128)
설명

seqseq: (integer - default: 0)
순번(추가 시 기본값 0으로 설정되며 최상단으로 추가됨)
이동	기능에서	사용되며	지정된	순번으로	이동합니다.

useuse: required (one of 0, 1)
사용 여부
사용	안함:	0,	사용:	1

action
action: required (one of 1, 2, 3)
인증 포털 설정
인증	프로파일:	1,	URL	리다이렉션:	2,	예외:	3
ap_type  속성이 디바이스(2) 일 경우는 URL 리다이렉션과 예외만 설정 가능

auth_prof_id
auth_prof_id: required (integer)
인증 프로파일 ID
action  속성이 1일 경우 설정

[인증 프로파일 목록 조회]
GET	{{url}}/api/sm/auth/profiles

redir_url
redir_url: required (string - maxLength: 256)
URL 리다이렉션 주소
action  속성이 2일 경우 설정

ap_type  속성이 디바이스(2) 일 경우 통합 클라이언트 다운로드 URL을 기본값으로 설정 가능
GET	{{url}}/api/po/auth-portal/client/download-url

ap_type
ap_type: required (integer)
타입 구분
사용자:	1,	디바이스:	2

SupportedVersion:	2.0.0

srcsrc: required (array of SetAddrObject)
출발지 객체 ID 목록

Items: SetAddrObject
Items

idid: required (integer)
주소 객체 ID (ANY:	0)

excpt
excpt: required (one of 0, 1)
예외 객체 여부
(예외	객체	아님:	0,	예외:	1)

dstdst: required (array of SetAddrObject)
목적지 객체 ID 목록

Items: SetAddrObject
Items

idid: required (integer)
주소 객체 ID (ANY:	0)

excpt
excpt: required (one of 0, 1)
예외 객체 여부
(예외	객체	아님:	0,	예외:	1)

srvsrv: required (array of integer)
서비스 객체 ID 목록

Example:
Example

{
		"use":	1,
		"name":	"인증	포탈	이름",
		"desc":	"인증	포탈	설명",
		"src":	[
				{"id":	1,	"excpt":	0}
		],
		"dst":	[
				{"id":	0,	"excpt":	0}
		],
		"srv":	[49],
		"ap_type":	2,
		"action":	2,
		"auth_prof_id":	null,
		"redir_url":	"http://example.com"
}

//	이동
{
		"seq":	2
}

Response

HTTP status code 200

인증 포탈 정책 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 31)
정책 이름

descdesc: (string - maxLength: 128)
설명

seqseq: (integer - default: 0)
순번(추가 시 기본값 0으로 설정되며 최상단으로 추가됨)
이동	기능에서	사용되며	지정된	순번으로	이동합니다.

useuse: required (one of 0, 1)
사용 여부
사용	안함:	0,	사용:	1

action
action: required (one of 1, 2, 3)
인증 포털 설정
인증	프로파일:	1,	URL	리다이렉션:	2,	예외:	3
ap_type  속성이 디바이스(2) 일 경우는 URL 리다이렉션과 예외만 설정 가능

auth_prof_id
auth_prof_id: required (integer)
인증 프로파일 ID
action  속성이 1일 경우 설정

[인증 프로파일 목록 조회]
GET	{{url}}/api/sm/auth/profiles

redir_url: required (string - maxLength: 256)
redir_url

URL 리다이렉션 주소
action  속성이 2일 경우 설정

ap_type  속성이 디바이스(2) 일 경우 통합 클라이언트 다운로드 URL을 기본값으로 설정 가능
GET	{{url}}/api/po/auth-portal/client/download-url

ap_type
ap_type: required (integer)
타입 구분
사용자:	1,	디바이스:	2

SupportedVersion:	2.0.0

auth_portal_rule_id
auth_portal_rule_id: required (integer)
인증 포털 정책 ID

auth_prof_name
auth_prof_name: required (string)
인증 프로파일 이름

srcsrc: required (array of 주소 객체)
출발지 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 주소 객체
Items

addr_obj_id: required (integer)
addr_obj_id
객체 ID (ANY:	0)

ip_ver
ip_ver: required (integer)
IP 버전
IPv4:	1,	IPv6:	2

name: required (string)
name
객체 이름 (ANY:	Any)

zone
zone: required (integer)
Zone
(내부망:	1,	외부망:	2,	DMZ:	3)

addr_obj_type
addr_obj_type: required (integer)
주소 객체 타입 (ANY:	0)
호스트객체:	1,	네트워크	객체(넷마스크):	2,	네트워크	객체(범위):	3

excpt
excpt: required (integer)
예외 객체 여부
예외	객체	아님:	0,	예외	객체:	1

dstdst: required (array of 주소 객체)
목적지 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID (ANY:	0)

ip_ver
ip_ver: required (integer)
IP 버전
IPv4:	1,	IPv6:	2

name: required (string)
name
객체 이름 (ANY:	Any)

zone
zone: required (integer)
Zone
(내부망:	1,	외부망:	2,	DMZ:	3)

addr_obj_type: required (integer)
addr_obj_type
주소 객체 타입 (ANY:	0)
호스트객체:	1,	네트워크	객체(넷마스크):	2,	네트워크	객체(범위):	3

excpt
excpt: required (integer)
예외 객체 여부
예외	객체	아님:	0,	예외	객체:	1

srvsrv: required (array of 서비스 객체)
서비스 객체 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: 서비스 객체
Items

srv_obj_id
srv_obj_id: required (integer)
서비스 객체 ID

srv_obj_type
srv_obj_type: required (integer)
서비스 객체 종류
서비스	객체:	1,	서비스	그룹	객체:	2

name
name: required (string)
서비스 명

descdesc: required (string)
객체 설명

pre_def
pre_def: required (integer)
기본 정의 프로토콜 여부
1:기본	정의,	2:	사용자	정의

Example:
Example

{
		"code":	"ok",
		"result":	{
				"auth_portal_rule_id":	2,
				"fw_vers":	1,
				"use":	1,
				"seq":	1,
				"name":	"인증	포탈	이름",
				"desc":	"인증	포탈	설명",
				"ap_type":	2,
				"action":	2,
				"auth_prof_id":	null,
				"auth_prof_name":	null,
				"redir_url":	"http://example.com",
				"src":	[{
						"addr_obj_id":	1,
						"addr_obj_type":	1,
						"ip_ver":	1,
						"excpt":	0,
						"name":	"호스트객체	이름",
						"zone":	3,
						"desc":	"호스트객체	설명"
				}],
				"dst":	[{
						"addr_obj_id":	0,
						"addr_obj_type":	0,
						"ip_ver":	3,
						"excpt":	0,
						"name":	"Any",
						"zone":	0,
						"desc":	""
				}],
				"srv":	[{
						"srv_obj_id":	49,
						"srv_obj_type":	1,
						"name":	"HTTP",
						"desc":	"",
						"pre_def":	1
				}]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/auth-portal/4/policies/{pk}

인증 포탈 정책을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
인증 포탈 정책 ID (auth_portal_rule_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증 포탈 정책 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

인증 포탈 정책 설정 적용/취소

PUT

/command/auth-portal-4-policies/apply

인증 포탈 정책 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

인증 포탈 정책 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/auth-portal-4-policies/cancel

인증 포탈 정책 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

인증 포탈 정책 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

