GET

POST

PUT

  DELETE

GET

PUT

  DELETE

POST

PUT

PUT

보호 도메인 정책 REST API 문서

https://{url}/api/po

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/po

보호 도메인 정책

/protect-domains

/protect-domains/{pk}

/protect-domains/check

보호 도메인 정책 적용/취소

/command/protect-domains/apply

/command/protect-domains/cancel

보호 도메인 정책

GET

/protect-domains

보호 도메인 정책 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

보호 도메인 정책 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ResProtectDomain)
결과 데이터

Items: ResProtectDomain
Items

ddos_prf_id
ddos_prf_id: required (integer)
DDoS 프로파일 아이디

name: required (string)
name
이름

useuse: required (one of 0, 1 - default: 0)
사용여부
사용안함:	0,	사용:	1

action
action: required (one of 0, 1 - default: 0)
조치
탐지:	0,	차단:	1

prtc_host_ip4
prtc_host_ip4: required (array of AddrObject)
Ipv4 보호 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

prtc_host_ip6
prtc_host_ip6: required (array of AddrObject)
Ipv6 보호 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

expt_host_ip4: required (array of AddrObject)
expt_host_ip4
Ipv4 예외 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name: required (string)
name
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type: required (one of 1, 2, 3, 4)
addr_obj_type
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

expt_host_ip6
expt_host_ip6: required (array of AddrObject)
Ipv6 예외 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

ddos_prf_name
ddos_prf_name: required (string)
DDos 프로파일 이름

descdesc: required (string)
설명

ddos_dmn_id
ddos_dmn_id: required (integer)
DDoS 도메인 아이디

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"ddos_dmn_id":	1,
				"ddos_prf_id":	1,
				"name":	"보호	도메인	1",
				"use":	0,
				"action":	0,
				"prtc_host_ip4":	[{
						"addr_obj_id":	1,
						"name":	"host_v4_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"prtc_host_ip6":	[{
						"addr_obj_id":	1,
						"name":	"host_v6_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"expt_host_ip4":	[{
						"addr_obj_id":	1,
						"name":	"expt_host_v4_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"expt_host_ip6":	[{
						"addr_obj_id":	1,
						"name":	"expt_host_v6_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"ddos_prf_name":	"DDoS	프로파일	1",
				"desc":	"설명	1"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/protect-domains

보호 도메인 정책를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

ddos_prf_id
ddos_prf_id: required (integer)
DDoS 프로파일 아이디

name
name: required (string)
이름

useuse: required (one of 0, 1 - default: 0)
사용여부
사용안함:	0,	사용:	1

action
action: required (one of 0, 1 - default: 0)
조치
탐지:	0,	차단:	1

descdesc: required (string)
설명

addr_obj
addr_obj: required (array of items)
Items: items
Items

type
type: required (one of 0, 1)
유형
보호:	0,	예외:	1

addr_obj_id_list
addr_obj_id_list: required (array of items)
객체 ID 목록
네트워크	객체의	ANY와	그	외	객체가	같이	요청	될	수	없음

Items: items
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID

Example:
Example

{
		"ddos_prf_id":	1,
		"name":	"보호	도메인	1",
		"use":	0,
		"action":	0,
		"desc":	"설명	1",
		"addr_obj":	[{
				"type":	0,
				"addr_obj_id_list":	[{
						"addr_obj_id":	1
				}]
		}]
}

Response

HTTP status code 200

보호 도메인 정책 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

ddos_prf_id
ddos_prf_id: required (integer)
DDoS 프로파일 아이디

name
name: required (string)
이름

useuse: required (one of 0, 1 - default: 0)
사용여부
사용안함:	0,	사용:	1

action
action: required (one of 0, 1 - default: 0)
조치
탐지:	0,	차단:	1

prtc_host_ip4: required (array of AddrObject)
prtc_host_ip4
Ipv4 보호 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type: required (one of 1, 2, 3, 4)
addr_obj_type
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

prtc_host_ip6
prtc_host_ip6: required (array of AddrObject)
Ipv6 보호 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name: required (string)
name
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

expt_host_ip4
expt_host_ip4: required (array of AddrObject)
Ipv4 예외 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

expt_host_ip6
expt_host_ip6: required (array of AddrObject)
Ipv6 예외 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone: required (one of 1, 2, 3)
zone
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

ddos_prf_name
ddos_prf_name: required (string)
DDos 프로파일 이름

descdesc: required (string)
설명

ddos_dmn_id
ddos_dmn_id: required (integer)
DDoS 도메인 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ddos_dmn_id":	1,
				"ddos_prf_id":	1,
				"name":	"보호	도메인	1",
				"use":	0,
				"action":	0,
				"prtc_host_ip4":	[{
						"addr_obj_id":	1,
						"name":	"host_v4_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"prtc_host_ip6":	[{
						"addr_obj_id":	1,
						"name":	"host_v6_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"expt_host_ip4":	[{
						"addr_obj_id":	1,
						"name":	"expt_host_v4_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"expt_host_ip6":	[{
						"addr_obj_id":	1,
						"name":	"expt_host_v6_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"ddos_prf_name":	"DDoS	프로파일	1",
				"desc":	"설명	1"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/protect-domains

보호 도메인 정책 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of ReqProtectDomainList)
수정 아이템 목록

Items: ReqProtectDomainList
Items

ddos_prf_id
ddos_prf_id: required (integer)
DDoS 프로파일 아이디

name
name: required (string)
이름

useuse: required (one of 0, 1 - default: 0)
사용여부
사용안함:	0,	사용:	1

action
action: required (one of 0, 1 - default: 0)
조치
탐지:	0,	차단:	1

descdesc: required (string)
설명

addr_obj
addr_obj: required (array of items)
Items: items
Items

type
type: required (one of 0, 1)
유형
보호:	0,	예외:	1

addr_obj_id_list
addr_obj_id_list: required (array of items)
객체 ID 목록
네트워크	객체의	ANY와	그	외	객체가	같이	요청	될	수	없음

Items: items
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID

ddos_dmn_id: required (integer)
ddos_dmn_id
DDoS 도메인 아이디

Example:
Example

{
		"items":	[{
				"ddos_dmn_id":	1,
				"ddos_prf_id":	1,
				"name":	"보호	도메인	2",
				"use":	1,
				"action":	1,
				"desc":	"설명	2",
				"addr_obj":	[{
				"type":	1,
						"addr_obj_id_list":	[{
						"addr_obj_id":	2
						}]
				}]
		}]
}

Response

HTTP status code 200

보호 도메인 정책 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors: required (array of object)
errors
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/protect-domains

보호 도메인 정책 목록을 (일괄)삭제한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제 아이템 목록

Example:
Example

{
		"items":	[
				{"ddos_dmn_id":	1},
				{"ddos_dmn_id":	2}
		]
}

Response

HTTP status code 200

보호 도메인 정책 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
삭제 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message

결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/protect-domains/{pk}

보호 도메인 정책을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
DDoS 도메인 아이디 ( ddos_dmn_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

보호 도메인 정책 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

ddos_prf_id
ddos_prf_id: required (integer)
DDoS 프로파일 아이디

name: required (string)
name

이름

useuse: required (one of 0, 1 - default: 0)
사용여부
사용안함:	0,	사용:	1

action
action: required (one of 0, 1 - default: 0)
조치
탐지:	0,	차단:	1

prtc_host_ip4
prtc_host_ip4: required (array of AddrObject)
Ipv4 보호 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name: required (string)
name
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

prtc_host_ip6
prtc_host_ip6: required (array of AddrObject)
Ipv6 보호 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

expt_host_ip4
expt_host_ip4: required (array of AddrObject)
Ipv4 예외 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone: required (one of 1, 2, 3)
zone
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

expt_host_ip6
expt_host_ip6: required (array of AddrObject)
Ipv6 예외 대상

Items: AddrObject
Items

addr_obj_id: required (integer)
addr_obj_id
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

ddos_prf_name: required (string)
ddos_prf_name
DDos 프로파일 이름

descdesc: required (string)
설명

ddos_dmn_id: required (integer)
ddos_dmn_id
DDoS 도메인 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ddos_dmn_id":	1,
				"ddos_prf_id":	1,
				"name":	"보호	도메인	1",
				"use":	0,
				"action":	0,
				"prtc_host_ip4":	[{
						"addr_obj_id":	1,
						"name":	"host_v4_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"prtc_host_ip6":	[{
						"addr_obj_id":	1,
						"name":	"host_v6_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"expt_host_ip4":	[{
						"addr_obj_id":	1,
						"name":	"expt_host_v4_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"expt_host_ip6":	[{
						"addr_obj_id":	1,
						"name":	"expt_host_v6_1",
						"zone":	1,
						"addr_obj_type":	1
				}],
				"ddos_prf_name":	"DDoS	프로파일	1",
				"desc":	"설명	1"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/protect-domains/{pk}

보호 도메인 정책을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
DDoS 도메인 아이디 ( ddos_dmn_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

ddos_prf_id
ddos_prf_id: required (integer)
DDoS 프로파일 아이디

name
name: required (string)
이름

useuse: required (one of 0, 1 - default: 0)
사용여부
사용안함:	0,	사용:	1

action
action: required (one of 0, 1 - default: 0)
조치
탐지:	0,	차단:	1

descdesc: required (string)
설명

addr_obj
addr_obj: required (array of items)
Items: items
Items

type
type: required (one of 0, 1)
유형
보호:	0,	예외:	1

addr_obj_id_list
addr_obj_id_list: required (array of items)
객체 ID 목록
네트워크	객체의	ANY와	그	외	객체가	같이	요청	될	수	없음

Items: items
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID

Example:
Example

{
		"ddos_prf_id":	1,
		"name":	"보호	도메인	2",
		"use":	1,
		"action":	1,
		"desc":	"설명	2",
		"addr_obj":	[{
				"type":	1,
				"addr_obj_id_list":	[{
						"addr_obj_id":	2
				}]
		}]
}

Response

HTTP status code 200

보호 도메인 정책 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

ddos_prf_id
ddos_prf_id: required (integer)
DDoS 프로파일 아이디

name
name: required (string)
이름

useuse: required (one of 0, 1 - default: 0)
사용여부
사용안함:	0,	사용:	1

action
action: required (one of 0, 1 - default: 0)
조치
탐지:	0,	차단:	1

prtc_host_ip4
prtc_host_ip4: required (array of AddrObject)
Ipv4 보호 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone: required (one of 1, 2, 3)
zone
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

prtc_host_ip6
prtc_host_ip6: required (array of AddrObject)
Ipv6 보호 대상

Items: AddrObject
Items

addr_obj_id: required (integer)
addr_obj_id
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

expt_host_ip4
expt_host_ip4: required (array of AddrObject)
Ipv4 예외 대상

Items: AddrObject
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

expt_host_ip6: required (array of AddrObject)
expt_host_ip6

Ipv6 예외 대상

Items: AddrObject
Items

addr_obj_id: required (integer)
addr_obj_id
주소 객체 아이디

name
name: required (string)
주소 객체 이름

zone
zone: required (one of 1, 2, 3)
Zone
내부:	1,	외부:	2,	DMZ:	3

addr_obj_type
addr_obj_type: required (one of 1, 2, 3, 4)
주소 객체 유형
호스트객체:	1,	넷마스크:	2,	범위:	3,	그룹객체:	4

ddos_prf_name: required (string)
ddos_prf_name
DDos 프로파일 이름

descdesc: required (string)
설명

ddos_dmn_id
ddos_dmn_id: required (integer)
DDoS 도메인 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ddos_dmn_id":	1,
				"ddos_prf_id":	1,
				"name":	"보호	도메인	2",
				"use":	1,
				"action":	1,
				"prtc_host_ip4":	[{
						"addr_obj_id":	2,
						"name":	"host_v4_2",
						"zone":	2,
						"addr_obj_type":	2
				}],
				"prtc_host_ip6":	[{
						"addr_obj_id":	2,
						"name":	"host_v6_2",
						"zone":	2,
						"addr_obj_type":	2
				}],
				"expt_host_ip4":	[{
						"addr_obj_id":	2,
						"name":	"expt_host_v4_2",
						"zone":	2,
						"addr_obj_type":	2
				}],
				"expt_host_ip6":	[{
						"addr_obj_id":	2,
						"name":	"expt_host_v6_2",
						"zone":	2,
						"addr_obj_type":	2
				}],
				"ddos_prf_name":	"DDoS	프로파일	2",
				"desc":	"설명	2"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code

결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/protect-domains/{pk}

보호 도메인 정책을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
DDoS 도메인 아이디 ( ddos_dmn_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

보호 도메인 정책 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

POST

/protect-domains/check

보호 도메인 중복 체크한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

ddos_dmn_id: required (integer)
ddos_dmn_id
DDoS 도메인 아이디

type
type: required (one of 1, 2, 3, 4)
도메인 유형
보호:	0,	예외:	1

addr_obj_id_list: required (array of items)
addr_obj_id_list
Items: items
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 아이디

Example:
Example

{
		"ddos_dmn_id":	1,
		"type":	0,
		"addr_obj_id_list":	[{
				"addr_obj_id":	1
		}]
}

Response

HTTP status code 200

보호 도메인 중복 체크 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

보호 도메인 중복 체크 요청 실패

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

보호 도메인 정책 적용/취소

PUT

/command/protect-domains/apply

보호 도메인 정책을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

보호 도메인 정책 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/protect-domains/cancel

보호 도메인 정책을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

보호 도메인 정책 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

