POST

GET

GET

DELETE

DELETE

POST

POST

POST

POST

POST

객체 자동 생성 마법사 REST API 문서

https://{host}/api

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

트래픽 분석

/lr/summary/start

/lr/summary/{request_id}/status

/lr/summary/{request_id}/tasks/{task_id}/{summary_type}

/lr/summary/{request_id}/stop

/lr/summary/{request_id}/end

서비스 객체 자동 생성

/po/auto/create/service/objects

주소 객체 자동 생성

/po/auto/create/address/objects

서비스 객체 PC로 저장

/po/auto/create/service/objects/save

주소 객체 PC로 저장

/po/auto/create/address/objects/save

동일 서브넷 주소 통합

/po/auto/create/address/merge/objects

트래픽 분석

POST

/lr/summary/start

트래픽 분석 요청을 시작한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language

언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
tasks
tasks: required (array of Task)
Items: Task
Items

summary_type: required (string)
summary_type
분석 타입
src_ip_top:	주소	TOP
service_top:	서비스	TOP

filter
filter: (array of Filter)
Items: Filter
Items

keykey: required (string)
필터 키

value
value: required (array of string)
필터 값 목록

is_not
is_not: required (boolean)
NOT 여부

value_type
value_type: (string)
필터 값의 종류

stime: required (string)
stime
검색 시작 시간

Example:
Example

2018-07-30	10:00:00

etime
etime: required (string)
검색 종료 시간

Example:
Example

2020-07-30	10:00:00

toptop: (integer)
TOP 건 수

Example:
Example

{
		"tasks":	[
				{
						"summary_type":	"service_top",
						"filter":	[]
				},
				{
						"summary_type":	"src_ip_top",
						"filter":	[
								{
										"key":	"in_zone",
										"value":	[
												"Internal"
										],
										"is_not":	false
								}
						]
				},
				{
						"summary_type":	"src_ip_top",
						"filter":	[
								{
										"key":	"in_zone",
										"value":	[
												"External"
										],
										"is_not":	false
								}
						]
				},
				{
						"summary_type":	"src_ip_top",
						"filter":	[
								{
										"key":	"in_zone",
										"value":	[
												"Dmz"
										],
										"is_not":	false
								}
						]
				}
		],
		"stime":	"2019-01-24	17:04:40",
		"etime":	"2021-07-30	16:00:01",
		"top":	10,
		"filters":	[]
}

Response

HTTP status code 200

트래픽 분석 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

request_id
request_id: required (string)
요청 ID
트래픽 검색 라이프사이클 전반에 사용됩니다.

Example:
Example

{
		"code":	"ok",
		"result":	{
						"request_id":	"00012170-000007"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

응답 결과 중 status 속성 값이  DONE 일 때까지 반복 요청(폴링)을 해야 합니다.

GET

/lr/summary/{request_id}/status

분석 진행 상태를 조회한다.

Request

URI Parameters

request_id
request_id: required (string)
조회 시작 시 응답에 포함된  요청	ID

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

트래픽 분석 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

status: required (string)
status
현재 상태
INIT:	초기	단계
IN_PROGRESS:	검색	중
DONE:	검색	완료

progress
progress: required (integer)
검색 진행률(0 ~ 100)

done_tasks
done_tasks: required (integer)
완료된 태스크 ID

total_task_count
total_task_count: required (integer)
총 실행 필요한 태스크 개수

Example:
Example

{
		"code":	"ok",
		"result":	{
				"status":	"DONE",
				"progress":	100,
				"done_tasks":	4,
				"total_task_count":	4
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/lr/summary/{request_id}/tasks/{task_id}/{summary_type}

태스크 별 분석된 정보를 조회한다.

Request

URI Parameters

request_id
request_id: required (string)
조회 시작 시 응답에 포함된  요청	ID

task_id: required (string)
task_id
태스크가 분석 요청이 되면 각 태스크는 1부턴 순차적인 시퀀스를 할당 받는데, 태스크의 해당 시퀀스를 의미

summary_type
summary_type: required (string)
분석 타입
src_ip_top:	주소	TOP
service_top:	서비스	TOP

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

트래픽 분석 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

task_id: required (integer)
task_id
태스크 ID

result
result: required (array of ServiceOrHost)
서비스 또는 주소

Items: ServiceOrHost
Items

useuse: required (integer)
사용 여부(GUI에서 사용, 1로 고정)
0:	사용	안함,	1:	사용

fwrule_id: required (integer)
fwrule_id
(서비스 정보) 정책 ID

fwrule_name
fwrule_name: required (string)
(서비스 정보) 정책 이름

prtcprtc: required (string)
(서비스 정보) 프로토콜 명

Example:
Example

TCP,	UDP,	ICMP

name
name: required (string)
(서비스 정보) 서비스 이름

srv_port
srv_port: required (integer)
(서비스 정보) 서비스 포트

direction
direction: required (string)
(서비스 정보) 방향

srv_cate: required (string)
srv_cate
(서비스 정보) 서비스 유형 이름

srv_cate_id
srv_cate_id: required (integer)
(서비스 정보) 서비스 유형 ID

life_time
life_time: required (integer)
(서비스 정보) Lifetime

ip_list
ip_list: required (string)
(주소) IP 주소

subnet_id
subnet_id: required (integer)
(주소) 동일 서브넷(C 클래스)를 의미하는 ID

subnet_count: required (integer)
subnet_count
(주소) 동일 서브넷(C 클래스)의 갯수

count
count: required (integer)
(서비스 정보, 주소) 세션 수

Example:
Example

//	서비스
{
		"code":	"ok",
		"result":	{
				"task_id":	1,
				"result":	[
						{
								"use":	1,
								"name":	"HTTPS",
								"fwrule_id":	7619,
								"fwrule_name":	"방화벽	정책	이름",
								"prtc":	"TCP",
								"srv_port":	443,
								"direction":	"Outbound",
								"srv_cate":	"HTTPS",
								"srv_cate_id":	7,
								"life_time":	1800,
								"count":	100
						}
				]
		}
}

//	주소
{
		"code":	"ok",
		"result":	{
				"task_id":	2,
				"result":	[
						{
								"use":	1,
								"ip_list":	"192.168.10.11",
								"subnet_id":	1,
								"subnet_count":	2,
								"count":	1000
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/lr/summary/{request_id}/stop

진행 중인 분석을 중지한다.

Request

URI Parameters

request_id: required (string)
request_id
조회 시작 시 응답에 포함된  요청	ID

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

검색 중지 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/lr/summary/{request_id}/end

진행 중인 분석을 종료한다.

Request

URI Parameters

request_id
request_id: required (string)
조회 시작 시 응답에 포함된  요청	ID

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

검색 종료

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

서비스 객체 자동 생성

POST

/po/auto/create/service/objects

서비스 객체를 자동생성 한다.

Request

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of 서비스)
items
Items: 서비스
Items

prtcprtc: required (string)
프로토콜 명

Example:
Example

TCP,	UDP,	ICMP

srv_port
srv_port: required (integer)
서비스 포트
값이	없을	경우	null

name: required (string)
name
서비스 이름
값이	없을	경우	null

srv_cate
srv_cate: required (string)
서비스 유형

Example:
Example

HTTP

life_time
life_time: required (integer - minimum: 1 - maximum: 604800)
Lifetime

Example:
Example

{
		"items":	[
				{
						"name":	"ssdp",
						"prtc":	"UDP",
						"srv_port":	1900,
						"srv_cate":	"NONE",
						"life_time":	20
				}
		]
}

Response

HTTP status code 200

서비스 객체 자동 생성 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (array of 결과)
result
결과 데이터

Items: 결과
Items

rowrow: required (integer)
추가 요청 인덱스(1부터 시작)

code
code: required (string)
추가 후 결과 코드

message
message: required (string)
추가 후 결과 메시지

itemitem: required (array of items)
Items: items
Items

idid: required (integer)
객체 ID

name
name: required (string)
객체 명

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"row":	1,
						"code":	"ok",
						"message":	"Success",
						"item:	{
								"id":	1,
								"name":	"ssdp_UDP_1900"
						}
				}
		]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

주소 객체 자동 생성

POST

/po/auto/create/address/objects

주소 객체 자동 생성를 추가한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of 주소)
Items: 주소
Items

ip_list
ip_list: required (string)
IP 또는 IP/Mask 형식의 문자열
Mask는 1 ~ 31 범위값

zone
zone: required (integer)
Zone
1:	내부망,	2:	외부망,	3:	DMZ

Example:
Example

{
		"items":	[
				{
						"ip_list":	"10.10.10.10",
						"zone":	1
				}
		]
}

Response

HTTP status code 200

주소 객체 자동 생성 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

result
result: required (array of 결과)
결과 데이터

Items: 결과
Items

rowrow: required (integer)
추가 요청 인덱스(1부터 시작)

code: required (string)
code
추가 후 결과 코드

message
message: required (string)
추가 후 결과 메시지

itemitem: required (array of items)
Items: items
Items

idid: required (integer)
객체 ID

name
name: required (string)
객체 명

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"row":	1,
						"code":	"ok",
						"message":	"Success",
						"item:	{
								"id":	1,
								"name":	"10.10.10.10_IH"
						}
				}
		]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

서비스 객체 PC로 저장

POST

/po/auto/create/service/objects/save

주소 객체를 자동생성 한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of 서비스)
Items: 서비스
Items

prtcprtc: required (string)
프로토콜 명

Example:
Example

TCP,	UDP,	ICMP

srv_port
srv_port: required (integer)
서비스 포트
값이	없을	경우	null

name
name: required (string)
서비스 이름
값이	없을	경우	null

srv_cate
srv_cate: required (string)
서비스 유형

Example:
Example

HTTP

life_time
life_time: required (integer - minimum: 1 - maximum: 604800)
Lifetime

Example:
Example

{
		"items":	[
				{
						"name":	"ssdp",
						"prtc":	"UDP",
						"srv_port":	1900,
						"srv_cate":	"NONE",
						"life_time":	20
				}
		]
}

Response

HTTP status code 200

서비스 객체를 PC로 저장 성공

Body

Media type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Media type

TypeType: file

주소 객체 PC로 저장

POST

/po/auto/create/address/objects/save

주소 객체을 PC로 저장합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of 주소)
Items: 주소
Items

ip_list
ip_list: required (string)
IP 또는 IP/Mask 형식의 문자열
Mask는 1 ~ 31 범위값

zone
zone: required (integer)
Zone
1:	내부망,	2:	외부망,	3:	DMZ

Example:
Example

{
		"items":	[
				{
						"ip_list":	"10.10.10.10",
						"zone":	1
				}
		]
}

Response

HTTP status code 200

주소 객체를 PC로 저장 성공

Body

Media type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Media type

TypeType: file

동일 서브넷 주소 통합

POST

/po/auto/create/address/merge/objects

동일 서브넷 주소 통합을 PC로 저장합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of 주소)
Items: 주소
Items

useuse: required (integer)
사용 여부(GUI에서 사용, 1로 고정)
0:	사용	안함,	1:	사용

ip_list
ip_list: required (string)
IP 또는 IP/Mask 형식의 문자열
Mask는 1 ~ 31 범위값

subnet_id: required (integer)
subnet_id

동일 서브넷(C 클래스)를 의미하는 ID

subnet_count
subnet_count: required (integer)
동일 서브넷(C 클래스)의 갯수

count: required (integer)
count
세션 수

mergemerge: required (integer)
통합 대상 여부
0:	대상	아님,	1:	통합	대상

Example:
Example

{
		"items":	[
				{
						"merge":	1,
						"ip_list":	"192.168.10.10",
						"subnet_id":	1,
						"subnet_count":	2,
						"count":	100
				},
				{
						"merge":	1,
						"ip_list":	"192.168.10.11",
						"subnet_id":	1,
						"subnet_count":	2,
						"count":	99
				},
				{
						"merge":	0,
						"ip_list":	"192.168.11.11",
						"subnet_id":	2,
						"subnet_count":	1,
						"count":	45
				},
				{
						"merge":	0,
						"ip_list":	"192.168.11.12",
						"subnet_id":	2,
						"subnet_count":	1,
						"count":	3489
				},
				{
						"merge":	0,
						"ip_list":	"192.168.11.0/24",
						"subnet_id":	0,
						"subnet_count":	0,
						"count":	19
				}
		]
}

Response

HTTP status code 200

동일 서브넷 주소 통합를 PC로 저장 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of 주소)
결과 데이터

Items: 주소
Items

useuse: required (integer)
사용 여부(GUI에서 사용, 1로 고정)
0:	사용	안함,	1:	사용

ip_list
ip_list: required (string)
IP 또는 IP/Mask 형식의 문자열
Mask는 1 ~ 31 범위값

subnet_id
subnet_id: required (integer)
동일 서브넷(C 클래스)를 의미하는 ID

subnet_count
subnet_count: required (integer)
동일 서브넷(C 클래스)의 갯수

count: required (integer)
count
세션 수

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"use":	1,
						"ip_list":	"192.168.11.12",
						"subnet_id":	2,
						"subnet_count":	1,
						"count":	3489
				},
				{
						"use":	1,
						"count":	199,
						"subnet_id":	0,
						"subnet_count":	0,
						"ip_list":	"192.168.10.0/24"
				},
				{
						"use":	1,
						"ip_list":	"192.168.11.11",
						"subnet_id":	2,
						"subnet_count":	1,
						"count":	45
				},
				{
						"use":	1,
						"ip_list":	"192.168.11.0/24",
						"subnet_id":	0,
						"subnet_count":	0,
						"count":	19
				}
		]
}

Media type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Media type

TypeType: file

