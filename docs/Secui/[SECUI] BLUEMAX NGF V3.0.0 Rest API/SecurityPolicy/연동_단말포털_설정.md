GET

GET

PUT

GET

GET

POST

  DELETE

PUT

  DELETE

GET

GET

POST

  DELETE

PUT

  DELETE

PUT

PUT

연동 디바이스 포털 설정 REST API 문서

https://{host}/api/po

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

연동 디바이스 포털 설정(GUI 조회용)

/device-portal/connect/view

연동 디바이스 포털 설정

/device-portal/connect/config

연동 디바이스 포털 설정

/device-portal/connect/servers

/device-portal/connect/servers/{pk}

대상 IP 설정

/device-portal/target/ips

/device-portal/target/ips/{pk}

연동 디바이스 포털 설정 적용/취소

/command/connect-device-portal/apply

/command/connect-device-portal/cancel

연동 디바이스 포털 설정(GUI 조회용)

GET

/device-portal/connect/view

연동 디바이스 포털 설정을(를) 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

연동 디바이스 포털 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

config
config: required (object)
연동 포털 설정

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용	1

poll_interval
poll_interval: required (integer - default: 30 - minimum: 30 - maximum: 3600)
Polling 주기(초)

servers
servers: required (object)
연동 포털 서버 목록

portal_name
portal_name: required (string - minLength: 1 - maxLength: 64)
연동 포털 서버 이름

is_local
is_local: required (one of 1, 2)
서버 종류
1:	PORTAL,	2:	CMS

conn_port
conn_port: required (integer - minimum: 1 - maximum: 65535)
접속 포트
기본값:	PORTAL(5579),	CMS(443)

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id
dvc_portal_serv_id: required (integer)
연동 포털 설정 ID(PK)

status
status: required (one of 0, 1)
연결 여부
연결	해제:	0,	연결:	1

dvc_up_tm
dvc_up_tm: required (string)
디바이스 정보 업데이트 일시

link_status
link_status: required (integer)
인터페이스 링크 상태
연결	해제:	0,	연결:	1

link_up_tm
link_up_tm: required (string)
인터페이스 링크 업 일시

ip_list
ip_list: required (array of GetPortalIP)
디바이스 포털 서버 IP 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: GetPortalIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
IPv4:	1,	IPv6:	2

ifc_name
ifc_name: required (string)
인터페이스 이름
ip	속성이	IPv6	일경우	사용되며	인터페이스	이름을	설정합니다.

[인터페이스 목록 조회]
인터페이스의	if_up과	link_up	속성이	1인	인터페이스	목록
GET	{{url}}/api/sm/interfaces

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id
dvc_portal_serv_id: (integer)
연동 포털 설정 ID

dvc_link_portal_ip_id
dvc_link_portal_ip_id: (integer)
디바이스 포털 서버(IP) ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"config":	{
						"use":	1,
						"poll_interval":	30
				},
				"servers":	[{
						"dvc_portal_serv_id":	1,
						"portal_name":	"연동	디바이스	포털	서버	이름",
						"desc":	"연동	디바이스	포털	서버	설명",
						"status":	1,
						"conn_port":	5579,
						"dvc_up_tm":	"1970-01-01	09:00:00",
						"link_status":	0,
						"link_up_tm":	"1970-01-01	09:00:00",
						"ip_list":	[{
								"dvc_portal_serv_id":	1,
								"dvc_link_portal_ip_id":	1,
								"ip":	"10.10.10.10",
								"ip_ver":	1,
								"ifc_name":	"",
								"desc":	"설명"
						}]
				}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

연동 디바이스 포털 설정

GET

/device-portal/connect/config

설정을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용	1

poll_interval
poll_interval: required (integer - default: 30 - minimum: 30 - maximum: 3600)
Polling 주기(초)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"use":	1,
				"poll_interval":	30
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/device-portal/connect/config

설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용	1

poll_interval
poll_interval: required (integer - default: 30 - minimum: 30 - maximum: 3600)
Polling 주기(초)

Example:
Example

{
		"use":	1,
		"poll_interval":	3600
}

Response

HTTP status code 200

설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용	1

poll_interval
poll_interval: required (integer - default: 30 - minimum: 30 - maximum: 3600)
Polling 주기(초)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"use":	1,
				"poll_interval":	3600
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

연동 디바이스 포털 설정

GET

/device-portal/connect/servers

연동 서버 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

연동 서버 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code

결과 코드

result
result: required (array of GetConnectServer)
결과 데이터

Items: GetConnectServer
Items

portal_name
portal_name: required (string - minLength: 1 - maxLength: 64)
연동 포털 서버 이름

is_local
is_local: required (one of 1, 2)
서버 종류
1:	PORTAL,	2:	CMS

conn_port
conn_port: required (integer - minimum: 1 - maximum: 65535)
접속 포트
기본값:	PORTAL(5579),	CMS(443)

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id
dvc_portal_serv_id: required (integer)
연동 포털 설정 ID(PK)

status
status: required (one of 0, 1)
연결 여부
연결	해제:	0,	연결:	1

dvc_up_tm
dvc_up_tm: required (string)
디바이스 정보 업데이트 일시

link_status
link_status: required (integer)
인터페이스 링크 상태
연결	해제:	0,	연결:	1

link_up_tm
link_up_tm: required (string)
인터페이스 링크 업 일시

ip_list
ip_list: required (array of GetPortalIP)
디바이스 포털 서버 IP 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: GetPortalIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
IPv4:	1,	IPv6:	2

ifc_name
ifc_name: required (string)
인터페이스 이름
ip	속성이	IPv6	일경우	사용되며	인터페이스	이름을	설정합니다.

[인터페이스 목록 조회]
인터페이스의	if_up과	link_up	속성이	1인	인터페이스	목록
GET	{{url}}/api/sm/interfaces

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id: (integer)
dvc_portal_serv_id
연동 포털 설정 ID

dvc_link_portal_ip_id
dvc_link_portal_ip_id: (integer)
디바이스 포털 서버(IP) ID

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"dvc_portal_serv_id":	1,
				"portal_name":	"연동	포털	서버	이름",
				"desc":	"연동	포털	서버	설명",
				"status":	1,
				"is_local":	1
				"conn_port":	5579,
				"dvc_up_tm":	"1970-01-01	09:00:00",
				"link_status":	0,
				"link_up_tm":	"1970-01-01	09:00:00",
				"ip_list":	[{
						"dvc_portal_serv_id":	1,
						"dvc_link_portal_ip_id":	1,
						"ip":	"10.10.10.10",
						"ip_ver":	1,
						"ifc_name":	"",
						"desc":	"설명"
				}]
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/device-portal/connect/servers

연동 서버를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

portal_name
portal_name: required (string - minLength: 1 - maxLength: 64)
연동 포털 서버 이름

is_local
is_local: required (one of 1, 2)
서버 종류
1:	PORTAL,	2:	CMS

conn_port
conn_port: required (integer - minimum: 1 - maximum: 65535)
접속 포트
기본값:	PORTAL(5579),	CMS(443)

descdesc: (string - maxLength: 128)
설명

pre_key
pre_key: required (string - minLength: 1 - maxLength: 64)
사전 공유키 (Base64	인코딩/디코딩)
CMS	일	경우	빈	문자열	또는	null로	설정

ip_list
ip_list: required (array of SetPortalIP)
디바이스 포털 서버 목록

Items: SetPortalIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
IPv4:	1,	IPv6:	2

ifc_name
ifc_name: required (string)
인터페이스 이름
ip	속성이	IPv6	일경우	사용되며	인터페이스	이름을	설정합니다.

[인터페이스 목록 조회]
인터페이스의	if_up과	link_up	속성이	1인	인터페이스	목록
GET	{{url}}/api/sm/interfaces

descdesc: (string - maxLength: 128)
설명

Example:
Example

{
		"portal_name":	"New	연동	포털	서버	이름",
		"desc":	"New	연동	포털	서버	설명",
		"is_local":	1,
		"conn_port":	5579,
		"pre_key":	"NjR1bzY2ZVE3WStzN1lPSQ==",
		"ip_list":	[{
				"ip":	"10.10.10.10",
				"ip_ver":	1,
				"ifc_name":	"",
				"desc":	"설명"
		}]
}

Response

HTTP status code 200

연동 서버 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

portal_name
portal_name: required (string - minLength: 1 - maxLength: 64)
연동 포털 서버 이름

is_local: required (one of 1, 2)
is_local
서버 종류
1:	PORTAL,	2:	CMS

conn_port
conn_port: required (integer - minimum: 1 - maximum: 65535)
접속 포트
기본값:	PORTAL(5579),	CMS(443)

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id
dvc_portal_serv_id: required (integer)
연동 포털 설정 ID(PK)

status: required (one of 0, 1)
status
연결 여부
연결	해제:	0,	연결:	1

dvc_up_tm
dvc_up_tm: required (string)
디바이스 정보 업데이트 일시

link_status: required (integer)
link_status
인터페이스 링크 상태
연결	해제:	0,	연결:	1

link_up_tm
link_up_tm: required (string)
인터페이스 링크 업 일시

ip_list: required (array of GetPortalIP)
ip_list
디바이스 포털 서버 IP 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: GetPortalIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
IPv4:	1,	IPv6:	2

ifc_name
ifc_name: required (string)
인터페이스 이름
ip	속성이	IPv6	일경우	사용되며	인터페이스	이름을	설정합니다.

[인터페이스 목록 조회]
인터페이스의	if_up과	link_up	속성이	1인	인터페이스	목록
GET	{{url}}/api/sm/interfaces

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id
dvc_portal_serv_id: (integer)
연동 포털 설정 ID

dvc_link_portal_ip_id
dvc_link_portal_ip_id: (integer)
디바이스 포털 서버(IP) ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"dvc_portal_serv_id":	2,
				"portal_name":	"New	연동	포털	서버	이름",
				"desc":	"New	연동	포털	서버	설명",
				"status":	1,
				"is_local":	1
				"conn_port":	5579,
				"dvc_up_tm":	"1970-01-01	09:00:00",
				"link_status":	0,
				"link_up_tm":	"1970-01-01	09:00:00",
				"ip_list":	[{
						"dvc_portal_serv_id":	2,
						"dvc_link_portal_ip_id":	1,
						"ip":	"10.10.10.10",
						"ip_ver":	1,
						"ifc_name":	"",
						"desc":	"설명"
				}]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/device-portal/connect/servers

연동 서버 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"dvc_portal_serv_id":	1},
				{"dvc_portal_serv_id":	2}
		]
}

Response

HTTP status code 200

연동 서버 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/device-portal/connect/servers/{pk}

연동 서버을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
연동 포털 설정 ID (dvc_portal_serv_id)

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

연동 서버 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

portal_name
portal_name: required (string - minLength: 1 - maxLength: 64)
연동 포털 서버 이름

is_local
is_local: required (one of 1, 2)
서버 종류
1:	PORTAL,	2:	CMS

conn_port
conn_port: required (integer - minimum: 1 - maximum: 65535)
접속 포트
기본값:	PORTAL(5579),	CMS(443)

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id
dvc_portal_serv_id: required (integer)
연동 포털 설정 ID(PK)

status
status: required (one of 0, 1)
연결 여부
연결	해제:	0,	연결:	1

dvc_up_tm
dvc_up_tm: required (string)
디바이스 정보 업데이트 일시

link_status
link_status: required (integer)
인터페이스 링크 상태
연결	해제:	0,	연결:	1

link_up_tm
link_up_tm: required (string)
인터페이스 링크 업 일시

ip_list
ip_list: required (array of GetPortalIP)
디바이스 포털 서버 IP 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: GetPortalIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
IPv4:	1,	IPv6:	2

ifc_name
ifc_name: required (string)
인터페이스 이름
ip	속성이	IPv6	일경우	사용되며	인터페이스	이름을	설정합니다.

[인터페이스 목록 조회]
인터페이스의	if_up과	link_up	속성이	1인	인터페이스	목록
GET	{{url}}/api/sm/interfaces

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id
dvc_portal_serv_id: (integer)
연동 포털 설정 ID

dvc_link_portal_ip_id
dvc_link_portal_ip_id: (integer)
디바이스 포털 서버(IP) ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"dvc_portal_serv_id":	1,
				"portal_name":	"연동	포털	서버	이름",
				"desc":	"연동	포털	서버	설명",
				"status":	1,
				"is_local":	1,
				"conn_port":	5579,
				"dvc_up_tm":	"1970-01-01	09:00:00",
				"link_status":	0,
				"link_up_tm":	"1970-01-01	09:00:00",
				"ip_list":	[{
						"dvc_portal_serv_id":	1,
						"dvc_link_portal_ip_id":	1,
						"ip":	"10.10.10.10",
						"ip_ver":	1,
						"ifc_name":	"",
						"desc":	"설명"
				}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/device-portal/connect/servers/{pk}

연동 서버을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
연동 포털 설정 ID (dvc_portal_serv_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

portal_name: required (string - minLength: 1 - maxLength: 64)
portal_name

연동 포털 서버 이름

is_local
is_local: required (one of 1, 2)
서버 종류
1:	PORTAL,	2:	CMS

conn_port
conn_port: required (integer - minimum: 1 - maximum: 65535)
접속 포트
기본값:	PORTAL(5579),	CMS(443)

descdesc: (string - maxLength: 128)
설명

pre_key
pre_key: required (string - minLength: 1 - maxLength: 64)
사전 공유키 (Base64	인코딩/디코딩)
CMS	일	경우	빈	문자열	또는	null로	설정

ip_list
ip_list: required (array of SetPortalIP)
디바이스 포털 서버 목록

Items: SetPortalIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

ip_ver: required (one of 1, 2)
ip_ver
IP 버전
IPv4:	1,	IPv6:	2

ifc_name
ifc_name: required (string)
인터페이스 이름
ip	속성이	IPv6	일경우	사용되며	인터페이스	이름을	설정합니다.

[인터페이스 목록 조회]
인터페이스의	if_up과	link_up	속성이	1인	인터페이스	목록
GET	{{url}}/api/sm/interfaces

descdesc: (string - maxLength: 128)
설명

Example:
Example

{
		"portal_name":	"연동	디바이스	포털	서버	이름",
		"desc":	"연동	디바이스	포털	서버	설명",
		"is_local":	2,
		"conn_port":	443,
		"pre_key":	"",
		"ip_list":	[{
				"ip":	"10.10.10.10",
				"ip_ver":	1,
				"ifc_name":	"",
				"desc":	"IPv4	설명"
		},	{
				"ip":	"1::1",
				"ip_ver":	2,
				"ifc_name":	"eth1",
				"desc":	"IPv6	설명"
		}]
}

Response

HTTP status code 200

연동 서버 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

portal_name
portal_name: required (string - minLength: 1 - maxLength: 64)
연동 포털 서버 이름

is_local: required (one of 1, 2)
is_local
서버 종류
1:	PORTAL,	2:	CMS

conn_port
conn_port: required (integer - minimum: 1 - maximum: 65535)
접속 포트
기본값:	PORTAL(5579),	CMS(443)

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id
dvc_portal_serv_id: required (integer)
연동 포털 설정 ID(PK)

status: required (one of 0, 1)
status
연결 여부
연결	해제:	0,	연결:	1

dvc_up_tm
dvc_up_tm: required (string)
디바이스 정보 업데이트 일시

link_status: required (integer)
link_status

인터페이스 링크 상태
연결	해제:	0,	연결:	1

link_up_tm
link_up_tm: required (string)
인터페이스 링크 업 일시

ip_list
ip_list: required (array of GetPortalIP)
디바이스 포털 서버 IP 목록
GUI에서	사용되지	않는	일부	속성은	표시하지	않음

Items: GetPortalIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
IPv4:	1,	IPv6:	2

ifc_name
ifc_name: required (string)
인터페이스 이름
ip	속성이	IPv6	일경우	사용되며	인터페이스	이름을	설정합니다.

[인터페이스 목록 조회]
인터페이스의	if_up과	link_up	속성이	1인	인터페이스	목록
GET	{{url}}/api/sm/interfaces

descdesc: (string - maxLength: 128)
설명

dvc_portal_serv_id
dvc_portal_serv_id: (integer)
연동 포털 설정 ID

dvc_link_portal_ip_id
dvc_link_portal_ip_id: (integer)
디바이스 포털 서버(IP) ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"dvc_portal_serv_id":	1,
				"portal_name":	"연동	포털	서버	이름",
				"desc":	"연동	포털	서버	설명",
				"status":	1,
				"is_local":	2,
				"conn_port":	443,
				"dvc_up_tm":	"1970-01-01	09:00:00",
				"link_status":	0,
				"link_up_tm":	"1970-01-01	09:00:00",
				"ip_list":	[{
						"dvc_portal_serv_id":	1,
						"dvc_link_portal_ip_id":	1,
						"ip":	"10.10.10.10",
						"ip_ver":	1,
						"ifc_name":	"",
						"desc":	"IPv4	설명"
				},	{
						"dvc_portal_serv_id":	1,
						"dvc_link_portal_ip_id":	2,
						"ip":	"1::1",
						"ip_ver":	2,
						"ifc_name":	"eth1",
						"desc":	"IPv6	설명"
				}]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/device-portal/connect/servers/{pk}

연동 서버을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
연동 포털 설정 ID (dvc_portal_serv_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

연동 서버 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

대상 IP 설정

GET

/device-portal/target/ips

대상 IP 설정 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

대상 IP 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of GetTargetIp)
결과 데이터

Items: GetTargetIp
Items

name
name: required (string - minLength: 1 - maxLength: 64)
이름

target_ip: required (string)
target_ip

IP 주소(IPv4, IPv6 모두 지원)

target_net
target_net: required (integer - minimum: 1)
넷마스크
target_ip	속성이	IPv4	주소는	Max	32,	IPv6	주소는	Max	128

descdesc: (string - maxLength: 128)
설명

dvc_target_id
dvc_target_id: required (integer)
대상 IP 설정 ID

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"dvc_target_id":	1,
				"name":	"대상	IP	이름",
				"target_ip":	"10.10.10.10",
				"target_net":	24,
				"desc":	"대상	IP	설명"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/device-portal/target/ips

대상 IP 설정를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 64)
이름

target_ip
target_ip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

target_net
target_net: required (integer - minimum: 1)
넷마스크
target_ip	속성이	IPv4	주소는	Max	32,	IPv6	주소는	Max	128

descdesc: (string - maxLength: 128)
설명

Example:
Example

{
		"name":	"New	대상	IP	이름",
		"target_ip":	"10.10.10.10",
		"target_net":	24,
		"desc":	"New	대상	IP	설명"
}

Response

HTTP status code 200

대상 IP 설정 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 64)
이름

target_ip
target_ip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

target_net
target_net: required (integer - minimum: 1)
넷마스크
target_ip	속성이	IPv4	주소는	Max	32,	IPv6	주소는	Max	128

descdesc: (string - maxLength: 128)
설명

dvc_target_id
dvc_target_id: required (integer)
대상 IP 설정 ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"dvc_target_id":	2,
				"name":	"New	대상	IP	이름",
				"target_ip":	"10.10.10.10",
				"target_net":	24,
				"desc":	"New	대상	IP	설명"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/device-portal/target/ips

대상 IP 설정 목록을 (일괄)삭제한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"dvc_target_id":	1},
				{"dvc_target_id":	2}
		]
}

Response

HTTP status code 200

대상 IP 설정 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/device-portal/target/ips/{pk}

대상 IP 설정을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
대상 IP 설정 ID (dvc_target_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

대상 IP 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 64)
이름

target_ip
target_ip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

target_net
target_net: required (integer - minimum: 1)
넷마스크
target_ip	속성이	IPv4	주소는	Max	32,	IPv6	주소는	Max	128

descdesc: (string - maxLength: 128)
설명

dvc_target_id
dvc_target_id: required (integer)
대상 IP 설정 ID

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"dvc_target_id":	1,
				"name":	"테스트망",
				"target_ip":	"10.10.10.10",
				"target_net":	24,
				"desc":	"설명"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/device-portal/target/ips/{pk}

대상 IP 설정을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
대상 IP 설정 ID (dvc_target_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 64)
이름

target_ip
target_ip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

target_net
target_net: required (integer - minimum: 1)
넷마스크
target_ip	속성이	IPv4	주소는	Max	32,	IPv6	주소는	Max	128

descdesc: (string - maxLength: 128)
설명

Example:
Example

{
		"name":	"대상	IP	이름",
		"target_ip":	"10.10.10.10",
		"target_net":	24,
		"desc":	"대상	IP	설명"
}

Response

HTTP status code 200

대상 IP 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 64)
이름

target_ip
target_ip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

target_net
target_net: required (integer - minimum: 1)
넷마스크
target_ip	속성이	IPv4	주소는	Max	32,	IPv6	주소는	Max	128

descdesc: (string - maxLength: 128)
설명

dvc_target_id
dvc_target_id: required (integer)
대상 IP 설정 ID

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"dvc_target_id":	1,
				"name":	"대상	IP	이름",
				"target_ip":	"10.10.10.10",
				"target_net":	24,
				"desc":	"대상	IP	설명"
		}]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/device-portal/target/ips/{pk}

대상 IP 설정을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
대상 IP 설정 ID (dvc_target_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

대상 IP 설정 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

연동 디바이스 포털 설정 적용/취소

PUT

/command/connect-device-portal/apply

연동 디바이스 포털 설정을 적용합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

연동 디바이스 포털 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/connect-device-portal/cancel

연동 디바이스 포털 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

연동 디바이스 포털 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

