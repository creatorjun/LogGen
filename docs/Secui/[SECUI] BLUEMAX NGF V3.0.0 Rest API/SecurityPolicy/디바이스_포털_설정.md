GET

PUT

GET

PUT

GET

GET

GET

GET

GET

POST

  DELETE

PUT

  DELETE

GET

PUT

PUT

디바이스 포털 설정 REST API 문서

https://{host}/api/po

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

공통 설정

/device-portal/os/config

인증 설정

/device-portal/auth/config

최신 통합 정책(OS 구분 없이)

/device-portal/itg/policy

OS별 최신 통합 정책

/device-portal/{pk}/itg/policy

/device-portal/{pk}/itg/policy/db

연동 방화벽 설정

/device-portal/firewalls

/device-portal/firewalls/{pk}

/device-portal/firewalls/{pk}/db

디바이스 포털 설정 적용/취소

/command/device-portal/apply

/command/device-portal/cancel

공통 설정

GET

/device-portal/os/config

OS 별 공통 설정을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language

언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

OS 별 공통 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

winwin: required (object)
Windows

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

linux
linux: required (object)
Linux

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
stat_info_send_interval
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use: required (one of 0, 1)
sslvpn_use
SSLVPN 기능
미사용:	0,	사용:	1

android
android: required (object)
Android

os_type: required (integer)
os_type
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

iosios: required (object)

iOS

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
stat_info_send_interval
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use: required (one of 0, 1)
sslvpn_use
SSLVPN 기능
미사용:	0,	사용:	1

macosmacos: required (object)
macOS

os_type: required (integer)
os_type
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

Example:
Example

{
		"code":	"ok",
		"result":	{
				"win":	{
						"os_type":	1,
						"stat_info_send_interval":	1,
						"dvc_info_coll_use":	1,
						"sslvpn_use":	1
				},
				"linux":	{
						"os_type":	2,
						"stat_info_send_interval":	1,
						"dvc_info_coll_use":	1,
						"sslvpn_use":	0
				},
				"android":	{
						"os_type":	3,
						"stat_info_send_interval":	60,
						"dvc_info_coll_use":	1,
						"sslvpn_use":	0
				},
				"ios":	{
						"os_type":	4,
						"stat_info_send_interval":	60,
						"dvc_info_coll_use":	0,
						"sslvpn_use":	0
				},
				"macos":	{
						"os_type":	6,
						"stat_info_send_interval":	60,
						"dvc_info_coll_use":	0,
						"sslvpn_use":	0
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/device-portal/os/config

OS 별 공통 설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

winwin: required (object)
Windows

os_type: required (integer)
os_type

OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use: required (one of 0, 1)
dvc_info_coll_use
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

linux
linux: required (object)
Linux

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

android
android: required (object)
Android

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

iosios: required (object)
iOS

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

macosmacos: required (object)
macOS

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

Example:
Example

{
		"win":	{
				"os_type":	1,
				"stat_info_send_interval":	1,
				"dvc_info_coll_use":	1,
				"sslvpn_use":	0
		},
		"linux":	{
				"os_type":	2,
				"stat_info_send_interval":	1,
				"dvc_info_coll_use":	1,
				"sslvpn_use":	0
		},
		"android":	{
				"os_type":	3,
				"stat_info_send_interval":	60,
				"dvc_info_coll_use":	1,
				"sslvpn_use":	0
		},
		"ios":	{
				"os_type":	4,
				"stat_info_send_interval":	60,
				"dvc_info_coll_use":	1,
				"sslvpn_use":	0
		},
		"macos":	{
				"os_type":	6,
				"stat_info_send_interval":	60,
				"dvc_info_coll_use":	1,
				"sslvpn_use":	0
		}
}

Response

HTTP status code 200

OS 별 공통 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

winwin: required (object)
Windows

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

linux: required (object)
linux
Linux

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

android
android: required (object)
Android

os_type: required (integer)
os_type

OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use: required (one of 0, 1)
dvc_info_coll_use
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

iosios: required (object)
iOS

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

macosmacos: required (object)
macOS

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

stat_info_send_interval
stat_info_send_interval: required (integer - minimum: 1 - maximum: 60)
상태 정보 전송 주기(분)

dvc_info_coll_use
dvc_info_coll_use: required (one of 0, 1)
디바이스 정보 수집 기능
미사용:	0,	사용:	1

sslvpn_use
sslvpn_use: required (one of 0, 1)
SSLVPN 기능
미사용:	0,	사용:	1

Example:
Example

{
		"code":	"ok",
		"result":	{
					"win":	{
						"os_type":	1,
						"stat_info_send_interval":	1,
						"dvc_info_coll_use":	1,
						"sslvpn_use":	0
				},
				"linux":	{
						"os_type":	2,
						"stat_info_send_interval":	1,
						"dvc_info_coll_use":	1,
						"sslvpn_use":	0
				},
				"android":	{
						"os_type":	3,
						"stat_info_send_interval":	60,
						"dvc_info_coll_use":	1,
						"sslvpn_use":	0
				},
				"ios":	{
						"os_type":	4,
						"stat_info_send_interval":	60,
						"dvc_info_coll_use":	1,
						"sslvpn_use":	0
				},
				"macos":	{
						"os_type":	4,
						"stat_info_send_interval":	60,
						"dvc_info_coll_use":	1,
						"sslvpn_use":	0
				}
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

인증 설정

GET

/device-portal/auth/config

인증 설정을(를) 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

auth_prof_id
auth_prof_id: required (integer)
인증 프로파일 ID

[인증 프로파일 목록 조회]
GET	{{url}}/api/sm/auth/profiles

useuse: required (one of 0, 1)
연동 활성화 여부
사용안함:	0,	사용:	1

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
IPv4:	1,	IPv6:	2

portal_ip
portal_ip: required (string)
IP(IPv4, IPv6 모두 지원)

portal_port: required (integer - minimum: 1 - maximum: 65535)
portal_port
포트

auth_prof_name
auth_prof_name: required (string)
인증 프로파일 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"auth_prof_id":	1,
				"auth_prof_name":	"인증	프로파일	이름",
				"use":	1,
				"ip_ver":	1,
				"portal_ip":	"0.0.0.0",
				"portal_port":	5579
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code

결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/device-portal/auth/config

인증 설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

auth_prof_id
auth_prof_id: required (integer)
인증 프로파일 ID

[인증 프로파일 목록 조회]
GET	{{url}}/api/sm/auth/profiles

useuse: required (one of 0, 1)
연동 활성화 여부
사용안함:	0,	사용:	1

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
IPv4:	1,	IPv6:	2

portal_ip: required (string)
portal_ip
IP(IPv4, IPv6 모두 지원)

portal_port
portal_port: required (integer - minimum: 1 - maximum: 65535)
포트

pre_key
pre_key: required (string - maxLength: 64)
사전 공유키
Base64	인코딩/디코딩	처리	필요

Example:
Example

{
		"auth_prof_id":	1,
		"use":	0,
		"ip_ver":	1,
		"portal_ip":	"0.0.0.0",
		"portal_port":	5579,
		"pre_key":	"QUJDREVGRy0yMDE4MDYxMy0xMDExMDA="
}

Response

HTTP status code 200

인증 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

auth_prof_id
auth_prof_id: required (integer)
인증 프로파일 ID

[인증 프로파일 목록 조회]
GET	{{url}}/api/sm/auth/profiles

useuse: required (one of 0, 1)
연동 활성화 여부
사용안함:	0,	사용:	1

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
IPv4:	1,	IPv6:	2

portal_ip: required (string)
portal_ip
IP(IPv4, IPv6 모두 지원)

portal_port
portal_port: required (integer - minimum: 1 - maximum: 65535)
포트

auth_prof_name
auth_prof_name: required (string)
인증 프로파일 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"auth_prof_id":	1,
				"auth_prof_name":	"인증	프로파일	이름",
				"use":	1,
				"ip_ver":	1,
				"portal_ip":	"0.0.0.0",
				"portal_port":	5579
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

최신 통합 정책(OS 구분 없이)

GET

/device-portal/itg/policy

최신 통합 정책 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

최신 통합 정책 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ItgPolicy)
결과 데이터

Items: ItgPolicy
Items

verver: required (string)
버전

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

gen_dt
gen_dt: required (string)
날짜

file_name
file_name: required (string)
정책 파일명

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"ver":	5,
						"os_type":	1,
						"gen_dt":	"2018-10-08	12:42:12",
						"file_name":	"/secui/test/rule1"
				},
				{
						"ver":	3,
						"os_type":	2,
						"gen_dt":	"2018-10-08	12:45:14",
						"file_name":	"/secui/test/rule3"
				},
				{
						"ver":	4,
						"os_type":	3,
						"gen_dt":	"2018-10-08	12:48:15",
						"file_name":	"/secui/test/rule4"
				},
				{
						"ver":	4,
						"os_type":	4,
						"gen_dt":	"2018-10-08	12:48:15",
						"file_name":	"/secui/test/rule4"
				},
				{
						"ver":	4,
						"os_type":	6,
						"gen_dt":	"2018-10-08	12:48:15",
						"file_name":	"/secui/test/rule4"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

OS별 최신 통합 정책

GET

/device-portal/{pk}/itg/policy

OS별 최신 통합 정책을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

OS별 최신 통합 정책 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

verver: required (string)
버전

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

gen_dt
gen_dt: required (string)
날짜

file_name
file_name: required (string)
정책 파일명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ver":	"12",
				"os_type":	1,
				"gen_dt":	"2018-01-01	10:10:10",
				"file_name":	"정책	파일	경로"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/device-portal/{pk}/itg/policy/db

통합 정책 DB 보기을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

통합 정책 DB 보기 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

security
security: required (array of Security)
정책 DB 보기 > Security 목록

Items: Security
Items

os_type: required (integer)
os_type
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

target
target: required (string)
대상

program
program: required (array of Program)
정책 DB 보기 Program 목록

Items: Program
Items

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

name
name: required (string)
이름

verver: required (string)
버전

Example:
Example

{
		"code":	"ok",
		"result":	{
				"security":	[{
						"os_type":	1,
						"target":	"Anti-virus	check"
				}],
				"program":	[{
						"os_type":	1,
						"name":	"visual",
						"ver":	"1.0.0"
				}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

연동 방화벽 설정

GET

/device-portal/firewalls

연동 방화벽 설정 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

연동 방화벽 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of GetFirewallConfig)
결과 데이터

Items: GetFirewallConfig
Items

fw_name
fw_name: required (string - minLength: 1 - maxLength: 64)
방화벽 이름

is_local
is_local: required (one of 0, 1)
로컬 IP 여부
0:	로컬	아님,	1:	로컬

dvc_portal_fw_set_id
dvc_portal_fw_set_id: required (integer)
방화벽 설정 ID(PK)

status
status: required (one of 0, 1)
연결 여부
(1:	연결,	0:	미연결)

up_tmup_tm: required (string)
업데이트 일시

ipip: required (array of GetFirewallIP)
방화벽 IP 목록

Items: GetFirewallIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

descdesc: (string - maxLength: 128)
설명

dvc_portal_fw_set_id: required (integer)
dvc_portal_fw_set_id
방화벽 ID

dvc_portal_fw_set_ip_id
dvc_portal_fw_set_ip_id: required (integer)
IP ID

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
(1:	IPv4,	2:	IPv6)

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"dvc_portal_fw_set_id":	1,
				"fw_name":	"방화벽	설정",
				"status":	0,
				"is_local":	1,
				"up_tm":	"1970-01-01	09:00:00",
				"ip":	[{
						"dvc_portal_fw_set_id":	1,
						"dvc_portal_fw_set_ip_id":	1,
						"ip":	"10.10.10.10",
						"desc":	"IP	설명",
						"ip_ver":	1
				}]
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/device-portal/firewalls

연동 방화벽 설정를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

fw_name
fw_name: required (string - minLength: 1 - maxLength: 64)
방화벽 이름

is_local
is_local: required (one of 0, 1)
로컬 IP 여부
0:	로컬	아님,	1:	로컬

ipip: required (array of FirewallIP)
방화벽 IP 목록

Items: FirewallIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

descdesc: (string - maxLength: 128)
설명

Example:
Example

{
		"fw_name":	"New	방화벽	이름",
		"is_local":	1,
		"ip":	[{
				"ip":	"1.1.1.1",
				"desc":	"방화벽	IP	설명"
		}]
}

Response

HTTP status code 200

연동 방화벽 설정 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of GetFirewallConfig)
결과 데이터

Items: GetFirewallConfig
Items

fw_name: required (string - minLength: 1 - maxLength: 64)
fw_name
방화벽 이름

is_local
is_local: required (one of 0, 1)
로컬 IP 여부
0:	로컬	아님,	1:	로컬

dvc_portal_fw_set_id
dvc_portal_fw_set_id: required (integer)
방화벽 설정 ID(PK)

status
status: required (one of 0, 1)
연결 여부
(1:	연결,	0:	미연결)

up_tmup_tm: required (string)
업데이트 일시

ipip: required (array of GetFirewallIP)
방화벽 IP 목록

Items: GetFirewallIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

descdesc: (string - maxLength: 128)
설명

dvc_portal_fw_set_id
dvc_portal_fw_set_id: required (integer)
방화벽 ID

dvc_portal_fw_set_ip_id
dvc_portal_fw_set_ip_id: required (integer)
IP ID

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
(1:	IPv4,	2:	IPv6)

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"dvc_portal_fw_set_id":	2,
				"fw_name":	"New	방화벽	설정",
				"status":	0,
				"is_local":	1,
				"up_tm":	"1970-01-01	09:00:00",
				"ip":	[
						{
								"dvc_portal_fw_set_id":	2,
								"dvc_portal_fw_set_ip_id":	3,
								"ip":	"10.10.10.10",
								"desc":	"IP	설명",
								"ip_ver":	1
						}
				]
		}]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/device-portal/firewalls

연동 방화벽 설정 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"dvc_portal_fw_set_id":	1},
				{"dvc_portal_fw_set_id":	2}
		]
}

Response

HTTP status code 200

연동 방화벽 설정 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/device-portal/firewalls/{pk}

연동 방화벽 설정을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
방화벽 설정 ID (dvc_portal_fw_set_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

연동 방화벽 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

fw_name
fw_name: required (string - minLength: 1 - maxLength: 64)
방화벽 이름

is_local
is_local: required (one of 0, 1)
로컬 IP 여부
0:	로컬	아님,	1:	로컬

dvc_portal_fw_set_id
dvc_portal_fw_set_id: required (integer)
방화벽 설정 ID(PK)

status
status: required (one of 0, 1)
연결 여부
(1:	연결,	0:	미연결)

up_tmup_tm: required (string)
업데이트 일시

ipip: required (array of GetFirewallIP)
방화벽 IP 목록

Items: GetFirewallIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

descdesc: (string - maxLength: 128)
설명

dvc_portal_fw_set_id
dvc_portal_fw_set_id: required (integer)
방화벽 ID

dvc_portal_fw_set_ip_id
dvc_portal_fw_set_ip_id: required (integer)
IP ID

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
(1:	IPv4,	2:	IPv6)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"dvc_portal_fw_set_id":	1,
				"fw_name":	"방화벽	설정",
				"status":	0,
				"is_local":	1,
				"up_tm":	"1970-01-01	09:00:00",
				"ip":	[
						{
								"dvc_portal_fw_set_id":	2,
								"dvc_portal_fw_set_ip_id":	3,
								"ip":	"10.10.10.10",
								"desc":	"IP	설명",
								"ip_ver":	1
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/device-portal/firewalls/{pk}

연동 방화벽 설정을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
방화벽 설정 ID (dvc_portal_fw_set_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

fw_name
fw_name: required (string - minLength: 1 - maxLength: 64)
방화벽 이름

is_local
is_local: required (one of 0, 1)
로컬 IP 여부
0:	로컬	아님,	1:	로컬

ipip: required (array of FirewallIP)
방화벽 IP 목록

Items: FirewallIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

descdesc: (string - maxLength: 128)
설명

Example:
Example

{
		"fw_name":	"방화벽	정책",
		"is_local":	1,
		"ip":	[
				{
						"ip":	"1.1.1.1",
						"desc":	"IP	설명"
				}
		]
}

Response

HTTP status code 200

연동 방화벽 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of GetFirewallConfig)
결과 데이터

Items: GetFirewallConfig
Items

fw_name
fw_name: required (string - minLength: 1 - maxLength: 64)
방화벽 이름

is_local
is_local: required (one of 0, 1)
로컬 IP 여부
0:	로컬	아님,	1:	로컬

dvc_portal_fw_set_id
dvc_portal_fw_set_id: required (integer)
방화벽 설정 ID(PK)

status
status: required (one of 0, 1)
연결 여부
(1:	연결,	0:	미연결)

up_tmup_tm: required (string)
업데이트 일시

ipip: required (array of GetFirewallIP)
방화벽 IP 목록

Items: GetFirewallIP
Items

ipip: required (string)
IP 주소(IPv4, IPv6 모두 지원)

descdesc: (string - maxLength: 128)
설명

dvc_portal_fw_set_id
dvc_portal_fw_set_id: required (integer)
방화벽 ID

dvc_portal_fw_set_ip_id
dvc_portal_fw_set_ip_id: required (integer)
IP ID

ip_ver: required (one of 1, 2)
ip_ver
IP 버전
(1:	IPv4,	2:	IPv6)

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"dvc_portal_fw_set_id":	2,
				"fw_name":	"New	방화벽	설정",
				"status":	0,
				"is_local":	1,
				"up_tm":	"1970-01-01	09:00:00",
				"ip":	[
						{
								"dvc_portal_fw_set_id":	2,
								"dvc_portal_fw_set_ip_id":	3,
								"ip":	"10.10.10.10",
								"desc":	"IP	설명",
								"ip_ver":	1
						}
				]
		}]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/device-portal/firewalls/{pk}

연동 방화벽 설정을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
방화벽 설정 ID (dvc_portal_fw_set_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

연동 방화벽 설정 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/device-portal/firewalls/{pk}/db

정책 DB 보기을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
방화벽 설정 ID (dvc_portal_fw_set_id)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

정책 DB 보기 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

security
security: required (array of Security)
정책 DB 보기 > Security 목록

Items: Security
Items

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

target
target: required (string)
대상

program
program: required (array of Program)
정책 DB 보기 Program 목록

Items: Program
Items

os_type
os_type: required (integer)
OS 타입
1:	Window
2:	Linux
3:	Android
4:	iOS
6:	macOS

name
name: required (string)
이름

verver: required (string)
버전

Example:
Example

{
		"code":	"ok",
		"result":	{
				"security":	[{
						"os_type":	1,
						"target":	"Anti-virus	check"
				}],
				"program":	[{
						"os_type":	1,
						"name":	"visual",
						"ver":	"1.0.0"
				}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

디바이스 포털 설정 적용/취소

PUT

/command/device-portal/apply

디바이스 포털 설정을 적용합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

디바이스 포털 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/device-portal/cancel

디바이스 포털 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

디바이스 포털 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

