블랙리스트 등록 REST API 문서

https://{host}/api/po

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

블랙리스트 전체 목록

/blacklist/all/ips

블랙리스트 등록

/blacklist/ips

/blacklist/ips/{blacklist_id}

/blacklist/ips/expired

/blacklist/ips/save

/blacklist/ips/batch

블랙리스트 등록 적용/취소

/command/blacklist-ips/apply

/command/blacklist-ips/cancel

블랙리스트 예외 IP 등록

/blacklist/exception-ips

/blacklist/exception-ips/{blist_excpt_ip_id}

/blacklist/exception-ips/save

/blacklist/exception-ips/batch

블랙리스트 예외 IP 등록 적용/취소

/command/blacklist-exception-ips/apply

/command/blacklist-exception-ips/cancel

현재상태 > 블랙리스트

/blacklist/logs

블랙리스트 전체 목록

GET

/blacklist/all/ips

블랙리스트 전체 목록 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

GET

GET

POST

  DELETE

GET

  DELETE

DELETE

GET

POST

PUT

PUT

GET

POST

PUT

  DELETE

GET

  DELETE

GET

POST

PUT

PUT

DELETE

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

columns
columns: (one of 0, 1)
컬럼별 ID 목록을 응답 결과의 첫 번째 행에 포함 여부를 결정
기본값은 0이고 컬럼의 속성 Key 목록을 포함하지 않는다.

Example:
Example

?columns=1

Response

HTTP status code 200

블랙리스트 전체 목록 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (array of 블랙리스트 정보)
result
블랙리스트 ID, IP버전, 출발지주소, 출발지포트, 목적지주소, 목적지포트, 프로토콜, 차단만료시간, 설명, 관리자, cfg_layer, 최근수정일 순의 배열 데이터
쿼리스트링으로 columns=1로 요청할 경우 첫 번째 행은 각 속성의 key를 출력한다.

[0] : 블랙리스트 ID(Number),  blacklist_id
[1] : IP버전(Number, 1: IPv4, 2: IPv6),  ip_ver
[2] : 출발지주소("ANY" 가능),  src_ip
[3] : 출발지포트("ANY" 또는 숫자),  src_port
[4] : 목적지주소("ANY" 가능),  dst_ip
[5] : 목적지포트("ANY" 또는 숫자),  dst_port
[6] : 프로토콜("ANY" 가능),  prtc
[7] : 차단만료시간(YYYY-MM-DD hh:mm:ss),  block_expire_tm
[8] : 설명,  desc
[9] : 관리자,  cfg_adm
[10] : TAMS 연동 여부(Number),  cfg_layer
[11] : 최근수정일(YYYY-MM-DD hh:mm:ss),  cfg_dt

Example:
Example

//	컬럼	ID	정보가	없을	경우(기본값)
{
		"code":	"ok",
		"result":	[
				[1,	1,	"1.1.1.1",	8080,	"ANY",	"ANY",	"TCP",	"2026-08-12	23:59:59",	"설명",	"admin",	0,	"2021-08-12	23:59:59"],
				[2,	1,	"2.2.2.2",	8080,	"192.168.174.100",	"ANY",	"TCP",	"2025-08-12	23:59:59",	"설명",	"admin",	0,	"2021-08-10	23:59:59"]
		]
}

//컬럼	ID	목록을	첫번째	행에	포함시킨	경우
{
		"code":	"ok",
		"result":	[
				["blacklist_id",	"ip_ver",	"src_ip",	"src_port",	"dst_ip",	"dst_port",	"prtc",	"block_expire_tm",	"desc",	"cfg_adm",	"cfg_layer",	"cfg_dt"],
				[1,	1,	"1.1.1.1",	8080,	"ANY",	"ANY",	"TCP",	"2026-08-12	23:59:59",	"설명",	"admin",	0,	"2021-08-12	23:59:59"],
				[2,	1,	"2.2.2.2",	8080,	"192.168.174.100",	"ANY",	"TCP",	"2025-08-12	23:59:59",	"설명",	"admin",	0,	"2021-08-10	23:59:59"]																		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

블랙리스트 등록

GET

/blacklist/ips

블랙리스트 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

max_page
max_page: required (integer)
페이지당 건수

page
page: required (integer)
현재 페이지 수

keyword: (string - maxLength: 128)
keyword
검색 내용(URI인코딩 처리 필요)

case
case: (one of 0, 1)
대소문자 구분
0:	대소문자	구분	없음,	1:	대소문자	구분

fields: (string)
fields

세미콜론으로 구분된 검색 컬럼 목록

ip_ver	:	버전
src_ip	:	출발지	주소
src_port	:	출발지	포트
dst_ip	:	목적지	주소
dst_port	:	목적지	포트
prtc	:	프로토콜(코드값으로	검색	가능)
desc	:	설명
block_expire_tm	:	차단	만료	시간
cfg_adm	:	관리자
cfg_layer	:	구분(코드값으로	검색	가능)
cfg_dt	:	최근	수정일

Example:
Example

ip_ver;src_ip;src_port;dst_ip;dst_port;prtc;desc

expire
expire: (one of 0, 1 - default: 0)
차단 만료 항목 검색 여부
0:	전체,	1:	차단	만료된	항목

sorted: (one of 0, 1, 2 - default: 0)
sorted
정렬 여부
0,	정렬하지	않음,	1:	오름차순,	2:	내림차순

sort_fields
sort_fields: (string)
단일 컬럼명

Example:
Example

src_ip

Response

HTTP status code 200

블랙리스트 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

count: required (integer)
count
등록된 블랙리스트 전체 갯수

blistblist: required (array of 블랙리스트)
블랙리스트 목록

Items: 블랙리스트
Items

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
1:	IPv4,	2:	IPv6

descdesc: (string - minLength: 0 - maxLength: 128)
설명

block_expire_tm
block_expire_tm: required (string)
차단 만료 시간
포맷: YYYY-MM-DD hh:mm:ss
최대:	현재	날짜	+	5year

Example:
Example

2021-01-10	15:20:59

blacklist_id
blacklist_id: required (integer)
블랙리스트 ID(PK)

src_ip
src_ip: required (string)
출발지 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

ANY는	문자열	ANY로	출력

src_port
src_port: required (integer - minimum: 0 - maximum: 65535)
출발지 포트
prtc  속성값이 TCP 또는 UDP 일 때 설정 가능. 그 외는 ANY로 설정

ANY는	문자열	ANY로	출력

dst_ip
dst_ip: required (string)
목적지 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

ANY는	문자열	ANY로	출력

dst_port
dst_port: required (integer - minimum: 0 - maximum: 65535)
목적지 포트
prtc  속성값이 TCP 또는 UDP 일 때 설정 가능. 그 외는 ANY로 설정

ANY는	문자열	ANY로	출력

prtcprtc: required (string)
프로토콜 이름
ANY는	문자열	ANY로	출력

Example:
Example

TCP

cfg_adm: required (string)
cfg_adm
최종으로 편집한 관리자 ID

Example:
Example

admin

cfg_layer: required (one of 0, 1)
cfg_layer
TAMS 연동을 위한 속성
1:	TASMS	연동	데이터

cfg_dt
cfg_dt: required (string)
최종 수정일

Example:
Example

{
		"code":	"ok",
		"result":	{
				"count":	1,
				"blist":[
						{
								"blacklist_id":	13,
								"ip_ver":	1,
								"src_ip":	"192.168.167.220",
								"src_port":	8080,
								"dst_ip":	"ANY",
								"dst_port":	"ANY",
								"prtc":	"TCP",
								"block_expire_tm":	"2026-06-18	23:59:59",
								"desc":	null,
								"cfg_adm":	"admin",
								"cfg_dt":	"2021-06-18	13:19:18",
								"cfg_layer":	0
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/blacklist/ips

블랙리스트를 추가한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
ip_ver
ip_ver: required (one of 1, 2)
IP 버전
1:	IPv4,	2:	IPv6

descdesc: (string - minLength: 0 - maxLength: 128)
설명

block_expire_tm
block_expire_tm: required (string)
차단 만료 시간
포맷: YYYY-MM-DD hh:mm:ss
최대:	현재	날짜	+	5year

Example:
Example

2021-01-10	15:20:59

src_ip
src_ip: required (string)
출발지 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

ANY는	문자열	0으로	설정

src_port
src_port: required (integer - minimum: 0 - maximum: 65535)
출발지 포트
prtc  속성값이 TCP 또는 UDP 일 때 설정 가능. 그 외는 ANY로 설정

ANY는	숫자	99999로	설정

dst_ip
dst_ip: required (string)
목적지 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

ANY는	문자열	0으로	설정

dst_port
dst_port: required (integer - minimum: 0 - maximum: 65535)
목적지 포트
prtc  속성값이 TCP 또는 UDP 일 때 설정 가능. 그 외는 ANY로 설정

ANY는	숫자	99999로	설정

prtcprtc: required (integer)
프로토콜 목록 조회 REST API(서비스 객체 문서 참고):
[GET]https://[IP]/api/op/service/protocols

출력결과에서 prtc 속성값으로 설정
예시) 6: TCP, 17: UDP

ANY는	숫자	99999로	설정

Example:
Example

{
		"ip_ver":	1,
		"src_ip":	"192.168.45.12",
		"src_port":	8080,
		"dst_ip":	"0",
		"dst_port":	99999,
		"prtc":	6,
		"desc":	"프로토콜	TCP로	설정",
		"block_expire_tm":	"2023-01-10	23:59:59"
}

Response

HTTP status code 200

블랙리스트 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

ip_ver: required (one of 1, 2)
ip_ver
IP 버전
1:	IPv4,	2:	IPv6

descdesc: (string - minLength: 0 - maxLength: 128)
설명

block_expire_tm: required (string)
block_expire_tm
차단 만료 시간
포맷: YYYY-MM-DD hh:mm:ss
최대:	현재	날짜	+	5year

Example:
Example

2021-01-10	15:20:59

blacklist_id
blacklist_id: required (integer)
블랙리스트 ID(PK)

src_ip
src_ip: required (string)
출발지 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

ANY는	문자열	ANY로	출력

src_port: required (integer - minimum: 0 - maximum: 65535)
src_port
출발지 포트
prtc  속성값이 TCP 또는 UDP 일 때 설정 가능. 그 외는 ANY로 설정

ANY는	문자열	ANY로	출력

dst_ip: required (string)
dst_ip
목적지 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

ANY는	문자열	ANY로	출력

dst_port: required (integer - minimum: 0 - maximum: 65535)
dst_port
목적지 포트
prtc  속성값이 TCP 또는 UDP 일 때 설정 가능. 그 외는 ANY로 설정

ANY는	문자열	ANY로	출력

prtcprtc: required (string)
프로토콜 이름
ANY는	문자열	ANY로	출력

Example:
Example

TCP

cfg_adm
cfg_adm: required (string)
최종으로 편집한 관리자 ID

Example:
Example

admin

cfg_layer
cfg_layer: required (one of 0, 1)
TAMS 연동을 위한 속성
1:	TASMS	연동	데이터

cfg_dt: required (string)
cfg_dt
최종 수정일

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"blacklist_id":	25,
						"ip_ver":	1,
						"src_ip":	"192.168.45.12",
						"src_port":	8080,
						"dst_ip":	"ANY",
						"dst_port":	"ANY",
						"prtc":	"TCP",
						"block_expire_tm":	"2026-08-12	23:59:59",
						"desc":	"프로토콜	TCP로	설정",
						"cfg_adm":	"admin",
						"cfg_dt":	"2018-09-29	18:50:06",
						"cfg_layer":	0
				}
		]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/blacklist/ips

블랙리스트 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"blist_id":	1},
				{"blist_id":	2}
		]
}

Response

HTTP status code 200

블랙리스트 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/blacklist/ips/{blacklist_id}

블랙리스트을(를) 조회한다.

Request

URI Parameters

blacklist_id
blacklist_id: required (integer)
블랙리스트 ID(PK)

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

블랙리스트 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
1:	IPv4,	2:	IPv6

descdesc: (string - minLength: 0 - maxLength: 128)
설명

block_expire_tm: required (string)
block_expire_tm
차단 만료 시간
포맷: YYYY-MM-DD hh:mm:ss
최대:	현재	날짜	+	5year

Example:
Example

2021-01-10	15:20:59

blacklist_id
blacklist_id: required (integer)
블랙리스트 ID(PK)

src_ip
src_ip: required (string)
출발지 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

ANY는	문자열	ANY로	출력

src_port: required (integer - minimum: 0 - maximum: 65535)
src_port

출발지 포트
prtc  속성값이 TCP 또는 UDP 일 때 설정 가능. 그 외는 ANY로 설정

ANY는	문자열	ANY로	출력

dst_ip
dst_ip: required (string)
목적지 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

ANY는	문자열	ANY로	출력

dst_port
dst_port: required (integer - minimum: 0 - maximum: 65535)
목적지 포트
prtc  속성값이 TCP 또는 UDP 일 때 설정 가능. 그 외는 ANY로 설정

ANY는	문자열	ANY로	출력

prtcprtc: required (string)
프로토콜 이름
ANY는	문자열	ANY로	출력

Example:
Example

TCP

cfg_adm
cfg_adm: required (string)
최종으로 편집한 관리자 ID

Example:
Example

admin

cfg_layer
cfg_layer: required (one of 0, 1)
TAMS 연동을 위한 속성
1:	TASMS	연동	데이터

cfg_dt
cfg_dt: required (string)
최종 수정일

Example:
Example

{
		"code":	"ok",
		"result":	{
				"blacklist_id":	25,
				"ip_ver":	1,
				"src_ip":	"192.168.45.12",
				"src_port":	8080,
				"dst_ip":	"ANY",
				"dst_port":	"ANY",
				"prtc":	"TCP",
				"block_expire_tm":	"2026-08-12	23:59:59",
				"desc":	"프로토콜	TCP로	설정",
				"cfg_adm":	"admin",
				"cfg_dt":	"2018-09-29	18:50:06",
				"cfg_layer":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code

결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/blacklist/ips/{blacklist_id}

블랙리스트을(를) 삭제한다.

Request

URI Parameters

blacklist_id
blacklist_id: required (integer)
블랙리스트 ID(PK)

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

블랙리스트 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/blacklist/ips/expired

만료된 블랙리스트 전체을(를) 삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

만료된 블랙리스트 전체 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/blacklist/ips/save

블랙리스트을 PC로 저장합니다.

[Task 진행 상태 조회]
GET	{{url}}/api/co/async/tasks/{{task_id}}

[상태가 성공인 Task에 대해 엑셀 파일을 다운로드]
GET	{{url}}/api/co/async/tasks/{{task_id}}/save

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

keyword
keyword: (string - maxLength: 128)
검색 내용(URI인코딩 처리 필요)

case
case: (one of 0, 1)
대소문자 구분
0:	대소문자	구분	없음,	1:	대소문자	구분

fields
fields: (string)
세미콜론으로 구분된 검색 컬럼 목록

ip_ver	:	버전
src_ip	:	출발지	주소
src_port	:	출발지	포트
dst_ip	:	목적지	주소
dst_port	:	목적지	포트
prtc	:	프로토콜(코드값으로	검색	가능)
desc	:	설명
block_expire_tm	:	차단	만료	시간
cfg_adm	:	관리자
cfg_layer	:	구분(코드값으로	검색	가능)
cfg_dt	:	최근	수정일

Example:
Example

ip_ver;src_ip;src_port;dst_ip;dst_port;prtc;desc

expire
expire: (one of 0, 1 - default: 0)
차단 만료 항목 검색 여부
0:	전체,	1:	차단	만료된	항목

sorted
sorted: (one of 0, 1, 2 - default: 0)
정렬 여부
0,	정렬하지	않음,	1:	오름차순,	2:	내림차순

sort_fields: (string)
sort_fields
단일 컬럼명

Example:
Example

src_ip

columns
columns: (string)
다운로드되는 엑셀 파일 내 테이블 헤더 값으로 사용될 값을 전달한다.(데이터 테이블에 표시되는 컬럼 순으로 지정)
columns 쿼리스트링에 값이 없을 경우 백엔드에 정의된 속성명으로 표시됩니다.
한글이	포함될	수	있기	때문에	UTF-8	인코딩되어야	합니다.

Example:
Example

?columns=(A,B,C)

Response

HTTP status code 202

블랙리스트를 비동기로 파일 생성 시작

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 메시지

idid: required (string)
태스크 ID

status
status: required (string)
태스크 상태
PENDING,	PROCESS,	FAILURE,	REVOKED,	SUCCESS

attributes
attributes: required (string)
태스크 속성

Example:
Example

{
		"code":	"async",
		"result":	{
				"id":	"38520e53-6a3e-473a-88d5-ffb9aa142524",
				"status":	"PENDING",
				"attributes":	{}
		}
}

블랙리스트 일괄 등록

POST

/blacklist/ips/batch

블랙리스트 일괄 등록을 수행한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

filefile: required (string)
업로드된 파일 이름

Example:
Example

{
		"file":	"3d892ed5-15cd-403c-8d72-7039b9b90861.xlsx"
}

Response

HTTP status code 200

블랙리스트 일괄 등록 추가 성공

Body

Media type: application/json
Media type

TypeType: any

Example:
Example

{
		"code":	"ok",
		"result":	{
				"id":	1,
				"status":	"SUCCESS",
				"attributes":	{
						"total":	100,
						"current":	100,
						"errors":	[
								{
										"block_expire_tm":	"",
										"desc":	"",
										"dst_ip":	"host	generator",
										"dst_port":	"True",
										"ip_ver":	3,
										"prtc":	"",
										"src_ip":	"DH_1.1.1.1",
										"src_port":	"1.1.1.1",
										"row":	2,
										"cause":	"잘못된	값이	입력되었습니다.(IP	version	:	3)(13000)"
								}
						]
				}
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

블랙리스트 등록 적용/취소

PUT

/command/blacklist-ips/apply

블랙리스트 등록을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

블랙리스트 등록 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/blacklist-ips/cancel

블랙리스트 등록을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

블랙리스트 등록 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

블랙리스트 예외 IP 등록

GET

/blacklist/exception-ips

블랙리스트 예외 IP 등록 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept

응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

블랙리스트 예외 IP 등록 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of 블랙리스트 예외 IP)
Items: 블랙리스트 예외 IP
Items

ip_ver
ip_ver: required (one of 1, 2)
IP 버전
1:	IPv4,	2:	IPv6

excpt_ip
excpt_ip: required (string)
예외 IP 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

srcsrc: required (one of 0, 1)
출발지 적용 범위
0:	OFF,	1:	ON

dstdst: required (one of 0, 1)
목적지 적용 범위
0:	OFF,	1:	ON

blist_excpt_ip_id
blist_excpt_ip_id: required (integer)
블랙리스트 예외 IP ID(PK)

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"blist_excpt_ip_id":	1,
						"excpt_ip":	"192.168.1.1",
						"ip_ver":	1,
						"src":	1,
						"dst":	1
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/blacklist/exception-ips

블랙리스트 예외 IP 등록를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
ip_ver
ip_ver: required (one of 1, 2)
IP 버전
1:	IPv4,	2:	IPv6

excpt_ip: required (string)
excpt_ip
예외 IP 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

srcsrc: required (one of 0, 1)
출발지 적용 범위
0:	OFF,	1:	ON

dstdst: required (one of 0, 1)
목적지 적용 범위
0:	OFF,	1:	ON

Example:
Example

{
		"excpt_ip":	"192.168.1.1",
		"ip_ver":	1,
		"src":	1,
		"dst":	1
}

Response

HTTP status code 200

블랙리스트 예외 IP 등록 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

ip_ver: required (one of 1, 2)
ip_ver
IP 버전
1:	IPv4,	2:	IPv6

excpt_ip
excpt_ip: required (string)
예외 IP 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

srcsrc: required (one of 0, 1)
출발지 적용 범위
0:	OFF,	1:	ON

dstdst: required (one of 0, 1)
목적지 적용 범위
0:	OFF,	1:	ON

blist_excpt_ip_id
blist_excpt_ip_id: required (integer)
블랙리스트 예외 IP ID(PK)

Example:
Example

{
		"blist_excpt_ip_id":	2,
		"excpt_ip":	"192.168.1.1",
		"ip_ver":	1,
		"src":	1,
		"dst":	1
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/blacklist/exception-ips

블랙리스트 예외 IP 등록 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of 블랙리스트 예외 IP)
수정될 정보 목록(최대 100건)

Items: 블랙리스트 예외 IP
Items

blist_excpt_ip_id: required (integer)
blist_excpt_ip_id
블랙리스트 예외 IP ID(PK)

srcsrc: required (one of 0, 1)
출발지 적용 범위
0:	OFF,	1:	ON

dstdst: required (one of 0, 1)
목적지 적용 범위
0:	OFF,	1:	ON

Example:
Example

{
		"items":	[
				{
						"blist_excpt_ip_id":	1,
						"src":	1,
						"dst":	0
				}
		]
}

Response

HTTP status code 200

블랙리스트 예외 IP 등록 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/blacklist/exception-ips

블랙리스트 예외 IP 등록 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"blist_excpt_ip_id":	1},
				{"blist_excpt_ip_id":	2}
		]
}

Response

HTTP status code 200

블랙리스트 예외 IP 등록 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/blacklist/exception-ips/{blist_excpt_ip_id}

블랙리스트 예외 IP 등록을(를) 조회한다.

Request

URI Parameters

blist_excpt_ip_id
blist_excpt_ip_id: required (integer)
블랙리스트 예외 IP ID(PK)

Example:
Example

10

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

블랙리스트 예외 IP 등록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

ip_ver: required (one of 1, 2)
ip_ver
IP 버전
1:	IPv4,	2:	IPv6

excpt_ip: required (string)
excpt_ip

예외 IP 주소
ip_ver  속성값이 1일 때는 IPv4 주소
ip_ver  속성값이 2일 때는 IPv6 주소

srcsrc: required (one of 0, 1)
출발지 적용 범위
0:	OFF,	1:	ON

dstdst: required (one of 0, 1)
목적지 적용 범위
0:	OFF,	1:	ON

blist_excpt_ip_id
blist_excpt_ip_id: required (integer)
블랙리스트 예외 IP ID(PK)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"blist_excpt_ip_id":	2,
				"excpt_ip":	"192.168.1.1",
				"ip_ver":	1,
				"src":	1,
				"dst":	1
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/blacklist/exception-ips/{blist_excpt_ip_id}

블랙리스트 예외 IP 등록을(를) 삭제한다.

Request

URI Parameters

blist_excpt_ip_id
blist_excpt_ip_id: required (integer)
블랙리스트 예외 IP ID(PK)

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

블랙리스트 예외 IP 등록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/blacklist/exception-ips/save

블랙리스트 예외 IP을 PC로 저장합니다.

[Task 진행 상태 조회]
GET	{{url}}/api/co/async/tasks/{{task_id}}

[상태가 성공인 Task에 대해 엑셀 파일을 다운로드]
GET	{{url}}/api/co/async/tasks/{{task_id}}/save

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

columns
columns: (string)
다운로드되는 엑셀 파일 내 테이블 헤더 값으로 사용될 값을 전달한다.(데이터 테이블에 표시되는 컬럼 순으로 지정)
columns 쿼리스트링에 값이 없을 경우 백엔드에 정의된 속성명으로 표시됩니다.
한글이	포함될	수	있기	때문에	UTF-8	인코딩되어야	합니다.

Example:
Example

?columns=(A,B,C)

Response

HTTP status code 202

블랙리스트 예외 IP를 비동기로 파일 생성 시작

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 메시지

idid: required (string)
태스크 ID

status
status: required (string)
태스크 상태
PENDING,	PROCESS,	FAILURE,	REVOKED,	SUCCESS

attributes
attributes: required (string)
태스크 속성

Example:
Example

{
		"code":	"async",
		"result":	{
				"id":	"38520e53-6a3e-473a-88d5-ffb9aa142524",
				"status":	"PENDING",
				"attributes":	{}
		}
}

[Task 진행 상태 조회]
배치를 실행 후 응답된 태스크 ID로 상태를 체크합니다.
상태:  PENDING,	PROGRESS,	FAILURE,	REVOKED,	SUCCESS

PENDING: 준비 중
PROGRESS: 처리 중
FAILURE: 실패
REVOKED: 중지 성공
SUCCESS: 성공

GET	{{url}}/api/co/async/tasks/{{task_id}}
{ "code": "ok", "result": { "id": "c295d300-9b7f-4c10-9468-6538c0abab66", "status": "SUCCESS", "attributes": { } } }

POST

/blacklist/exception-ips/batch

블랙리스트 예외 IP을 배치 작업을 시작합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

filefile: required (string)
파일 업로드 후 응답 데이터의 파일명을 인자로 전달

[파일 업로드 API]
POST	{{url}}/api/co/file/import

Example:
Example

{
		"file":	"batch_file.xlsx"
}

Response

HTTP status code 202

블랙리스트 예외 IP 배치 실행

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

idid: required (string)
비동기 태스크 ID

Example:
Example

f810f701-e732-4698-a5a9-a93000a828d9

status
status: required (string)
상태( PENDING,	PROGRESS,	FAILURE,	REVOKED,	SUCCESS )

PENDING: 준비 중
PROGRESS: 처리 중
FAILURE: 실패
REVOKED: 중지 성공
SUCCESS: 성공

attributes: required (object)
attributes

부가 정보

Example:
Example

{
		"code":	"async",
		"result":	{
				"id":	"f810f701-e732-4698-a5a9-a93000a828d9",
				"status":	"PENDING",
				"attributes":	{}
		}
}

HTTP status code 400

블랙리스트 예외 IP 배치 실행 실패

블랙리스트 예외 IP 등록 적용/취소

PUT

/command/blacklist-exception-ips/apply

블랙리스트 예외 IP 등록을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

블랙리스트 예외 IP 등록 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/blacklist-exception-ips/cancel

블랙리스트 예외 IP 등록을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

블랙리스트 예외 IP 등록 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

현재상태 > 블랙리스트

DELETE

/blacklist/logs

블랙리스트를 삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
del_all
del_all: required (one of 0, 1)
전체 삭제 유무 (삭제:	0,	전체	삭제:	1)

ip_ver
ip_ver: required (one of 1, 2)
IP 버전 종류 (IPv4:	1,	IPv6:	2)

src_ip: required (string)
src_ip
출발지 주소(IPv4 or IPv6 Address) 0은	ANY를	의미한다.

src_port
src_port: required (integer - minimum: 0 - maximum: 65535)
출발지 포트 99999는	ANY를	의미한다.

src_port_e
src_port_e: required (integer - minimum: 0 - maximum: 65535)
출발지 포트 99999는	ANY를	의미한다.

dst_ip
dst_ip: required (string)
목적지 주소(IPv4 or IPv6 Address) 0은	ANY를	의미한다.

dst_port
dst_port: required (integer - minimum: 0 - maximum: 65535)
목록지 포트 99999는	ANY를	의미한다.

dst_port_e
dst_port_e: required (integer - minimum: 0 - maximum: 65535)
목록지 포트 99999는	ANY를	의미한다.

prtcprtc: required (integer - minimum: 0 - maximum: 255)
프로토콜 99999는	ANY를	의미한다.
프로토콜 0은 IPv6 주소일 경우에만 입력이 가능하다.

type
type: required (one of 1, 2, 16, 32, 48, 64, 80, 96, 112, 128)
블랙리스트 타입
user	defined:	1
pre	defined:	2
session	Shaping:	16
Anti-Spam:	32

Application	Control:	48
IAP:	64
zombie:	80
APT:	96
Office	Keeper:	112
SITE	BLIST:	128

Example:
Example

//	블랙리스트	단일	삭제
{
		"del_all":	0,
		"ip_ver":	1,
		"src_ip":	"1.1.1.1",
		"src_port":	0,
		"src_port_e":	0,
		"dst_ip":	"",
		"dst_port":	0,
		"dst_port_e":	0,
		"prtc":	1,
		"type":	32
}

//	IPv4	블랙리스트	전체	삭제
{
		"del_all":	1,
		"ip_ver":	1
}

//	IPv6	블랙리스트	전체	삭제
{
		"del_all":	1,
		"ip_ver":	2
}

Response

HTTP status code 200

현재상태의 블랙리스트 삭제 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

