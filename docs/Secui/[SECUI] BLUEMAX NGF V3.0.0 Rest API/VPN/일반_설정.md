GET

PUT

PUT

PUT

(2.0)Group VPN 일반 설정 REST API 문서

https://{url}/api/vp

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/vp

일반 설정

/groupvpn/general

일반 설정 적용/취소

/command/groupvpn-general/apply

/command/groupvpn-general/cancel

일반 설정

GET

/groupvpn/general

일반 설정을(를) 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

일반 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

max_rexmit_cnt
max_rexmit_cnt: required (integer - default: 4 - minimum: 1 - maximum: 10)
최대 재전송 횟수(회)

sa_chk_intval
sa_chk_intval: required (integer - default: 60 - minimum: 1 - maximum: 180)
SA 점검 간격 시간(초)

rexmit_inc
rexmit_inc: required (integer - default: 2 - minimum: 0 - maximum: 10)
재전송 시간간격 증가값(초)

init_rexmit_tm_out
init_rexmit_tm_out: required (integer - default: 7 - minimum: 1 - maximum: 10)
초기 재전송 타임아웃(초)

key_exchng_port
key_exchng_port: required (integer - default: 500 - minimum: 1 - maximum: 65535)
키 교환 포트

dos_prtect_use
dos_prtect_use: required (one of 0, 1 - default: 0)
IKEv2 DoS 공격 방어 기능 사용 여부
사용안함:	0,	사용	1

dld_tm_out
dld_tm_out: required (integer - default: 10 - minimum: 1 - maximum: 999)
DLD 타임아웃(초)

dld_chk_intval
dld_chk_intval: required (integer - default: 5 - minimum: 1 - maximum: 999)
DLD 검사 간격

max_dld_retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 10)
max_dld_retry_cnt
최대 DLD 시도 횟수

mtu_use
mtu_use: required (one of 0, 1)
IPv4 정책, IPSec MTU 사용 여부
사용안함:	0,	사용	1

mtu_size: (integer - default: 1380 - minimum: 512 - maximum: 1500)
mtu_size
IPv4 정책, IPSec MTU 크기
IPSec MTU Block Fragmentaion 사용 시 설정

Example:
Example

{
		"code":	"ok",
		"result":	{
				"max_rexmit_cnt":	4,
				"sa_chk_intval":	60,
				"rexmit_inc":	2,
				"init_rexmit_tm_out":	7,
				"key_exchng_port":	500,
				"dos_prtect_use":	0,
				"dld_tm_out":	10,
				"dld_chk_intval":	5,
				"max_dld_retry_cnt":	3,
				"mtu_use":	1,
				"mtu_size":	1380
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/groupvpn/general

일반 설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

max_rexmit_cnt
max_rexmit_cnt: required (integer - default: 4 - minimum: 1 - maximum: 10)
최대 재전송 횟수(회)

sa_chk_intval
sa_chk_intval: required (integer - default: 60 - minimum: 1 - maximum: 180)
SA 점검 간격 시간(초)

rexmit_inc: required (integer - default: 2 - minimum: 0 - maximum: 10)
rexmit_inc
재전송 시간간격 증가값(초)

init_rexmit_tm_out
init_rexmit_tm_out: required (integer - default: 7 - minimum: 1 - maximum: 10)
초기 재전송 타임아웃(초)

key_exchng_port: required (integer - default: 500 - minimum: 1 - maximum: 65535)
key_exchng_port
키 교환 포트

dos_prtect_use
dos_prtect_use: required (one of 0, 1 - default: 0)
IKEv2 DoS 공격 방어 기능 사용 여부
사용안함:	0,	사용	1

dld_tm_out: required (integer - default: 10 - minimum: 1 - maximum: 999)
dld_tm_out
DLD 타임아웃(초)

dld_chk_intval
dld_chk_intval: required (integer - default: 5 - minimum: 1 - maximum: 999)
DLD 검사 간격

max_dld_retry_cnt
max_dld_retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 10)
최대 DLD 시도 횟수

mtu_use
mtu_use: required (one of 0, 1)
IPv4 정책, IPSec MTU 사용 여부
사용안함:	0,	사용	1

mtu_size: (integer - default: 1380 - minimum: 512 - maximum: 1500)
mtu_size

IPv4 정책, IPSec MTU 크기
IPSec MTU Block Fragmentaion 사용 시 설정

Example:
Example

{
		"max_rexmit_cnt":	4,
		"sa_chk_intval":	60,
		"rexmit_inc":	2,
		"init_rexmit_tm_out":	7,
		"key_exchng_port":	500,
		"dos_prtect_use":	0,
		"dld_tm_out":	10,
		"dld_chk_intval":	5,
		"max_dld_retry_cnt":	3,
		"mtu_use":	1,
		"mtu_size":	1380
}

Response

HTTP status code 200

일반 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

max_rexmit_cnt
max_rexmit_cnt: required (integer - default: 4 - minimum: 1 - maximum: 10)
최대 재전송 횟수(회)

sa_chk_intval
sa_chk_intval: required (integer - default: 60 - minimum: 1 - maximum: 180)
SA 점검 간격 시간(초)

rexmit_inc: required (integer - default: 2 - minimum: 0 - maximum: 10)
rexmit_inc
재전송 시간간격 증가값(초)

init_rexmit_tm_out
init_rexmit_tm_out: required (integer - default: 7 - minimum: 1 - maximum: 10)
초기 재전송 타임아웃(초)

key_exchng_port
key_exchng_port: required (integer - default: 500 - minimum: 1 - maximum: 65535)
키 교환 포트

dos_prtect_use
dos_prtect_use: required (one of 0, 1 - default: 0)
IKEv2 DoS 공격 방어 기능 사용 여부
사용안함:	0,	사용	1

dld_tm_out
dld_tm_out: required (integer - default: 10 - minimum: 1 - maximum: 999)
DLD 타임아웃(초)

dld_chk_intval
dld_chk_intval: required (integer - default: 5 - minimum: 1 - maximum: 999)
DLD 검사 간격

max_dld_retry_cnt
max_dld_retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 10)
최대 DLD 시도 횟수

mtu_use
mtu_use: required (one of 0, 1)
IPv4 정책, IPSec MTU 사용 여부
사용안함:	0,	사용	1

mtu_size
mtu_size: (integer - default: 1380 - minimum: 512 - maximum: 1500)
IPv4 정책, IPSec MTU 크기
IPSec MTU Block Fragmentaion 사용 시 설정

Example:
Example

{
		"code":	"ok",
		"result":	{
				"max_rexmit_cnt":	4,
				"sa_chk_intval":	60,
				"rexmit_inc":	2,
				"init_rexmit_tm_out":	7,
				"key_exchng_port":	500,
				"dos_prtect_use":	0,
				"dld_tm_out":	10,
				"dld_chk_intval":	5,
				"max_dld_retry_cnt":	3,
				"mtu_use":	1,
				"mtu_size":	1380
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

일반 설정 적용/취소

PUT

/command/groupvpn-general/apply

일반 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

일반 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/groupvpn-general/cancel

일반 설정을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

일반 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

