GET

POST

PUT

  DELETE

GET

PUT

  DELETE

PUT

PUT

PUT

지점 연결 설정 REST API 문서

https://{host}/api/vp

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

지점 연결 설정

/ipsec/4/branches

/ipsec/4/branches/{pk}

/ipsec/4/bulk/branches

지점 연결 설정 적용/취소

/command/ipsec-ipv4-branches/apply

/command/ipsec-ipv4-branches/cancel

지점 연결 설정

GET

/ipsec/4/branches

지점 연결 설정 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

지점 연결 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ResIpsecBranch)
결과 데이터

Items: ResIpsecBranch
Items

vpn_policy: required (object)
vpn_policy

vpn_policy_id
vpn_policy_id: required (integer)
보안 정책 설정 ID

name
name: required (string)
보안 정책 설정 이름

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

name: required (string)
name
연결 이름

center_ip
center_ip: required (string)
센터 장비IP 주소

auth_type
auth_type: required (one of 0, 1 - default: 0)
인증 방법
사전	공유키:	0,	RSA	인증서:	1

idid: required (string)
지점 ID

descdesc: required (string)
설명

interconn_use
interconn_use: required (one of 0, 1 - default: 0)
다른 장비 접속
사용안함:	0,	사용:	1

divdiv: required (one of 0, 1 - default: 0)
구분
Initiator:	0,	Rseponder:	1

conn_mode
conn_mode: required (one of 0, 1, 2)
연결 모드
Aggressive:	0,	Main:	1,	IKEv2:	2

local_ip
local_ip: required (string)
로컬IP 주소

op_mode
op_mode: required (one of 0, 1, 2, 3 - default: 0)
동작 모드
ESP-Tunnel:	0,	ESP-Transport:	1,	AH-Tunnel:	2,	AH-Transport:	3

lftmlftm: required (integer - default: 80000 - minimum: 60 - maximum: 500000)
생명 주기

ike_lftm
ike_lftm: required (integer - default: 160000 - minimum: 120 - maximum: 1000000)
IKE 생명 주기

peer_ike_port
peer_ike_port: required (integer - default: 500 - minimum: 1 - maximum: 65535)
센터 IKE 포트

udp_encap
udp_encap: required (one of 0, 1 - default: 0)
UDP encap 강제 적용
사용안함:	0,	사용:	1

extnd_mode_use
extnd_mode_use: required (integer)
확장 모드 여부
0:	OFF,	1:	ON

center: required (array of 주소 객체)
center
센터 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone
zone: required (one of 1, 2, 3)
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

branch
branch: required (array of 주소 객체)
지점 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone: required (one of 1, 2, 3)
zone

Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1: required (string)
ip_info1
IP 주소

ip_info2
ip_info2: required (string)
서브넷

center_branch: required (array of CenterBranch)
center_branch
센터/지점 객체
확장 모드가  OFF  일 경우 설정

Items: CenterBranch
Items

center
center: required (object)
외부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone: required (one of 1, 2, 3)
zone
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type: required (one of 1, 2, 4)
addr_obj_type
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

branch
branch: required (object)
내부망	객체

addr_obj_id: required (integer)
addr_obj_id
객체 ID(PK)

name
name: required (string)
객체 이름

zone
zone: required (one of 1, 2, 3)
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

vpn_branch_id
vpn_branch_id: required (integer)
지점 연결 설정 ID

ip_ver
ip_ver: required (one of 1, 2)
지점 연결 설정 유형
IPv4:	1,	IPv6:	2

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"vpn_branch_id":	1,
						"vpn_policy":	{
								"vpn_policy_id":	1,
								"name":	"ARIA-128-SHA-256-Group14"
						},
						"ip_ver":	1,
						"use":	0,
						"name":	"지점	연결	1",
						"center_ip":	"1.1.1.1,2.2.2.2",
						"auth_type":	0,
						"id":	"branch1",
						"desc":	"설명",
						"interconn_use":	0,
						"div":	0,
						"conn_mode":	0,
						"local_ip":	"2.2.2.2",
						"op_mode":	0,
						"lftm":	80000,
						"ike_lftm":	160000,
						"peer_ike_port":	500,
						"udp_encap":	0,
						"extnd_mode_use":	1,
						"center":	[
								{
										"addr_obj_id":	1,
										"name":	"192.168.110.10_IH",
										"zone":	1,
										"desc":	"",
										"addr_obj_type":	1,
										"ip_info1":	"192.168.110.10",
										"ip_info2":	"32"
								}
						],
						"branch":	[
								{
										"addr_obj_id":	1,
										"name":	"192.168.110.12_IH",
										"zone":	1,
										"desc":	"",
										"addr_obj_type":	1,
										"ip_info1":	"192.168.110.12",
										"ip_info2":	"32"
								}
						],
						"center_branch":	[
								{
										"center":	{
												"addr_obj_id":	1,
												"name":	"192.168.110.12_IH",
												"zone":	1,
												"desc":	"",
												"addr_obj_type":	1,
												"ip_info1":	"192.168.110.12",
												"ip_info2":	"32"
										},
										"branch":	{
												"addr_obj_id":	1,
												"name":	"192.168.110.10_IH",
												"zone":	1,
												"desc":	"",
												"addr_obj_type":	1,
												"ip_info1":	"192.168.110.10",
												"ip_info2":	"32"
										}
								}
						]
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/ipsec/4/branches

지점 연결 설정를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

vpn_policy: required (object)
vpn_policy

vpn_policy_id
vpn_policy_id: required (integer)
보안 정책 설정 ID

name
name: required (string)
보안 정책 설정 이름

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

name: required (string)
name
연결 이름

center_ip
center_ip: required (string)
센터 장비IP 주소

auth_type
auth_type: required (one of 0, 1 - default: 0)
인증 방법
사전	공유키:	0,	RSA	인증서:	1

idid: required (string)
지점 ID

passwd
passwd: required (string)
인증 비밀번호

descdesc: required (string)
설명

interconn_use
interconn_use: required (one of 0, 1 - default: 0)
다른 장비 접속
사용안함:	0,	사용:	1

divdiv: required (one of 0, 1 - default: 0)
구분
Initiator:	0,	Rseponder:	1

conn_mode: required (one of 0, 1, 2)
conn_mode
연결 모드
Aggressive:	0,	Main:	1,	IKEv2:	2

local_ip
local_ip: required (string)
로컬IP 주소

op_mode: required (one of 0, 1, 2, 3 - default: 0)
op_mode
동작 모드
ESP-Tunnel:	0,	ESP-Transport:	1,	AH-Tunnel:	2,	AH-Transport:	3

lftmlftm: required (integer - default: 80000 - minimum: 60 - maximum: 500000)
생명 주기

ike_lftm: required (integer - default: 160000 - minimum: 120 - maximum: 1000000)
ike_lftm
IKE 생명 주기

peer_ike_port
peer_ike_port: required (integer - default: 500 - minimum: 1 - maximum: 65535)
센터 IKE 포트

udp_encap
udp_encap: required (one of 0, 1 - default: 0)
UDP encap 강제 적용
사용안함:	0,	사용:	1

extnd_mode_use
extnd_mode_use: required (integer)
확장 모드 여부
0:	OFF,	1:	ON

center: required (array of 주소 객체)
center
센터 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id: required (integer)
addr_obj_id
객체 ID(PK)

branch
branch: required (array of 주소 객체)
지점 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

center_branch: required (array of SetCenterBranch)
center_branch
센터/지점 객체
확장 모드가  OFF  일 경우 설정

Items: SetCenterBranch
Items

center: required (object)
center
외부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

branch
branch: required (object)
내부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

Example:
Example

{
		"vpn_policy":	{
				"vpn_policy_id":	1,
				"name":	"ARIA-128-SHA-256-Group14"
		},
		"use":	0,
		"name":	"지점	연결	1",
		"center_ip":	"1.1.1.1,2.2.2.2",
		"auth_type":	0,
		"id":	"branch1",
		"passwd":	"password",
		"desc":	"설명",
		"interconn_use":	0,
		"div":	0,
		"conn_mode":	0,
		"local_ip":	"2.2.2.2",
		"op_mode":	0,
		"lftm":	80000,
		"ike_lftm":	160000,
		"peer_ike_port":	500,
		"udp_encap":	0,
		"extnd_mode_use":	1,
		"center":	[
				{
						"addr_obj_id":	10
				}
		],
		"branch":	[
				{
						"addr_obj_id":	1
				}
		],
		"center_branch":	[
				{
						"branch":	{
								"addr_obj_id":	115
						},
						"center":	{
								"addr_obj_id":	63
						}
				}
		]
}

Response

HTTP status code 200

지점 연결 설정 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

vpn_policy: required (object)
vpn_policy

vpn_policy_id
vpn_policy_id: required (integer)
보안 정책 설정 ID

name
name: required (string)
보안 정책 설정 이름

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

name
name: required (string)
연결 이름

center_ip
center_ip: required (string)
센터 장비IP 주소

auth_type: required (one of 0, 1 - default: 0)
auth_type
인증 방법
사전	공유키:	0,	RSA	인증서:	1

idid: required (string)
지점 ID

descdesc: required (string)
설명

interconn_use
interconn_use: required (one of 0, 1 - default: 0)
다른 장비 접속
사용안함:	0,	사용:	1

divdiv: required (one of 0, 1 - default: 0)
구분
Initiator:	0,	Rseponder:	1

conn_mode
conn_mode: required (one of 0, 1, 2)
연결 모드
Aggressive:	0,	Main:	1,	IKEv2:	2

local_ip
local_ip: required (string)
로컬IP 주소

op_mode
op_mode: required (one of 0, 1, 2, 3 - default: 0)
동작 모드
ESP-Tunnel:	0,	ESP-Transport:	1,	AH-Tunnel:	2,	AH-Transport:	3

lftmlftm: required (integer - default: 80000 - minimum: 60 - maximum: 500000)
생명 주기

ike_lftm
ike_lftm: required (integer - default: 160000 - minimum: 120 - maximum: 1000000)
IKE 생명 주기

peer_ike_port
peer_ike_port: required (integer - default: 500 - minimum: 1 - maximum: 65535)
센터 IKE 포트

udp_encap
udp_encap: required (one of 0, 1 - default: 0)
UDP encap 강제 적용
사용안함:	0,	사용:	1

extnd_mode_use
extnd_mode_use: required (integer)
확장 모드 여부
0:	OFF,	1:	ON

center
center: required (array of 주소 객체)
센터 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone
zone: required (one of 1, 2, 3)
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

branch
branch: required (array of 주소 객체)
지점 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone: required (one of 1, 2, 3)
zone
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type: required (one of 1, 2, 4)
addr_obj_type
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

center_branch
center_branch: required (array of CenterBranch)
센터/지점 객체
확장 모드가  OFF  일 경우 설정

Items: CenterBranch
Items

center
center: required (object)
외부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone
zone: required (one of 1, 2, 3)
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

branch
branch: required (object)
내부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone: required (one of 1, 2, 3)
zone
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

vpn_branch_id
vpn_branch_id: required (integer)
지점 연결 설정 ID

ip_ver
ip_ver: required (one of 1, 2)
지점 연결 설정 유형
IPv4:	1,	IPv6:	2

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"vpn_branch_id":	1,
						"vpn_policy":	{
								"vpn_policy_id":	1,
								"name":	"ARIA-128-SHA-256-Group14"
						},
						"ip_ver":	1,
						"use":	0,
						"name":	"지점	연결	1",
						"center_ip":	"1.1.1.1,2.2.2.2",
						"auth_type":	0,
						"id":	"branch1",
						"desc":	"설명",
						"interconn_use":	0,
						"div":	0,
						"conn_mode":	0,
						"local_ip":	"2.2.2.2",
						"op_mode":	0,
						"lftm":	80000,
						"ike_lftm":	160000,
						"peer_ike_port":	500,
						"udp_encap":	0,
						"extnd_mode_use":	1,
						"center":	[
								{
										"addr_obj_id":	1,
										"name":	"192.168.110.10_IH",
										"zone":	1,
										"desc":	"",
										"addr_obj_type":	1,
										"ip_info1":	"192.168.110.10",
										"ip_info2":	"32"
								}
						],
						"branch":	[
								{
										"addr_obj_id":	1,
										"name":	"192.168.110.12_IH",
										"zone":	1,
										"desc":	"",
										"addr_obj_type":	1,
										"ip_info1":	"192.168.110.12",
										"ip_info2":	"32"
								}
						],
						"center_branch":	[
								{
										"center":	{
												"addr_obj_id":	1,
												"name":	"192.168.110.12_IH",
												"zone":	1,
												"desc":	"",
												"addr_obj_type":	1,
												"ip_info1":	"192.168.110.12",
												"ip_info2":	"32"
										},
										"branch":	{
												"addr_obj_id":	1,
												"name":	"192.168.110.10_IH",
												"zone":	1,
												"desc":	"",
												"addr_obj_type":	1,
												"ip_info1":	"192.168.110.10",
												"ip_info2":	"32"
										}
								}
						]
				}
		]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/ipsec/4/branches

지점 연결 설정 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of ReqIpsecBranchList)
수정 아이템 목록

Items: ReqIpsecBranchList
Items

vpn_branch_id
vpn_branch_id: required (integer)
지점 연결 설정 ID

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

Example:
Example

{
		"items":	[
				{"vpn_branch_id":	1,	"use":	1}
		]
}

Response

HTTP status code 200

지점 연결 설정 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result: required (object)
result
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/ipsec/4/branches

지점 연결 설정 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제 아이템 목록

Example:
Example

{
		"items":	[
				{"vpn_branch_id":	1},
				{"vpn_branch_id":	2}
		]
}

Response

HTTP status code 200

지점 연결 설정 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
삭제 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/ipsec/4/branches/{pk}

지점 연결 설정을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
지점 연결 아이디 ( vpn_branch_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

지점 연결 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

vpn_policy: required (object)
vpn_policy

vpn_policy_id
vpn_policy_id: required (integer)
보안 정책 설정 ID

name
name: required (string)
보안 정책 설정 이름

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

name
name: required (string)
연결 이름

center_ip
center_ip: required (string)
센터 장비IP 주소

auth_type
auth_type: required (one of 0, 1 - default: 0)
인증 방법
사전	공유키:	0,	RSA	인증서:	1

idid: required (string)
지점 ID

descdesc: required (string)
설명

interconn_use
interconn_use: required (one of 0, 1 - default: 0)
다른 장비 접속
사용안함:	0,	사용:	1

divdiv: required (one of 0, 1 - default: 0)
구분
Initiator:	0,	Rseponder:	1

conn_mode
conn_mode: required (one of 0, 1, 2)
연결 모드
Aggressive:	0,	Main:	1,	IKEv2:	2

local_ip
local_ip: required (string)
로컬IP 주소

op_mode
op_mode: required (one of 0, 1, 2, 3 - default: 0)
동작 모드
ESP-Tunnel:	0,	ESP-Transport:	1,	AH-Tunnel:	2,	AH-Transport:	3

lftmlftm: required (integer - default: 80000 - minimum: 60 - maximum: 500000)
생명 주기

ike_lftm
ike_lftm: required (integer - default: 160000 - minimum: 120 - maximum: 1000000)
IKE 생명 주기

peer_ike_port
peer_ike_port: required (integer - default: 500 - minimum: 1 - maximum: 65535)
센터 IKE 포트

udp_encap
udp_encap: required (one of 0, 1 - default: 0)
UDP encap 강제 적용
사용안함:	0,	사용:	1

extnd_mode_use
extnd_mode_use: required (integer)
확장 모드 여부
0:	OFF,	1:	ON

center: required (array of 주소 객체)
center
센터 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone
zone: required (one of 1, 2, 3)
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

branch
branch: required (array of 주소 객체)
지점 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone
zone: required (one of 1, 2, 3)
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type: required (one of 1, 2, 4)
addr_obj_type
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

center_branch: required (array of CenterBranch)
center_branch

센터/지점 객체
확장 모드가  OFF  일 경우 설정

Items: CenterBranch
Items

center
center: required (object)
외부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone
zone: required (one of 1, 2, 3)
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

branch
branch: required (object)
내부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone: required (one of 1, 2, 3)
zone
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type: required (one of 1, 2, 4)
addr_obj_type
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

vpn_branch_id
vpn_branch_id: required (integer)
지점 연결 설정 ID

ip_ver
ip_ver: required (one of 1, 2)
지점 연결 설정 유형
IPv4:	1,	IPv6:	2

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"vpn_branch_id":	1,
						"vpn_policy":	{
								"vpn_policy_id":	1,
								"name":	"ARIA-128-SHA-256-Group14"
						},
						"ip_ver":	1,
						"use":	0,
						"name":	"지점	연결	1",
						"center_ip":	"1.1.1.1,2.2.2.2",
						"auth_type":	0,
						"id":	"branch1",
						"desc":	"설명",
						"interconn_use":	0,
						"div":	0,
						"conn_mode":	0,
						"local_ip":	"2.2.2.2",
						"op_mode":	0,
						"lftm":	80000,
						"ike_lftm":	160000,
						"peer_ike_port":	500,
						"udp_encap":	0,
						"extnd_mode_use":	1,
						"center":	[
								{
										"addr_obj_id":	1,
										"name":	"192.168.110.10_IH",
										"zone":	1,
										"desc":	"",
										"addr_obj_type":	1,
										"ip_info1":	"192.168.110.10",
										"ip_info2":	"32"
								}
						],
						"branch":	[
								{
										"addr_obj_id":	1,
										"name":	"192.168.110.12_IH",
										"zone":	1,
										"desc":	"",
										"addr_obj_type":	1,
										"ip_info1":	"192.168.110.12",
										"ip_info2":	"32"
								}
						],
						"center_branch":	[
								{
										"center":	{
												"addr_obj_id":	1,
												"name":	"192.168.110.12_IH",
												"zone":	1,
												"desc":	"",
												"addr_obj_type":	1,
												"ip_info1":	"192.168.110.12",
												"ip_info2":	"32"
										},
										"branch":	{
												"addr_obj_id":	1,
												"name":	"192.168.110.10_IH",
												"zone":	1,
												"desc":	"",
												"addr_obj_type":	1,
												"ip_info1":	"192.168.110.10",
												"ip_info2":	"32"
										}
								}
						]
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/ipsec/4/branches/{pk}

지점 연결 설정을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
지점 연결 아이디 ( vpn_branch_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

vpn_policy: required (object)
vpn_policy

vpn_policy_id
vpn_policy_id: required (integer)
보안 정책 설정 ID

name
name: required (string)
보안 정책 설정 이름

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

name
name: required (string)
연결 이름

center_ip
center_ip: required (string)
센터 장비IP 주소

auth_type
auth_type: required (one of 0, 1 - default: 0)
인증 방법
사전	공유키:	0,	RSA	인증서:	1

idid: required (string)
지점 ID

passwd
passwd: required (string)
인증 비밀번호

descdesc: required (string)
설명

interconn_use
interconn_use: required (one of 0, 1 - default: 0)
다른 장비 접속
사용안함:	0,	사용:	1

divdiv: required (one of 0, 1 - default: 0)
구분
Initiator:	0,	Rseponder:	1

conn_mode
conn_mode: required (one of 0, 1, 2)
연결 모드
Aggressive:	0,	Main:	1,	IKEv2:	2

local_ip
local_ip: required (string)
로컬IP 주소

op_mode
op_mode: required (one of 0, 1, 2, 3 - default: 0)
동작 모드
ESP-Tunnel:	0,	ESP-Transport:	1,	AH-Tunnel:	2,	AH-Transport:	3

lftmlftm: required (integer - default: 80000 - minimum: 60 - maximum: 500000)
생명 주기

ike_lftm
ike_lftm: required (integer - default: 160000 - minimum: 120 - maximum: 1000000)
IKE 생명 주기

peer_ike_port
peer_ike_port: required (integer - default: 500 - minimum: 1 - maximum: 65535)
센터 IKE 포트

udp_encap
udp_encap: required (one of 0, 1 - default: 0)
UDP encap 강제 적용
사용안함:	0,	사용:	1

extnd_mode_use
extnd_mode_use: required (integer)
확장 모드 여부
0:	OFF,	1:	ON

center
center: required (array of 주소 객체)
센터 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

branch
branch: required (array of 주소 객체)
지점 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

center_branch
center_branch: required (array of SetCenterBranch)
센터/지점 객체
확장 모드가  OFF  일 경우 설정

Items: SetCenterBranch
Items

center
center: required (object)
외부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

branch
branch: required (object)
내부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

Example:
Example

{
		"vpn_policy":	{
				"vpn_policy_id":	1,
				"name":	"ARIA-128-SHA-256-Group14"
		},
		"use":	0,
		"name":	"지점	연결	1",
		"center_ip":	"1.1.1.1,2.2.2.2",
		"auth_type":	0,
		"id":	"branch1",
		"passwd":	"password",
		"desc":	"설명",
		"interconn_use":	0,
		"div":	0,
		"conn_mode":	0,
		"local_ip":	"2.2.2.2",
		"op_mode":	0,
		"lftm":	80000,
		"ike_lftm":	160000,
		"peer_ike_port":	500,
		"udp_encap":	0,
		"extnd_mode_use":	1,
		"center":	[
				{
						"addr_obj_id":	10
				}
		],
		"branch":	[
				{
						"addr_obj_id":	1
				}
		],
		"center_branch":	[
				{
						"branch":	{
								"addr_obj_id":	115
						},
						"center":	{
								"addr_obj_id":	63
						}
				}
		]
}

Response

HTTP status code 200

지점 연결 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

vpn_policy: required (object)
vpn_policy

vpn_policy_id
vpn_policy_id: required (integer)
보안 정책 설정 ID

name
name: required (string)
보안 정책 설정 이름

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

name
name: required (string)
연결 이름

center_ip
center_ip: required (string)
센터 장비IP 주소

auth_type: required (one of 0, 1 - default: 0)
auth_type
인증 방법
사전	공유키:	0,	RSA	인증서:	1

idid: required (string)
지점 ID

descdesc: required (string)
설명

interconn_use
interconn_use: required (one of 0, 1 - default: 0)
다른 장비 접속
사용안함:	0,	사용:	1

divdiv: required (one of 0, 1 - default: 0)
구분
Initiator:	0,	Rseponder:	1

conn_mode
conn_mode: required (one of 0, 1, 2)
연결 모드
Aggressive:	0,	Main:	1,	IKEv2:	2

local_ip
local_ip: required (string)
로컬IP 주소

op_mode
op_mode: required (one of 0, 1, 2, 3 - default: 0)
동작 모드
ESP-Tunnel:	0,	ESP-Transport:	1,	AH-Tunnel:	2,	AH-Transport:	3

lftmlftm: required (integer - default: 80000 - minimum: 60 - maximum: 500000)
생명 주기

ike_lftm
ike_lftm: required (integer - default: 160000 - minimum: 120 - maximum: 1000000)
IKE 생명 주기

peer_ike_port
peer_ike_port: required (integer - default: 500 - minimum: 1 - maximum: 65535)
센터 IKE 포트

udp_encap
udp_encap: required (one of 0, 1 - default: 0)
UDP encap 강제 적용
사용안함:	0,	사용:	1

extnd_mode_use
extnd_mode_use: required (integer)
확장 모드 여부
0:	OFF,	1:	ON

center
center: required (array of 주소 객체)
센터 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone
zone: required (one of 1, 2, 3)
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

branch
branch: required (array of 주소 객체)
지점 설정
확장 모드가  ON  일 경우 설정

Items: 주소 객체
Items

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone: required (one of 1, 2, 3)
zone
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type: required (one of 1, 2, 4)
addr_obj_type
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

center_branch
center_branch: required (array of CenterBranch)
센터/지점 객체
확장 모드가  OFF  일 경우 설정

Items: CenterBranch
Items

center
center: required (object)
외부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone
zone: required (one of 1, 2, 3)
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

branch
branch: required (object)
내부망	객체

addr_obj_id
addr_obj_id: required (integer)
객체 ID(PK)

name
name: required (string)
객체 이름

zone: required (one of 1, 2, 3)
zone
Zone 정보
1:	내부,	2:	외부,	3:	DMZ

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (one of 1, 2, 4)
객체 타입
호스트객체:	1
네트워크객체(IP	주소	형식):	2
그룹객체:	4

ip_info1
ip_info1: required (string)
IP 주소

ip_info2
ip_info2: required (string)
서브넷

vpn_branch_id
vpn_branch_id: required (integer)
지점 연결 설정 ID

ip_ver
ip_ver: required (one of 1, 2)
지점 연결 설정 유형
IPv4:	1,	IPv6:	2

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"vpn_branch_id":	1,
						"vpn_policy":	{
								"vpn_policy_id":	1,
								"name":	"ARIA-128-SHA-256-Group14"
						},
						"ip_ver":	1,
						"use":	0,
						"name":	"지점	연결	1",
						"center_ip":	"1.1.1.1,2.2.2.2",
						"auth_type":	0,
						"id":	"branch1",
						"desc":	"설명",
						"interconn_use":	0,
						"div":	0,
						"conn_mode":	0,
						"local_ip":	"2.2.2.2",
						"op_mode":	0,
						"lftm":	80000,
						"ike_lftm":	160000,
						"peer_ike_port":	500,
						"udp_encap":	0,
						"extnd_mode_use":	1,
						"center":	[
								{
										"addr_obj_id":	1,
										"name":	"192.168.110.10_IH",
										"zone":	1,
										"desc":	"",
										"addr_obj_type":	1,
										"ip_info1":	"192.168.110.10",
										"ip_info2":	"32"
								}
						],
						"branch":	[
								{
										"addr_obj_id":	1,
										"name":	"192.168.110.12_IH",
										"zone":	1,
										"desc":	"",
										"addr_obj_type":	1,
										"ip_info1":	"192.168.110.12",
										"ip_info2":	"32"
								}
						],
						"center_branch":	[
								{
										"center":	{
												"addr_obj_id":	1,
												"name":	"192.168.110.12_IH",
												"zone":	1,
												"desc":	"",
												"addr_obj_type":	1,
												"ip_info1":	"192.168.110.12",
												"ip_info2":	"32"
										},
										"branch":	{
												"addr_obj_id":	1,
												"name":	"192.168.110.10_IH",
												"zone":	1,
												"desc":	"",
												"addr_obj_type":	1,
												"ip_info1":	"192.168.110.10",
												"ip_info2":	"32"
										}
								}
						]
				}
		]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/ipsec/4/branches/{pk}

지점 연결 설정을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
지점 연결 아이디 ( vpn_branch_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

지점 연결 설정 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/ipsec/4/bulk/branches

사용 여부를 일괄 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1)
사용 여부

items: required (array of 지점 연결 설정)
items
일괄 수정 대상 목록

Items: 지점 연결 설정
Items

vpn_branch_id
vpn_branch_id: required (integer)
지점 연결 설정 ID

Example:
Example

{
		"use":	1,
		"items":	[
				{
						"vpn_branch_id":	1
				},
				{
						"vpn_branch_id":	2
				}
		]
}

Response

HTTP status code 200

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{
		}
}

지점 연결 설정 적용/취소

PUT

/command/ipsec-ipv4-branches/apply

지점 연결 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

지점 연결 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/ipsec-ipv4-branches/cancel

지점 연결 설정을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

지점 연결 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

