SSL VPN 사용자 REST API 문서

https://{host}/api/vp

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

(신한은행)사용자 활성화

/sslvpn/cmds/user/access-allow

사용자

/sslvpn/users

/sslvpn/users/{pk}

PUT

GET

GET

POST

  DELETE

PUT

  DELETE

/sslvpn/users/readonly/save?columns=(ID,비밀번호,이름,이메일,전화번호,소속,설명,인증DB,계정유효기간시작일,계정유효기간만료일,원격IP주소,임대IP주소,그룹,계정상태,
인증상태,인증실패횟수,인증서발급횟수,계정만료,비밀번호만료,비밀번호만료일)

GET

/sslvpn/users/save?columns=(ID,비밀번호,이름,이메일,전화번호,소속,설명,인증DB,계정유효기간시작일,계정유효기간만료일)

/sslvpn/users/batch

/sslvpn/users/batch/status

/sslvpn/users/batch/stop

/sslvpn/users/partial/access-allow

/sslvpn/users/partial/access-blocking

/sslvpn/users/partial/cert-count

/sslvpn/users/partial/cert-fail

/sslvpn/users/partial/expire-account

/sslvpn/users/partial/expire-passwd

/sslvpn/users/partial/always-passwd-valid

방화벽 사용자

/sslvpn/fw/users

/sslvpn/fw/users/{pk}

SSLVPN 사용자 추가(벌크 방식)

Full URL: https://{host}/api/co/custom/restapi

/co/custom/restapi

사용자 적용/취소

/command/sslvpn-users/apply

/command/sslvpn-users/cancel

(신한은행)사용자 활성화

PUT

/sslvpn/cmds/user/access-allow

사용자를 활성화 한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

GET

POST

GET

DELETE

PUT

PUT

PUT

PUT

PUT

PUT

PUT

GET

GET

POST

PUT

PUT

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

start_dt
start_dt: required (string)
계정 유효기간 시작일시

Example:
Example

2020-09-01	00:00:00

end_dt
end_dt: required (string)
계정 유효기간 종료일시

Example:
Example

2020-09-30	23:59:59

phone
phone: required (string)
사용자 핸드폰 번호

Example:
Example

010-1234-5678

descdesc: required (string)
행번

Example:
Example

{
		"desc":	"12345678",
		"start_dt":	"2020-09-01	00:00:00",
		"end_dt":	"2020-09-30	23:59:59",
		"phone":	"010-1234-5678"
}

Response

HTTP status code 200

사용자 활성화 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

login_id
login_id: required (string)
shb + 행번

Example:
Example

{
		"code":	"ok",
		"result":	{
				"login_id":	"shb12345678"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

사용자

GET

/sslvpn/users

사용자 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

사용자 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ResSslvpnUserList)
결과 데이터

Items: ResSslvpnUserList
Items

usr_obj_id: required (integer)
usr_obj_id
사용자 객체의 고유한 아이디

useuse: required (one of 0, 1 - default: 1)
사용자 SSLVPN 고급 사용 여부
사용안함:	0,	사용함:	1

access_type
access_type: required (one of 0, 1, 2, 3, 4 - default: 0)
접속 제한 유형
허용:	0,	패스워드	연속	실패	차단:	1,	기간만료	차단	:	2	,	휴면	계정	차단:	3,	강제차단:	4

passwd_valid_type
passwd_valid_type: required (one of 0, 1 - default: 1)
비밀번호 유효 유형
항상:	0,	만료주기:	1

passwd_expire_prd: required (integer - default: 30 - minimum: 30 - maximum: 3650)
passwd_expire_prd
비밀번호 만료 기간

accnt_valid_type
accnt_valid_type: required (one of 0, 1 - default: 0)
계정 유효 유형
항상:	0,	특정	기간:	1

accnt_valid_start_dt: required (string)
accnt_valid_start_dt
계정 유효 시작 날짜

accnt_valid_end_dt
accnt_valid_end_dt: required (string)
계정 유효 특정 기간 끝 날짜

otp_key
otp_key: required (string)
OTP 인증 키
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx

google_otp_key: required (string)
google_otp_key

2.5에	추가된	속성
구글 OTP 키(4자리씩 하이펀(-)으로 구분된 32자리 문자)
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx

clnt_ip_use
clnt_ip_use: required (one of 0, 1 - default: 0)
클라이언트 내부 IP 주소 사용 여부
사용안함:	0,	사용함:	1

lease_ip
lease_ip: required (string)
임대 IP

sslvpn_grp_id
sslvpn_grp_id: required (integer)
SSLVPN 그룹 아이디

descdesc: required (string)
사용자에 대한 설명

login_id
login_id: required (string)
사용자 로그인 아이디

type
type: required (one of 1, 2 - default: 1)
사용자 유형
내부사용자:	1,	외부사용자:	2

name
name: required (string)
사용자 이름

email
email: required (string)
사용자 이메일

phone: required (string)
phone
사용자 전화번호

depart
depart: required (string)
로컬 사용자의 부서

passwd_expire_dt
passwd_expire_dt: required (string)
비밀번호 만료 날짜

passwd_expire
passwd_expire: required (one of 0, 1)
비밀번호 만료 여부
유지:	0,	만료:	1

accnt_expire
accnt_expire: required (one of 0, 1)
계정 만료 여부
유지:	0,	만료:	1

ipip: required (string)
실제 IP

cert_cnt
cert_cnt: required (integer - minimum: 0 - maximum: 3)
사설 인증서 발급 횟수

login_fail_cnt: required (integer - minimum: 0 - maximum: 5)
login_fail_cnt
로그인 실패 횟수

login_stat
login_stat: required (one of 0, 1, 2 - default: 2)
로그인 상태
로그인	실패:	0,	로그인	성공:	1,	로그아웃:	2

login_dt: required (string)
login_dt
로그인 날짜

tun_oid
tun_oid: required (integer)
터널 OID

otp_num
otp_num: required (integer)
OTP

otp_prd
otp_prd: required (integer - default: 3)
OTP 유효 기간

otp_dt
otp_dt: required (string)
OTP 발생 날짜

host_info: required (string)
host_info
호스트 정보

sslvpn_rule_id
sslvpn_rule_id: required (integer)
SSLVPN 정책의 고유한 아이디

os_type
os_type: required (one of 1, 2, 3, 4)
OS 유형
Windows:	1,	Linux:	2,	Android:	3,	iOS:	4

sslvpn_grp_name
sslvpn_grp_name: required (string)
SSLVPN 사용자 그룹 이름

accnt_lock
accnt_lock: required (integer - default: 5)
계정 락 횟수

cert_max_cnt: required (integer - default: 1)
cert_max_cnt
사설 인증서 최대 발급 횟수

cfg_adm_id
cfg_adm_id: required (integer - default: 1)
등록자 ID

cfg_adm
cfg_adm: required (string)
등록자

cfg_dt
cfg_dt: required (string)
최근 수정일

cfg_create_dt
cfg_create_dt: required (string)
사용자 최초 생성일

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"usr_obj_id":	1,
				"login_id":	"admin",
				"type":	1,
				"name":	"관리자",
				"email":	"admin@secui.com",
				"phone":	"010-0000-0000",
				"depart":	"부서명",
				"use":	1,
				"access_type":	0,
				"passwd_valid_type":	1,
				"passwd_expire_prd":	30,
				"passwd_expire_dt":	null,
				"passwd_expire":	0,
				"accnt_valid_type":	0,
				"accnt_valid_start_dt":	"2018-09-20	00:00:00",
				"accnt_valid_end_dt":	"2018-10-20	00:00:00",
				"accnt_expire":	0,
				"otp_key":	"FFFF-FFFF-FFFF-FFFF",
				"google_otp_key":	"ABCD-ABCD-ABCD-ABCD-1234-1234-1234-1234",
				"clnt_ip_use":	0,
				"ip":	"0.0.0.0",
				"lease_ip":	"1.2.3.0",
				"cert_cnt":	0,
				"login_fail_cnt":	0,
				"login_stat":	2,
				"login_dt":	null,
				"tun_oid":	null,
				"otp_num":	null,
				"otp_prd":	3,
				"otp_dt":	null,
				"host_info":	null,
				"sslvpn_rule_id":	null,
				"os_type":	null,
				"sslvpn_grp_id":	1,
				"sslvpn_grp_name":	"기본	그룹",
				"accnt_lock":	5,
				"cert_max_cnt":	1,
				"desc":	"사용자에	대한	설명",
				"cfg_adm_id":	1,
				"cfg_adm":	"admin",
				"cfg_dt":	"2017-10-10	15:36:45",
				"cfg_create_dt":	"2017-10-10	15:36:45"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/sslvpn/users

사용자를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

usr_obj_id
usr_obj_id: required (integer)
사용자 객체의 고유한 아이디

useuse: required (one of 0, 1 - default: 1)
사용자 SSLVPN 고급 사용 여부
사용안함:	0,	사용함:	1

access_type
access_type: required (one of 0, 1, 2, 3, 4 - default: 0)
접속 제한 유형
허용:	0,	패스워드	연속	실패	차단:	1,	기간만료	차단	:	2	,	휴면	계정	차단:	3,	강제차단:	4

passwd_valid_type
passwd_valid_type: required (one of 0, 1 - default: 1)
비밀번호 유효 유형
항상:	0,	만료주기:	1

passwd_expire_prd
passwd_expire_prd: required (integer - default: 30 - minimum: 30 - maximum: 3650)
비밀번호 만료 기간

accnt_valid_type
accnt_valid_type: required (one of 0, 1 - default: 0)
계정 유효 유형
항상:	0,	특정	기간:	1

accnt_valid_start_dt
accnt_valid_start_dt: required (string)
계정 유효 시작 날짜

accnt_valid_end_dt: required (string)
accnt_valid_end_dt
계정 유효 특정 기간 끝 날짜

otp_key
otp_key: required (string)
OTP 인증 키
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx

google_otp_key
google_otp_key: required (string)
2.5에	추가된	속성
구글 OTP 키(4자리씩 하이펀(-)으로 구분된 32자리 문자)
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx

clnt_ip_use
clnt_ip_use: required (one of 0, 1 - default: 0)
클라이언트 내부 IP 주소 사용 여부
사용안함:	0,	사용함:	1

lease_ip
lease_ip: required (string)
임대 IP

sslvpn_grp_id
sslvpn_grp_id: required (integer)
SSLVPN 그룹 아이디

descdesc: required (string)
사용자에 대한 설명

sslvpn_usr_ip_auth_list: required (array of items)
sslvpn_usr_ip_auth_list
Items: items
Items

ipip: required (string)
IP(IPv4 or IPv6)

useuse: required (one of 0, 1 - default: 0)

수집된 IP의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_mac_auth_list: required (array of items)
sslvpn_usr_mac_auth_list
Items: items
Items

macmac: required (string)
MAC 주소

useuse: required (one of 0, 1 - default: 0)
수집된 MAC의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_tmn_auth_list
sslvpn_usr_tmn_auth_list: required (array of items)
Items: items
Items

auth_key: required (string)
auth_key
인증 키

useuse: required (one of 0, 1 - default: 0)
수집된 단말 인증 키의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_clnt_ip_list
sslvpn_usr_clnt_ip_list: required (array of items)
Items: items
Items

ipip: required (string)
IP

netmask
netmask: required (string)
넷마스크

Example:
Example

{
		"usr_obj_id":	1,
		"use":	1,
		"access_type":	0,
		"passwd_valid_type":	1,
		"passwd_expire_prd":	30,
		"accnt_valid_type":	0,
		"accnt_valid_start_dt":	"2018-09-20	00:00:00",
		"accnt_valid_end_dt":	"2018-10-20	00:00:00",
		"otp_key":	"FFFF-FFFF-FFFF-FFFF",
		"google_otp_key":	"ABCD-ABCD-ABCD-ABCD-1234-1234-1234-1234",
		"clnt_ip_use":	0,
		"lease_ip":	"1.2.3.0",
		"sslvpn_grp_id":	1,
		"desc":	"사용자에	대한	설명",
		"sslvpn_usr_ip_auth_list":	[{
				"ip":	"1.1.1.1",
				"use":	0
		}],
		"sslvpn_usr_mac_auth_list":	[{
				"mac":	"FF:FF:FF:FF:FF:FF",
				"use":	0
		}],
		"sslvpn_usr_tmn_auth_list":	[{
				"auth_key":	"FFFF-FFFF-FFFF-FFFF",
				"use":	0
		}],
		"sslvpn_usr_clnt_ip_list":	[{
				"ip":	"1.1.1.1",
				"netmask":	"255.255.255.255"
		}]
}

Response

HTTP status code 200

사용자 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

usr_obj_id
usr_obj_id: required (integer)
사용자 객체의 고유한 아이디

useuse: required (one of 0, 1 - default: 1)
사용자 SSLVPN 고급 사용 여부
사용안함:	0,	사용함:	1

access_type
access_type: required (one of 0, 1, 2, 3, 4 - default: 0)
접속 제한 유형
허용:	0,	패스워드	연속	실패	차단:	1,	기간만료	차단	:	2	,	휴면	계정	차단:	3,	강제차단:	4

passwd_valid_type: required (one of 0, 1 - default: 1)
passwd_valid_type

비밀번호 유효 유형
항상:	0,	만료주기:	1

passwd_expire_prd
passwd_expire_prd: required (integer - default: 30 - minimum: 30 - maximum: 3650)
비밀번호 만료 기간

accnt_valid_type
accnt_valid_type: required (one of 0, 1 - default: 0)
계정 유효 유형
항상:	0,	특정	기간:	1

accnt_valid_start_dt
accnt_valid_start_dt: required (string)
계정 유효 시작 날짜

accnt_valid_end_dt
accnt_valid_end_dt: required (string)
계정 유효 특정 기간 끝 날짜

otp_key
otp_key: required (string)
OTP 인증 키
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx

google_otp_key: required (string)
google_otp_key
2.5에	추가된	속성
구글 OTP 키(4자리씩 하이펀(-)으로 구분된 32자리 문자)
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx

clnt_ip_use
clnt_ip_use: required (one of 0, 1 - default: 0)
클라이언트 내부 IP 주소 사용 여부
사용안함:	0,	사용함:	1

lease_ip: required (string)
lease_ip
임대 IP

sslvpn_grp_id
sslvpn_grp_id: required (integer)
SSLVPN 그룹 아이디

descdesc: required (string)
사용자에 대한 설명

login_id
login_id: required (string)
사용자 로그인 아이디

type
type: required (one of 1, 2 - default: 1)
사용자 유형
내부사용자:	1,	외부사용자:	2

name
name: required (string)
사용자 이름

email
email: required (string)
사용자 이메일

phone
phone: required (string)
사용자 전화번호

depart
depart: required (string)
로컬 사용자의 부서

passwd_expire_dt: required (string)
passwd_expire_dt
비밀번호 만료 날짜

passwd_expire
passwd_expire: required (one of 0, 1)
비밀번호 만료 여부
유지:	0,	만료:	1

accnt_expire: required (one of 0, 1)
accnt_expire
계정 만료 여부
유지:	0,	만료:	1

ipip: required (string)
실제 IP

cert_cnt: required (integer - minimum: 0 - maximum: 3)
cert_cnt
사설 인증서 발급 횟수

login_fail_cnt
login_fail_cnt: required (integer - minimum: 0 - maximum: 5)
로그인 실패 횟수

login_stat
login_stat: required (one of 0, 1, 2 - default: 2)
로그인 상태
로그인	실패:	0,	로그인	성공:	1,	로그아웃:	2

login_dt
login_dt: required (string)
로그인 날짜

tun_oid
tun_oid: required (integer)
터널 OID

otp_num
otp_num: required (integer)
OTP

otp_prd
otp_prd: required (integer - default: 3)
OTP 유효 기간

otp_dt
otp_dt: required (string)
OTP 발생 날짜

host_info
host_info: required (string)
호스트 정보

sslvpn_rule_id: required (integer)
sslvpn_rule_id
SSLVPN 정책의 고유한 아이디

os_type
os_type: required (one of 1, 2, 3, 4)
OS 유형
Windows:	1,	Linux:	2,	Android:	3,	iOS:	4

sslvpn_grp_name
sslvpn_grp_name: required (string)
SSLVPN 사용자 그룹 이름

accnt_lock
accnt_lock: required (integer - default: 5)
계정 락 횟수

cert_max_cnt
cert_max_cnt: required (integer - default: 1)
사설 인증서 최대 발급 횟수

cfg_adm_id
cfg_adm_id: required (integer - default: 1)
등록자 ID

cfg_adm
cfg_adm: required (string)
등록자

cfg_dt
cfg_dt: required (string)
최근 수정일

cfg_create_dt: required (string)
cfg_create_dt
사용자 최초 생성일

sslvpn_usr_ip_auth_list
sslvpn_usr_ip_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_ip_auth_id
sslvpn_usr_ip_auth_id: required (integer)
SSLVPN 사용자 IP 인증의 고유한 아이디

ipip: required (string)
IP(IPv4 or IPv6)

useuse: required (one of 0, 1 - default: 0)
수집된 IP의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_mac_auth_list
sslvpn_usr_mac_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_mac_auth_id
sslvpn_usr_mac_auth_id: required (integer)
SSLVPN 사용자 MAC 인증의 고유한 아이디

macmac: required (string)
MAC 주소

useuse: required (one of 0, 1 - default: 0)
수집된 MAC의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_tmn_auth_list
sslvpn_usr_tmn_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_tmn_auth_id
sslvpn_usr_tmn_auth_id: required (integer)
SSLVPN 사용자 단말 인증의 고유한 아이디

auth_key
auth_key: required (string)
인증 키

useuse: required (one of 0, 1 - default: 0)
수집된 단말 인증 키의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_clnt_ip_list
sslvpn_usr_clnt_ip_list: required (array of items)
Items: items
Items

sslvpn_usr_clnt_ip_id
sslvpn_usr_clnt_ip_id: required (integer)
SSLVPN 사용자 클라이언트 IP 아이디

ipip: required (string)
IP

netmask
netmask: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	{
				"usr_obj_id":	1,
				"login_id":	"admin",
				"type":	1,
				"name":	"관리자",
				"email":	"admin@secui.com",
				"phone":	"010-0000-0000",
				"depart":	"부서명",
				"use":	1,
				"access_type":	0,
				"passwd_valid_type":	1,
				"passwd_expire_prd":	30,
				"passwd_expire_dt":	null,
				"passwd_expire":	0,
				"accnt_valid_type":	0,
				"accnt_valid_start_dt":	"2018-09-20	00:00:00",
				"accnt_valid_end_dt":	"2018-10-20	00:00:00",
				"accnt_expire":	0,
				"otp_key":	"FFFF-FFFF-FFFF-FFFF",
				"google_otp_key":	"ABCD-ABCD-ABCD-ABCD-1234-1234-1234-1234",
				"clnt_ip_use":	0,
				"ip":	"0.0.0.0",
				"lease_ip":	"1.2.3.0",
				"cert_cnt":	0,
				"login_fail_cnt":	0,
				"login_stat":	2,
				"login_dt":	null,
				"tun_oid":	null,
				"otp_num":	null,
				"otp_prd":	3,
				"otp_dt":	null,
				"host_info":	null,
				"sslvpn_rule_id":	null,
				"os_type":	null,
				"sslvpn_grp_id":	1,
				"sslvpn_grp_name":	"기본	그룹",
				"accnt_lock":	5,
				"cert_max_cnt":	1,
				"desc":	"사용자에	대한	설명",
				"cfg_adm_id":	1,
				"cfg_adm":	"admin",
				"cfg_dt":	"2017-10-10	15:36:45",
				"cfg_create_dt":	"2017-10-10	15:36:45",
				"sslvpn_usr_ip_auth_list":	[{
						"sslvpn_usr_ip_auth_id":	1,
						"ip":	"1.1.1.1",
						"use":	0
				}],
				"sslvpn_usr_mac_auth_list":	[{
						"sslvpn_usr_mac_auth_id":	1,
						"mac":	"FF:FF:FF:FF:FF:FF",
						"use":	0
				}],
				"sslvpn_usr_tmn_auth_list":	[{
						"sslvpn_usr_tmn_auth_id":	1,
						"auth_key":	"FFFF-FFFF-FFFF-FFFF",
						"use":	0
				}],
				"sslvpn_usr_clnt_ip_list":	[{
						"sslvpn_usr_clnt_ip_id":	1,
						"ip":	"1.1.1.1",
						"netmask":	"255.255.255.255"
				}]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/sslvpn/users

사용자 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제 아이템 목록

Example:
Example

{
		"items":	[
				{"usr_obj_id":	1},
				{"usr_obj_id":	2}
		]
}

Response

HTTP status code 200

사용자 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
삭제 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/sslvpn/users/{pk}

사용자을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
사용자 객체의 고유한 아이디 ( usr_obj_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language

언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

사용자 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

usr_obj_id: required (integer)
usr_obj_id
사용자 객체의 고유한 아이디

useuse: required (one of 0, 1 - default: 1)
사용자 SSLVPN 고급 사용 여부
사용안함:	0,	사용함:	1

access_type: required (one of 0, 1, 2, 3, 4 - default: 0)
access_type
접속 제한 유형
허용:	0,	패스워드	연속	실패	차단:	1,	기간만료	차단	:	2	,	휴면	계정	차단:	3,	강제차단:	4

passwd_valid_type
passwd_valid_type: required (one of 0, 1 - default: 1)
비밀번호 유효 유형
항상:	0,	만료주기:	1

passwd_expire_prd
passwd_expire_prd: required (integer - default: 30 - minimum: 30 - maximum: 3650)
비밀번호 만료 기간

accnt_valid_type
accnt_valid_type: required (one of 0, 1 - default: 0)
계정 유효 유형
항상:	0,	특정	기간:	1

accnt_valid_start_dt
accnt_valid_start_dt: required (string)
계정 유효 시작 날짜

accnt_valid_end_dt
accnt_valid_end_dt: required (string)
계정 유효 특정 기간 끝 날짜

otp_key: required (string)
otp_key
OTP 인증 키
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx

google_otp_key
google_otp_key: required (string)
2.5에	추가된	속성
구글 OTP 키(4자리씩 하이펀(-)으로 구분된 32자리 문자)
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx

clnt_ip_use
clnt_ip_use: required (one of 0, 1 - default: 0)
클라이언트 내부 IP 주소 사용 여부
사용안함:	0,	사용함:	1

lease_ip
lease_ip: required (string)
임대 IP

sslvpn_grp_id
sslvpn_grp_id: required (integer)
SSLVPN 그룹 아이디

descdesc: required (string)
사용자에 대한 설명

login_id
login_id: required (string)
사용자 로그인 아이디

type: required (one of 1, 2 - default: 1)
type
사용자 유형
내부사용자:	1,	외부사용자:	2

name
name: required (string)
사용자 이름

email
email: required (string)
사용자 이메일

phone
phone: required (string)
사용자 전화번호

depart
depart: required (string)
로컬 사용자의 부서

passwd_expire_dt
passwd_expire_dt: required (string)
비밀번호 만료 날짜

passwd_expire
passwd_expire: required (one of 0, 1)
비밀번호 만료 여부
유지:	0,	만료:	1

accnt_expire
accnt_expire: required (one of 0, 1)
계정 만료 여부
유지:	0,	만료:	1

ipip: required (string)
실제 IP

cert_cnt
cert_cnt: required (integer - minimum: 0 - maximum: 3)
사설 인증서 발급 횟수

login_fail_cnt
login_fail_cnt: required (integer - minimum: 0 - maximum: 5)
로그인 실패 횟수

login_stat
login_stat: required (one of 0, 1, 2 - default: 2)
로그인 상태
로그인	실패:	0,	로그인	성공:	1,	로그아웃:	2

login_dt
login_dt: required (string)
로그인 날짜

tun_oid
tun_oid: required (integer)
터널 OID

otp_num: required (integer)
otp_num
OTP

otp_prd
otp_prd: required (integer - default: 3)
OTP 유효 기간

otp_dt
otp_dt: required (string)
OTP 발생 날짜

host_info
host_info: required (string)
호스트 정보

sslvpn_rule_id
sslvpn_rule_id: required (integer)
SSLVPN 정책의 고유한 아이디

os_type
os_type: required (one of 1, 2, 3, 4)
OS 유형
Windows:	1,	Linux:	2,	Android:	3,	iOS:	4

sslvpn_grp_name
sslvpn_grp_name: required (string)
SSLVPN 사용자 그룹 이름

accnt_lock
accnt_lock: required (integer - default: 5)
계정 락 횟수

cert_max_cnt
cert_max_cnt: required (integer - default: 1)
사설 인증서 최대 발급 횟수

cfg_adm_id: required (integer - default: 1)
cfg_adm_id
등록자 ID

cfg_adm
cfg_adm: required (string)
등록자

cfg_dt
cfg_dt: required (string)
최근 수정일

cfg_create_dt
cfg_create_dt: required (string)
사용자 최초 생성일

sslvpn_usr_ip_auth_list
sslvpn_usr_ip_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_ip_auth_id
sslvpn_usr_ip_auth_id: required (integer)
SSLVPN 사용자 IP 인증의 고유한 아이디

ipip: required (string)
IP(IPv4 or IPv6)

useuse: required (one of 0, 1 - default: 0)
수집된 IP의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_mac_auth_list
sslvpn_usr_mac_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_mac_auth_id
sslvpn_usr_mac_auth_id: required (integer)
SSLVPN 사용자 MAC 인증의 고유한 아이디

macmac: required (string)
MAC 주소

useuse: required (one of 0, 1 - default: 0)
수집된 MAC의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_tmn_auth_list
sslvpn_usr_tmn_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_tmn_auth_id
sslvpn_usr_tmn_auth_id: required (integer)
SSLVPN 사용자 단말 인증의 고유한 아이디

auth_key
auth_key: required (string)
인증 키

useuse: required (one of 0, 1 - default: 0)

수집된 단말 인증 키의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_clnt_ip_list: required (array of items)
sslvpn_usr_clnt_ip_list
Items: items
Items

sslvpn_usr_clnt_ip_id
sslvpn_usr_clnt_ip_id: required (integer)
SSLVPN 사용자 클라이언트 IP 아이디

ipip: required (string)
IP

netmask
netmask: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	{
				"usr_obj_id":	1,
				"login_id":	"admin",
				"type":	1,
				"name":	"관리자",
				"email":	"admin@secui.com",
				"phone":	"010-0000-0000",
				"depart":	"부서명",
				"use":	1,
				"access_type":	0,
				"passwd_valid_type":	1,
				"passwd_expire_prd":	30,
				"passwd_expire_dt":	null,
				"passwd_expire":	0,
				"accnt_valid_type":	0,
				"accnt_valid_start_dt":	"2018-09-20	00:00:00",
				"accnt_valid_end_dt":	"2018-10-20	00:00:00",
				"accnt_expire":	0,
				"otp_key":	"FFFF-FFFF-FFFF-FFFF",
				"google_otp_key":	"ABCD-ABCD-ABCD-ABCD-1234-1234-1234-1234",
				"clnt_ip_use":	0,
				"ip":	"0.0.0.0",
				"lease_ip":	"1.2.3.0",
				"cert_cnt":	0,
				"login_fail_cnt":	0,
				"login_stat":	2,
				"login_dt":	null,
				"tun_oid":	null,
				"otp_num":	null,
				"otp_prd":	3,
				"otp_dt":	null,
				"host_info":	null,
				"sslvpn_rule_id":	null,
				"os_type":	null,
				"sslvpn_grp_id":	1,
				"sslvpn_grp_name":	"기본	그룹",
				"accnt_lock":	5,
				"cert_max_cnt":	1,
				"desc":	"사용자에	대한	설명",
				"cfg_adm_id":	1,
				"cfg_adm":	"admin",
				"cfg_dt":	"2017-10-10	15:36:45",
				"cfg_create_dt":	"2017-10-10	15:36:45",
				"sslvpn_usr_ip_auth_list":	[{
						"sslvpn_usr_ip_auth_id":	1,
						"ip":	"1.1.1.1",
						"use":	0
				}],
				"sslvpn_usr_mac_auth_list":	[{
						"sslvpn_usr_mac_auth_id":	1,
						"mac":	"FF:FF:FF:FF:FF:FF",
						"use":	0
				}],
				"sslvpn_usr_tmn_auth_list":	[{
						"sslvpn_usr_tmn_auth_id":	1,
						"auth_key":	"FFFF-FFFF-FFFF-FFFF",
						"use":	0
				}],
				"sslvpn_usr_clnt_ip_list":	[{
						"sslvpn_usr_clnt_ip_id":	1,
						"ip":	"1.1.1.1",
						"netmask":	"255.255.255.255"
				}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message

결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/sslvpn/users/{pk}

사용자을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
사용자 객체의 고유한 아이디 ( usr_obj_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

usr_obj_id: required (integer)
usr_obj_id
사용자 객체의 고유한 아이디

useuse: required (one of 0, 1 - default: 1)
사용자 SSLVPN 고급 사용 여부
사용안함:	0,	사용함:	1

access_type
access_type: required (one of 0, 1, 2, 3, 4 - default: 0)
접속 제한 유형
허용:	0,	패스워드	연속	실패	차단:	1,	기간만료	차단	:	2	,	휴면	계정	차단:	3,	강제차단:	4

passwd_valid_type
passwd_valid_type: required (one of 0, 1 - default: 1)
비밀번호 유효 유형
항상:	0,	만료주기:	1

passwd_expire_prd
passwd_expire_prd: required (integer - default: 30 - minimum: 30 - maximum: 3650)
비밀번호 만료 기간

accnt_valid_type
accnt_valid_type: required (one of 0, 1 - default: 0)
계정 유효 유형
항상:	0,	특정	기간:	1

accnt_valid_start_dt
accnt_valid_start_dt: required (string)
계정 유효 시작 날짜

accnt_valid_end_dt
accnt_valid_end_dt: required (string)
계정 유효 특정 기간 끝 날짜

otp_key
otp_key: required (string)
OTP 인증 키
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx

google_otp_key
google_otp_key: required (string)
2.5에	추가된	속성
구글 OTP 키(4자리씩 하이펀(-)으로 구분된 32자리 문자)
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx

clnt_ip_use
clnt_ip_use: required (one of 0, 1 - default: 0)
클라이언트 내부 IP 주소 사용 여부
사용안함:	0,	사용함:	1

lease_ip
lease_ip: required (string)
임대 IP

sslvpn_grp_id
sslvpn_grp_id: required (integer)
SSLVPN 그룹 아이디

descdesc: required (string)
사용자에 대한 설명

sslvpn_usr_ip_auth_list
sslvpn_usr_ip_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_ip_auth_id
sslvpn_usr_ip_auth_id: required (integer)
SSLVPN 사용자 IP 인증의 고유한 아이디

ipip: required (string)
IP(IPv4 or IPv6)

useuse: required (one of 0, 1 - default: 0)
수집된 아이피의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_mac_auth_list
sslvpn_usr_mac_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_mac_auth_id
sslvpn_usr_mac_auth_id: required (integer)
SSLVPN 사용자 MAC 인증의 고유한 아이디

macmac: required (string)
MAC 주소

useuse: required (one of 0, 1 - default: 0)
수집된 MAC의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_tmn_auth_list
sslvpn_usr_tmn_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_tmn_auth_id
sslvpn_usr_tmn_auth_id: required (integer)
SSLVPN 사용자 단말 인증의 고유한 아이디

auth_key
auth_key: required (string)
인증 키

useuse: required (one of 0, 1 - default: 0)
수집된 단말 인증 키의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_clnt_ip_list
sslvpn_usr_clnt_ip_list: required (array of items)
Items: items
Items

sslvpn_usr_clnt_ip_id
sslvpn_usr_clnt_ip_id: required (integer)
SSLVPN 사용자 클라이언트 아이피 아이디

ipip: required (string)
아이피

netmask
netmask: required (string)
넷마스크

Example:
Example

{
		"use":	0,
		"access_type":	1,
		"passwd_valid_type":	0,
		"passwd_expire_prd":	60,
		"accnt_valid_type":	1,
		"accnt_valid_start_dt":	"2018-09-20	00:00:00",
		"accnt_valid_end_dt":	"2018-10-20	00:00:00",
		"otp_key":	"FFFF-FFFF-FFFF-FF00",
		"google_otp_key":	"ABCD-ABCD-ABCD-ABCD-1234-1234-1234-1234",
		"clnt_ip_use":	1,
		"lease_ip":	"1.2.3.0",
		"sslvpn_grp_id":	1,
		"desc":	"사용자에	대한	설명",
		"sslvpn_usr_ip_auth_list":	[{
				"sslvpn_usr_ip_auth_id":	1,
				"ip":	"2.2.2.2",
				"use":	1
		}],
		"sslvpn_usr_mac_auth_list":	[{
				"sslvpn_usr_mac_auth_id":	1,
				"mac":	"FF:FF:FF:FF:FF:00",
				"use":	1
		}],
		"sslvpn_usr_tmn_auth_list":	[{
				"sslvpn_usr_tmn_auth_id":	1,
				"auth_key":	"FFFF-FFFF-FFFF-0000",
				"use":	1
		}],
		"sslvpn_usr_clnt_ip_list":	[{
				"sslvpn_usr_clnt_ip_id":	1,
				"ip":	"2.2.2.2",
				"netmask":	"255.255.255.255"
		}]
}

Response

HTTP status code 200

사용자 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

usr_obj_id: required (integer)
usr_obj_id
사용자 객체의 고유한 아이디

useuse: required (one of 0, 1 - default: 1)
사용자 SSLVPN 고급 사용 여부
사용안함:	0,	사용함:	1

access_type: required (one of 0, 1, 2, 3, 4 - default: 0)
access_type
접속 제한 유형
허용:	0,	패스워드	연속	실패	차단:	1,	기간만료	차단	:	2	,	휴면	계정	차단:	3,	강제차단:	4

passwd_valid_type
passwd_valid_type: required (one of 0, 1 - default: 1)
비밀번호 유효 유형
항상:	0,	만료주기:	1

passwd_expire_prd
passwd_expire_prd: required (integer - default: 30 - minimum: 30 - maximum: 3650)
비밀번호 만료 기간

accnt_valid_type
accnt_valid_type: required (one of 0, 1 - default: 0)
계정 유효 유형
항상:	0,	특정	기간:	1

accnt_valid_start_dt
accnt_valid_start_dt: required (string)
계정 유효 시작 날짜

accnt_valid_end_dt
accnt_valid_end_dt: required (string)
계정 유효 특정 기간 끝 날짜

otp_key
otp_key: required (string)
OTP 인증 키
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx

google_otp_key
google_otp_key: required (string)
2.5에	추가된	속성
구글 OTP 키(4자리씩 하이펀(-)으로 구분된 32자리 문자)
값이 없을 경우 null 로 설정

Example:
Example

xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx-xxxx

clnt_ip_use: required (one of 0, 1 - default: 0)
clnt_ip_use
클라이언트 내부 IP 주소 사용 여부
사용안함:	0,	사용함:	1

lease_ip
lease_ip: required (string)
임대 IP

sslvpn_grp_id
sslvpn_grp_id: required (integer)
SSLVPN 그룹 아이디

descdesc: required (string)
사용자에 대한 설명

login_id: required (string)
login_id
사용자 로그인 아이디

type
type: required (one of 1, 2 - default: 1)
사용자 유형
내부사용자:	1,	외부사용자:	2

name: required (string)
name
사용자 이름

email
email: required (string)
사용자 이메일

phone
phone: required (string)
사용자 전화번호

depart
depart: required (string)
로컬 사용자의 부서

passwd_expire_dt
passwd_expire_dt: required (string)
비밀번호 만료 날짜

passwd_expire
passwd_expire: required (one of 0, 1)
비밀번호 만료 여부
유지:	0,	만료:	1

accnt_expire
accnt_expire: required (one of 0, 1)
계정 만료 여부
유지:	0,	만료:	1

ipip: required (string)
실제 IP

cert_cnt
cert_cnt: required (integer - minimum: 0 - maximum: 3)
사설 인증서 발급 횟수

login_fail_cnt
login_fail_cnt: required (integer - minimum: 0 - maximum: 5)
로그인 실패 횟수

login_stat
login_stat: required (one of 0, 1, 2 - default: 2)
로그인 상태
로그인	실패:	0,	로그인	성공:	1,	로그아웃:	2

login_dt
login_dt: required (string)
로그인 날짜

tun_oid
tun_oid: required (integer)
터널 OID

otp_num: required (integer)
otp_num
OTP

otp_prd
otp_prd: required (integer - default: 3)
OTP 유효 기간

otp_dt
otp_dt: required (string)
OTP 발생 날짜

host_info
host_info: required (string)
호스트 정보

sslvpn_rule_id
sslvpn_rule_id: required (integer)
SSLVPN 정책의 고유한 아이디

os_type
os_type: required (one of 1, 2, 3, 4)
OS 유형
Windows:	1,	Linux:	2,	Android:	3,	iOS:	4

sslvpn_grp_name
sslvpn_grp_name: required (string)
SSLVPN 사용자 그룹 이름

accnt_lock
accnt_lock: required (integer - default: 5)
계정 락 횟수

cert_max_cnt
cert_max_cnt: required (integer - default: 1)
사설 인증서 최대 발급 횟수

cfg_adm_id: required (integer - default: 1)
cfg_adm_id
등록자 ID

cfg_adm
cfg_adm: required (string)
등록자

cfg_dt
cfg_dt: required (string)
최근 수정일

cfg_create_dt
cfg_create_dt: required (string)
사용자 최초 생성일

sslvpn_usr_ip_auth_list
sslvpn_usr_ip_auth_list: required (array of items)
Items: items
Items

sslvpn_usr_ip_auth_id
sslvpn_usr_ip_auth_id: required (integer)
SSLVPN 사용자 IP 인증의 고유한 아이디

ipip: required (string)
IP(IPv4 or IPv6)

useuse: required (one of 0, 1 - default: 0)

수집된 IP의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_mac_auth_list: required (array of items)
sslvpn_usr_mac_auth_list
Items: items
Items

sslvpn_usr_mac_auth_id
sslvpn_usr_mac_auth_id: required (integer)
SSLVPN 사용자 MAC 인증의 고유한 아이디

macmac: required (string)
MAC 주소

useuse: required (one of 0, 1 - default: 0)
수집된 MAC의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_tmn_auth_list: required (array of items)
sslvpn_usr_tmn_auth_list
Items: items
Items

sslvpn_usr_tmn_auth_id
sslvpn_usr_tmn_auth_id: required (integer)
SSLVPN 사용자 단말 인증의 고유한 아이디

auth_key
auth_key: required (string)
인증 키

useuse: required (one of 0, 1 - default: 0)
수집된 단말 인증 키의 인증 사용 여부
사용안함:	0,	사용함:	1

sslvpn_usr_clnt_ip_list: required (array of items)
sslvpn_usr_clnt_ip_list
Items: items
Items

sslvpn_usr_clnt_ip_id
sslvpn_usr_clnt_ip_id: required (integer)
SSLVPN 사용자 클라이언트 IP 아이디

ipip: required (string)
IP

netmask
netmask: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	{
				"usr_obj_id":	1,
				"login_id":	"admin",
				"type":	1,
				"name":	"관리자2",
				"email":	"admin1@secui.com",
				"phone":	"010-0000-1111",
				"depart":	"부서명	수정",
				"use":	0,
				"access_type":	1,
				"passwd_valid_type":	0,
				"passwd_expire_prd":	60,
				"passwd_expire_dt":	"2018-10-20	23:59:59",
				"passwd_expire":	0,
				"accnt_valid_type":	1,
				"accnt_valid_start_dt":	"2018-09-20	00:00:00",
				"accnt_valid_end_dt":	"2018-10-20	00:00:00",
				"accnt_expire":	0,
				"otp_key":	"FFFF-FFFF-FFFF-FF00",
				"google_otp_key":	"ABCD-ABCD-ABCD-ABCD-1234-1234-1234-1234",
				"clnt_ip_use":	1,
				"ip":	"0.0.0.0",
				"lease_ip":	"1.2.3.0",
				"cert_cnt":	0,
				"login_fail_cnt":	0,
				"login_stat":	2,
				"login_dt":	"2018-10-10	15:36:45",
				"tun_oid":	12345,
				"otp_num":	12345,
				"otp_prd":	3,
				"otp_dt":	"2018-10-10	15:36:45",
				"host_info":	"host",
				"sslvpn_rule_id":	1,
				"os_type":	1,
				"sslvpn_grp_id":	1,
				"sslvpn_grp_name":	"기본	그룹",
				"accnt_lock":	5,
				"cert_max_cnt":	1,
				"desc":	"사용자에	대한	설명",
				"cfg_adm_id":	1,
				"cfg_adm":	"admin",
				"cfg_dt":	"2018-10-10	15:36:45",
				"cfg_create_dt":	"2017-10-10	15:36:45",
				"sslvpn_usr_ip_auth_list":	[{
						"sslvpn_usr_ip_auth_id":	1,
						"ip":	"2.2.2.2",
						"use":	1
				}],
				"sslvpn_usr_mac_auth_list":	[{
						"sslvpn_usr_mac_auth_id":	1,
						"mac":	"FF:FF:FF:FF:FF:00",
						"use":	1
				}],
				"sslvpn_usr_tmn_auth_list":	[{
						"sslvpn_usr_tmn_auth_id":	1,
						"auth_key":	"FFFF-FFFF-FFFF-0000",
						"use":	1
				}],
				"sslvpn_usr_clnt_ip_list":	[{
						"sslvpn_usr_clnt_ip_id":	1,
						"ip":	"2.2.2.2",
						"netmask":	"255.255.255.255"
				}]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/sslvpn/users/{pk}

사용자을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
사용자 객체의 고유한 아이디 ( usr_obj_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

사용자 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/sslvpn/users/readonly/save?columns=(ID,비밀번호,이름,이메일,전화번호,소속,설명,인증DB,계정유효기간시작일,계정유효기간만료일,원격IP주소,임대IP주소,그룹,계정상태,인증상
태,인증실패횟수,인증서발급횟수,계정만료,비밀번호만료,비밀번호만료일)

읽기용 사용자 엑셀 다운로드을 PC로 저장합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

columns
columns: (string)
콤마(,)로 구분된 엑셀에 표시될 헤더 컬럼명(URI 인코딩 처리 필요)

Example:
Example

?columns=(ID,비밀번호,이름,이메일,전화번호,소속,설명,인증DB,계정유효기간시작일,계정유효기간만료일,원격IP주소,임대IP주소,그룹,계정상태,인증상태,인증실패횟수,인증서발급횟수,계정만료,비밀번호만료,비밀번호만료일)

Response

HTTP status code 200

읽기용 사용자 엑셀 다운로드를 PC로 저장 성공

Body

Media type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Media type

TypeType: file

GET

/sslvpn/users/save?columns=(ID,비밀번호,이름,이메일,전화번호,소속,설명,인증DB,계정유효기간시작일,계정유효기간만료일)

배치용 사용자 엑셀 다운로드을 PC로 저장합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

columns
columns: (string)
콤마(,)로 구분된 엑셀에 표시될 헤더 컬럼명(URI 인코딩 처리 필요)

Example:
Example

?columns=(ID,비밀번호,이름,이메일,전화번호,소속,설명,인증DB,계정유효기간시작일,계정유효기간만료일)

Response

HTTP status code 200

배치용 사용자 엑셀 다운로드를 PC로 저장 성공

Body

Media type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Media type

TypeType: file

POST

/sslvpn/users/batch

사용자 일괄 등록한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

filefile: required (string)
파일 업로드 후 반환된 파일명을 인자로 전달
파일 업로드 API로 배치 파일 업로드 후 호출
https://{url}/api/co/file/import

Example:
Example

{
		"file":	"batch_file.xlsx"
}

Response

HTTP status code 200

사용자 일괄 등록 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	[]
}

HTTP status code 400

사용자 일괄 등록 요청 실패

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/sslvpn/users/batch/status

사용자 일괄 등록 상태을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

사용자 일괄 등록 상태 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

idid: required (integer)
비동기 태스크 ID

status
status: required (string)
비동기 상태
PENDING,	PROGRESS,	SUCCESS

attributes: required (object)
attributes
total
total: required (integer)
전체 개수

current
current: required (integer)
현재 진행 개수

errors
errors: required (array of items)
Items: items
Items

Example:
Example

{
		"code":	"ok",
		"result":	{
				"id":	1,
				"status":	"SUCCESS",
				"attributes":	{
						"total":	100,
						"current":	100,
						"errors":	[]
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/sslvpn/users/batch/stop

사용자 일괄 등록 중지한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

사용자 일괄 등록 중지 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

사용자 일괄 등록 중지 요청 실패

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/sslvpn/users/partial/access-allow

접속 허용을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language

언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
수정 아이템 목록

Example:
Example

{
		"items":	[
				{"usr_obj_id":	1}
		]
}

Response

HTTP status code 200

접속 허용 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/sslvpn/users/partial/access-blocking

접속 차단을(를) 수정한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
수정 아이템 목록

Example:
Example

{
		"items":	[
				{"usr_obj_id":	1}
		]
}

Response

HTTP status code 200

접속 차단 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/sslvpn/users/partial/cert-count

사설인증서 발급 횟수 초기화을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
수정 아이템 목록

Example:
Example

{
		"items":	[
				{"usr_obj_id":	1}
		]
}

Response

HTTP status code 200

사설인증서 발급 횟수 초기화 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/sslvpn/users/partial/cert-fail

인증 실패 횟수 초기화을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
수정 아이템 목록

Example:
Example

{
		"items":	[
				{"usr_obj_id":	1}
		]
}

Response

HTTP status code 200

인증 실패 횟수 초기화 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/sslvpn/users/partial/expire-account

계정 유효 기간 강제 만료을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
수정 아이템 목록

Example:
Example

{
		"items":	[
				{"usr_obj_id":	1}
		]
}

Response

HTTP status code 200

계정 유효 기간 강제 만료 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/sslvpn/users/partial/expire-passwd

다음 접속시 비밀번호 변경을(를) 수정한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
수정 아이템 목록

Example:
Example

{
		"items":	[
				{"usr_obj_id":	1}
		]
}

Response

HTTP status code 200

다음 접속시 비밀번호 변경 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/sslvpn/users/partial/always-passwd-valid

선택된 사용자에 대해 항상 비밀번호 유효 설정을 한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
수정 아이템 목록

Example:
Example

{
		"items":	[
				{"usr_obj_id":	1}
		]
}

Response

HTTP status code 200

항상 비밀번호 유효 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

방화벽 사용자

GET

/sslvpn/fw/users

방화벽 사용자을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

방화벽 사용자 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of SslvpnFwUser)
결과 데이터

Items: SslvpnFwUser
Items

usr_obj_id
usr_obj_id: required (integer - default: 1)
사용자 객체의 고유한 아이디

login_id
login_id: required (string)
사용자 로그인 아이디

name
name: required (string)
사용자의 이름

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"usr_obj_id":	1,
				"login_id":	"admin",
				"name":	"관리자"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/sslvpn/fw/users/{pk}

방화벽 사용자을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
사용자 객체의 고유한 아이디 ( usr_obj_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

방화벽 사용자 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

usr_obj_id
usr_obj_id: required (integer - default: 1)
사용자 객체의 고유한 아이디

login_id
login_id: required (string)
사용자 로그인 아이디

name
name: required (string)
사용자의 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"usr_obj_id":	1,
				"login_id":	"admin",
				"name":	"관리자"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

SSLVPN 사용자 추가(벌크 방식)

POST

/co/custom/restapi

N건의 SSLVPN 사용자를 추가한다.
한번에 최대 2,000건 까지만 가능함

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept

응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

cmdcmd: required (string)
SET_SSLVPN_USR_RESTAPI 로 고정

parameters: required (object)
parameters

users
users: required (array of 사용자)
사용자 목록
한번에 최대 2,000 건까지만 처리 가능

Items: 사용자
Items

login_id
login_id: required (string - minLength: 1 - maxLength: 40)
사용자 ID

name
name: required (string - maxLength: 64)
사용자 이름

depart: required (string - maxLength: 64)
depart
사용자 부서

email
email: required (string - maxLength: 320)
E-Mail
이메일 형식

phone
phone: required (string - maxLength: 32)
휴대폰 번호
포맷:	[3자리]-[4자리]-[4자리]	형식

descdesc: required (string - maxLength: 128)
설명

access_type
access_type: required (one of 0, 1 - default: 0)
접속 제한 유형
0:	허용
1:	차단

start_dt
start_dt: required (string)
계정 유효 기간 시작일
포맷:	YYYY-MM-DD

Example:
Example

2020-01-01

end_dt
end_dt: required (string)
계정 유효 기간 종료일
포맷:	YYYY-MM-DD

Example:
Example

2020-12-31

Example:
Example

{
		"cmd":	"SET_SSLVPN_USR_RESTAPI",
		"parameters":	{
				"users":	[
						{
								"login_id":	"sslvpn_user01",
								"name":	"테스트	사용자",
								"depart":	"개발실",
								"email":	"test@example.com",
								"phone":	"010-1234-5678",
								"desc":	"테스트	사용자",
								"access_type":	0,
								"start_dt":	"2020-12-20",
								"end_dt":	"2021-01-31"
						}
				]
		}
}

Response

HTTP status code 200

SSLVPN 사용자 추가 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ResResult)
결과 데이터

Items: ResResult
Items

status
status: required (string)
사용자 추가 후 결과 코드
OK:	성공,	FAIL:	실패

usr_obj_id
usr_obj_id: required (integer)
사용자 객체 ID
사용자 추가가 안된 경우 null로 출력

Example:
Example

//추가	성공
{
		"code":	"ok",
		"result":	[
				{
						"status":	"OK",
						"usr_obj_id":	4
				}
		]
}

//추가	실패
{
		"code":	"ok",
		"result":	[
				{
						"status":	"FAIL",
						"usr_obj_id":	null
				}
		]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

사용자 적용/취소

PUT

/command/sslvpn-users/apply

사용자을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

사용자 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/sslvpn-users/cancel

사용자을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

사용자 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

