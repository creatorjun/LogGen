GET

미 접속 사용자 REST API 문서

https://{url}/api/vp

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/vp

미 접속 사용자

/sslvpn/unconnected-user

미 접속 사용자

GET

/sslvpn/unconnected-user

미 접속 사용자 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

미 접속 사용자 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of SslvpnUnConnectedUser)
결과 데이터

Items: SslvpnUnConnectedUser
Items

login_dt
login_dt: required (string)
접속시간

login_id
login_id: required (string)
사용자 아이디

ipip: required (string)
원격 아이피

lease_ip
lease_ip: required (string)
임대 아이피

access_type: required (one of 0, 1, 2, 3, 4)
access_type
접속 제한 유형  허용:	0,	패스워드	연속	실패	차단:	1,	휴먼	계정	차단:	2,	강제차단:	3,	계정만료:	4

login_fail_cnt
login_fail_cnt: required (integer - default: 0)
로그인 실패 횟수

accnt_lock
accnt_lock: required (integer - default: 5)
로그인 실패 최대 횟수

login_stat
login_stat: required (NaN - default: 2)
로그인 상태  로그인	실패:	2

login_fail_reason
login_fail_reason: required (string)
로그인 실패 사유

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"login_dt":	"2017-10-10	15:36:45",
				"login_id":	"usr1",
				"ip	":	"1.1.1.1",
				"lease_ip":	"2.2.2.2",
				"access_type":	0,
				"login_fail_cnt":	0,
				"accnt_lock":	5,
				"login_stat	":	2,
				"login_fail_reason":	"아이디	또는	패스워드가	맞지	않습니다."
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

