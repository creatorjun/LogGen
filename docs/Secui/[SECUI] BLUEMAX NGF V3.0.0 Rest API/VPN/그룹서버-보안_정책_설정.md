GET

GET

GET

GET

GET

POST

  DELETE

PUT

  DELETE

PUT

PUT

(2.0)Group VPN 보안 정책 설정 REST API 문서

https://{url}/api/vp

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/vp

암호 알고리즘

/groupvpn/crypto/algorithms

인증 알고리즘

/groupvpn/auth/algorithms

DH 그룹

/groupvpn/dh/groups

보안 정책 설정

/groupvpn/server/security/policies

/groupvpn/server/security/policies/{pk}

보안 정책 설정 적용/취소

/command/groupvpn-server-security-policies/apply

/command/groupvpn-server-security-policies/cancel

암호 알고리즘

GET

/groupvpn/crypto/algorithms

암호 알고리즘 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

암호 알고리즘 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of 암호 알고리즘)
결과 데이터

Items: 암호 알고리즘
Items

vpn_crypto_algr_id
vpn_crypto_algr_id: required (integer)
암호 알고리즘 ID

name: required (string)
name
암호 알고리즘 이름

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"vpn_crypto_algr_id":	1,
				"name":	"AES-256"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

인증 알고리즘

GET

/groupvpn/auth/algorithms

인증 알고리즘 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증 알고리즘 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (array of 인증 알고리즘)
result
결과 데이터

Items: 인증 알고리즘
Items

vpn_auth_algr_id
vpn_auth_algr_id: required (integer)
인증 알고리즘 ID

name
name: required (string)
인증 알고리즘 이름

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"vpn_auth_algr_id":	1,
				"name":	"SHA-256"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

DH 그룹

GET

/groupvpn/dh/groups

DH 그룹 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

DH 그룹 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of DH 그룹)
결과 데이터

Items: DH 그룹
Items

vpn_dh_grp_id
vpn_dh_grp_id: required (integer)
DH 그룹 ID

name
name: required (string)
DH 그룹 이름

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"vpn_dh_grp_id":	1,
				"name":	"MODP-2048"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

보안 정책 설정

GET

/groupvpn/server/security/policies

보안 정책 설정 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

보안 정책 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of 보안 정책)
결과 데이터

Items: 보안 정책
Items

name
name: required (string - minLength: 1 - maxLength: 32)
보안 정책 이름
숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

lftmlftm: required (integer - default: 14400 - minimum: 1800 - maximum: 500000)
생명주기(초)

descdesc: required (string - maxLength: 128)
설명

grp_vpn_policy_id
grp_vpn_policy_id: required (integer)
보안 정책 ID(PK)

p2_crypto_algr
p2_crypto_algr: required (object)
암호 알고리즘

vpn_crypto_algr_id
vpn_crypto_algr_id: required (integer)
암호 알고리즘 ID

name
name: required (string)
암호 알고리즘 이름

p2_auth_algr
p2_auth_algr: required (object)
인증 알고리즘

vpn_auth_algr_id
vpn_auth_algr_id: required (integer)
인증 알고리즘 ID

name: required (string)
name
인증 알고리즘 이름

p2_dh_grp
p2_dh_grp: required (object)
DH 그룹

vpn_dh_grp_id
vpn_dh_grp_id: required (integer)
DH 그룹 ID

name
name: required (string)
DH 그룹 이름

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"grp_vpn_policy_id":	1,
				"name":	"AES-256-SHA-256-Group14-3600s",
				"p2_crypto_algr":	{
						"vpn_crypto_algr_id":	1,
						"name":	"AES-256"
				},
				"p2_auth_algr":	{
						"vpn_auth_algr_id":	1,
						"name":	"SHA-256"
				},
				"p2_dh_grp":	{
						"vpn_dh_grp_id":	1,
						"name":	"MODP-2048"
				},
				"lftm":	14400,
				"desc":	""
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/groupvpn/server/security/policies

보안 정책 설정를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 32)
보안 정책 이름
숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

lftmlftm: required (integer - default: 14400 - minimum: 1800 - maximum: 500000)
생명주기(초)

descdesc: required (string - maxLength: 128)
설명

p2_crypto_algr
p2_crypto_algr: required (object)
암호 알고리즘

vpn_crypto_algr_id
vpn_crypto_algr_id: required (integer)
암호 알고리즘 ID

p2_auth_algr
p2_auth_algr: required (object)
인증 알고리즘

vpn_auth_algr_id: required (integer)
vpn_auth_algr_id
인증 알고리즘 ID

p2_dh_grp
p2_dh_grp: required (object)
DH 그룹

vpn_dh_grp_id
vpn_dh_grp_id: required (integer)
DH 그룹 ID

Example:
Example

{
		"name":	"AES-256-SHA-256-Group14-3600s",
		"p2_crypto_algr":	{
				"vpn_crypto_algr_id":	1
		},
		"p2_auth_algr":	{
				"vpn_auth_algr_id":	1
		},
		"p2_dh_grp":	{
				"vpn_dh_grp_id":	1
		},
		"lftm":	14400,
		"desc":	""
}

Response

HTTP status code 200

보안 정책 설정 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 32)
보안 정책 이름
숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

lftmlftm: required (integer - default: 14400 - minimum: 1800 - maximum: 500000)
생명주기(초)

descdesc: required (string - maxLength: 128)
설명

grp_vpn_policy_id: required (integer)
grp_vpn_policy_id
보안 정책 ID(PK)

p2_crypto_algr
p2_crypto_algr: required (object)
암호 알고리즘

vpn_crypto_algr_id: required (integer)
vpn_crypto_algr_id
암호 알고리즘 ID

name
name: required (string)
암호 알고리즘 이름

p2_auth_algr
p2_auth_algr: required (object)
인증 알고리즘

vpn_auth_algr_id
vpn_auth_algr_id: required (integer)
인증 알고리즘 ID

name
name: required (string)
인증 알고리즘 이름

p2_dh_grp: required (object)
p2_dh_grp
DH 그룹

vpn_dh_grp_id
vpn_dh_grp_id: required (integer)
DH 그룹 ID

name
name: required (string)
DH 그룹 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"grp_vpn_policy_id":	1,
				"name":	"AES-256-SHA-256-Group14-3600s",
				"p2_crypto_algr":	{
						"vpn_crypto_algr_id":	1,
						"name":	"AES-256"
				},
				"p2_auth_algr":	{
						"vpn_auth_algr_id":	1,
						"name":	"SHA-256"
				},
				"p2_dh_grp":	{
						"vpn_dh_grp_id":	1,
						"name":	"MODP-2048"
				},
				"lftm":	14400,
				"desc":	""
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/groupvpn/server/security/policies

보안 정책 설정 목록을 (일괄)삭제한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of 보안 정책 ID)
삭제 아이템 목록

Items: 보안 정책 ID
Items

grp_vpn_policy_id
grp_vpn_policy_id: required (integer)
보안 정책 ID(PK)

Example:
Example

{
		"items":	[
				{"grp_vpn_policy_id":	1},
				{"grp_vpn_policy_id":	2}
		]
}

Response

HTTP status code 200

보안 정책 설정 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result: required (object)
result
결과 데이터

errors
errors: required (array of object)
삭제 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/groupvpn/server/security/policies/{pk}

보안 정책 설정을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
보안 정책 설정 ID ( grp_vpn_policy_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

보안 정책 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 32)
보안 정책 이름
숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

lftmlftm: required (integer - default: 14400 - minimum: 1800 - maximum: 500000)
생명주기(초)

descdesc: required (string - maxLength: 128)
설명

grp_vpn_policy_id
grp_vpn_policy_id: required (integer)
보안 정책 ID(PK)

p2_crypto_algr: required (object)
p2_crypto_algr
암호 알고리즘

vpn_crypto_algr_id
vpn_crypto_algr_id: required (integer)
암호 알고리즘 ID

name
name: required (string)
암호 알고리즘 이름

p2_auth_algr
p2_auth_algr: required (object)
인증 알고리즘

vpn_auth_algr_id
vpn_auth_algr_id: required (integer)
인증 알고리즘 ID

name
name: required (string)
인증 알고리즘 이름

p2_dh_grp
p2_dh_grp: required (object)
DH 그룹

vpn_dh_grp_id: required (integer)
vpn_dh_grp_id

DH 그룹 ID

name
name: required (string)
DH 그룹 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"grp_vpn_policy_id":	1,
				"name":	"AES-256-SHA-256-Group14-3600s",
				"p2_crypto_algr":	{
						"vpn_crypto_algr_id":	1,
						"name":	"AES-256"
				},
				"p2_auth_algr":	{
						"vpn_auth_algr_id":	1,
						"name":	"SHA-256"
				},
				"p2_dh_grp":	{
						"vpn_dh_grp_id":	1,
						"name":	"MODP-2048"
				},
				"lftm":	14400,
				"desc":	""
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/groupvpn/server/security/policies/{pk}

보안 정책 설정을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
보안 정책 설정 ID ( grp_vpn_policy_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 32)
보안 정책 이름
숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

lftmlftm: required (integer - default: 14400 - minimum: 1800 - maximum: 500000)
생명주기(초)

descdesc: required (string - maxLength: 128)
설명

p2_crypto_algr
p2_crypto_algr: required (object)
암호 알고리즘

vpn_crypto_algr_id
vpn_crypto_algr_id: required (integer)
암호 알고리즘 ID

p2_auth_algr
p2_auth_algr: required (object)
인증 알고리즘

vpn_auth_algr_id
vpn_auth_algr_id: required (integer)
인증 알고리즘 ID

p2_dh_grp
p2_dh_grp: required (object)
DH 그룹

vpn_dh_grp_id: required (integer)
vpn_dh_grp_id
DH 그룹 ID

Example:
Example

{
		"p2_crypto_algr":	{
				"vpn_crypto_algr_id":	1,
				"name":	"AES-256"
		},
		"p2_auth_algr":	{
				"vpn_auth_algr_id":	1,
				"name":	"SHA-256"
		},
		"p2_dh_grp":	{
				"vpn_dh_grp_id":	1,
				"name":	"MODP-2048"
		},
		"lftm":	14400,
		"desc":	""
}

Response

HTTP status code 200

보안 정책 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 32)
보안 정책 이름
숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

lftmlftm: required (integer - default: 14400 - minimum: 1800 - maximum: 500000)
생명주기(초)

descdesc: required (string - maxLength: 128)
설명

grp_vpn_policy_id: required (integer)
grp_vpn_policy_id
보안 정책 ID(PK)

p2_crypto_algr
p2_crypto_algr: required (object)
암호 알고리즘

vpn_crypto_algr_id
vpn_crypto_algr_id: required (integer)
암호 알고리즘 ID

name
name: required (string)
암호 알고리즘 이름

p2_auth_algr
p2_auth_algr: required (object)
인증 알고리즘

vpn_auth_algr_id
vpn_auth_algr_id: required (integer)
인증 알고리즘 ID

name
name: required (string)
인증 알고리즘 이름

p2_dh_grp: required (object)
p2_dh_grp
DH 그룹

vpn_dh_grp_id
vpn_dh_grp_id: required (integer)
DH 그룹 ID

name
name: required (string)
DH 그룹 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"grp_vpn_policy_id":	1,
				"name":	"AES-256-SHA-256-Group14-3600s",
				"p2_crypto_algr":	{
						"vpn_crypto_algr_id":	1,
						"name":	"AES-256"
				},
				"p2_auth_algr":	{
						"vpn_auth_algr_id":	1,
						"name":	"SHA-256"
				},
				"p2_dh_grp":	{
						"vpn_dh_grp_id":	1,
						"name":	"MODP-2048"
				},
				"lftm":	14400,
				"desc":	""
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/groupvpn/server/security/policies/{pk}

보안 정책 설정을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
보안 정책 설정 ID ( grp_vpn_policy_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

보안 정책 설정 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

보안 정책 설정 적용/취소

PUT

/command/groupvpn-server-security-policies/apply

보안 정책 설정을 적용합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

보안 정책 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/groupvpn-server-security-policies/cancel

보안 정책 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

보안 정책 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

