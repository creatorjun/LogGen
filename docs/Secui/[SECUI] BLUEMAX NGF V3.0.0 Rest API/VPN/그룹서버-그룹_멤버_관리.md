GET

GET

POST

  DELETE

PUT

  DELETE

PUT

PUT

(2.0)Group VPN 그룹 멤버 관리 REST API 문서

https://{url}/api/vp

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/vp

그룹 멤버 관리

/groupvpn/4/server/members

/groupvpn/4/server/members/{pk}

그룹 멤버 관리 적용/취소

/command/groupvpn-4-server-members/apply

/command/groupvpn-4-server-members/cancel

그룹 멤버 관리

GET

/groupvpn/4/server/members

그룹 멤버 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

그룹 멤버 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of 그룹 멤버)
결과 데이터

Items: 그룹 멤버
Items

idid: required (string - minLength: 1 - maxLength: 32)
장비 ID  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

passwd
passwd: required (string - minLength: 1 - maxLength: 32)
비밀번호
영문,	숫자,	특수문자만	허용

mmbr_ip
mmbr_ip: required (string)
장비 주소
IPv4	형식

descdesc: required (string - maxLength: 128)
설명

grp_vpn_svr_mmbr_id
grp_vpn_svr_mmbr_id: required (integer)
그룹 멤버 ID(PK)

local_network_list
local_network_list: required (array of 객체)
로컬 네트워크 객체 목록

Items: 객체
Items

addr_obj_id: required (integer)
addr_obj_id
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone: required (one of 1, 2, 3)
zone
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (integer)
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1
ip_info1: required (string)
IP

ip_info2
ip_info2: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"grp_vpn_svr_mmbr_id":	1,
				"id":	"GM#1",
				"mmbr_ip":	"10.10.10.10",
				"local_network_list':	[
						{
								"addr_obj_id":	1,
								"name":	"호스트	객체",
								"zone":	1,
								"desc":	"설명",
								"addr_obj_type":	1,
								"ip_list":	["10.10.10.10"]
						}
				],
				"desc":	"설명"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/groupvpn/4/server/members

그룹 멤버를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

idid: required (string - minLength: 1 - maxLength: 32)
장비 ID  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

passwd
passwd: required (string - minLength: 1 - maxLength: 32)
비밀번호
영문,	숫자,	특수문자만	허용

mmbr_ip
mmbr_ip: required (string)
장비 주소
IPv4	형식

descdesc: required (string - maxLength: 128)
설명

local_network_list
local_network_list: required (array of 객체)
로컬 네트워크 객체 목록

Items: 객체
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

Example:
Example

{
		"id":	"GM#1",
		"passwd":	"비밀번호",
		"mmbr_ip":	"10.10.10.10",
		"local_network_list":	[
				{
						"addr_obj_id":	1
				}
		],
		"desc":	"설명"
}

Response

HTTP status code 200

그룹 멤버 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

idid: required (string - minLength: 1 - maxLength: 32)
장비 ID  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

passwd
passwd: required (string - minLength: 1 - maxLength: 32)
비밀번호
영문,	숫자,	특수문자만	허용

mmbr_ip
mmbr_ip: required (string)
장비 주소
IPv4	형식

descdesc: required (string - maxLength: 128)
설명

grp_vpn_svr_mmbr_id
grp_vpn_svr_mmbr_id: required (integer)
그룹 멤버 ID(PK)

local_network_list
local_network_list: required (array of 객체)
로컬 네트워크 객체 목록

Items: 객체
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name: required (string)
name
이름

zone
zone: required (one of 1, 2, 3)
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (integer)
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1
ip_info1: required (string)
IP

ip_info2
ip_info2: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	{
				"grp_vpn_svr_mmbr_id":	1,
				"id":	"GM#1",
				"passwd":	"비밀번호",
				"mmbr_ip":	"10.10.10.10",
				"local_network_list':	[
						{
								"addr_obj_id":	1,
								"name":	"호스트	객체",
								"zone":	1,
								"desc":	"설명",
								"addr_obj_type":	1,
								"ip_list":	["10.10.10.10"]
						}
				],
				"desc":	"설명"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/groupvpn/4/server/members

그룹 멤버 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of 그룹 멤버 ID)
삭제 아이템 목록

Items: 그룹 멤버 ID
Items

grp_vpn_svr_mmbr_id
grp_vpn_svr_mmbr_id: required (integer)
그룹 멤버 ID(PK)

Example:
Example

{
		"items":	[
				{"grp_vpn_svr_mmbr_id":	1},
				{"grp_vpn_svr_mmbr_id":	2}
		]
}

Response

HTTP status code 200

그룹 멤버 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
삭제 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/groupvpn/4/server/members/{pk}

그룹 멤버을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
그룹 멤버 ID ( grp_vpn_svr_mmbr_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

그룹 멤버 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

idid: required (string - minLength: 1 - maxLength: 32)
장비 ID  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

passwd
passwd: required (string - minLength: 1 - maxLength: 32)
비밀번호
영문,	숫자,	특수문자만	허용

mmbr_ip
mmbr_ip: required (string)
장비 주소
IPv4	형식

descdesc: required (string - maxLength: 128)
설명

grp_vpn_svr_mmbr_id
grp_vpn_svr_mmbr_id: required (integer)
그룹 멤버 ID(PK)

local_network_list
local_network_list: required (array of 객체)
로컬 네트워크 객체 목록

Items: 객체
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name: required (string)
name

이름

zone
zone: required (one of 1, 2, 3)
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (integer)
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1: required (string)
ip_info1
IP

ip_info2
ip_info2: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	{
				"grp_vpn_svr_mmbr_id":	1,
				"id":	"GM#1",
				"mmbr_ip":	"10.10.10.10",
				"local_network_list':	[
						{
								"addr_obj_id":	1,
								"name":	"호스트	객체",
								"zone":	1,
								"desc":	"설명",
								"addr_obj_type":	1,
								"ip_list":	["10.10.10.10"]
						}
				],
				"desc":	"설명"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/groupvpn/4/server/members/{pk}

그룹 멤버을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
그룹 멤버 ID ( grp_vpn_svr_mmbr_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

idid: required (string - minLength: 1 - maxLength: 32)
장비 ID  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

passwd
passwd: required (string - minLength: 1 - maxLength: 32)
비밀번호
영문,	숫자,	특수문자만	허용

mmbr_ip
mmbr_ip: required (string)
장비 주소
IPv4	형식

descdesc: required (string - maxLength: 128)
설명

local_network_list
local_network_list: required (array of 객체)
로컬 네트워크 객체 목록

Items: 객체
Items

addr_obj_id: required (integer)
addr_obj_id
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

Example:
Example

{
		"passwd":	"비밀번호",
		"mmbr_ip":	"10.10.10.10",
		"local_network_list":	[
				{
						"addr_obj_id":	1
				}
		],
		"desc":	"설명"
}

Response

HTTP status code 200

그룹 멤버 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

idid: required (string - minLength: 1 - maxLength: 32)
장비 ID  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

passwd
passwd: required (string - minLength: 1 - maxLength: 32)
비밀번호
영문,	숫자,	특수문자만	허용

mmbr_ip: required (string)
mmbr_ip
장비 주소
IPv4	형식

descdesc: required (string - maxLength: 128)
설명

grp_vpn_svr_mmbr_id: required (integer)
grp_vpn_svr_mmbr_id
그룹 멤버 ID(PK)

local_network_list
local_network_list: required (array of 객체)
로컬 네트워크 객체 목록

Items: 객체
Items

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone
zone: required (one of 1, 2, 3)
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type: required (integer)
addr_obj_type
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1
ip_info1: required (string)
IP

ip_info2: required (string)
ip_info2
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	{
				"grp_vpn_svr_mmbr_id":	1,
				"id":	"GM#1",
				"passwd":	"비밀번호",
				"mmbr_ip":	"10.10.10.10",
				"local_network_list':	[
						{
								"addr_obj_id":	1,
								"name":	"호스트	객체",
								"zone":	1,
								"desc":	"설명",
								"addr_obj_type":	1,
								"ip_list":	["10.10.10.10"]
						}
				],
				"desc":	"설명"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/groupvpn/4/server/members/{pk}

그룹 멤버을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
그룹 멤버 ID ( grp_vpn_svr_mmbr_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

그룹 멤버 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

그룹 멤버 관리 적용/취소

PUT

/command/groupvpn-4-server-members/apply

그룹 멤버 관리을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

그룹 멤버 관리 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/groupvpn-4-server-members/cancel

그룹 멤버 관리을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

그룹 멤버 관리 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

