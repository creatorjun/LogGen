GET

GET

POST

  DELETE

PUT

  DELETE

PUT

PUT

(2.0)Group VPN 보안 정책 우회 목록 REST API 문서

https://{url}/api/vp

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/vp

보안 정책 우회 설정

/groupvpn/4/server/bypass/policies

/groupvpn/4/server/bypass/policies/{pk}

보안 정책 우회 목록 적용/취소

/command/groupvpn-4-server-bypass-policies/apply

/command/groupvpn-4-server-bypass-policies/cancel

보안 정책 우회 설정

GET

/groupvpn/4/server/bypass/policies

보안 정책 우회 설정 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

보안 정책 우회 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of 보안 정책 우회 설정)
결과 데이터

Items: 보안 정책 우회 설정
Items

name: required (string - minLength: 1 - maxLength: 32)
name
이름  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

descdesc: required (string - maxLength: 128)
설명

grp_vpn_svr_bypass_id
grp_vpn_svr_bypass_id: required (integer)
우회 설정 ID(PK)

src_dst
src_dst: required (array of 출발지/목적지 객체 정보 )
Items: 출발지/목적지 객체 정보
Items

srcsrc: required (object)
출발지 객체

addr_obj_id: required (integer)
addr_obj_id
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone
zone: required (one of 1, 2, 3)
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (integer)
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1
ip_info1: required (string)
IP

ip_info2
ip_info2: required (string)
넷마스크

dstdst: required (object)
목적지 객체

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone
zone: required (one of 1, 2, 3)
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (integer)
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1: required (string)
ip_info1
IP

ip_info2
ip_info2: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"grp_vpn_svr_bypass_id":	1,
				"name":	"group-bypass-01",
				"src_dst":	[
						{
								"src":	{
										"addr_obj_id":	1,
										"name":	"호스트	객체",
										"zone":	1,
										"desc":	"설명",
										"addr_obj_type":	1,
										"ip_list":	["10.10.10.10"]
								},
								"dst":	{
										"addr_obj_id":	2
										"name":	"네트워크	객체",
										"zone":	1,
										"desc":	"설명",
										"addr_obj_type":	2,
										"ip_info1":	"20.20.20.20",
										"ip_info2":	24
								}
						}
				],
				"desc":	"설명"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/groupvpn/4/server/bypass/policies

보안 정책 우회 설정를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 32)
이름  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

descdesc: required (string - maxLength: 128)
설명

src_dst: required (array of 출발지/목적지 객체 정보 )
src_dst
Items: 출발지/목적지 객체 정보
Items

srcsrc: required (object)
출발지 객체

addr_obj_id: required (integer)
addr_obj_id

주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

dstdst: required (object)
목적지 객체

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

Example:
Example

{
		"name":	"group-bypass-01",
		"src_dst":	[
				{
						"src":	{
								"addr_obj_id":	1
						},
						"dst":	{
								"addr_obj_id":	2
						}
				}
		],
		"desc":	"설명"
}

Response

HTTP status code 200

보안 정책 우회 설정 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 32)
이름  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

descdesc: required (string - maxLength: 128)
설명

grp_vpn_svr_bypass_id
grp_vpn_svr_bypass_id: required (integer)
우회 설정 ID(PK)

src_dst: required (array of 출발지/목적지 객체 정보 )
src_dst
Items: 출발지/목적지 객체 정보
Items

srcsrc: required (object)
출발지 객체

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone: required (one of 1, 2, 3)
zone
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (integer)
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1
ip_info1: required (string)
IP

ip_info2
ip_info2: required (string)
넷마스크

dstdst: required (object)
목적지 객체

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone
zone: required (one of 1, 2, 3)
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (integer)
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1
ip_info1: required (string)
IP

ip_info2
ip_info2: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	{
				"grp_vpn_svr_bypass_id":	1,
				"name":	"group-bypass-01",
				"src_dst":	[
						{
								"src":	{
										"addr_obj_id":	1,
										"name":	"호스트	객체",
										"zone":	1,
										"desc":	"설명",
										"addr_obj_type":	1,
										"ip_list":	["10.10.10.10"]
								},
								"dst":	{
										"addr_obj_id":	2
										"name":	"네트워크	객체",
										"zone":	1,
										"desc":	"설명",
										"addr_obj_type":	2,
										"ip_info1":	"20.20.20.20",
										"ip_info2":	24
								}
						}
				],
				"desc":	"설명"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/groupvpn/4/server/bypass/policies

보안 정책 우회 설정 목록을 (일괄)삭제한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of 우회 설정 ID)
삭제 아이템 목록

Items: 우회 설정 ID
Items

grp_vpn_svr_bypass_id
grp_vpn_svr_bypass_id: required (integer)
우회 설정 ID(PK)

Example:
Example

{
		"items":	[
				{"grp_vpn_svr_bypass_id":	1},
				{"grp_vpn_svr_bypass_id":	2}
		]
}

Response

HTTP status code 200

보안 정책 우회 설정 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
삭제 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/groupvpn/4/server/bypass/policies/{pk}

보안 정책 우회 설정을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
보안 정책 우회 설정 ID ( grp_vpn_svr_bypass_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

보안 정책 우회 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

name: required (string - minLength: 1 - maxLength: 32)
name
이름  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

descdesc: required (string - maxLength: 128)
설명

grp_vpn_svr_bypass_id
grp_vpn_svr_bypass_id: required (integer)
우회 설정 ID(PK)

src_dst
src_dst: required (array of 출발지/목적지 객체 정보 )
Items: 출발지/목적지 객체 정보
Items

srcsrc: required (object)
출발지 객체

addr_obj_id: required (integer)
addr_obj_id
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone
zone: required (one of 1, 2, 3)
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type: required (integer)
addr_obj_type
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1
ip_info1: required (string)
IP

ip_info2
ip_info2: required (string)
넷마스크

dstdst: required (object)
목적지 객체

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone
zone: required (one of 1, 2, 3)
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (integer)
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1: required (string)
ip_info1
IP

ip_info2
ip_info2: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	{
				"grp_vpn_svr_bypass_id":	1,
				"name":	"group-bypass-01",
				"src_dst":	[
						{
								"src":	{
										"addr_obj_id":	1,
										"name":	"호스트	객체",
										"zone":	1,
										"desc":	"설명",
										"addr_obj_type":	1,
										"ip_list":	["10.10.10.10"]
								},
								"dst":	{
										"addr_obj_id":	2
										"name":	"네트워크	객체",
										"zone":	1,
										"desc":	"설명",
										"addr_obj_type":	2,
										"ip_info1":	"20.20.20.20",
										"ip_info2":	24
								}
						}
				],
				"desc":	"설명"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/groupvpn/4/server/bypass/policies/{pk}

보안 정책 우회 설정을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
보안 정책 우회 설정 ID ( grp_vpn_svr_bypass_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 32)
이름  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

descdesc: required (string - maxLength: 128)
설명

src_dst
src_dst: required (array of 출발지/목적지 객체 정보 )
Items: 출발지/목적지 객체 정보
Items

srcsrc: required (object)
출발지 객체

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

dstdst: required (object)
목적지 객체

addr_obj_id
addr_obj_id: required (integer)
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

Example:
Example

{
		"src_dst":	[
				{
						"src":	{
								"addr_obj_id":	1
						},
						"dst":	{
								"addr_obj_id":	2
						}
				}
		],
		"desc":	"설명"
}

Response

HTTP status code 200

보안 정책 우회 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 32)
이름  숫자,	영문(A-Za-z),	특수문자('-',	'_')	만	허용,	편집	불가

descdesc: required (string - maxLength: 128)
설명

grp_vpn_svr_bypass_id
grp_vpn_svr_bypass_id: required (integer)
우회 설정 ID(PK)

src_dst: required (array of 출발지/목적지 객체 정보 )
src_dst
Items: 출발지/목적지 객체 정보
Items

srcsrc: required (object)
출발지 객체

addr_obj_id: required (integer)
addr_obj_id
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone: required (one of 1, 2, 3)
zone
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type: required (integer)
addr_obj_type

객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1
ip_info1: required (string)
IP

ip_info2
ip_info2: required (string)
넷마스크

dstdst: required (object)
목적지 객체

addr_obj_id: required (integer)
addr_obj_id
주소 객체 ID
내부	존이면서	호스트	객체와,	네트워크(IP주소)	객체만	설정	허용
객체에	대한	상세한	정보는	객체	설정	참고

name
name: required (string)
이름

zone: required (one of 1, 2, 3)
zone
Zone 정보( 내부:	1,	외부:	2,	DMZ:	3 )

descdesc: required (string)
설명

addr_obj_type
addr_obj_type: required (integer)
객체 타입
1:	호스트	객체,	2:	네트워크(IP	주소)	객체

ip_info1
ip_info1: required (string)
IP

ip_info2
ip_info2: required (string)
넷마스크

Example:
Example

{
		"code":	"ok",
		"result":	{
				"grp_vpn_svr_bypass_id":	1,
				"name":	"group-bypass-01",
				"src_dst":	[
						{
								"src":	{
										"addr_obj_id":	1,
										"name":	"호스트	객체",
										"zone":	1,
										"desc":	"설명",
										"addr_obj_type":	1,
										"ip_list":	["10.10.10.10"]
								},
								"dst":	{
										"addr_obj_id":	2
										"name":	"네트워크	객체",
										"zone":	1,
										"desc":	"설명",
										"addr_obj_type":	2,
										"ip_info1":	"20.20.20.20",
										"ip_info2":	24
								}
						}
				],
				"desc":	"설명"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/groupvpn/4/server/bypass/policies/{pk}

보안 정책 우회 설정을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
보안 정책 우회 설정 ID ( grp_vpn_svr_bypass_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

보안 정책 우회 설정 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message

결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

보안 정책 우회 목록 적용/취소

PUT

/command/groupvpn-4-server-bypass-policies/apply

보안 정책 우회 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

보안 정책 우회 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/groupvpn-4-server-bypass-policies/cancel

보안 정책 우회 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

보안 정책 우회 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

