GET

POST

  DELETE

GET

POST

GET

PUT

  DELETE

GET

GET

GET

POST

  DELETE

PUT

  DELETE

GET

POST

PUT

PUT

웹카테고리 객체 REST API 문서

https://{host}/api/op

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

웹 카테고리 객체

/webcategory/objects

/webcategory/objects/save

/webcategory/objects/batch

/webcategory/objects/{pk}

/webcategory/objects/{pk}/ref

/webcategory/objects/{pid}/urls

/webcategory/objects/{pid}/urls/{pk}

/webcategory/objects/{pid}/urls/save

/webcategory/objects/{pid}/urls/batch

객체 설정 적용/취소

/command/objects/apply

/command/objects/cancel

웹 카테고리 객체

GET

/webcategory/objects

웹 카테고리 객체 목록을 조회한다.

Request

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

웹 카테고리 객체 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of WebCategoryGetObjList)
결과 데이터

Items: WebCategoryGetObjList
Items

name
name: required (string - minLength: 1 - maxLength: 48)
웹 카테고리 객체 이름

descdesc: required (string - maxLength: 128)
웹 카테고리 객체 설명

web_cate_obj_id
web_cate_obj_id: required (integer)
객체 ID

upper_web_cate_obj_id
upper_web_cate_obj_id: required (integer)
상위 객체 ID

upper_web_cate_name
upper_web_cate_name: required (string)
상위 객체 이름

predef
predef: required (one of 0, 1)
사용자 정의 객체 여부
사용자	정의:	0,	시스템	정의:	1

example_site
example_site: (integer)
예제 사이트
시스템	정의(1)  웹 카테고리 객체일 경우 표시

refref: required (object)
참조 정보

fw_ref: required (one of true, false)
fw_ref
방화벽 정책 참조 여부

fwv6_ref
fwv6_ref: required (one of true, false)
IPv6 방화벽 정책 참조 여부

nat_ref
nat_ref: required (one of true, false)
NAT 정책 참조 여부

nat66_ref
nat66_ref: required (one of true, false)
IPv6 NAT 정책 참조 여부

grp_ref
grp_ref: required (one of true, false)
그룹 객체 참조 여부

apptag_ref: required (one of true, false)
apptag_ref
애플리케이션 태깅 참조 여부

apptagv6_ref
apptagv6_ref: required (one of true, false)
IPv6 애플리케이션 태깅 참조 여부

vpn_ref
vpn_ref: required (one of true, false)
IPSec VPN 참조 여부

grp_vpn_ref
grp_vpn_ref: required (one of true, false)
Group VPN 참조 여부

sslvpn_ref
sslvpn_ref: required (one of true, false)
SSL VPN 참조 여부

ipsddos_ref: required (one of true, false)
ipsddos_ref
IPS/DDoS 참조 여부

content_ref
content_ref: required (one of true, false)
콘텐츠 보안 참조 여부

auth_ref
auth_ref: required (one of true, false)
인증 포털 정책 참조 여부

authv6_ref
authv6_ref: required (one of true, false)
IPv6 인증 포털 정책 참조 여부

qos_ref
qos_ref: required (one of true, false)
QoS 참조 여부

sdwan_ref
sdwan_ref: required (one of true, false)
SD-WAN 참조 여부

V3.0.0	에서	추가됨

Example:
Example

{
		"fw_ref":	true,
		"fwv6_ref":	true,
		"nat_ref":	true,
		"nat66_ref":	true,
		"grp_ref":	true,
		"apptag_ref":	true,
		"apptagv6_ref":	true,
		"vpn_ref":	true,
		"grp_vpn_ref":	true,
		"sslvpn_ref":	true,
		"ipsddos_ref":	true,
		"content_ref":	true,
		"auth_ref":	true,
		"authv6_ref":	true,
		"qos_ref":	true,
		"sdwan_ref":	false
}

cfg_adm_id
cfg_adm_id: required (string)
수정한 관리자 ID

cfg_adm
cfg_adm: required (string)
수정한 관리자 이름

cfg_dt
cfg_dt: required (string)
수정 날짜

mmbr_count
mmbr_count: required (integer)
그룹에 포함된 URL 수

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"web_cate_obj_id":	901,
				"name":	"웹	카테고리	객체	이름",
				"upper_web_cate_obj_id":	1,
				"upper_web_cate_name":	"상위	웹	카테고리	객체	이름",
				"desc":	"웹	카테고리	객체	설명",
				"predef":	0,
				"mmbr_count":	2,
				"cfg_adm_id":	1,
				"cfg_adm":	"admin",
				"cfg_dt":	"2018-03-01	12:12:12",
				"ref":	{
						//	상세	속성	정의	참고
				}
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/webcategory/objects

웹 카테고리 객체를 추가한다.

Request

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 48)
웹 카테고리 객체 이름

descdesc: required (string - maxLength: 128)
웹 카테고리 객체 설명

Example:
Example

{
		"name":	"New	웹	카테고리	객체	이름",
		"desc":	"New	웹	카테고리	객체	설명"
}

Response

HTTP status code 200

웹 카테고리 객체 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 48)
웹 카테고리 객체 이름

descdesc: required (string - maxLength: 128)
웹 카테고리 객체 설명

web_cate_obj_id
web_cate_obj_id: required (integer)
객체 ID

upper_web_cate_obj_id: required (integer)
upper_web_cate_obj_id
상위 객체 ID

upper_web_cate_name
upper_web_cate_name: required (string)
상위 객체 이름

predef
predef: required (one of 0, 1)
사용자 정의 객체 여부
사용자	정의:	0,	시스템	정의:	1

example_site
example_site: (integer)
예제 사이트
시스템	정의(1)  웹 카테고리 객체일 경우 표시

refref: required (object)
참조 정보

fw_ref
fw_ref: required (one of true, false)
방화벽 정책 참조 여부

fwv6_ref
fwv6_ref: required (one of true, false)
IPv6 방화벽 정책 참조 여부

nat_ref
nat_ref: required (one of true, false)
NAT 정책 참조 여부

nat66_ref
nat66_ref: required (one of true, false)
IPv6 NAT 정책 참조 여부

grp_ref
grp_ref: required (one of true, false)
그룹 객체 참조 여부

apptag_ref: required (one of true, false)
apptag_ref
애플리케이션 태깅 참조 여부

apptagv6_ref
apptagv6_ref: required (one of true, false)
IPv6 애플리케이션 태깅 참조 여부

vpn_ref
vpn_ref: required (one of true, false)
IPSec VPN 참조 여부

grp_vpn_ref
grp_vpn_ref: required (one of true, false)
Group VPN 참조 여부

sslvpn_ref
sslvpn_ref: required (one of true, false)
SSL VPN 참조 여부

ipsddos_ref: required (one of true, false)
ipsddos_ref
IPS/DDoS 참조 여부

content_ref
content_ref: required (one of true, false)
콘텐츠 보안 참조 여부

auth_ref
auth_ref: required (one of true, false)
인증 포털 정책 참조 여부

authv6_ref
authv6_ref: required (one of true, false)
IPv6 인증 포털 정책 참조 여부

qos_ref
qos_ref: required (one of true, false)
QoS 참조 여부

sdwan_ref
sdwan_ref: required (one of true, false)
SD-WAN 참조 여부

V3.0.0	에서	추가됨

Example:
Example

{
		"fw_ref":	true,
		"fwv6_ref":	true,
		"nat_ref":	true,
		"nat66_ref":	true,
		"grp_ref":	true,
		"apptag_ref":	true,
		"apptagv6_ref":	true,
		"vpn_ref":	true,
		"grp_vpn_ref":	true,
		"sslvpn_ref":	true,
		"ipsddos_ref":	true,
		"content_ref":	true,
		"auth_ref":	true,
		"authv6_ref":	true,
		"qos_ref":	true,
		"sdwan_ref":	false
}

cfg_adm_id
cfg_adm_id: required (string)
수정한 관리자 ID

cfg_adm: required (string)
cfg_adm
수정한 관리자 이름

cfg_dt
cfg_dt: required (string)
수정 날짜

Example:
Example

{
		"code":	"ok",
		"result":	{
				"web_cate_obj_id":	901,
				"name":	"New	웹	카테고리	객체	이름",
				"upper_web_cate_obj_id":	900,
				"upper_web_cate_name":	"상위	웹	카테고리	객체	이름",
				"desc":	"New	웹	카테고리	객체	설명",
				"predef":	0,
				"mmbr_count":	0,
				"example_site":	"",
				"cfg_adm_id":	1,
				"cfg_adm":	"admin",
				"cfg_dt":	"2018-03-01	12:12:12",
				"ref":	{
						//	상세	속성	정의	참고
				}
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/webcategory/objects

웹 카테고리 객체 목록을 (일괄)삭제한다.

Request

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"web_cate_obj_id":	1},
				{"web_cate_obj_id":	2}
		]
}

Response

HTTP status code 200

웹 카테고리 객체 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/webcategory/objects/save

웹 카테고리 객체을 PC로 저장합니다.

Request

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

columns
columns: (string)
다운로드되는 엑셀 파일 내 테이블 헤더 값으로 사용될 값을 전달한다.(데이터 테이블에 표시되는 컬럼 순으로 지정)
columns 쿼리스트링에 값이 없을 경우 백엔드에 정의된 속성명으로 표시됩니다.
한글이	포함될	수	있기	때문에	UTF-8	인코딩되어야	합니다.

Example:
Example

?columns=(타입,웹	카테고리	이름,상위	카테고리	이름,URL	카운트,설명,사용여부)

Response

HTTP status code 200

웹 카테고리 객체를 PC로 저장 성공

Body

Media type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Media type

TypeType: file

[Task 진행 상태 조회]
배치를 실행 후 응답된 태스크 ID로 상태를 체크합니다.
상태:  PENDING,	PROGRESS,	FAILURE,	REVOKED,	SUCCESS

PENDING: 준비 중
PROGRESS: 처리 중
FAILURE: 실패
REVOKED: 중지 성공
SUCCESS: 성공

GET	{{url}}/api/co/async/tasks/{{task_id}}
{ "code": "ok", "result": { "id": "c295d300-9b7f-4c10-9468-6538c0abab66", "status": "SUCCESS", "attributes": { } } }

POST

/webcategory/objects/batch

웹카테고리 객체을 배치 작업을 시작합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

filefile: required (string)
파일 업로드 후 응답 데이터의 파일명을 인자로 전달

[파일 업로드 API]
POST	{{url}}/api/co/file/import

Example:
Example

{
		"file":	"batch_file.xlsx"
}

Response

HTTP status code 202

웹카테고리 객체 배치 실행

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

idid: required (string)
비동기 태스크 ID

Example:
Example

f810f701-e732-4698-a5a9-a93000a828d9

status
status: required (string)
상태( PENDING,	PROGRESS,	FAILURE,	REVOKED,	SUCCESS )

PENDING: 준비 중
PROGRESS: 처리 중
FAILURE: 실패
REVOKED: 중지 성공
SUCCESS: 성공

attributes
attributes: required (object)
부가 정보

Example:
Example

{
		"code":	"async",
		"result":	{
				"id":	"f810f701-e732-4698-a5a9-a93000a828d9",
				"status":	"PENDING",
				"attributes":	{}
		}
}

HTTP status code 400

웹카테고리 객체 배치 실행 실패

GET

/webcategory/objects/{pk}

웹 카테고리 객체을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
객체 ID

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

웹 카테고리 객체 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 48)
웹 카테고리 객체 이름

descdesc: required (string - maxLength: 128)
웹 카테고리 객체 설명

web_cate_obj_id: required (integer)
web_cate_obj_id
객체 ID

upper_web_cate_obj_id
upper_web_cate_obj_id: required (integer)
상위 객체 ID

upper_web_cate_name: required (string)
upper_web_cate_name
상위 객체 이름

predef
predef: required (one of 0, 1)
사용자 정의 객체 여부
사용자	정의:	0,	시스템	정의:	1

example_site: (integer)
example_site
예제 사이트
시스템	정의(1)  웹 카테고리 객체일 경우 표시

refref: required (object)
참조 정보

fw_ref: required (one of true, false)
fw_ref
방화벽 정책 참조 여부

fwv6_ref
fwv6_ref: required (one of true, false)
IPv6 방화벽 정책 참조 여부

nat_ref
nat_ref: required (one of true, false)
NAT 정책 참조 여부

nat66_ref
nat66_ref: required (one of true, false)
IPv6 NAT 정책 참조 여부

grp_ref
grp_ref: required (one of true, false)
그룹 객체 참조 여부

apptag_ref
apptag_ref: required (one of true, false)
애플리케이션 태깅 참조 여부

apptagv6_ref
apptagv6_ref: required (one of true, false)
IPv6 애플리케이션 태깅 참조 여부

vpn_ref: required (one of true, false)
vpn_ref
IPSec VPN 참조 여부

grp_vpn_ref
grp_vpn_ref: required (one of true, false)
Group VPN 참조 여부

sslvpn_ref
sslvpn_ref: required (one of true, false)
SSL VPN 참조 여부

ipsddos_ref
ipsddos_ref: required (one of true, false)
IPS/DDoS 참조 여부

content_ref
content_ref: required (one of true, false)
콘텐츠 보안 참조 여부

auth_ref: required (one of true, false)
auth_ref
인증 포털 정책 참조 여부

authv6_ref
authv6_ref: required (one of true, false)
IPv6 인증 포털 정책 참조 여부

qos_ref
qos_ref: required (one of true, false)
QoS 참조 여부

sdwan_ref
sdwan_ref: required (one of true, false)
SD-WAN 참조 여부

V3.0.0	에서	추가됨

Example:
Example

{
		"fw_ref":	true,
		"fwv6_ref":	true,
		"nat_ref":	true,
		"nat66_ref":	true,
		"grp_ref":	true,
		"apptag_ref":	true,
		"apptagv6_ref":	true,
		"vpn_ref":	true,
		"grp_vpn_ref":	true,
		"sslvpn_ref":	true,
		"ipsddos_ref":	true,
		"content_ref":	true,
		"auth_ref":	true,
		"authv6_ref":	true,
		"qos_ref":	true,
		"sdwan_ref":	false
}

cfg_adm_id
cfg_adm_id: required (string)
수정한 관리자 ID

cfg_adm
cfg_adm: required (string)
수정한 관리자 이름

cfg_dt
cfg_dt: required (string)
수정 날짜

Example:
Example

{
		"code":	"ok",
		"result":	{
				"web_cate_obj_id":	901,
				"name":	"웹	카테고리	객체	이름",
				"upper_web_cate_obj_id":	1,
				"upper_web_cate_name":	"상위	웹	카테고리	객체	이름",
				"desc":	"웹	카테고리	객체	설명",
				"predef":	0,
				"mmbr_count":	2,
				"example_site":	"",
				"cfg_adm_id":	1,
				"cfg_adm":	"admin",
				"cfg_dt":	"2018-03-01	12:12:12",
				"ref":	{
						//	상세	속성	정의	참고
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/webcategory/objects/{pk}

웹 카테고리 객체을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
객체 ID

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 48)
웹 카테고리 객체 이름

descdesc: required (string - maxLength: 128)
웹 카테고리 객체 설명

Example:
Example

{
		"name":	"웹	카테고리	객체	이름2",
		"desc":	"웹	카테고리	객체	설명2"
}

Response

HTTP status code 200

웹 카테고리 객체 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

name
name: required (string - minLength: 1 - maxLength: 48)
웹 카테고리 객체 이름

descdesc: required (string - maxLength: 128)
웹 카테고리 객체 설명

web_cate_obj_id
web_cate_obj_id: required (integer)
객체 ID

upper_web_cate_obj_id: required (integer)
upper_web_cate_obj_id
상위 객체 ID

upper_web_cate_name
upper_web_cate_name: required (string)
상위 객체 이름

predef: required (one of 0, 1)
predef

사용자 정의 객체 여부
사용자	정의:	0,	시스템	정의:	1

example_site
example_site: (integer)
예제 사이트
시스템	정의(1)  웹 카테고리 객체일 경우 표시

refref: required (object)
참조 정보

fw_ref
fw_ref: required (one of true, false)
방화벽 정책 참조 여부

fwv6_ref
fwv6_ref: required (one of true, false)
IPv6 방화벽 정책 참조 여부

nat_ref
nat_ref: required (one of true, false)
NAT 정책 참조 여부

nat66_ref
nat66_ref: required (one of true, false)
IPv6 NAT 정책 참조 여부

grp_ref
grp_ref: required (one of true, false)
그룹 객체 참조 여부

apptag_ref: required (one of true, false)
apptag_ref
애플리케이션 태깅 참조 여부

apptagv6_ref
apptagv6_ref: required (one of true, false)
IPv6 애플리케이션 태깅 참조 여부

vpn_ref
vpn_ref: required (one of true, false)
IPSec VPN 참조 여부

grp_vpn_ref
grp_vpn_ref: required (one of true, false)
Group VPN 참조 여부

sslvpn_ref
sslvpn_ref: required (one of true, false)
SSL VPN 참조 여부

ipsddos_ref: required (one of true, false)
ipsddos_ref
IPS/DDoS 참조 여부

content_ref
content_ref: required (one of true, false)
콘텐츠 보안 참조 여부

auth_ref: required (one of true, false)
auth_ref
인증 포털 정책 참조 여부

authv6_ref
authv6_ref: required (one of true, false)
IPv6 인증 포털 정책 참조 여부

qos_ref
qos_ref: required (one of true, false)
QoS 참조 여부

sdwan_ref
sdwan_ref: required (one of true, false)
SD-WAN 참조 여부

V3.0.0	에서	추가됨

Example:
Example

{
		"fw_ref":	true,
		"fwv6_ref":	true,
		"nat_ref":	true,
		"nat66_ref":	true,
		"grp_ref":	true,
		"apptag_ref":	true,
		"apptagv6_ref":	true,
		"vpn_ref":	true,
		"grp_vpn_ref":	true,
		"sslvpn_ref":	true,
		"ipsddos_ref":	true,
		"content_ref":	true,
		"auth_ref":	true,
		"authv6_ref":	true,
		"qos_ref":	true,
		"sdwan_ref":	false
}

cfg_adm_id
cfg_adm_id: required (string)
수정한 관리자 ID

cfg_adm: required (string)
cfg_adm
수정한 관리자 이름

cfg_dt
cfg_dt: required (string)
수정 날짜

Example:
Example

{
		"code":	"ok",
		"result":	{
				"web_cate_obj_id":	101,
				"name":	"웹	카테고리	객체	이름2",
				"upper_web_cate_obj_id":	1,
				"upper_web_cate_name":	"상위	웹	카테고리	객체	이름",
				"desc":	"웹	카테고리	객체	설명2",
				"predef":	0,
				"mmbr_count":	2,
				"example_site":	"",
				"cfg_adm_id":	1,
				"cfg_adm":	"admin",
				"cfg_dt":	"2018-03-01	12:12:12",
				"ref":	{
						//	상세	속성	정의	참고
				}
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/webcategory/objects/{pk}

웹 카테고리 객체을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
객체 ID

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

웹 카테고리 객체 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/webcategory/objects/{pk}/ref

객체가 참조되고 있는 상세 항목을 조회한다.

Request

URI Parameters

pkpk: required (integer)
객체 ID

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

객체 참조 상세 보기 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

fw_ref
fw_ref: required (one of true, false)
방화벽 정책 참조 여부

fwv6_ref
fwv6_ref: required (one of true, false)
IPv6 방화벽 정책 참조 여부

nat_ref
nat_ref: required (one of true, false)
NAT 정책 참조 여부

nat66_ref
nat66_ref: required (one of true, false)
IPv6 NAT 정책 참조 여부

grp_ref
grp_ref: required (one of true, false)
그룹 객체 참조 여부

apptag_ref
apptag_ref: required (one of true, false)
애플리케이션 태깅 참조 여부

apptagv6_ref: required (one of true, false)
apptagv6_ref
IPv6 애플리케이션 태깅 참조 여부

vpn_ref
vpn_ref: required (one of true, false)
IPSec VPN 참조 여부

grp_vpn_ref
grp_vpn_ref: required (one of true, false)
Group VPN 참조 여부

sslvpn_ref
sslvpn_ref: required (one of true, false)
SSL VPN 참조 여부

ipsddos_ref
ipsddos_ref: required (one of true, false)
IPS/DDoS 참조 여부

content_ref
content_ref: required (one of true, false)
콘텐츠 보안 참조 여부

auth_ref
auth_ref: required (one of true, false)
인증 포털 정책 참조 여부

authv6_ref: required (one of true, false)
authv6_ref
IPv6 인증 포털 정책 참조 여부

qos_ref
qos_ref: required (one of true, false)
QoS 참조 여부

sdwan_ref: required (one of true, false)
sdwan_ref

SD-WAN 참조 여부

V3.0.0	에서	추가됨

fwfw: required (array of 방화벽 정책)
방화벽 정책 참조 상세 정보
fw_ref  속성이 true 일 경우 출력됨

Items: 방화벽 정책
Items

fw_set_id
fw_set_id: required (integer)
세트 ID

fw_set_name
fw_set_name: required (string)
세트명

fw_rule_id
fw_rule_id: required (integer)
정책 ID

fw_rule_name: required (string)
fw_rule_name
정책 명

is_backup
is_backup: required (integer)
백업 여부(타임라인으로 저장된 세트)
0:	정책	세트,	1:	백업	세트

SupportedVersion:	2.0.0

fwv6
fwv6: required (array of 방화벽 정책)
IPv6 방화벽 정책 참조 상세 정보
fwv6_ref  속성이 true 일 경우 출력됨

Items: 방화벽 정책
Items

fw_set_id
fw_set_id: required (integer)
세트 ID

fw_set_name
fw_set_name: required (string)
세트명

fw_rule_id
fw_rule_id: required (integer)
정책 ID

fw_rule_name
fw_rule_name: required (string)
정책 명

is_backup: required (integer)
is_backup
백업 여부(타임라인으로 저장된 세트)
0:	정책	세트,	1:	백업	세트

SupportedVersion:	2.0.0

natnat: required (array of NAT 정책)
NAT 정책 참조 상세 정보
nat_ref  속성이 true 일 경우 출력됨

Items: NAT 정책
Items

nat_set_id: required (integer)
nat_set_id
세트 ID

nat_set_name
nat_set_name: required (string)
세트명

nat_rule_id
nat_rule_id: required (integer)
정책 ID

nat_rule_name
nat_rule_name: required (string)
정책 명

nat66
nat66: required (array of NAT 정책)
IPv6 NAT 정책 참조 상세 정보
nat66_ref  속성이 true 일 경우 출력됨

Items: NAT 정책
Items

nat_set_id
nat_set_id: required (integer)
세트 ID

nat_set_name: required (string)
nat_set_name
세트명

nat_rule_id
nat_rule_id: required (integer)
정책 ID

nat_rule_name
nat_rule_name: required (string)
정책 명

grpgrp: required (array of 그룹 객체)
그룹 객체 참조 상세 정보
grp_ref  속성이 true 일 경우 출력됨

Items: 그룹 객체
Items

idid: required (integer)
그룹 객체 ID

name
name: required (string)
그룹 객체 이름

apptag
apptag: required (array of 애플리케이션 태깅)
애플리케이션 태깅 참조 상세 정보
apptag_ref  속성이 true 일 경우 출력됨

Items: 애플리케이션 태깅
Items

app_tag_rule_id
app_tag_rule_id: required (integer)
애플리케이션 태깅 ID

name
name: required (string)
애플리케이션 태깅 정책 이름

apptagv6
apptagv6: required (array of 애플리케이션 태깅)
IPv6 애플리케이션 태깅 참조 상세 정보
apptagv6_ref  속성이 true 일 경우 출력됨

Items: 애플리케이션 태깅
Items

app_tag_rule_id
app_tag_rule_id: required (integer)
애플리케이션 태깅 ID

name
name: required (string)
애플리케이션 태깅 정책 이름

vpnvpn: required (object)
IPSec VPN 참조 상세 정보
vpn_ref  속성이 true 일 경우 출력됨

vpn_branch
vpn_branch: required (array of 지점 연결 설정)
Items: 지점 연결 설정
Items

vpn_branch_id
vpn_branch_id: required (integer)
지점 연결 설정 ID(pk)

name
name: required (string)
연결 이름

vpn_branch_policy
vpn_branch_policy: required (array of 지점 등록)
Items: 지점 등록
Items

vpn_branch_policy_id
vpn_branch_policy_id: required (integer)
지점 등록 ID(PK)

branch_id
branch_id: required (string)
지점 ID

vpn_remote_usr
vpn_remote_usr: required (array of 원격 사용자 등록)
Items: 원격 사용자 등록
Items

vpn_remote_usr_id
vpn_remote_usr_id: required (integer)
원격 사용자 등록 ID(PK)

usr_id
usr_id: required (string)
사용자 ID

vpn_bypass_obj
vpn_bypass_obj: required (array of IPSec 우회 목록)
Items: IPSec 우회 목록
Items

vpn_byapss_obj_id
vpn_byapss_obj_id: required (integer)
우회 목록 ID(PK)

idid: required (string)
ID

vpn_branch_v6
vpn_branch_v6: required (array of 지점 연결 설정)
Items: 지점 연결 설정
Items

vpn_branch_id
vpn_branch_id: required (integer)
지점 연결 설정 ID(pk)

name
name: required (string)
연결 이름

vpn_branch_policy_v6
vpn_branch_policy_v6: required (array of 지점 등록)
Items: 지점 등록
Items

vpn_branch_policy_id
vpn_branch_policy_id: required (integer)
지점 등록 ID(PK)

branch_id
branch_id: required (string)
지점 ID

vpn_remote_usr_v6
vpn_remote_usr_v6: required (array of 원격 사용자 등록)
Items: 원격 사용자 등록
Items

vpn_remote_usr_id
vpn_remote_usr_id: required (integer)
원격 사용자 등록 ID(PK)

usr_id
usr_id: required (string)
사용자 ID

vpn_bypass_obj_v6
vpn_bypass_obj_v6: required (array of IPSec 우회 목록)
Items: IPSec 우회 목록
Items

vpn_byapss_obj_id
vpn_byapss_obj_id: required (integer)
우회 목록 ID(PK)

idid: required (string)
ID

grep_ipip_tnl
grep_ipip_tnl: required (array of GREP/IPIP 터널링)
Items: GREP/IPIP 터널링
Items

gre_ipip_tnl_id
gre_ipip_tnl_id: required (string)
터널링 ID

name
name: required (string)
연결 이름

pptp_tnl
pptp_tnl: required (array of PPTP)
Items: PPTP
Items

name
name: required (string)
이름(PPTP)

l2tp_tnl
l2tp_tnl: required (array of L2TP)
Items: L2TP
Items

name
name: required (string)
이름(L2TP)

grp_vpn
grp_vpn: required (object)
Group VPN 참조 상세 정보
grp_vpn_ref  속성이 true 일 경우 출력됨

TEST1: required (integer)
TEST1
세트 ID

sslvpn
sslvpn: required (array of SSL VPN)
SSL VPN 참조 상세 정보
sslvpn_ref  속성이 true 일 경우 출력됨

Items: SSL VPN
Items

sslvpn_grp_id
sslvpn_grp_id: required (integer)
그룹 ID

name
name: required (string)
그룹 이름

ipsddos
ipsddos: required (object)
IPS/DDoS 참조 상세 정보
ipsddos_ref  속성이 true 일 경우 출력됨

ipsips: required (array of IPS)
Items: IPS
Items

ips_prf_id
ips_prf_id: required (integer)
IPS 프로파일 ID

name
name: required (string)
IPS 프로파일 이름

domain
domain: required (array of IPS)
Items: IPS
Items

ddos_dmn_id: required (integer)
ddos_dmn_id
보호 도메인 ID

name
name: required (string)
보호 도메인 이름

ddos: required (array of DDoS)
ddos
Items: DDoS
Items

ddos_prf_id
ddos_prf_id: required (integer)
DDoS 프로파일 ID

name
name: required (string)
DDoS 프로파일 이름

content
content: required (object)
콘텐츠 보안 참조 상세 정보
content_ref  속성이 true 일 경우 출력됨

sslv4: required (array of 복호화 정책 설정)
sslv4
Items: 복호화 정책 설정
Items

ssl_trfc_id
ssl_trfc_id: required (integer)
ID

sslv6: required (array of 복호화 정책 설정)
sslv6
Items: 복호화 정책 설정
Items

ssl_trfc_id
ssl_trfc_id: required (integer)
ID

ssl_excpt
ssl_excpt: required (array of 복호화 예외 목록)
Items: 복호화 예외 목록
Items

ssl_trfc_id
ssl_trfc_id: required (integer)
ID

wfwf: required (array of 웹 필터)
Items: 웹 필터
Items

wf_prf_id
wf_prf_id: required (integer)
프로파일 ID

name
name: required (string)
프로파일 이름

dlpdlp: required (array of DLP 예외 프로파일)
Items: DLP 예외 프로파일
Items

dlp_excpt_prf_id
dlp_excpt_prf_id: required (integer)
DLP 예외 프로파일 ID

name
name: required (string)
DLP 예외 프로파일 이름

filetype
filetype: required (array of 파일 유형 제어 프로파일)
Items: 파일 유형 제어 프로파일
Items

file_type_id
file_type_id: required (integer)
파일 유형 제어 프로파일 ID

name: required (string)
name

파일 유형 제어 프로파일 이름

authauth: required (array of 인증 포털 정책)
인증 포털 정책 참조 상세 정보
auth_ref  속성이 true 일 경우 출력됨

Items: 인증 포털 정책
Items

auth_portal_rule_id
auth_portal_rule_id: required (integer)
인증 포털 정책 ID

auth_portal_rule_name
auth_portal_rule_name: required (string)
인증 포털 정책 이름

authv6
authv6: required (array of 인증 포털 정책)
IPv6 인증 포털 정책 참조 상세 정보
authv6_ref  속성이 true 일 경우 출력됨

Items: 인증 포털 정책
Items

auth_portal_rule_id
auth_portal_rule_id: required (integer)
인증 포털 정책 ID

auth_portal_rule_name: required (string)
auth_portal_rule_name
인증 포털 정책 이름

qosqos: required (array of QoS)
QoS 참조 상세 정보
qos_ref  속성이 true 일 경우 출력됨

Items: QoS
Items

qos_prf_id
qos_prf_id: required (integer)
QoS 프로파일 ID(PK)

qos_prf_name
qos_prf_name: required (string)
QoS 프로파일 이름

qos_class_id
qos_class_id: required (integer)
QoS 큐 ID

qos_class_name
qos_class_name: required (string)
QoS 큐 이름

sdwan
sdwan: required (array of SD-WAN 정책)
SD-WAN 참조 상세 정보
sdwan_ref  속성이 true 일 경우 출력됨

V3.0.0	에서	추가됨

Items: SD-WAN 정책
Items

idid: required (integer)
SD-WAN 정책 ID

name
name: required (string)
SD-WAN 정책 이름

Example:
Example

{
		"fw_ref":	true,
		"fwv6_ref":	true,
		"nat_ref":	true,
		"nat66_ref":	true,
		"grp_ref":	true,
		"apptag_ref":	true,
		"apptagv6_ref":	true,
		"vpn_ref":	true,
		"grp_vpn_ref":	true,
		"sslvpn_ref":	true,
		"ipsddos_ref":	true,
		"content_ref":	true,
		"auth_ref":	true,
		"authv6_ref":	true,
		"qos_ref":	true,
		"sdwan_ref":	true,
		"fw":	[{"fw_set_id":	1,	"fw_set_name":	"test_set",	"fw_rule_id":	2,	"fw_rule_name":	"test_rule"}],
		"fwv6":	[{"fw_set_id":	1,	"fw_set_name":	"test_set",	"fw_rule_id":	2,	"fw_rule_name":	"test_rule"}],
		"nat":	[{"nat_set_id":	1,	"nat_set_name":	"test_set",	"nat_rule_id":	2,	"nat_rule_name":	"test_rule"}],
		"nat66":	[{"nat_set_id":	1,	"nat_set_name":	"test_set",	"nat_rule_id":	2,	"nat_rule_name":	"test_rule"}],
		"grp":	[{"id":	1,	"name":	"test_grp"}],
		"apptag":	[{"app_tag_rule_id":	1,	"name":	"test_rule"}],
		"apptagv6":	[{"app_tag_rule_id":	1,	"name":	"test_rule"}],
		"vpn":	{
				"vpn_branch":	[{"vpn_branch_id":	1,	"name":	"test"}],
				"vpn_branch_policy":	[{"vpn_branch_policy_id":	1,	"branch_id":	"test"}],
				"vpn_remote_usr":	[{"vpn_remote_usr_id":	1,	"usr_id":	"test"}],
				"vpn_bypass_obj":	[{"vpn_bypass_obj_id":	1,	"id":	"test"}],
				"vpn_branch_v6":	[{"vpn_branch_id":	1,	"name":	"test"}],
				"vpn_branch_policy_v6":	[{"vpn_branch_policy_id":	1,	"branch_id":	"test"}],
				"vpn_remote_usr_v6":	[{"vpn_remote_usr_id":	1,	"usr_id":	"test"}],
				"vpn_bypass_obj_v6":	[{"vpn_byapss_obj_id":	1,	"id":	"test"}],
				"grep_ipip_tnl":	[{"gre_ipip_tnl_id":	1,	"name":	"test"}],
				"pptp_tnl":	[{"name":	"True"}],
				"l2tp_tnl":	[{"name":	"True"}]
		},
		"grp_vpn":	{
				"TEST1":	1
		},
		"sslvpn":	[{"sslvpn_grp_id":	1,	"name":	"test"}],
		"ipsddos":	{
				"ips":	[{"ips_prf_id":	1,	"name":	"test"}],
				"domain":	[{"ddos_dmn_id":	1,	"name":	"test"}],
				"ddos":	[{"ddos_prf_id":	1,	"name":	"test"}]
		},
		"content":	{
				"sslv4":	[{"ssl_trfc_id":	1}],
				"sslv6":	[{"ssl_trfc_id":	1}],
				"ssl_excpt":	[{"ssl_trfc_id":	1}],
				"wf":	[{"wf_prf_id":	1,	"name":	"test"}],
				"dlp":	[{"dlp_excpt_prf_id":	1,	"name":	"test"}],
				"filetype":	[{"file_type_id":	1,	"name":	"test"}]
		},
		"auth":	[{"auth_portal_rule_id":	1,	"auth_portal_rule_name":	"test_rule"}],
		"authv6":	[{"auth_portal_rule_id":	1,	"auth_portal_rule_name":	"test_rule"}],
		"qos":	[{"qos_prf_id":	1,	"qos_prf_name":	"test_prf",	"qos_class_id":	2,	"qos_class_name":	"test_rule"}],
		"sdwan":	[{"id":	1,	"name":	"test"}]
}

Example:
Example

{
		"code":	"ok",
		"result":	{
				//	상세	속성	정의	참고
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/webcategory/objects/{pid}/urls

URL 목록을 조회한다.

Request

URI Parameters

pidpid: required (integer)
웹 카테고리 객체 ID

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

max_page
max_page: required (integer)
페이지당 건수

page
page: required (integer)
현재 페이지 수

qq: (object)
상세 검색 조건
q=속성 %3D 값 , 속성 %3D 값```

case
case: (string)
대소문자 구분
구분	없음:	0,	구분:	1

keyword
keyword: (string)
검색 내용

fields
fields: (string)
검색 항목
URL:	url
검사	방식:	chk_type
설명:	desc

Example:
Example

?q=case%3D0,keyword%3D1.1.1.1,fields%3Durl%3Bdesc%3Bchk_type

Response

HTTP status code 200

URL 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

count
count: required (integer)
조회(검색)된 전체 URL 건수

url_list
url_list: required (array of WebCategoryGetUrl)
URL 목록

Items: WebCategoryGetUrl
Items

urlurl: required (string - minLength: 1 - maxLength: 1024)
사용자 정의 URL
영문,	숫자,	-,	.,	_,	~,	:,	/,	?,	#,	[,	],	@,	!,	$,	&,	‘,	(,	),	*,	+,	,

chk_type
chk_type: required (one of 1, 2, 3)
검사 방식
와일드카드:1,	정규표현식:2,	일반:	3

descdesc: (string - maxLength: 128)
사용자 정의 URL 설명

web_cate_obj_id: required (integer)
web_cate_obj_id
웹 카테고리 객체 ID

web_cate_url_id
web_cate_url_id: required (integer)
URL ID(PK)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"count":	1,
				"url_list":	[{
						"web_cate_obj_id":	901,
						"web_cate_url_id":	1,
						"url":	"www.example.com",
						"chk_type":	1,
						"desc":	"사용자	정의	URL	설명"
				}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message

결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/webcategory/objects/{pid}/urls

URL를 추가한다.

Request

URI Parameters

pidpid: required (integer)
웹 카테고리 객체 ID

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

urlurl: required (string - minLength: 1 - maxLength: 1024)
사용자 정의 URL
영문,	숫자,	-,	.,	_,	~,	:,	/,	?,	#,	[,	],	@,	!,	$,	&,	‘,	(,	),	*,	+,	,

chk_type
chk_type: required (one of 1, 2, 3)
검사 방식
와일드카드:1,	정규표현식:2,	일반:	3

descdesc: (string - maxLength: 128)
사용자 정의 URL 설명

Example:
Example

{
		"url":	"www.example.com",
		"chk_type":	1,
		"desc":	"사용자	정의	URL	설명"
}

Response

HTTP status code 200

URL 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

urlurl: required (string - minLength: 1 - maxLength: 1024)
사용자 정의 URL
영문,	숫자,	-,	.,	_,	~,	:,	/,	?,	#,	[,	],	@,	!,	$,	&,	‘,	(,	),	*,	+,	,

chk_type
chk_type: required (one of 1, 2, 3)
검사 방식
와일드카드:1,	정규표현식:2,	일반:	3

descdesc: (string - maxLength: 128)
사용자 정의 URL 설명

web_cate_obj_id
web_cate_obj_id: required (integer)
웹 카테고리 객체 ID

web_cate_url_id
web_cate_url_id: required (integer)
URL ID(PK)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"web_cate_obj_id":	901,
				"web_cate_url_id":	1,
				"url":	"www.example.com",
				"chk_type":	1,
				"desc":	"사용자	정의	URL	설명"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/webcategory/objects/{pid}/urls

URL 목록을 (일괄)삭제한다.

Request

URI Parameters

pidpid: required (integer)
웹 카테고리 객체 ID

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of object)
items
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"web_cate_url_id":	1},
				{"web_cate_url_id":	2}
		]
}

Response

HTTP status code 200

URL 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/webcategory/objects/{pid}/urls/{pk}

URL을(를) 조회한다.

Request

URI Parameters

pidpid: required (integer)
웹 카테고리 객체 ID

pkpk: required (integer)
URL ID(PK)

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

URL 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

urlurl: required (string - minLength: 1 - maxLength: 1024)
사용자 정의 URL
영문,	숫자,	-,	.,	_,	~,	:,	/,	?,	#,	[,	],	@,	!,	$,	&,	‘,	(,	),	*,	+,	,

chk_type
chk_type: required (one of 1, 2, 3)
검사 방식
와일드카드:1,	정규표현식:2,	일반:	3

descdesc: (string - maxLength: 128)
사용자 정의 URL 설명

web_cate_obj_id
web_cate_obj_id: required (integer)
웹 카테고리 객체 ID

web_cate_url_id
web_cate_url_id: required (integer)
URL ID(PK)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"web_cate_obj_id":	901,
				"web_cate_url_id":	1,
				"url":	"www.example.com",
				"chk_type":	1,
				"desc":	"사용자	정의	URL	설명"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/webcategory/objects/{pid}/urls/{pk}

URL을(를) 수정한다.

Request

URI Parameters

pidpid: required (integer)
웹 카테고리 객체 ID

pkpk: required (integer)
URL ID(PK)

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

urlurl: required (string - minLength: 1 - maxLength: 1024)
사용자 정의 URL
영문,	숫자,	-,	.,	_,	~,	:,	/,	?,	#,	[,	],	@,	!,	$,	&,	‘,	(,	),	*,	+,	,

chk_type
chk_type: required (one of 1, 2, 3)
검사 방식
와일드카드:1,	정규표현식:2,	일반:	3

descdesc: (string - maxLength: 128)
사용자 정의 URL 설명

Example:
Example

{
		"url":	"www.example.com",
		"chk_type":	1,
		"desc":	"사용자	정의	URL	설명"
}

Response

HTTP status code 200

URL 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

urlurl: required (string - minLength: 1 - maxLength: 1024)
사용자 정의 URL
영문,	숫자,	-,	.,	_,	~,	:,	/,	?,	#,	[,	],	@,	!,	$,	&,	‘,	(,	),	*,	+,	,

chk_type: required (one of 1, 2, 3)
chk_type
검사 방식
와일드카드:1,	정규표현식:2,	일반:	3

descdesc: (string - maxLength: 128)
사용자 정의 URL 설명

web_cate_obj_id: required (integer)
web_cate_obj_id
웹 카테고리 객체 ID

web_cate_url_id
web_cate_url_id: required (integer)
URL ID(PK)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"web_cate_obj_id":	901,
				"web_cate_url_id":	1,
				"url":	"www.example.com",
				"chk_type":	1,
				"desc":	"사용자	정의	URL	설명"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/webcategory/objects/{pid}/urls/{pk}

URL을(를) 삭제한다.

Request

URI Parameters

pidpid: required (integer)
웹 카테고리 객체 ID

pkpk: required (integer)
URL ID(PK)

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

URL 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/webcategory/objects/{pid}/urls/save

URL을 PC로 저장합니다.

Request

URI Parameters

pidpid: required (integer)
웹 카테고리 객체 ID

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

columns
columns: (string)
다운로드되는 엑셀 파일 내 테이블 헤더 값으로 사용될 값을 전달한다.(데이터 테이블에 표시되는 컬럼 순으로 지정)
columns 쿼리스트링에 값이 없을 경우 백엔드에 정의된 속성명으로 표시됩니다.
한글이	포함될	수	있기	때문에	UTF-8	인코딩되어야	합니다.

Example:
Example

?columns=(URL,검사	방식,설명)

Response

HTTP status code 200

URL를 PC로 저장 성공

Body

Media type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Media type

TypeType: file

POST

/webcategory/objects/{pid}/urls/batch

URL 목록을 배치로 추가합니다.

Request

URI Parameters

pidpid: required (integer)
웹 카테고리 객체 ID

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

filefile: required (string)
업로드된 엑셀 파일 이름

Example:
Example

{
		"file":	"web_category_url_batch.xlsx"
}

Response

HTTP status code 200

URL 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

객체 설정 적용/취소

PUT

/command/objects/apply

객체 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

객체 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/objects/cancel

객체 설정을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

객체 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

