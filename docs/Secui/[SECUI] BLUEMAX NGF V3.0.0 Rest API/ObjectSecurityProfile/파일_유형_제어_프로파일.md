GET

GET

GET

POST

PUT

  DELETE

GET

PUT

  DELETE

GET

POST

PUT

  DELETE

GET

PUT

  DELETE

PUT

PUT

PUT

파일 유형 제어 프로파일 REST API 문서

https://{url}/api/op

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/op

파일 유형 제어 DB 체크

/filetype/dbcheck

파일 유형

/filetype/types

파일 유형 제어 프로파일

/filetype/profiles

/filetype/profiles/{pk}

파일 유형 제어 정책

/filetype/profiles/{ppk}/policies

/filetype/profiles/{ppk}/policies/{pk}

/filetype/profiles/{ppk}/policies/{pk}/move

파일 유형 제어 프로파일 목록 적용/취소

/command/filetype-profiles/apply

/command/filetype-profiles/cancel

파일 유형 제어 DB 체크

GET

/filetype/dbcheck

파일 유형 제어 DB 체크을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

파일 유형 제어 DB 체크 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

result
result: required (integer)
DB 업데이트 여부
업데이트	완료:	1,	업데이트	필요:	0

Example:
Example

{
		"code":	"ok",
		"result":	{
				"result":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

파일 유형

GET

/filetype/types

파일 유형 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

파일 유형 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of FileType)
결과 데이터

Items: FileType
Items

file_type_cate_id: required (integer)
file_type_cate_id
파일 유형 카테고리 ID

file_type_cate_name
file_type_cate_name: required (string)
파일 유형 카테고리 이름

file_type_cate_desc
file_type_cate_desc: required (string)
파일 유형 카테고리 설명

file_type_id
file_type_id: required (integer)
파일 유형 ID

file_type_name
file_type_name: required (string)
파일 유형 이름

file_type_desc: required (string)
file_type_desc

파일 유형 설명

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"file_type_id":	10001,
				"file_type_name":	"pdf",
				"file_type_desc":	"Portable	101	Format	(PDF)	is	a	file	format.",
				"file_type_cate_id":	101,
				"file_type_cate_name":	"document",
				"file_type_cate_desc":	"A	document	file	format	is	a	text	or	binary	file	format	fo"
		},	{
				"file_type_id":	10002,
				"file_type_name":	"doc",
				"file_type_desc":	"A	DOC	is	a	101	file	format	used	by	Microsoft...",
				"file_type_cate_id":	101,
				"file_type_cate_name":	"document",
				"file_type_cate_desc":	"A	document	file	format	is	a	text	or	binary	file	format	fo"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

파일 유형 제어 프로파일

GET

/filetype/profiles

파일 유형 제어 프로파일 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

파일 유형 제어 프로파일 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of FileTypeGetProfile)
결과 데이터

Items: FileTypeGetProfile
Items

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

name
name: required (string - minLength: 1 - maxLength: 32)
이름

descdesc: (string - maxLength: 128)
설명

file_type_ctrl_prf_id
file_type_ctrl_prf_id: required (integer)
프로파일 ID

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"file_type_ctrl_prf_id":	1,
				"use":	1,
				"name":	"프로파일	이름",
				"desc":	"프로파일	설명"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/filetype/profiles

파일 유형 제어 프로파일를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

name
name: required (string - minLength: 1 - maxLength: 32)
이름

descdesc: (string - maxLength: 128)
설명

Example:
Example

{
		"use":	1,
		"name":	"New	프로파일	이름",
		"desc":	"New	프로파일	설명"
}

Response

HTTP status code 200

파일 유형 제어 프로파일 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

name
name: required (string - minLength: 1 - maxLength: 32)
이름

descdesc: (string - maxLength: 128)
설명

file_type_ctrl_prf_id: required (integer)
file_type_ctrl_prf_id

프로파일 ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"file_type_ctrl_prf_id":	2,
				"use":	1,
				"name":	"New	프로파일	이름",
				"desc":	"New	프로파일	설명"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/filetype/profiles

파일 유형 제어 프로파일 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of object)
items
수정될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"file_type_ctrl_prf_id":	1,	"use":	0},
				{"file_type_ctrl_prf_id":	2,	"use":	0}
		]
}

Response

HTTP status code 200

파일 유형 제어 프로파일 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result: required (object)
result
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/filetype/profiles

파일 유형 제어 프로파일 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of object)
items
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"file_type_ctrl_prf_id":	1},
				{"file_type_ctrl_prf_id":	2}
		]
}

Response

HTTP status code 200

파일 유형 제어 프로파일 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/filetype/profiles/{pk}

파일 유형 제어 프로파일을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

파일 유형 제어 프로파일 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

name
name: required (string - minLength: 1 - maxLength: 32)
이름

descdesc: (string - maxLength: 128)
설명

file_type_ctrl_prf_id
file_type_ctrl_prf_id: required (integer)
프로파일 ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"file_type_ctrl_prf_id":	1,
				"use":	1,
				"name":	"프로파일	이름",
				"desc":	"프로파일	설명"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/filetype/profiles/{pk}

파일 유형 제어 프로파일을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

name: required (string - minLength: 1 - maxLength: 32)
name
이름

descdesc: (string - maxLength: 128)
설명

Example:
Example

{
		"use":	0,
		"name":	"프로파일	이름",
		"desc":	"프로파일	설명"
}

Response

HTTP status code 200

파일 유형 제어 프로파일 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

name
name: required (string - minLength: 1 - maxLength: 32)
이름

descdesc: (string - maxLength: 128)
설명

file_type_ctrl_prf_id
file_type_ctrl_prf_id: required (integer)
프로파일 ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"file_type_ctrl_prf_id":	1,
				"use":	0,
				"name":	"프로파일	이름",
				"desc":	"프로파일	설명"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/filetype/profiles/{pk}

파일 유형 제어 프로파일을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

파일 유형 제어 프로파일 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

파일 유형 제어 정책

GET

/filetype/profiles/{ppk}/policies

파일 유형 제어 정책 목록을 조회한다.

Request

URI Parameters

ppkppk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

파일 유형 제어 정책 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of FileTypeGetPolicy)
결과 데이터

Items: FileTypeGetPolicy
Items

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

direct
direct: required (one of 0, 1, 2 - default: 0)
탐지 방향
양방향:	0,	보내기:	1,	받기:	2

action
action: required (one of 0, 1 - default: 0)
탐지/차단
탐지:	0,	차단:	1

log_use
log_use: required (one of 0, 1 - default: 0)
로그 사용 여부
사용안함:	0,	사용:	1

dump_use
dump_use: required (one of 0, 1 - default: 0)
패킷 덤프 사용 여부
사용안함:	0,	사용:	1

email_use
email_use: required (one of 0, 1 - default: 0)
메일 송신 사용 여부
사용안함:	0,	사용:	1

descdesc: (string - maxLength: 128)
설명

file_type_ctrl_prf_id
file_type_ctrl_prf_id: required (integer)
프로파일 ID

file_type_ctrl_id
file_type_ctrl_id: required (integer)
정책 ID

seqseq: required (integer)
순번

file_type_list
file_type_list: required (array of FileTypeObject)
파일 유형 목록

Items: FileTypeObject
Items

file_type_id
file_type_id: required (integer)
파일 유형 ID

name
name: required (string)
파일 유형 이름

file_type_app_list
file_type_app_list: required (array of FileTypeAppObject)
애플리케이션 객체 목록

Items: FileTypeAppObject
Items

file_type_app_id
file_type_app_id: required (integer)
앱 객체 ID

name: required (string)
name
앱 객체 이름

file_type_ctrl_excpt
file_type_ctrl_excpt: required (object)
파일 유형 제어 예외 프로파일

file_type_ctrl_excpt_prf_id
file_type_ctrl_excpt_prf_id: required (integer)
예외 프로파일 ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"file_type_ctrl_prf_id":	1,
				"file_type_ctrl_id":	1,
				"seq":	1,
				"use":	1,
				"direct":	1,
				"action":	1,
				"log_use":	1,
				"dump_use":	1,
				"email_use":	1,
				"desc":	"정책	설명",
				"file_type_list":	[
						{"file_type_id":	10001,	"name":	"pdf"},
						{"file_type_id":	10002,	"name":	"ppt"}
				],
				"file_type_app_list":	[
						{"file_type_app_id":	1,	"name":	"앱	이름"}
				],
				"file_type_ctrl_excpt":	{
						"file_type_ctrl_excpt_prf_id":	1,
						"name":	"예외	프로파일	이름"
				}
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/filetype/profiles/{ppk}/policies

파일 유형 제어 정책를 추가한다.

Request

URI Parameters

ppkppk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

direct
direct: required (one of 0, 1, 2 - default: 0)
탐지 방향
양방향:	0,	보내기:	1,	받기:	2

action
action: required (one of 0, 1 - default: 0)
탐지/차단
탐지:	0,	차단:	1

log_use
log_use: required (one of 0, 1 - default: 0)
로그 사용 여부
사용안함:	0,	사용:	1

dump_use
dump_use: required (one of 0, 1 - default: 0)
패킷 덤프 사용 여부
사용안함:	0,	사용:	1

email_use: required (one of 0, 1 - default: 0)
email_use
메일 송신 사용 여부
사용안함:	0,	사용:	1

descdesc: (string - maxLength: 128)
설명

file_type_list: required (array of integer)
file_type_list
파일 유형 ID 목록(ANY: [])
GET	{{url}}/api/op/filetype/types

file_type_app_list
file_type_app_list: required (array of integer)
애플리케이션 ID 목록(ANY: [])
GET	{{url}}/api/op/app/simple/objects
애플리케이션	객체	REST	API	문서	참고

file_type_ctrl_excpt_prf_id
file_type_ctrl_excpt_prf_id: required (integer)
파일 유형 제어 예외 프로파일 ID
GET	{{url}}/api/op/filetype/exceptions

Example:
Example

{
		"use":	1,
		"direct":	1,
		"action":	1,
		"log_use":	1,
		"dump_use":	1,
		"email_use":	1,
		"desc":	"New	정책	설명",
		"file_type_list":	[10001,	10002],
		"file_type_app_list":	[1,	2],
		"file_type_ctrl_excpt_prf_id":	1
}

Response

HTTP status code 200

파일 유형 제어 정책 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

direct
direct: required (one of 0, 1, 2 - default: 0)
탐지 방향
양방향:	0,	보내기:	1,	받기:	2

action
action: required (one of 0, 1 - default: 0)
탐지/차단
탐지:	0,	차단:	1

log_use: required (one of 0, 1 - default: 0)
log_use
로그 사용 여부
사용안함:	0,	사용:	1

dump_use
dump_use: required (one of 0, 1 - default: 0)
패킷 덤프 사용 여부
사용안함:	0,	사용:	1

email_use
email_use: required (one of 0, 1 - default: 0)
메일 송신 사용 여부
사용안함:	0,	사용:	1

descdesc: (string - maxLength: 128)
설명

file_type_ctrl_prf_id
file_type_ctrl_prf_id: required (integer)
프로파일 ID

file_type_ctrl_id
file_type_ctrl_id: required (integer)
정책 ID

seqseq: required (integer)
순번

file_type_list
file_type_list: required (array of FileTypeObject)
파일 유형 목록

Items: FileTypeObject
Items

file_type_id
file_type_id: required (integer)
파일 유형 ID

name
name: required (string)
파일 유형 이름

file_type_app_list
file_type_app_list: required (array of FileTypeAppObject)
애플리케이션 객체 목록

Items: FileTypeAppObject
Items

file_type_app_id: required (integer)
file_type_app_id
앱 객체 ID

name
name: required (string)
앱 객체 이름

file_type_ctrl_excpt: required (object)
file_type_ctrl_excpt
파일 유형 제어 예외 프로파일

file_type_ctrl_excpt_prf_id
file_type_ctrl_excpt_prf_id: required (integer)
예외 프로파일 ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"file_type_ctrl_prf_id":	1,
				"file_type_ctrl_id":	2,
				"seq":	1,
				"use":	1,
				"direct":	1,
				"action":	1,
				"log_use":	1,
				"dump_use":	1,
				"email_use":	1,
				"desc":	"New	정책	설명",
				"file_type_list":	[
						{"file_type_id":	10001,	"name":	"pdf"},
						{"file_type_id":	10002,	"name":	"ppt"}
				],
				"file_type_app_list":	[
						{"file_type_app_id":	1,	"name":	"앱	이름1"},
						{"file_type_app_id":	2,	"name":	"앱	이름2"}
				],
				"file_type_ctrl_excpt":	{
						"file_type_ctrl_excpt_prf_id":	1,
						"name":	"예외	프로파일	이름"
				}
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/filetype/profiles/{ppk}/policies

파일 유형 제어 정책 목록을 (일괄)수정한다.

Request

URI Parameters

ppkppk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
수정될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"file_type_ctrl_id":	1,	"use":	0},
				{"file_type_ctrl_id":	2,	"use":	0}
		]
}

Response

HTTP status code 200

파일 유형 제어 정책 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/filetype/profiles/{ppk}/policies

파일 유형 제어 정책 목록을 (일괄)삭제한다.

Request

URI Parameters

ppkppk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"file_type_ctrl_id":	1},
				{"file_type_ctrl_id":	2}
		]
}

Response

HTTP status code 200

파일 유형 제어 정책 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/filetype/profiles/{ppk}/policies/{pk}

파일 유형 제어 정책을(를) 조회한다.

Request

URI Parameters

ppkppk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

pkpk: required (integer)
정책 ID( file_type_ctrl_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

파일 유형 제어 정책 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

direct
direct: required (one of 0, 1, 2 - default: 0)
탐지 방향
양방향:	0,	보내기:	1,	받기:	2

action
action: required (one of 0, 1 - default: 0)
탐지/차단
탐지:	0,	차단:	1

log_use
log_use: required (one of 0, 1 - default: 0)
로그 사용 여부
사용안함:	0,	사용:	1

dump_use
dump_use: required (one of 0, 1 - default: 0)
패킷 덤프 사용 여부
사용안함:	0,	사용:	1

email_use: required (one of 0, 1 - default: 0)
email_use
메일 송신 사용 여부
사용안함:	0,	사용:	1

descdesc: (string - maxLength: 128)
설명

file_type_ctrl_prf_id: required (integer)
file_type_ctrl_prf_id
프로파일 ID

file_type_ctrl_id
file_type_ctrl_id: required (integer)
정책 ID

seqseq: required (integer)
순번

file_type_list
file_type_list: required (array of FileTypeObject)
파일 유형 목록

Items: FileTypeObject
Items

file_type_id: required (integer)
file_type_id
파일 유형 ID

name
name: required (string)
파일 유형 이름

file_type_app_list: required (array of FileTypeAppObject)
file_type_app_list

애플리케이션 객체 목록

Items: FileTypeAppObject
Items

file_type_app_id: required (integer)
file_type_app_id
앱 객체 ID

name
name: required (string)
앱 객체 이름

file_type_ctrl_excpt: required (object)
file_type_ctrl_excpt
파일 유형 제어 예외 프로파일

file_type_ctrl_excpt_prf_id
file_type_ctrl_excpt_prf_id: required (integer)
예외 프로파일 ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"file_type_ctrl_prf_id":	1,
				"file_type_ctrl_id":	2,
				"seq":	1,
				"use":	1,
				"direct":	1,
				"action":	1,
				"log_use":	1,
				"dump_use":	1,
				"email_use":	1,
				"desc":	"정책	설명",
				"file_type_list":	[
						{"file_type_id":	10001,	"name":	"pdf"},
						{"file_type_id":	10002,	"name":	"ppt"}
				],
				"file_type_app_list":	[
						{"file_type_app_id":	1,	"name":	"앱	이름1"},
						{"file_type_app_id":	2,	"name":	"앱	이름2"}
				],
				"file_type_ctrl_excpt":	{
						"file_type_ctrl_excpt_prf_id":	1,
						"name":	"예외	프로파일	이름"
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/filetype/profiles/{ppk}/policies/{pk}

파일 유형 제어 정책을(를) 수정한다.

Request

URI Parameters

ppkppk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

pkpk: required (integer)
정책 ID( file_type_ctrl_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

direct: required (one of 0, 1, 2 - default: 0)
direct
탐지 방향
양방향:	0,	보내기:	1,	받기:	2

action
action: required (one of 0, 1 - default: 0)
탐지/차단
탐지:	0,	차단:	1

log_use
log_use: required (one of 0, 1 - default: 0)
로그 사용 여부
사용안함:	0,	사용:	1

dump_use
dump_use: required (one of 0, 1 - default: 0)
패킷 덤프 사용 여부
사용안함:	0,	사용:	1

email_use
email_use: required (one of 0, 1 - default: 0)
메일 송신 사용 여부
사용안함:	0,	사용:	1

descdesc: (string - maxLength: 128)
설명

file_type_list
file_type_list: required (array of integer)
파일 유형 ID 목록(ANY: [])
GET	{{url}}/api/op/filetype/types

file_type_app_list: required (array of integer)
file_type_app_list

애플리케이션 ID 목록(ANY: [])
GET	{{url}}/api/op/app/simple/objects
애플리케이션	객체	REST	API	문서	참고

file_type_ctrl_excpt_prf_id: required (integer)
file_type_ctrl_excpt_prf_id
파일 유형 제어 예외 프로파일 ID
GET	{{url}}/api/op/filetype/exceptions

Example:
Example

{
		"use":	1,
		"direct":	1,
		"action":	1,
		"log_use":	0,
		"dump_use":	0,
		"email_use":	0,
		"desc":	"프로파일	설명2",
		"file_type_list":	[10003,	10004],
		"file_type_app_list":	[1,	2],
		"file_type_ctrl_excpt_prf_id":	1
}

Response

HTTP status code 200

파일 유형 제어 정책 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

direct
direct: required (one of 0, 1, 2 - default: 0)
탐지 방향
양방향:	0,	보내기:	1,	받기:	2

action: required (one of 0, 1 - default: 0)
action
탐지/차단
탐지:	0,	차단:	1

log_use
log_use: required (one of 0, 1 - default: 0)
로그 사용 여부
사용안함:	0,	사용:	1

dump_use
dump_use: required (one of 0, 1 - default: 0)
패킷 덤프 사용 여부
사용안함:	0,	사용:	1

email_use
email_use: required (one of 0, 1 - default: 0)
메일 송신 사용 여부
사용안함:	0,	사용:	1

descdesc: (string - maxLength: 128)
설명

file_type_ctrl_prf_id
file_type_ctrl_prf_id: required (integer)
프로파일 ID

file_type_ctrl_id
file_type_ctrl_id: required (integer)
정책 ID

seqseq: required (integer)
순번

file_type_list: required (array of FileTypeObject)
file_type_list
파일 유형 목록

Items: FileTypeObject
Items

file_type_id
file_type_id: required (integer)
파일 유형 ID

name
name: required (string)
파일 유형 이름

file_type_app_list
file_type_app_list: required (array of FileTypeAppObject)
애플리케이션 객체 목록

Items: FileTypeAppObject
Items

file_type_app_id
file_type_app_id: required (integer)
앱 객체 ID

name
name: required (string)
앱 객체 이름

file_type_ctrl_excpt
file_type_ctrl_excpt: required (object)
파일 유형 제어 예외 프로파일

file_type_ctrl_excpt_prf_id
file_type_ctrl_excpt_prf_id: required (integer)
예외 프로파일 ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"file_type_ctrl_prf_id":	1,
				"file_type_ctrl_id":	2,
				"seq":	1,
				"use":	1,
				"direct":	1,
				"action":	1,
				"log_use":	0,
				"dump_use":	0,
				"email_use":	0,
				"desc":	"정책	설명",
				"file_type_list":	[
						{"file_type_id":	10003,	"name":	"ppt"},
						{"file_type_id":	10004,	"name":	"xls"}
				],
				"file_type_app_list":	[
						{"file_type_app_id":	1,	"name":	"앱	이름1"},
						{"file_type_app_id":	2,	"name":	"앱	이름2"}
				],
				"file_type_ctrl_excpt_list":	{
						"file_type_ctrl_excpt_prf_id":	1,	"name":	"예외	프로파일	이름"
				}
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/filetype/profiles/{ppk}/policies/{pk}

파일 유형 제어 정책을(를) 삭제한다.

Request

URI Parameters

ppkppk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

pkpk: required (integer)
정책 ID( file_type_ctrl_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

파일 유형 제어 정책 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/filetype/profiles/{ppk}/policies/{pk}/move

파일 유형 정책을 이동한다.

Request

URI Parameters

ppkppk: required (integer)
파일 유형 제어 프로파일 ID( file_type_ctrl_prf_id )

pkpk: required (integer)
정책 ID( file_type_ctrl_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

before_seq
before_seq: required (integer)
변경 전 순번

after_seq
after_seq: required (integer)
변경 될 순번

Example:
Example

{
		"before_seq":	1,
		"after_seq":	4
}

Response

HTTP status code 200

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용 여부
사용안함:	0,	사용:	1

direct
direct: required (one of 0, 1, 2 - default: 0)
탐지 방향
양방향:	0,	보내기:	1,	받기:	2

action: required (one of 0, 1 - default: 0)
action
탐지/차단
탐지:	0,	차단:	1

log_use
log_use: required (one of 0, 1 - default: 0)
로그 사용 여부
사용안함:	0,	사용:	1

dump_use
dump_use: required (one of 0, 1 - default: 0)
패킷 덤프 사용 여부
사용안함:	0,	사용:	1

email_use: required (one of 0, 1 - default: 0)
email_use
메일 송신 사용 여부
사용안함:	0,	사용:	1

descdesc: (string - maxLength: 128)
설명

file_type_ctrl_prf_id: required (integer)
file_type_ctrl_prf_id
프로파일 ID

file_type_ctrl_id
file_type_ctrl_id: required (integer)
정책 ID

seqseq: required (integer)
순번

file_type_list
file_type_list: required (array of FileTypeObject)
파일 유형 목록

Items: FileTypeObject
Items

file_type_id: required (integer)
file_type_id
파일 유형 ID

name
name: required (string)
파일 유형 이름

file_type_app_list: required (array of FileTypeAppObject)
file_type_app_list
애플리케이션 객체 목록

Items: FileTypeAppObject
Items

file_type_app_id
file_type_app_id: required (integer)
앱 객체 ID

name
name: required (string)
앱 객체 이름

file_type_ctrl_excpt
file_type_ctrl_excpt: required (object)
파일 유형 제어 예외 프로파일

file_type_ctrl_excpt_prf_id
file_type_ctrl_excpt_prf_id: required (integer)
예외 프로파일 ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"file_type_ctrl_prf_id":	1,
				"file_type_ctrl_id":	2,
				"seq":	4,
				"use":	1,
				"direct":	1,
				"action":	1,
				"log_use":	0,
				"dump_use":	0,
				"email_use":	0,
				"desc":	"정책	설명",
				"file_type_list":	[
						{"file_type_id":	10001,	"name":	"pdf"},
						{"file_type_id":	10002,	"name":	"ppt"}
				],
				"file_type_app_list":	[
						{"file_type_app_id":	1,	"name":	"앱	이름1"},
						{"file_type_app_id":	2,	"name":	"앱	이름2"}
				],
				"file_type_ctrl_excpt":	{
						"file_type_ctrl_excpt_prf_id":	1,	"name":	"예외	프로파일	이름"
				}
		}
}

파일 유형 제어 프로파일 목록 적용/취소

PUT

/command/filetype-profiles/apply

파일 유형 제어 프로파일을 적용합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

파일 유형 제어 프로파일 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/filetype-profiles/cancel

파일 유형 제어 프로파일을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type

요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

파일 유형 제어 프로파일 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

