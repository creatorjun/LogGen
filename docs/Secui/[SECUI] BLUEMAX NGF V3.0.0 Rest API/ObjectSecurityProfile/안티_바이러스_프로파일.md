GET

GET

GET

GET

POST

  DELETE

PUT

  DELETE

PUT

PUT

안티바이러스 프로파일 REST API 문서

https://{url}/api/op

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/op

안티바이러스 컨텐츠 DB 체크

/av/dbcheck

안티바이러스 엔진 설정

/av/engine

안티바이러스 프로파일

/av/profiles

/av/profiles/{pk}

안티바이러스 프로파일 적용/취소

/command/av-profiles/apply

/command/av-profiles/cancel

안티바이러스 컨텐츠 DB 체크

GET

/av/dbcheck

안티바이러스 컨텐츠 DB 체크을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

안티바이러스 컨텐츠 DB 체크 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

type
type: required (integer)
엔진타입
SSII:	1,	KAV:	2

result
result: required (integer)
DB 업데이트 여부
업데이트	완료:	1,	업데이트	필요:	0

Example:
Example

{
		"code":	"ok",
		"result":	{
				"type":	1,
				"result":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

안티바이러스 엔진 설정

GET

/av/engine

안티바이러스 엔진 설정을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

안티바이러스 엔진 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

type
type: required (one of 1, 2, 3)
안티바이러스엔진에 사용할 백신타입
SSII:	1,	KAV:	2,	VSE:	3

tm_out
tm_out: required (integer - default: 2 - minimum: 1 - maximum: 300)
안티바이러스 검사 타임아웃 값
GUI에서	사용되지	않음

http_partial_cntn
http_partial_cntn: required (one of 0, 1 - default: 0)
HTTP Partial contents 트래픽 검사 여부( 통과:	0,	차단:	1 )
GUI에서	사용되지	않음

http_scan_direct
http_scan_direct: required (one of 0, 1, 2 - default: 0)
HTTP 트래픽 검사 방향성( Bi-direction:	0,	Upload:	1,	Download:	2 )
GUI에서	사용되지	않음

url_cache_use: required (integer)
url_cache_use
HTTP URL 캐시 기능 사용여부
GUI에서	사용되지	않음

ur_cache_purpose
ur_cache_purpose: required (integer)
URL 캐시 사용 목적
GUI에서	사용되지	않음

black_cache_expire_tm_rel
black_cache_expire_tm_rel: required (integer)
black-cache만료시간(상대시간)
GUI에서	사용되지	않음

black_cache_expire_tm_abs: required (integer)
black_cache_expire_tm_abs

black-cache만료시간(절대시간)
GUI에서	사용되지	않음

black_cache_max_hit
black_cache_max_hit: required (integer)
black-cache 최대 히트수
GUI에서	사용되지	않음

white_cache_expire_tm_rel
white_cache_expire_tm_rel: required (integer)
white-cache만료시간(상대시간)
GUI에서	사용되지	않음

white_cache_expire_tm_abs: required (integer)
white_cache_expire_tm_abs
white-cache만료시간(절대시간)
GUI에서	사용되지	않음

white_cache_max_hit
white_cache_max_hit: required (integer)
white-cache 최대 히트수
GUI에서	사용되지	않음

cntn_skip_use
cntn_skip_use: required (integer)
컨텐츠타입 스킵 여부
GUI에서	사용되지	않음

Example:
Example

{
		"code":	"ok",
		"result":	{
				"type":	1,
				"tm_out":	2,
				"http_partial_cntn":	0,
				"http_scan_direct":	0,
				"url_cache_use":	1,
				"url_cache_purpose":	0,
				"black_cache_expire_tm_rel":	3600,
				"black_cache_expire_tm_abs":	21600,
				"black_cache_max_hit":	1000,
				"white_cache_expire_tm_rel":	30,
				"white_cache_expire_tm_abs":	300,
				"white_cache_max_hit":	1000,
				"cntn_skip_use":	1
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

안티바이러스 프로파일

GET

/av/profiles

안티바이러스 프로파일 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

안티바이러스 프로파일 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ResAvProfile)
결과 데이터

Items: ResAvProfile
Items

name
name: required (string - minLength: 1 - maxLength: 32)
프로파일 이름

descdesc: (string - maxLength: 128)
설명

av_prf_id
av_prf_id: required (integer)
안티바이러스 프로파일 ID

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"av_prf_id":	1,
				"name":	"안티바이러스	프로파일1",
				"desc":	"안티바이러스	프로파일1"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/av/profiles

안티바이러스 프로파일를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
name
name: required (string - minLength: 1 - maxLength: 32)
프로파일 이름

descdesc: (string - maxLength: 128)
설명

Example:
Example

{
		"name":	"New	안티바이러스	프로파일",
		"desc":	"New	안티바이러스	프로파일"
}

Response

HTTP status code 200

안티바이러스 프로파일 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

http_chk_use: (one of 0, 1 - default: 1)
http_chk_use
HTTP 검사 여부
사용안함:	0,	사용:	1

http_scan_opt
http_scan_opt: (one of 0, 1 - default: 0)
HTTP 대응동작
탐지:	0,	차단:	1

http_chk_file_size
http_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
HTTP 파일검사 크기

smtp_chk_use
smtp_chk_use: (one of 0, 1 - default: 1)
SMTP 검사 여부
사용안함:	0,	사용:	1

smtp_scan_opt
smtp_scan_opt: (one of 0, 1, 2 - default: 0)
SMTP 대응동작
탐지:	0,	차단:	1,	알림:	2

smtp_chk_file_size
smtp_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
SMTP 파일검사 크기

pop3_chk_use
pop3_chk_use: (one of 0, 1 - default: 1)
POP3 검사 여부
사용안함:	0,	사용:	1

pop3_scan_opt
pop3_scan_opt: (one of 0, 1 - default: 0)
POP3 대응동작
탐지:	0,	차단:	1

pop3_chk_file_size
pop3_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
POP3 파일검사 크기

ftp_chk_use
ftp_chk_use: (one of 0, 1 - default: 1)
FTP 검사 여부
사용안함:	0,	사용:	1

ftp_scan_opt
ftp_scan_opt: (one of 0, 1 - default: 0)
FTP 대응동작
탐지:	0,	차단:	1

ftp_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
ftp_chk_file_size
FTP 파일검사 크기

imap_chk_use
imap_chk_use: (one of 0, 1 - default: 1)
IMAP 검사 여부
사용안함:	0,	사용:	1

imap_scan_opt: (one of 0, 1 - default: 0)
imap_scan_opt
IMAP 대응동작
탐지:	0,	차단:	1

imap_chk_file_size
imap_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
IMAP 파일검사 크기

sndr_addr: (string - maxLength: 50)
sndr_addr
알림메일 송신자 주소
영문(a-zA-Z),	숫자(0-9),	특수문자(@,	.)로	구성된	메일	주소

smtp_alert_msg
smtp_alert_msg: (string - maxLength: 128)
알림메일 메시지

av_prf_id: required (integer)
av_prf_id
안티바이러스 프로파일 ID

name
name: required (string - minLength: 1 - maxLength: 32)
프로파일 이름

descdesc: (string - maxLength: 128)
설명

av_excpt_list
av_excpt_list: (object)
예외 검사 목록

av_excpt_list_id
av_excpt_list_id: required (integer)
예외 ID

name
name: required (string)
예외 이름

descdesc: required (string)
예외 설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"av_prf_id":	1,
				"name":	"New	안티바이러스	프로파일",
				"desc":	"New	안티바이러스	프로파일",
				"http_chk_use":	1,
				"http_scan_opt":	0,
				"http_chk_file_size":	1024,
				"smtp_chk_use":	1,
				"smtp_scan_opt":	0,
				"smtp_chk_file_size":	1024,
				"pop3_chk_use":	1,
				"pop3_scan_opt":	0,
				"pop3_chk_file_size":	1024,
				"ftp_chk_use":	1,
				"ftp_scan_opt":	0,
				"ftp_chk_file_size":	1024,
				"imap_chk_use":	1,
				"imap_scan_opt":	0,
				"imap_chk_file_size":	1024,
				"sndr_addr":	"",
				"smtp_alert_msg":	"",
				"av_excpt_list":	null
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/av/profiles

안티바이러스 프로파일 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"av_prf_id":	1},
				{"av_prf_id":	2}
		]
}

Response

HTTP status code 200

안티바이러스 프로파일 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/av/profiles/{pk}

안티바이러스 프로파일을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
안티바이러스 프로파일 ID( av_prf_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

안티바이러스 프로파일 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

http_chk_use
http_chk_use: (one of 0, 1 - default: 1)
HTTP 검사 여부
사용안함:	0,	사용:	1

http_scan_opt
http_scan_opt: (one of 0, 1 - default: 0)
HTTP 대응동작
탐지:	0,	차단:	1

http_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
http_chk_file_size
HTTP 파일검사 크기

smtp_chk_use
smtp_chk_use: (one of 0, 1 - default: 1)
SMTP 검사 여부
사용안함:	0,	사용:	1

smtp_scan_opt: (one of 0, 1, 2 - default: 0)
smtp_scan_opt
SMTP 대응동작
탐지:	0,	차단:	1,	알림:	2

smtp_chk_file_size
smtp_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
SMTP 파일검사 크기

pop3_chk_use: (one of 0, 1 - default: 1)
pop3_chk_use
POP3 검사 여부
사용안함:	0,	사용:	1

pop3_scan_opt
pop3_scan_opt: (one of 0, 1 - default: 0)
POP3 대응동작
탐지:	0,	차단:	1

pop3_chk_file_size
pop3_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
POP3 파일검사 크기

ftp_chk_use
ftp_chk_use: (one of 0, 1 - default: 1)
FTP 검사 여부
사용안함:	0,	사용:	1

ftp_scan_opt
ftp_scan_opt: (one of 0, 1 - default: 0)
FTP 대응동작
탐지:	0,	차단:	1

ftp_chk_file_size
ftp_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
FTP 파일검사 크기

imap_chk_use
imap_chk_use: (one of 0, 1 - default: 1)
IMAP 검사 여부
사용안함:	0,	사용:	1

imap_scan_opt
imap_scan_opt: (one of 0, 1 - default: 0)
IMAP 대응동작
탐지:	0,	차단:	1

imap_chk_file_size
imap_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
IMAP 파일검사 크기

sndr_addr
sndr_addr: (string - maxLength: 50)
알림메일 송신자 주소
영문(a-zA-Z),	숫자(0-9),	특수문자(@,	.)로	구성된	메일	주소

smtp_alert_msg
smtp_alert_msg: (string - maxLength: 128)
알림메일 메시지

av_prf_id
av_prf_id: required (integer)
안티바이러스 프로파일 ID

name
name: required (string - minLength: 1 - maxLength: 32)
프로파일 이름

descdesc: (string - maxLength: 128)
설명

av_excpt_list
av_excpt_list: (object)
예외 검사 목록

av_excpt_list_id
av_excpt_list_id: required (integer)
예외 ID

name: required (string)
name
예외 이름

descdesc: required (string)
예외 설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"av_prf_id":	1,
				"name":	"안티바이러스	프로파일1",
				"desc":	"안티바이러스	프로파일1",
				"av_excpt_list":	{
						"av_excpt_list_id":	1,
						"name":	"예외이름",
						"desc":	"예외설명"
				},
				"http_chk_use":	1,
				"http_scan_opt":	0,
				"http_chk_file_size":	1024,
				"smtp_chk_use":	1,
				"smtp_scan_opt":	0,
				"smtp_chk_file_size":	1024,
				"pop3_chk_use":	1,
				"pop3_scan_opt":	0,
				"pop3_chk_file_size":	1024,
				"ftp_chk_use":	1,
				"ftp_scan_opt":	0,
				"ftp_chk_file_size":	1024,
				"imap_chk_use":	1,
				"imap_scan_opt":	0,
				"imap_chk_file_size":	1024,
				"sndr_addr":	"secui@secui.com",
				"smtp_alert_msg":	"SMTP	알림	메시지"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/av/profiles/{pk}

안티바이러스 프로파일을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
안티바이러스 프로파일 ID( av_prf_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

http_chk_use: (one of 0, 1 - default: 1)
http_chk_use
HTTP 검사 여부
사용안함:	0,	사용:	1

http_scan_opt
http_scan_opt: (one of 0, 1 - default: 0)
HTTP 대응동작
탐지:	0,	차단:	1

http_chk_file_size
http_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
HTTP 파일검사 크기

smtp_chk_use
smtp_chk_use: (one of 0, 1 - default: 1)
SMTP 검사 여부
사용안함:	0,	사용:	1

smtp_scan_opt
smtp_scan_opt: (one of 0, 1, 2 - default: 0)
SMTP 대응동작
탐지:	0,	차단:	1,	알림:	2

smtp_chk_file_size
smtp_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
SMTP 파일검사 크기

pop3_chk_use
pop3_chk_use: (one of 0, 1 - default: 1)
POP3 검사 여부
사용안함:	0,	사용:	1

pop3_scan_opt
pop3_scan_opt: (one of 0, 1 - default: 0)
POP3 대응동작
탐지:	0,	차단:	1

pop3_chk_file_size
pop3_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
POP3 파일검사 크기

ftp_chk_use
ftp_chk_use: (one of 0, 1 - default: 1)
FTP 검사 여부
사용안함:	0,	사용:	1

ftp_scan_opt
ftp_scan_opt: (one of 0, 1 - default: 0)
FTP 대응동작
탐지:	0,	차단:	1

ftp_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
ftp_chk_file_size
FTP 파일검사 크기

imap_chk_use
imap_chk_use: (one of 0, 1 - default: 1)
IMAP 검사 여부
사용안함:	0,	사용:	1

imap_scan_opt: (one of 0, 1 - default: 0)
imap_scan_opt
IMAP 대응동작
탐지:	0,	차단:	1

imap_chk_file_size
imap_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
IMAP 파일검사 크기

sndr_addr: (string - maxLength: 50)
sndr_addr

알림메일 송신자 주소
영문(a-zA-Z),	숫자(0-9),	특수문자(@,	.)로	구성된	메일	주소

smtp_alert_msg
smtp_alert_msg: (string - maxLength: 128)
알림메일 메시지

name
name: required (string - minLength: 1 - maxLength: 32)
프로파일 이름

descdesc: (string - maxLength: 128)
설명

av_excpt_list_id
av_excpt_list_id: (integer)
안티바이러스 검사 예외목록 ID
예외	목록을	선택하지	않을	경우	null

Example:
Example

{
		"name":	"안티바이러스	프로파일1",
		"desc":	"안티바이러스	프로파일1",
		"av_excpt_list_id":	2,
		"http_chk_use":	1,
		"http_scan_opt":	0,
		"http_chk_file_size":	1024,
		"smtp_chk_use":	1,
		"smtp_scan_opt":	0,
		"smtp_chk_file_size":	1024,
		"pop3_chk_use":	1,
		"pop3_scan_opt":	0,
		"pop3_chk_file_size":	1024,
		"ftp_chk_use":	1,
		"ftp_scan_opt":	0,
		"ftp_chk_file_size":	1024,
		"imap_chk_use":	1,
		"imap_scan_opt":	0,
		"imap_chk_file_size":	1024,
		"sndr_addr":	"secui@secui.com",
		"smtp_alert_msg":	"SMTP	알림	메시지"
}

Response

HTTP status code 200

안티바이러스 프로파일 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

http_chk_use
http_chk_use: (one of 0, 1 - default: 1)
HTTP 검사 여부
사용안함:	0,	사용:	1

http_scan_opt
http_scan_opt: (one of 0, 1 - default: 0)
HTTP 대응동작
탐지:	0,	차단:	1

http_chk_file_size
http_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
HTTP 파일검사 크기

smtp_chk_use
smtp_chk_use: (one of 0, 1 - default: 1)
SMTP 검사 여부
사용안함:	0,	사용:	1

smtp_scan_opt
smtp_scan_opt: (one of 0, 1, 2 - default: 0)
SMTP 대응동작
탐지:	0,	차단:	1,	알림:	2

smtp_chk_file_size
smtp_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
SMTP 파일검사 크기

pop3_chk_use
pop3_chk_use: (one of 0, 1 - default: 1)
POP3 검사 여부
사용안함:	0,	사용:	1

pop3_scan_opt
pop3_scan_opt: (one of 0, 1 - default: 0)
POP3 대응동작
탐지:	0,	차단:	1

pop3_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
pop3_chk_file_size
POP3 파일검사 크기

ftp_chk_use
ftp_chk_use: (one of 0, 1 - default: 1)
FTP 검사 여부
사용안함:	0,	사용:	1

ftp_scan_opt: (one of 0, 1 - default: 0)
ftp_scan_opt
FTP 대응동작
탐지:	0,	차단:	1

ftp_chk_file_size
ftp_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
FTP 파일검사 크기

imap_chk_use: (one of 0, 1 - default: 1)
imap_chk_use

IMAP 검사 여부
사용안함:	0,	사용:	1

imap_scan_opt
imap_scan_opt: (one of 0, 1 - default: 0)
IMAP 대응동작
탐지:	0,	차단:	1

imap_chk_file_size
imap_chk_file_size: (integer - default: 1024 - minimum: 1 - maximum: 10240)
IMAP 파일검사 크기

sndr_addr
sndr_addr: (string - maxLength: 50)
알림메일 송신자 주소
영문(a-zA-Z),	숫자(0-9),	특수문자(@,	.)로	구성된	메일	주소

smtp_alert_msg
smtp_alert_msg: (string - maxLength: 128)
알림메일 메시지

av_prf_id
av_prf_id: required (integer)
안티바이러스 프로파일 ID

name
name: required (string - minLength: 1 - maxLength: 32)
프로파일 이름

descdesc: (string - maxLength: 128)
설명

av_excpt_list
av_excpt_list: (object)
예외 검사 목록

av_excpt_list_id
av_excpt_list_id: required (integer)
예외 ID

name: required (string)
name
예외 이름

descdesc: required (string)
예외 설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"av_prf_id":	1,
				"name":	"안티바이러스	프로파일1",
				"desc":	"안티바이러스	프로파일1",
				"av_excpt_list":	{
						"av_excpt_list_id":	2,
						"name":	"예외이름2",
						"desc":	"예외설명2"
				},
				"http_chk_use":	1,
				"http_scan_opt":	0,
				"http_chk_file_size":	1024,
				"smtp_chk_use":	1,
				"smtp_scan_opt":	0,
				"smtp_chk_file_size":	1024,
				"pop3_chk_use":	1,
				"pop3_scan_opt":	0,
				"pop3_chk_file_size":	1024,
				"ftp_chk_use":	1,
				"ftp_scan_opt":	0,
				"ftp_chk_file_size":	1024,
				"imap_chk_use":	1,
				"imap_scan_opt":	0,
				"imap_chk_file_size":	1024,
				"sndr_addr":	"secui@secui.com",
				"smtp_alert_msg":	"SMTP	알림	메시지"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/av/profiles/{pk}

안티바이러스 프로파일을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
안티바이러스 프로파일 ID( av_prf_id )

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

안티바이러스 프로파일 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

안티바이러스 프로파일 적용/취소

PUT

/command/av-profiles/apply

안티바이러스 프로파일을 적용합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

안티바이러스 프로파일 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code

결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/av-profiles/cancel

안티바이러스 프로파일을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

안티바이러스 프로파일 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

