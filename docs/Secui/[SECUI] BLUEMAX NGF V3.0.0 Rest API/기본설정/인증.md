인증 REST API 문서

https://{host}/api/au

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

[외부연동]API 토큰 발급

NGF GUI에 로그인한 다음 해당 관리자 편집으로  외부	클라이언트	연동 을 활성화를 하고 Client ID/Client Secret 을 발급하고 이 두 값을 외부 연동용 로그인 정보로 사용함

/external/login

[외부연동]API 토큰 갱신

발급된 API 토큰은 24시간의 유효하기 때문에 만료 전에 갱신이 필요하다. 인증 시 함께 발급된 refresh_token을 이용해서 API 토큰을 갱신할 수 있다.

/external/refresh-token

[외부연동]로그아웃

인증 후 API 토큰이 발급되면 24시간 유지되고 또한 인증 세션도 24시간 동안 유지된다.
로그아웃 되지 않은 상태에서 재로그인(force 옵션 없이)할 경우 인증 실패 오류가 출력될 수 있기 때문에 유의가 필요하다.

/external/logout

UI 초기 데이터

/ui-data

/ui-data/menu

대시보드 위젯

/static/widgets

사용자 인증 REST API

Security	Policy	>	사용자	인지/인증	>	사용자	인증	고급	설정	>	인증	포털	활성화  기능이 켜져야 사용 가능

URL:	https://{host}:{port}/api/userauth/authentication
{port}:	인증	웹	페이지	접속	포트(기본값:	12345)

/userauth/authentication

[외부연동]API 토큰 발급

POST

/external/login

API 토큰을 발급한다.

Request

Headers

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저 환경에서 요청되지 않을 경우 필수로 설정해줘야 된다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Accept: required (string)
Accept
Example:
Example

application/json

Content-Type
Content-Type: required (string)
Example:
Example

POST

PUT

DELETE

GET

GET

GET

POST

application/json

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

ext_clnt_id
ext_clnt_id: required (string)
외부 클라이언트 연동 기능의 Client ID

ext_clnt_secret
ext_clnt_secret: required (string)
외부 클라이언트 연동 기능의 Client Secret

lang
lang: required (one of ko, en)
언어 코드
국문:	ko,	영문:	en

force
force: (integer)
기존에 로그인된 계정 정보를 로그아웃 시키고 새로운 세션을 생성하도록 하는 옵션 속성(설정이 필요 할 대  1 로 설정)
편집 중인 계정에 대해서 세션이 만료되었거나 연동 장비 장애로 정상적으로 로그아웃이 되지 않을 경우 force 속성으로 재 로그인을 해야지 NGF GUI에서 설정을 편집할 수 있다.

!주의 -  force:	1 을 사용하여 로그인 할 경우 동일 계정에서 편집 후 적용되지 않은 내용은 삭제됩니다.

Example:
Example

{
		"ext_clnt_id":	"a911d17b5ac4460996c0963a7cecbfbd",
		"ext_clnt_secret":	"7cda11e72708dc0efa47f0700349524a1ea526cc10b545e8ee35d98bdc9909f2",
		"lang":	"ko"
}

//	force
{
		"ext_clnt_id":	"0471a4e9a3134f45b4b8961dbae91e47",
		"ext_clnt_secret":	"483d76595723e5a1900b7596fabe4797d19cf3a65d270ac8957328059c4bf1b8",
		"lang":	"ko",
		"force":	1
}

Response

HTTP status code 200

API 토큰을 생성한다.

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

Example:
Example

ok,	fail...

result
result: required (object)
응답 결과

api_token
api_token: required (string)
API 토큰
REST API 호출 시 Authorization 헤더에 설정으로 인증 가능
API 토큰은 24간 동안 유효하기 때문에 만료되기 전에 refresh_token으로 갱신하거나 재로그인(force 옵션 설정)으로 재발급해야 합니다.

refresh_token: required (string)
refresh_token
Refresh 토큰
API 토큰을 갱신 할 때 사용

issue_date
issue_date: required (string)
API 토큰의 만료 기간(장비 시간 기준)
장비 시간은 /api/co/current-time 로 조회 할 수 있음

Example:
Example

{
		"code":	"ok",
		"result":	{
				"api_token":	"eyJhbGciOiJI.....aAJ9nGY_7BQ",
				"refresh_token":	"eyJhbGciOiJIUzI1NiJ9.....qKQOXXIQd7llL7C87Q",
				"issue_date":	"2020-01-01	10:56:56"
		}
}

[외부연동]API 토큰 갱신

PUT

/external/refresh-token

API 토큰을 갱신한다.

Request

Headers

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저 환경에서 요청되지 않을 경우 필수로 설정해줘야 된다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Accept: required (string)
Accept
Example:
Example

application/json

Content-Type
Content-Type: required (string)
Example:
Example

application/json

Authorization
Authorization: required (string)
Refresh Token
인증 후 응답에 포함된  refresh_token  을 설정한다.

Response

HTTP status code 200

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
응답 결과

api_token
api_token: required (string)
API 토큰
REST API 호출 시 Authorization 헤더에 설정으로 인증 가능
API 토큰은 24간 동안 유효하기 때문에 만료되기 전에 refresh_token으로 갱신하거나 재로그인(force 옵션 설정)으로 재발급해야 합니다.

refresh_token
refresh_token: required (string)
Refresh 토큰
API 토큰을 갱신 할 때 사용

issue_date: required (string)
issue_date
API 토큰의 만료 기간(장비 시간 기준)
장비 시간은 /api/co/current-time 로 조회 할 수 있음

Example:
Example

{
		"code":	"ok",
		"result":	{
				"api_token":	"eyJhbGciOiJI.....aAJ9nGY_7BQ",
				"refresh_token":	"eyJhbGciOiJIUzI1NiJ9.....qKQOXXIQd7llL7C87Q",
				"issue_date":	"2018-09-19	10:56:56"
		}
}

[외부연동]로그아웃

DELETE

/external/logout

로그아웃 한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

로그아웃 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message

결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

UI 초기 데이터

GET

/ui-data

UI 초기 데이터 조회

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

UI 데이터 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
응답 결과

system
system: required (object)
장비 접속 정보

modelmodel: required (string)
장비 모델

current_time
current_time: required (string)
장비 현재 시간

vs_mode
vs_mode: required (one of GUI, VSI, HOST)
VS 모드

contents_mgmt
contents_mgmt: required (one of self, host)
컨텐츠 DB 관리 방식

host_uuid: required (string)
host_uuid
장비 UUID

cc_flag
cc_flag: required (one of 0, 1)
CC 사용 (OFF:	0,	ON:	1)

gs_flag
gs_flag: required (one of 0, 1)
GS 사용 (OFF:	0,	ON:	1)

ipip: required (string)
장비 접속 IP

gui_port
gui_port: required (integer - minimum: 0 - maximum: 65535)
장비 접속 Port

gui_idle_exit
gui_idle_exit: required (one of 0, 1)
유휴 상태 설정 사용 (OFF:	0,	ON:	1)

gui_idle_use
gui_idle_use: required (one of 0, 1)
유휴 상태시 GUI 종료 사용 (OFF:	0,	ON:	1)

gui_idle_tm: required (integer)
gui_idle_tm
최대 유휴 시간(분)

ve_type
ve_type: required (one of 0, 1, 2, 3, 4, 5, 6, 7)
NGF VE 모델 전용 속성

VE 플랫폼 정보
0:	VE	모델	아님
1:	citrix-xen(microsoft)
2:	kvm
3:	vmware
4:	aws(xen)
5:	azure
6:	openstack
7:	hyper-v

ve_license
ve_license: required (one of 0, 1)
NGF VE 모델 전용 속성

볼륨 라이선스 초과 여부
0:	볼륨	라이선스	초과
1:	라이선스	정상

ve_autoscaling
ve_autoscaling: required (one of 0, 1, 2)
NGF VE 모델 전용 속성

AutoScaling Master 여부
0:	Standalone
1:	AutoScaling	Master
2:	Autoscaling	Member

ve_is_cloud: required (one of 0, 1)
ve_is_cloud
NGF VE 모델 전용 속성

클라우드 플랫폼 여부
0:	클라우드	플랫폼	아님
1:	클라우드	플랫폼(aws,	azure,	openstack)

update_license
update_license: required (one of 0, 1)
라이선스가 자동 업데이트 되었는지 이력 관리 속성 (업데이트	안됨:	0,	업데이트됨:	1)

reboot
reboot: required (one of 0, 1)
재시작 버튼 (Invisible:	0,	Visible:	1)

update_list
update_list: required (array of any)
라이선스 자동 업데이트된 장비 이름 리스트

user
user: required (object)
관리자 정보

adm_id: required (integer)
adm_id

ID

login_id
login_id: required (string)
관리자 ID

type: required (one of 0, 1, 2)
type
관리자 유형 (admin:	0,	vs	admin:	1,	super	admin:	2)

name
name: required (string)
관리자 이름

level: required (integer)
level
관리자 레벨
로그인	후	결정되는	레벨,	설정된	레벨이	3이더라도	로그인	시	1	레벨(읽기전용)로	설정될	수	있음

org_level
org_level: required (integer)
관리자 추가 시 설정된 관리자 레벨

menumenu: required (array of any)
UI 메뉴

sub_menu
sub_menu: required (array of any)
Sub UI 메뉴

license
license: required (object)
장비 라이선스 정보

favorite
favorite: required (object)
즐겨찾기 메뉴 정보

haha: required (object)
HA 멤버 목록

modemode: required (one of Stand-Alone, Active-Active)
동작 모드

mmbr_num
mmbr_num: required (integer)
HA 멤버 개수

mmbrmmbr: required (array of Member)
Items: Member
Items

ipip: required (string)
멤버 IP 주소

mmbr_uuid
mmbr_uuid: required (string)
멤버 고유 ID

mmbr_name
mmbr_name: required (string)
멤버 이름

rolerole: required (one of master, slave)
역할

state: required (one of master, backup)
state
상태

system_spec
system_spec: required (object)
시스템 Spec 정보

Example:
Example

{
		"code":	"ok",
		"result":	{
				"system":	{
						"model":	"NGFW	1000",
						"current_time":	"2018-05-12	04:46:49.901442",
						"vs_mode":	"GUI",
						"contents_mgmt":	"self",
						"host_uuid":	"...",
						"cc_flag":	0,
						"gs_flag":	0,
						"ip":	"192.168.174.10",
						"gui_port":	80,
						"gui_idle_tm":	30,
						"gui_idle_use":	0,
						"gui_idle_exit":	0,
						"ve_type":	4,
						"ve_license":	1,
						"ve_autoscaling":	1,
						"ve_is_cloud":	1,
						"update_license":	0,
						"reboot":	0,
						"update_list":	["BLUEMAX_MASTER","BLUEMAX_BACKUP1"]
				},
				"user":	{
						"adm_id":	1,
						"login_id":	"admin",
						"type":	0,
						"name":	"secui",
						"level":	3,
						"org_level":	3
				},
				"menu":	[
						{
								"key":	"securityPolicy",
								"name":	"Security	Policy",
								"isUsed":	true,
								"children":	[]
						}
				],
				"sub_menu":	[
						{
								"key":	"securityPolicy",
								"name":	"Security	Policy",
								"isUsed":	true,
								"children":	[]
						}
				],
				"license":	{},
				"favorite":	{},
				"ha":	{
						"mode":	"Active-Active",
						"mmbr_num":	2,
						"mmbr":	[
								{
										"mmbr_name":	"Master-30",
										"mmbr_uuid":	"NGF-755764681504",
										"role":	"master",
										"state":	"master",
										"ip":	"192.168.174.30"
								},
								{
										"mmbr_name":	"Backup-29",
										"mmbr_uuid":	"NGF-443317153470",
										"role":	"slave",
										"state":	"backup",
										"ip":	"192.168.174.29"
								}
						]
				},
				"system_spec":{}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/ui-data/menu

UI 메뉴 데이터 조회

Request

Headers

Authorization
Authorization: required (string)
API Token

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

UI 메뉴 데이터 조회 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
응답 결과

menumenu: required (array of )
UI 메뉴

Example:
Example

{
		"code":	"ok",
		"result":	{
				"menu":	[
						{
								"key":	"securityPolicy",
								"name":	"Security	Policy",
								"isUsed":	true,
								"children":	[]
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

대시보드 위젯

GET

/static/widgets

대시보드 위젯 목록 조회

Request

Headers

Authorization: required (string)
Authorization
API Token

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

대시보드 위젯 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of object)
응답 결과

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"key":	"fw",
				"title":	"titleFwTraffic",
				"children":	[
						{
								"key":	"dash_fw_summary",
								"target":	"widgetFwSummary",
								"license":	"basic"
						}
				]
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

사용자 인증 REST API

POST

/userauth/authentication

사용자 인증 REST API

Request

Headers

Accept
Accept: required (string)
Example:
Example

application/json

Content-Type
Content-Type: required (string)
Example:
Example

application/json

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

flagflag: required (one of login, update, logout)
구분
login:	로그인,	update:	업데이트,	logout:	로그아웃

idid: required (string)
사용자 ID

ipip: required (string)
사용자(클라이언트) IP

timeout
timeout: required (integer)
로그인 타임아웃

group
group: required (string)
그룹명

Example:
Example

{
		"flag":	"login",
		"id":	"user1",
		"ip":	"10.10.10.10",
		"timeout":	60,
		"group":	"TestGroup"
}

Response

HTTP status code 200

사용자 인증 성공

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

사용자 인증 실패

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{
		"code":	"94003",
		"dev_t":	"Rest	API	Request	Error",
		"message":	"요청에	사용된	속성	또는	값이	잘못되었습니다."
}

