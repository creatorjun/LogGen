GET

PUT

PUT

PUT

GET

PUT

통합 클라이언트 설정 REST API 문서

https://{host}/api/sm

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

설정

/clientdownload/config

통합 클라이언트 설정 적용/취소

/command/clientdownload-config/apply

/command/clientdownload-config/cancel

페이지 스타일 정보 설정

/clientdownload/style/config

설정

GET

/clientdownload/config

설정을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

useuse: required (one of 0, 1)
Client 다운로드 시 사용자 인증 여부
사용	안함:	0,	사용:	1

auth_prof_id
auth_prof_id: (integer)
인증 프로파일 ID

인증 프로파일 목록 조회
[GET] https:///api/sm/auth/profiles/names/0

conn_port
conn_port: (integer - default: 8333 - minimum: 1 - maximum: 65535)
디바이스 포털 및 클라이언트 다운로드 페이지 접속 포트

Example:
Example

{
		"code":	"ok",
		"result":	{
				"use":	1,
				"auth_prof_id":	1,
				"conn_port":	8333
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/clientdownload/config

설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1)
Client 다운로드 시 사용자 인증 여부
사용	안함:	0,	사용:	1

auth_prof_id: (integer)
auth_prof_id
인증 프로파일 ID

인증 프로파일 목록 조회
[GET] https:///api/sm/auth/profiles/names/0

conn_port: (integer - default: 8333 - minimum: 1 - maximum: 65535)
conn_port
디바이스 포털 및 클라이언트 다운로드 페이지 접속 포트

Example:
Example

{
		"use":	1,
		"auth_prof_id":	1,
		"conn_port":	8333
}

Response

HTTP status code 200

설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

useuse: required (one of 0, 1)
Client 다운로드 시 사용자 인증 여부
사용	안함:	0,	사용:	1

auth_prof_id
auth_prof_id: (integer)
인증 프로파일 ID

인증 프로파일 목록 조회
[GET] https:///api/sm/auth/profiles/names/0

conn_port
conn_port: (integer - default: 8333 - minimum: 1 - maximum: 65535)
디바이스 포털 및 클라이언트 다운로드 페이지 접속 포트

Example:
Example

{
		"code":	"ok",
		"result":	{
				"use":	1,
				"auth_prof_id":	1
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

통합 클라이언트 설정 적용/취소

PUT

/command/clientdownload-config/apply

통합 클라이언트 설정을 적용합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

통합 클라이언트 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/clientdownload-config/cancel

통합 클라이언트 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

통합 클라이언트 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

페이지 스타일 정보 설정

GET

/clientdownload/style/config

페이지 스타일 정보을(를) 조회한다.

Request

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

페이지 스타일 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

page_opt
page_opt: (one of 1, 2 - default: 1)
상단영역 설정
(텍스트	설정:	1,	이미지	설정:	2)

title_txt
title_txt: required (string - default: BLUEMAX CLIENT Download)
다운로드 페이지 제목

title_img
title_img: (string - default: NULL)
다운로드 페이지에 사용되는 이미지의
Base 64 인코딩 데이터
파일 포맷: png
파일크기: 45KB 이하

txt_color
txt_color: required (string - default: #FFFFFF(화이트))
텍스트 색상

bg_color
bg_color: required (string - default: #498DDA)
다운로드 페이지 배경 색상

show_client_win: (one of 0, 1 - default: 1)
show_client_win
Client_Windows
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_mac
show_client_mac: (one of 0, 1 - default: 1)
Client_macOS
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_linux
show_client_linux: (one of 0, 1 - default: 1)
Client_Linux
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_android
show_client_android: (one of 0, 1 - default: 1)
Client_Android
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_ios
show_client_ios: (one of 0, 1 - default: 1)
Client_IOS
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_otp_android: (one of 0, 1 - default: 1)
show_otp_android

OTP_Android
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_otp_ios: (one of 0, 1 - default: 1)
show_otp_ios
OTP_IOS
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

Example:
Example

{
		"code":	"ok",
		"result":	{
				"page_opt":	1,
				"title_txt":	"BLUEMAX	CLIENT	Download",
				"title_img":	"xxxxxxxxxxxxx",
				"txt_color":	"#FFFFFF",
				"bg_color":	"#498DDA",
				"show_client_win":	1,
				"show_client_mac":	1,
				"show_client_linux":	1,
				"show_client_android":	1,
				"show_client_ios":	1,
				"show_otp_android":	1,
				"show_otp_ios":	1
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/clientdownload/style/config

페이지 스타일 정보을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

page_opt
page_opt: (one of 1, 2 - default: 1)
상단영역 설정
(텍스트	설정:	1,	이미지	설정:	2)

title_txt
title_txt: required (string - default: BLUEMAX CLIENT Download)
다운로드 페이지 제목

title_img
title_img: (string - default: NULL)
다운로드 페이지에 사용되는 이미지의
Base 64 인코딩 데이터
파일 포맷: png
파일크기: 45KB 이하

txt_color
txt_color: required (string - default: #FFFFFF(화이트))
텍스트 색상

bg_color
bg_color: required (string - default: #498DDA)
다운로드 페이지 배경 색상

show_client_win
show_client_win: (one of 0, 1 - default: 1)
Client_Windows
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_mac
show_client_mac: (one of 0, 1 - default: 1)
Client_macOS
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_linux
show_client_linux: (one of 0, 1 - default: 1)
Client_Linux
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_android
show_client_android: (one of 0, 1 - default: 1)
Client_Android
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_ios
show_client_ios: (one of 0, 1 - default: 1)
Client_IOS
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_otp_android
show_otp_android: (one of 0, 1 - default: 1)
OTP_Android
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_otp_ios: (one of 0, 1 - default: 1)
show_otp_ios
OTP_IOS
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

Example:
Example

{
		"page_opt":	1,
		"title_txt":	"BLUEMAX	CLIENT	Download",
		"title_img":	"xxxxxxxxxxxxx",
		"txt_color":	"#FFFFFF",
		"bg_color":	"#498DDA",
		"show_client_win":	1,
		"show_client_mac":	1,
		"show_client_linux":	1,
		"show_client_android":	1,
		"show_client_ios":	1,
		"show_otp_android":	1,
		"show_otp_ios":	1
}

Response

HTTP status code 200

페이지 스타일 정보 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

page_opt
page_opt: (one of 1, 2 - default: 1)
상단영역 설정
(텍스트	설정:	1,	이미지	설정:	2)

title_txt: required (string - default: BLUEMAX CLIENT Download)
title_txt
다운로드 페이지 제목

title_img
title_img: (string - default: NULL)
다운로드 페이지에 사용되는 이미지의
Base 64 인코딩 데이터
파일 포맷: png
파일크기: 45KB 이하

txt_color
txt_color: required (string - default: #FFFFFF(화이트))
텍스트 색상

bg_color
bg_color: required (string - default: #498DDA)
다운로드 페이지 배경 색상

show_client_win
show_client_win: (one of 0, 1 - default: 1)
Client_Windows
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_mac: (one of 0, 1 - default: 1)
show_client_mac
Client_macOS
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_linux
show_client_linux: (one of 0, 1 - default: 1)
Client_Linux
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_android
show_client_android: (one of 0, 1 - default: 1)
Client_Android
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_client_ios
show_client_ios: (one of 0, 1 - default: 1)
Client_IOS
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_otp_android: (one of 0, 1 - default: 1)
show_otp_android
OTP_Android
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

show_otp_ios
show_otp_ios: (one of 0, 1 - default: 1)
OTP_IOS
(0:	숨김,	1:	보임)
Client/OTP	숨김설정

Example:
Example

{
		"code":	"ok",
		"result":	{
				"page_opt":	1,
				"title_txt":	"BLUEMAX	CLIENT	Download",
				"title_img":	"xxxxxxxxxxxxx",
				"txt_color":	"#FFFFFF",
				"bg_color":	"#498DDA",
				"show_client_win":	1,
				"show_client_mac":	1,
				"show_client_linux":	1,
				"show_client_android":	1,
				"show_client_ios":	1,
				"show_otp_android":	1,
				"show_otp_ios":	1
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

