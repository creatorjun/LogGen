GET

GET

PUT

GET

PUT

GET

시스템 정보 REST API 문서

https://{host}/api/sm

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

운영 정보

/info/operation

장비 정보

/info/equipment

시스템 시간

/info/time

/info/time/{task_id}

운영 정보

GET

/info/operation

운영 정보을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

운영 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

modelmodel: required (string)
모델 이름

hostname
hostname: required (string)
장비 이름

sw_version
sw_version: required (string)
소프트웨어 버전

patch_date
patch_date: required (string)
릴리즈 일자

uptime: required (string)
uptime
시스템 동작 시간

hamode
hamode: required (string)
HA 모드

hwaddr
hwaddr: required (string)
대표 MAC

license
license: required (string)
라이선스 기간

hwserial
hwserial: required (string)
하드웨어 시리얼 번호

revision
revision: required (string)
OS 버전

ver_info
ver_info: required (object)
DB 버전 정보

firm_ver: required (string)
firm_ver
소프트웨어 패치 버전

ips_sig_db
ips_sig_db: required (string)
IPS 시그니처 DB 버전

ips_db
ips_db: (string)
IPS DB 버전 (MGMT	HOST	전용)

ips_light_db
ips_light_db: (string)
IPS Light DB 버전 (MGMT	HOST	전용)

app_db
app_db: required (string)
애플리케이션 DB 버전

anonymizer_db
anonymizer_db: required (string)
Anonymizer 버전

safenet_db
safenet_db: required (string)
SafeNET 버전

safenet_light_db: (string)
safenet_light_db
SafeNET Light 버전 (MGMT	HOST	전용)

wfcloud_db
wfcloud_db: required (string)
클라우드 악성 URL DB 버전

categorized_url_db
categorized_url_db: required (string)
Categorized URL DB 버전

virus_db
virus_db: required (string)
안티 바이러스 DB 버전

stream_based_db
stream_based_db: (string)
Stream Based DB 버전 (MGMT	HOST	전용)

file_based_db: (string)
file_based_db
File Based DB 버전 (MGMT	HOST	전용)

file_based_light_db
file_based_light_db: (string)
File Based Light DB 버전 (MGMT	HOST	전용)

blacklist_db: required (string)
blacklist_db
블랙리스트 DB 버전

dlp_db
dlp_db: required (string)
DLP DB 버전

apt_db
apt_db: required (string)
APT DB 버전

filetype_db
filetype_db: required (string)
파일 유형 제어 DB 버전

ssl_ex_db
ssl_ex_db: required (string)
SSL 예외 목록 버전

sslcert_db: required (string)
sslcert_db
사설 인증서 DB 버전

device_portal_db
device_portal_db: required (string)
디바이스 포털 DB 버전

dnssec_db
dnssec_db: required (string)
DNS 보안 DB

geo_ip_db
geo_ip_db: required (string)
Geo IP DB 버전

client_ver
client_ver: required (array of ClientVer)
클라이언트 버전 목록

Items: ClientVer
Items

os_type
os_type: required (one of 1, 2, 3, 4, 6)
운영체제 타입
1:	Windows:	1
2:	Linux
3:	Android
4:	iOS
6:	macOS

dvc_type
dvc_type: required (one of 1, 2)
클라이언트 타입
1:	Client
2:	OTP

verver: required (string)
CC 버전

Example:
Example

{
		"code":	"ok",
		"result":	{
				"model":	"SECUI	MF2	3100	V1.0",
				"hostname":	"localhost",
				"sw_version":	"1.0.1",
				"patch_date":	"2017-04-27",
				"uptime":	"6	days	3	hrs	33	mins",
				"hamode":	"stand-alone",
				"hwaddr":	"00:05:66:37:f3:d9"
				"license":	"2017-04-27	~	2022-10-15",
				"hwserial":	"No	serial	number",
				"revision":	"SecuiOS	V1.0(64bit)",
				"ver_info":	{
						"firm_ver":"2011.01.01.0000",
						"ips_sig_db":"2011.01.01.0000",
						"app_db":"2011.01.01.0000",
						"anonymizer_db":"2011.01.01.0000",
						"safenet_db":"2011.01.01.0000",
						"wfcloud_db":"2011.01.01.0000",
						"categorized_url_db":"2011.01.01.0000",
						"virus_db":"2011.01.01.0000",
						"blacklist_db":"2011.01.01.0000",
						"dlp_db":"2011.01.01.0000",
						"apt_db":"2011.01.01.0000",
						"filetype_db":"2011.01.01.0000",
						"ssl_ex_db":"2011.01.01.0000",
						"sslcert_db":"2011.01.01.0000",
						"device_portal_db":"2011.01.01.0000",
						"dnssec_db":"2011.01.01.0000",
						"geo_ip_db":"2011.01.01.0000",
						"client_ver":	[
								{"os_type":	1,	"dvc_type":	1,	"ver":	"1.3.0"},
								{"os_type":	2,	"dvc_type":	1,	"ver":	"-"}
						]
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code

결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

장비 정보

GET

/info/equipment

장비 정보을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

장비 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

hostname
hostname: required (string)
장비 이름

location
location: (one of KR, ..., 국가코드)
장비 위치 설정

descdesc: (string)
장비 설명

country_list
country_list: required (array of Country)
국가 목록

Items: Country
Items

full
full: required (string)
국가 이름

type
type: required (string)
국가 코드(2자리)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"hostname":	"localhost",
				"location":	"KR",
				"desc":	"secui.com	UTM	mf2"
				"country_list":	[
						{
								"full":	"Qatar",
								"type":	"QA"
						},
						{
								"full":	"Korea,	Republic	of",
								"type":	"KR"
						},
						{
								"full":	"China",
								"type":	"CN"
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/info/equipment

장비 정보을(를) 수정한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

hostname
hostname: required (string)
장비 이름

location
location: (one of KR, ..., 국가코드)
장비 위치 설정

descdesc: (string)
장비 설명

Example:
Example

{
		"hostname":	"localhost",
		"location":	"KR",
		"desc":	"secui.com	UTM	mf2"
}

Response

HTTP status code 200

장비 정보 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

hostname
hostname: required (string)
장비 이름

location
location: (one of KR, ..., 국가코드)
장비 위치 설정

descdesc: (string)
장비 설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"hostname":	"localhost",
				"location":	"KR",
				"desc":	"secui.com	UTM	mf2"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"",
		"message":	""
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

시스템 시간

GET

/info/time

시스템 시간 정보을(를) 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

시스템 시간 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

type: required (one of 0, 1)
type
설정 타입  (수동	설정:	0,	NTP	설정:	1)

now_clock
now_clock: required (string)
수동 설정 시간

ntpntp: required (object)
NTP 설정

svr1svr1: required (string)
NTP 서버1 주소

svr2svr2: (string)
NTP 서버2 주소

interval
interval: required (integer - default: 1800 - minimum: 10 - maximum: 14400)
업데이트 주기(초)

modemode: required (one of 1, 2)
동작 방식  (slew:	1,	step:	2)

tz_type: required (one of 1, 2)
tz_type
Timezone 설정 방식  (지역/도시:	1,	Offset:	2)

tz_data
tz_data: required (string)
지역/도시 또는 Offset

Example:
Example

(T)	지역/도시	정보	(예시	:	‘America/Santiago’)
(O)	offset	정보	(예시	:	‘+,1,1,1’)

tz_list
tz_list: required (array of Timezone)
Items: Timezone
Items

timezone
timezone: required (string)
타임존

comment
comment: required (string)
도시 정보

Example:
Example

{
		"code":	"ok",
		"result":	{
				"type":	1,
				"now_clock":	"2018-04-03	02:15:53",
				"ntp":	{
						"svr1":	"kr.pool.ntp.org",
						"svr2":	"",
						"interval":	1800,
						"mode":	2
				},
				"tz_type":	2,
				"tz_data":	"+,1,1,1"
				"tz_list":	[
						{
								"timezone":	"Etc/GMT-12",
								"comment":	"(UTC	-12:00)	International	Date	Line	West"
						},
						{
								"timezone":	"Etc/GMT-11",
								"comment":	"(UTC	-11:00)	Coordinated	Universal	Time-11"
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/info/time

시스템 시간 정보을(를) 수정한다.
시스템 시간 적용 후 웹서버가 재시작됩니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

type
type: required (one of 0, 1)
설정 타입  (수동	설정:	0,	NTP	설정:	1)

now_clock
now_clock: required (string)
수동 설정 시간

ntpntp: required (object)
NTP 설정

svr1svr1: required (string)
NTP 서버1 주소

svr2svr2: (string)
NTP 서버2 주소

interval
interval: required (integer - default: 1800 - minimum: 10 - maximum: 14400)
업데이트 주기(초)

modemode: required (one of 1, 2)
동작 방식  (slew:	1,	step:	2)

tz_type
tz_type: required (one of 1, 2)
Timezone 설정 방식  (지역/도시:	1,	Offset:	2)

tz_data
tz_data: required (string)
지역/도시 또는 Offset

Example:
Example

(T)	지역/도시	정보	(예시	:	‘America/Santiago’)
(O)	offset	정보	(예시	:	‘+,1,1,1’)

Example:
Example

{
		"type":	1,
		"now_clock":	"2018-04-03	02:15:53",
		"ntp":	{
				"svr1":	"kr.pool.ntp.org",
				"svr2":	"",
				"interval":	1800,
				"mode":	2
		},
		"tz_type":	1,
		"tz_data":	"America/Santiago"
}

Response

HTTP status code 200

시스템 시간 정보 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result
idid: required (string)
태스크 ID

status
status: required (one of PENDING, PROGRESS, SUCCESS, REVOKED, FAILURE)
태스크 상태  ("태스크	실행	대기	상태":	"PENDING",	"태스크	실행	상태":	"PROGRESS",	"태스크	완료	상태":	"SUCCESS",	"태스크	중지	상태":	"REVOKED",	"태스크	실패	상태":	"FAILURE")

attributes
attributes: required (object)
logs
logs: required (array of string)
업데이트 진행 결과

Example:
Example

{
		"code":	"async",
		"result":	{
				"id":	"f810f701-e732-4698-a5a9-a93000a828d9",
				"status":	"PENDING",
				"attributes":	{
						"logs":[]
				}
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/info/time/{task_id}

시스템 시간 태스크 결과 조회

Request

URI Parameters

task_id
task_id: required (string)
태스크 ID

Example:
Example

f810f701-e732-4698-a5a9-a93000a828d9

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

시스템 시간 태스크 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
idid: required (string)
태스크 ID

status
status: required (one of PENDING, PROGRESS, SUCCESS, REVOKED, FAILURE)
태스크 상태  ("태스크	실행	대기	상태":	"PENDING",	"태스크	실행	상태":	"PROGRESS",	"태스크	완료	상태":	"SUCCESS",	"태스크	중지	상태":	"REVOKED",	"태스크	실패	상태":	"FAILURE")

attributes
attributes: required (object)
logs: required (array of string)
logs
업데이트 진행 결과

Example:
Example

{
		"code":	"ok",
		"result":	{
						"id":	"f810f701-e732-4698-a5a9-a93000a828d9",
						"status":	"SUCCESS",
						"attributes":	{
								"logs":	[]
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

