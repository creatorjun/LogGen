인증 서버 REST API 문서

https://{url}/api/sm

urlurl: required (string)
장비 IP:Port

AD 인증 서버

/auth/ad/servers

/auth/ad/servers/names/{auth_only}

/auth/ad/servers/{id}

/auth/ad/servers/{id}/reference

AD 인증 서버 적용/취소

/auth/ad/apply

/auth/ad/cancel

LDAP 인증 서버

/auth/ldap/servers

/auth/ldap/servers/names/{auth_only}

/auth/ldap/servers/{id}

/auth/ldap/servers/{id}/reference

LDAP 인증 서버 적용/취소

/auth/ldap/apply

/auth/ldap/cancel

RADIUS 인증 서버

/auth/radius/servers

/auth/radius/servers/names/{auth_only}

/auth/radius/servers/{id}

/auth/radius/servers/{id}/reference

RADIUS 인증 서버 적용/취소

/auth/radius/apply

/auth/radius/cancel

TACACS 인증 서버

/auth/tacacs/servers

/auth/tacacs/servers/names/{auth_only}

/auth/tacacs/servers/{id}

/auth/tacacs/servers/{id}/reference

TACACS 인증 서버 적용/취소

/auth/tacacs/apply

/auth/tacacs/cancel

DBMS 인증 서버

/auth/dbms/servers

GET

POST

  DELETE

GET

GET

PUT

  DELETE

GET

PUT

PUT

GET

POST

  DELETE

GET

GET

PUT

  DELETE

GET

PUT

PUT

GET

POST

  DELETE

GET

GET

PUT

  DELETE

GET

PUT

PUT

GET

POST

  DELETE

GET

GET

PUT

  DELETE

GET

PUT

PUT

GET

POST

  DELETE

GET

GET

PUT

  DELETE

GET

PUT

PUT

GET

GET

POST

  DELETE

PUT

  DELETE

GET

POST

PUT

PUT

/auth/dbms/servers/names

/auth/dbms/servers/{id}

/auth/dbms/servers/{id}/reference

DBMS 인증 서버 적용/취소

/auth/dbms/apply

/auth/dbms/cancel

SAML IDP

/auth/saml-idp/servers

/auth/saml-idp/servers/{id}

/auth/saml-idp/servers/{id}/reference

SAML IDP 메타데이터 업로드

/auth/saml-idp/metadata

SAML IDP 적용/취소

/auth/saml-idp/apply

/auth/saml-idp/cancel

AD 인증 서버

GET

/auth/ad/servers

AD 인증 서버 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

AD 인증 서버 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of ADConfig)
Items: ADConfig
Items

authsvr_ad_id: (integer)
authsvr_ad_id
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of ADServer)
서버 목록

Items: ADServer
Items

ad_svr_id
ad_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 389 - minimum: 0 - maximum: 65535)
서버 포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type: required (one of 0, 1, 2)
conn_type
보안 연결
(NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit
tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

account_type
account_type: required (one of 0, 1, 2 - default: 0)
사용자 계정 종류
0:	sAMAccountName,	1:	CN,	2:	UID

SupportedVersion:	2.0.0

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

is_sec_group_use
is_sec_group_use: required (one of 0, 1 - default: 0)
보안 그룹 사용 여부
(OFF:	0,	ON:	1)

sec_group: required (string - maxLength: 128)
sec_group
보안 그룹 목록
콤마(,)로	구분,	is_sec_group_use이	1일	때	필수

use_admin_level_check
use_admin_level_check: required (one of 0, 1 - default: 0)
관리자 허용 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1 - default: 0)
접속 주소 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

isused
isused: (one of 0, 1)
참조 기능 존재 여부
(없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64 인코딩)

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_ad_id":1,
						"name":"AD_server1",
						"base_dn":	"CN=Users,DC=secui,DC=com",
						"bind_dn":	"tester\\administrator",
						"conn_type":0,
						"tm_limit":30,
						"bind_tm_limit":30,
						"retry_intval":300,
						"auth_only":0,
						"account_type":	0,
						"is_sec_group_use":	1,
						"sec_group":	"그룹1,그룹2,그룹3",
						"use_admin_level_check":	1,
						"use_admin_ip_check":	1,
						"desc":"desc1",
						"isused":	0,
						"servers":[
								{
										"ad_svr_id":1,
										"name":"server1",
										"ip":"1.1.1.1",
										"port":389
								}
						]
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/auth/ad/servers

AD 인증 서버를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language

언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_ad_id
authsvr_ad_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of ADServer)
서버 목록

Items: ADServer
Items

ad_svr_id: (integer)
ad_svr_id
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 389 - minimum: 0 - maximum: 65535)
서버 포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결
(NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit
tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

account_type
account_type: required (one of 0, 1, 2 - default: 0)
사용자 계정 종류
0:	sAMAccountName,	1:	CN,	2:	UID

SupportedVersion:	2.0.0

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

is_sec_group_use
is_sec_group_use: required (one of 0, 1 - default: 0)
보안 그룹 사용 여부
(OFF:	0,	ON:	1)

sec_group: required (string - maxLength: 128)
sec_group
보안 그룹 목록
콤마(,)로	구분,	is_sec_group_use이	1일	때	필수

use_admin_level_check
use_admin_level_check: required (one of 0, 1 - default: 0)
관리자 허용 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1 - default: 0)
접속 주소 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

isused
isused: (one of 0, 1)
참조 기능 존재 여부
(없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64 인코딩)

Example:
Example

{
		"name":"AD_server1",
		"servers":[
				{
						"name":"server1",
						"ip":"1.1.1.1",
						"port":389
				}
		],
		"base_dn":	"CN=Users,DC=secui,DC=com",
		"bind_dn":	"tester\\administrator",
		"passwd":"x2xAyWkwMEAh",
		"conn_type":0,
		"account_type":	0,
		"auth_only":	1,
		"is_sec_group_use":	1,
		"sec_group":	"그룹1,그룹2,그룹3",
		"use_admin_level_check":	1,
		"use_admin_ip_check":	1,
		"tm_limit":30,
		"bind_tm_limit":30,
		"retry_intval":300,
		"auth_only":0,
		"desc":"desc1"
}

Response

HTTP status code 200

AD 인증 서버 추가 성공

Headers

Location: required (string)
Location
Example:
Example

/auth/ad/servers/123

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_ad_id
authsvr_ad_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of ADServer)
서버 목록

Items: ADServer
Items

ad_svr_id
ad_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 389 - minimum: 0 - maximum: 65535)
서버 포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결
(NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit
tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

account_type: required (one of 0, 1, 2 - default: 0)
account_type
사용자 계정 종류
0:	sAMAccountName,	1:	CN,	2:	UID

SupportedVersion:	2.0.0

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

is_sec_group_use: required (one of 0, 1 - default: 0)
is_sec_group_use

보안 그룹 사용 여부
(OFF:	0,	ON:	1)

sec_group
sec_group: required (string - maxLength: 128)
보안 그룹 목록
콤마(,)로	구분,	is_sec_group_use이	1일	때	필수

use_admin_level_check
use_admin_level_check: required (one of 0, 1 - default: 0)
관리자 허용 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1 - default: 0)
접속 주소 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

isused
isused: (one of 0, 1)
참조 기능 존재 여부
(없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64 인코딩)

Example:
Example

{
		"authsvr_ad_id":1,
		"name":"AD_server1",
		"base_dn":	"CN=Users,DC=secui,DC=com",
		"bind_dn":	"tester\\administrator",
		"conn_type":0,
		"tm_limit":30,
		"bind_tm_limit":30,
		"retry_intval":300,
		"auth_only":0,
		"account_type":	0,
		"is_sec_group_use":	1,
		"sec_group":	"그룹1,그룹2,그룹3",
		"use_admin_level_check":	1,
		"use_admin_ip_check":	1,
		"desc":"desc1",
		"isused":	0,
		"servers":[
				{
						"ad_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"port":389
				}
		],
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/auth/ad/servers

AD 인증 서버 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of object)
items
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"authsvr_ad_id":	1},
				{"authsvr_ad_id":	2}
		]
}

Response

HTTP status code 200

AD 인증 서버 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/ad/servers/names/{auth_only}

AD 인증 서버 이름 목록을 조회한다.

Request

URI Parameters

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

Annotations

SupportedVersion:
SupportedVersion

"2.0.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

AD 인증 서버 이름 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of items)
서버 목록

Items: items
Items

authsvr_ad_id
authsvr_ad_id: required (integer)
ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_ad_id":1,
						"name":"AD_server1"
				},
				{
						"authsvr_ad_id":2,
						"name":"AD_server2"
				},
				{
						"authsvr_ad_id":3,
						"name":"AD_server3"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/ad/servers/{id}

AD 인증 서버을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
AD 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

AD 인증 서버 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result
authsvr_ad_id
authsvr_ad_id: (integer)
ID

name: required (string - maxLength: 32)
name
이름

servers
servers: required (array of ADServer)
서버 목록

Items: ADServer
Items

ad_svr_id
ad_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 389 - minimum: 0 - maximum: 65535)
서버 포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결
(NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit
tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
검색 시간 제한(초)

bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
bind_tm_limit
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

account_type
account_type: required (one of 0, 1, 2 - default: 0)
사용자 계정 종류
0:	sAMAccountName,	1:	CN,	2:	UID

SupportedVersion:	2.0.0

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

is_sec_group_use
is_sec_group_use: required (one of 0, 1 - default: 0)
보안 그룹 사용 여부
(OFF:	0,	ON:	1)

sec_group
sec_group: required (string - maxLength: 128)
보안 그룹 목록
콤마(,)로	구분,	is_sec_group_use이	1일	때	필수

use_admin_level_check
use_admin_level_check: required (one of 0, 1 - default: 0)
관리자 허용 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1 - default: 0)
접속 주소 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

isused
isused: (one of 0, 1)
참조 기능 존재 여부
(없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64 인코딩)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_ad_id":1,
				"name":"AD_server1",
				"base_dn":	"CN=Users,DC=secui,DC=com",
				"bind_dn":	"tester\\administrator",
				"conn_type":0,
				"tm_limit":30,
				"bind_tm_limit":30,
				"retry_intval":300,
				"auth_only":0,
				"account_type":	0,
				"is_sec_group_use":	1,
				"sec_group":	"그룹1,그룹2,그룹3",
				"use_admin_level_check":	1,
				"use_admin_ip_check":	1,
				"desc":"desc1",
				"isused":	0,
				"servers":[
						{
								"ad_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"port":389
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/auth/ad/servers/{id}

AD 인증 서버을(를) 수정한다.

Request

URI Parameters

idid: required (integer)
AD 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_ad_id
authsvr_ad_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of ADServer)
서버 목록

Items: ADServer
Items

ad_svr_id
ad_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 389 - minimum: 0 - maximum: 65535)
서버 포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결
(NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit
tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

account_type: required (one of 0, 1, 2 - default: 0)
account_type
사용자 계정 종류
0:	sAMAccountName,	1:	CN,	2:	UID

SupportedVersion:	2.0.0

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

is_sec_group_use
is_sec_group_use: required (one of 0, 1 - default: 0)
보안 그룹 사용 여부
(OFF:	0,	ON:	1)

sec_group
sec_group: required (string - maxLength: 128)
보안 그룹 목록
콤마(,)로	구분,	is_sec_group_use이	1일	때	필수

use_admin_level_check: required (one of 0, 1 - default: 0)
use_admin_level_check
관리자 허용 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

use_admin_ip_check: required (one of 0, 1 - default: 0)
use_admin_ip_check
접속 주소 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

isused
isused: (one of 0, 1)
참조 기능 존재 여부
(없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64 인코딩)

Example:
Example

{
		"name":"AD_server1",
		"base_dn":	"CN=Users,DC=secui,DC=com",
		"bind_dn":	"tester\\administrator",
		"passwd":"x2xAyWkwMEAh",
		"conn_type":0,
		"tm_limit":30,
		"bind_tm_limit":30,
		"retry_intval":300,
		"auth_only":0,
		"account_type":	0,
		"is_sec_group_use":	1,
		"sec_group":	"그룹1,그룹2,그룹3",
		"use_admin_level_check":	1,
		"use_admin_ip_check":	1,
		"desc":"desc1",
		"servers":[
				{
						"name":"server1",
						"ip":"1.1.1.1",
						"port":389
				}
		]
}

Response

HTTP status code 200

AD 인증 서버 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
authsvr_ad_id: (integer)
authsvr_ad_id
ID

name
name: required (string - maxLength: 32)
이름

servers: required (array of ADServer)
servers
서버 목록

Items: ADServer
Items

ad_svr_id
ad_svr_id: (integer)
ID

name: required (string - maxLength: 32)
name
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 389 - minimum: 0 - maximum: 65535)
서버 포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결
(NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit
tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

account_type
account_type: required (one of 0, 1, 2 - default: 0)
사용자 계정 종류
0:	sAMAccountName,	1:	CN,	2:	UID

SupportedVersion:	2.0.0

auth_only: required (one of 0, 1)
auth_only
관리자 인증 전용
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

is_sec_group_use: required (one of 0, 1 - default: 0)
is_sec_group_use
보안 그룹 사용 여부
(OFF:	0,	ON:	1)

sec_group
sec_group: required (string - maxLength: 128)
보안 그룹 목록
콤마(,)로	구분,	is_sec_group_use이	1일	때	필수

use_admin_level_check
use_admin_level_check: required (one of 0, 1 - default: 0)
관리자 허용 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1 - default: 0)
접속 주소 제한 여부
(OFF:	0,	ON:	1)

SupportedVersion:	2.0.0

isused
isused: (one of 0, 1)
참조 기능 존재 여부
(없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64 인코딩)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_ad_id":1,
				"name":"AD_server1",
				"base_dn":	"CN=Users,DC=secui,DC=com",
				"bind_dn":	"tester\\administrator",
				"conn_type":0,
				"tm_limit":30,
				"bind_tm_limit":30,
				"retry_intval":300,
				"auth_only":0,
				"account_type":	0,
				"is_sec_group_use":	1,
				"sec_group":	"그룹1,그룹2,그룹3",
				"use_admin_level_check":	1,
				"use_admin_ip_check":	1,
				"desc":"desc1",
				"isused":	0,
				"servers":[
						{
								"ad_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"port":389
						}
				]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/auth/ad/servers/{id}

AD 인증 서버을(를) 삭제한다.

Request

URI Parameters

idid: required (integer)
AD 인증 서버 ID

Example:
Example

10

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

AD 인증 서버 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/auth/ad/servers/{id}/reference

AD 인증 서버 참조을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
AD 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

AD 인증 서버 참조 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

auth_profile
auth_profile: required (array of items)
인증 프로파일 참조 목록

Items: items
Items

auth_profile_id: required (integer)
auth_profile_id

ID

name
name: required (string)
이름

ip_map
ip_map: required (array of items)
IP 매핑 사용자 참조 목록

Items: items
Items

ip_map_svr_id: required (integer)
ip_map_svr_id
ID

name
name: required (string)
이름

ad_grp: required (array of items)
ad_grp
AD 그룹 매핑 참조 목록

Items: items
Items

ad_grp_map_id
ad_grp_map_id: required (integer)
ID

name: required (string)
name
이름

sslvpn
sslvpn: required (array of items)
SSLVPN 참조 목록

Items: items
Items

sslvpn_grp_id
sslvpn_grp_id: required (integer)
ID

name
name: required (string)
이름

authsvr_ad_id
authsvr_ad_id: required (integer)
ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_ad_id":1,
				"auth_profile":[{"auth_profile_id":1,"name":	"인증	프로파일"}],
				"ip_map":[{"ip_map_svr_id":1,"name":	"IP	매핑	프로파일"}],
				"ad_grp":[{"ad_grp_map_id":1,"name":	"AD	그룹	매핑"}],
				"sslvpn":[{"sslvpn_grp_id":1,"name":	"SSLVPN"}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

AD 인증 서버 적용/취소

PUT

/auth/ad/apply

AD 인증 서버을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

AD 인증 서버 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/auth/ad/cancel

AD 인증 서버을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

AD 인증 서버 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

LDAP 인증 서버

GET

/auth/ldap/servers

LDAP 인증 서버 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

LDAP 인증 서버 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of LDAPConfig)
Items: LDAPConfig
Items

authsvr_ldap_id
authsvr_ldap_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers: required (array of LDAPServer)
servers
서버 목록

Items: LDAPServer
Items

ldap_svr_id
ldap_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn: required (string - maxLength: 128)
bind_dn

Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결  (NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
tm_limit
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

use_admin_level_check
use_admin_level_check: required (one of 0, 1)
관리자 허용 제한  (OFF:	0,	ON:	1)

use_admin_ip_check: required (one of 0, 1)
use_admin_ip_check
접속 주소 제한  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64인코딩)

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_ldap_id":1,
						"name":"LDAP_server1",
						"servers":[
								{
										"ldap_svr_id":1,
										"name":"server1",
										"ip":"1.1.1.1",
										"port":389
								},
								{
										"ldap_svr_id":2,
										"name":"server2",
										"ip":"1.1.1.2",
										"port":390
								}
						],
						"base_dn":"Base1",
						"bind_dn":"Bind1",
						"conn_type":0,
						"tm_limit":30,
						"bind_tm_limit":30,
						"retry_intval":300,
						"auth_only":0,
						"use_admin_level_check":0,
						"use_admin_ip_check":0,
						"isused":	0,
						"desc":"desc1"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/auth/ldap/servers

LDAP 인증 서버를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_ldap_id
authsvr_ldap_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of LDAPServer)
서버 목록

Items: LDAPServer
Items

ldap_svr_id: (integer)
ldap_svr_id
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결  (NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
tm_limit
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

use_admin_level_check
use_admin_level_check: required (one of 0, 1)
관리자 허용 제한  (OFF:	0,	ON:	1)

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1)
접속 주소 제한  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)

설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64인코딩)

Example:
Example

{
		"name":"LDAP_server1",
		"servers":[
				{
						"ldap_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"port":389
				},
				{
						"ldap_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"port":390
				}
		],
		"base_dn":"Base1",
		"bind_dn":"Bind1",
		"passwd":	"x2xAyWkwMEAh",
		"conn_type":0,
		"tm_limit":30,
		"bind_tm_limit":30,
		"retry_intval":300,
		"auth_only":0,
		"use_admin_level_check":0,
		"use_admin_ip_check":0,
		"desc":"desc1"
}

Response

HTTP status code 200

LDAP 인증 서버 추가 성공

Headers

Location
Location: required (string)
Example:
Example

/auth/ldap/servers/123

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_ldap_id
authsvr_ldap_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of LDAPServer)
서버 목록

Items: LDAPServer
Items

ldap_svr_id: (integer)
ldap_svr_id
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결  (NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
tm_limit
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

use_admin_level_check
use_admin_level_check: required (one of 0, 1)
관리자 허용 제한  (OFF:	0,	ON:	1)

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1)
접속 주소 제한  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64인코딩)

Example:
Example

{
		"authsvr_ldap_id":1,
		"name":"LDAP_server1",
		"servers":[
				{
						"ldap_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"port":389
				},
				{
						"ldap_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"port":390
				}
		],
		"base_dn":"Base1",
		"bind_dn":"Bind1",
		"conn_type":0,
		"tm_limit":30,
		"bind_tm_limit":30,
		"retry_intval":300,
		"auth_only":0,
		"use_admin_level_check":0,
		"use_admin_ip_check":0,
		"isused":	0,
		"desc":"desc1"
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/auth/ldap/servers

LDAP 인증 서버 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"authsvr_ldap_id":	1},
				{"authsvr_ldap_id":	2}
		]
}

Response

HTTP status code 200

LDAP 인증 서버 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result: required (object)
result
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/ldap/servers/names/{auth_only}

LDAP 인증 서버 이름 목록을 조회한다.

Request

URI Parameters

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

LDAP 인증 서버 이름 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of items)
서버 목록

Items: items
Items

authsvr_ldap_id
authsvr_ldap_id: required (integer)
ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_ldap_id":1,
						"name":"LDAP_server1"
				},
				{
						"authsvr_ldap_id":2,
						"name":"LDAP_server2"
				},
				{
						"authsvr_ldap_id":3,
						"name":"LDAP_server3"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/ldap/servers/{id}

LDAP 인증 서버을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
LDAP 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

LDAP 인증 서버 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
authsvr_ldap_id
authsvr_ldap_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers: required (array of LDAPServer)
servers
서버 목록

Items: LDAPServer
Items

ldap_svr_id
ldap_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
포트

base_dn
base_dn: required (string - maxLength: 128)
Base DN

bind_dn: required (string - maxLength: 128)
bind_dn
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결  (NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit
tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

use_admin_level_check
use_admin_level_check: required (one of 0, 1)
관리자 허용 제한  (OFF:	0,	ON:	1)

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1)
접속 주소 제한  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64인코딩)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_ldap_id":1,
				"name":"LDAP_server1",
				"servers":[
						{
								"ldap_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"port":389
						},
						{
								"ldap_svr_id":2,
								"name":"server2",
								"ip":"1.1.1.2",
								"port":390
						}
				],
				"base_dn":"Base1",
				"bind_dn":"Bind1",
				"conn_type":0,
				"tm_limit":30,
				"bind_tm_limit":30,
				"retry_intval":300,
				"auth_only":0,
				"use_admin_level_check":0,
				"use_admin_ip_check":0,
				"isused":	0,
				"desc":"desc1"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/auth/ldap/servers/{id}

LDAP 인증 서버을(를) 수정한다.

Request

URI Parameters

idid: required (integer)
LDAP 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_ldap_id
authsvr_ldap_id: (integer)
ID

name: required (string - maxLength: 32)
name
이름

servers
servers: required (array of LDAPServer)
서버 목록

Items: LDAPServer
Items

ldap_svr_id
ldap_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
포트

base_dn: required (string - maxLength: 128)
base_dn
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결  (NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit
tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

use_admin_level_check
use_admin_level_check: required (one of 0, 1)
관리자 허용 제한  (OFF:	0,	ON:	1)

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1)
접속 주소 제한  (OFF:	0,	ON:	1)

isused: required (one of 0, 1)
isused
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64인코딩)

Example:
Example

{
		"name":"LDAP_server1",
		"servers":[
				{
						"ldap_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"port":389
				},
				{
						"ldap_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"port":390
				}
		],
		"base_dn":"Base1",
		"bind_dn":"Bind1",
		"conn_type":0,
		"tm_limit":30,
		"bind_tm_limit":30,
		"retry_intval":300,
		"auth_only":0,
		"use_admin_level_check":0,
		"use_admin_ip_check":0,
		"desc":"desc1"
}

Response

HTTP status code 200

LDAP 인증 서버 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result
authsvr_ldap_id
authsvr_ldap_id: (integer)
ID

name: required (string - maxLength: 32)
name
이름

servers
servers: required (array of LDAPServer)
서버 목록

Items: LDAPServer
Items

ldap_svr_id
ldap_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
포트

base_dn: required (string - maxLength: 128)
base_dn
Base DN

bind_dn
bind_dn: required (string - maxLength: 128)
Bind DN

conn_type
conn_type: required (one of 0, 1, 2)
보안 연결  (NONE:	0,	LDAPS:	1,	Start	TLS:	2)

tm_limit
tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
검색 시간 제한(초)

bind_tm_limit
bind_tm_limit: required (integer - default: 30 - minimum: 1 - maximum: 30)
Bind 시간 제한(초)

retry_intval
retry_intval: required (integer - default: 300 - minimum: 1 - maximum: 3600)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

use_admin_level_check
use_admin_level_check: required (one of 0, 1)
관리자 허용 제한  (OFF:	0,	ON:	1)

use_admin_ip_check
use_admin_ip_check: required (one of 0, 1)
접속 주소 제한  (OFF:	0,	ON:	1)

isused: required (one of 0, 1)
isused
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

passwd
passwd: (string - maxLength: 128)
비밀번호(Base64인코딩)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_ldap_id":1,
				"name":"LDAP_server1",
				"servers":[
						{
								"ldap_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"port":389
						},
						{
								"ldap_svr_id":2,
								"name":"server2",
								"ip":"1.1.1.2",
								"port":390
						}
				],
				"base_dn":"Base1",
				"bind_dn":"Bind1",
				"conn_type":0,
				"tm_limit":30,
				"bind_tm_limit":30,
				"retry_intval":300,
				"auth_only":0,
				"use_admin_level_check":0,
				"use_admin_ip_check":0,
				"isused":	0,
				"desc":"desc1"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/auth/ldap/servers/{id}

LDAP 인증 서버을(를) 삭제한다.

Request

URI Parameters

idid: required (integer)
LDAP 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

LDAP 인증 서버 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/auth/ldap/servers/{id}/reference

LDAP 인증 서버 참조을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
LDAP 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

LDAP 인증 서버 참조 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

auth_profile
auth_profile: required (array of items)
인증 프로파일 참조 목록

Items: items
Items

auth_profile_id: required (integer)
auth_profile_id

ID

name
name: required (string)
이름

ip_map
ip_map: required (array of items)
IP 매핑 사용자 참조 목록

Items: items
Items

ip_map_svr_id: required (integer)
ip_map_svr_id
ID

name
name: required (string)
이름

ad_grp: required (array of items)
ad_grp
AD 그룹 매핑 참조 목록

Items: items
Items

ad_grp_map_id
ad_grp_map_id: required (integer)
ID

name: required (string)
name
이름

sslvpn
sslvpn: required (array of items)
SSLVPN 참조 목록

Items: items
Items

sslvpn_grp_id
sslvpn_grp_id: required (integer)
ID

name
name: required (string)
이름

authsvr_ldap_id
authsvr_ldap_id: required (integer)
ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_ldap_id":1,
				"auth_profile":[{"auth_profile_id":1,"name":	"인증	프로파일"}],
				"ip_map":[{"ip_map_svr_id":1,"name":	"IP	매핑	프로파일"}],
				"ad_grp":[{"ad_grp_map_id":1,"name":	"AD	그룹	매핑"}],
				"sslvpn":[{"sslvpn_grp_id":1,"name":	"SSLVPN"}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

LDAP 인증 서버 적용/취소

PUT

/auth/ldap/apply

LDAP 인증 서버을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

LDAP 인증 서버 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/auth/ldap/cancel

LDAP 인증 서버을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

LDAP 인증 서버 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

RADIUS 인증 서버

GET

/auth/radius/servers

RADIUS 인증 서버 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

RADIUS 인증 서버 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of RADIUSConfig)
Items: RADIUSConfig
Items

authsvr_radius_id
authsvr_radius_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers: required (array of RADIUSServer)
servers
서버 목록

Items: RADIUSServer
Items

radius_svr_id
radius_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

auth_port: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
auth_port
인증 포트

accnt_port
accnt_port: required (integer - default: 1813 - minimum: 0 - maximum: 65535)
계정 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1)
인증 방식  (PAP:	0,	CHAP:	1)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused: required (one of 0, 1)
isused
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_radius_id":1,
						"name":"RADIUS_server1",
						"servers":	[
								{
										"radius_svr_id":1,
										"name":"server1",
										"ip":"1.1.1.1",
										"auth_port":1812,
										"accnt_port":1813,
								},
								{
										"radius_svr_id":2,
										"name":"server2",
										"ip":"1.1.1.2",
										"auth_port":1812,
										"accnt_port":1813,
								}
						],
						"auth_type":0,
						"tm_out":30,
						"retry_cnt":3,
						"auth_only":0,
						"isused":	0,
						"desc":"desc1"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/auth/radius/servers

RADIUS 인증 서버를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_radius_id
authsvr_radius_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of RADIUSServer)
서버 목록

Items: RADIUSServer
Items

radius_svr_id
radius_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

auth_port: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
auth_port
인증 포트

accnt_port
accnt_port: required (integer - default: 1813 - minimum: 0 - maximum: 65535)
계정 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1)
인증 방식  (PAP:	0,	CHAP:	1)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"name":"RADIUS_server1",
		"servers":[
				{
						"radius_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"auth_port":1812,
						"accnt_port":1813,
						"passwd":"x2xAyWkwMEAh"
				},
				{
						"radius_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"auth_port":1812,
						"accnt_port":1813,
						"passwd":"x2xAyWkwMEAh"
				}
		],
		"auth_type":0,
		"tm_out":30,
		"retry_cnt":3,
		"auth_only":0,
		"desc":"desc1"
}

Response

HTTP status code 200

RADIUS 인증 서버 추가 성공

Headers

Location
Location: required (string)
Example:
Example

/auth/radius/servers/123

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_radius_id: (integer)
authsvr_radius_id
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of RADIUSServer)
서버 목록

Items: RADIUSServer
Items

radius_svr_id
radius_svr_id: (integer)
ID

name: required (string - maxLength: 32)
name
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

auth_port
auth_port: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
인증 포트

accnt_port
accnt_port: required (integer - default: 1813 - minimum: 0 - maximum: 65535)
계정 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1)
인증 방식  (PAP:	0,	CHAP:	1)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
retry_cnt
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"authsvr_radius_id":1,
		"name":"RADIUS_server1",
		"servers":[
				{
						"radius_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"auth_port":1812,
						"accnt_port":1813,
				},
				{
						"radius_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"auth_port":1812,
						"accnt_port":1813,
				}
		],
		"auth_type":0,
		"tm_out":30,
		"retry_cnt":3,
		"auth_only":0,
		"isused":	0,
		"desc":"desc1"
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/auth/radius/servers

RADIUS 인증 서버 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"authsvr_radius_id":	1},
				{"authsvr_radius_id":	2}
		]
}

Response

HTTP status code 200

RADIUS 인증 서버 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/radius/servers/names/{auth_only}

RADIUS 인증 서버 이름 목록을 조회한다.

Request

URI Parameters

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

RADIUS 인증 서버 이름 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of items)
서버 목록

Items: items
Items

authsvr_radius_id
authsvr_radius_id: required (integer)
ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_radius_id":1,
						"name":"RADIUS_server1"
				},
				{
						"authsvr_radius_id":2,
						"name":"RADIUS_server2"
				},
				{
						"authsvr_radius_id":3,
						"name":"RADIUS_server3"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/radius/servers/{id}

RADIUS 인증 서버을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
RADIUS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

RADIUS 인증 서버 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

authsvr_radius_id: (integer)
authsvr_radius_id
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of RADIUSServer)
서버 목록

Items: RADIUSServer
Items

radius_svr_id
radius_svr_id: (integer)
ID

name: required (string - maxLength: 32)
name
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

auth_port
auth_port: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
인증 포트

accnt_port
accnt_port: required (integer - default: 1813 - minimum: 0 - maximum: 65535)
계정 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1)
인증 방식  (PAP:	0,	CHAP:	1)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
retry_cnt
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_radius_id":1,
				"name":"RADIUS_server1",
				"servers":[
						{
								"radius_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"auth_port":1812,
								"accnt_port":1813
						},
						{
								"radius_svr_id":2,
								"name":"server2",
								"ip":"1.1.1.2",
								"auth_port":1812,
								"accnt_port":1813
						}
				],
				"auth_type":0,
				"tm_out":30,
				"retry_cnt":3,
				"auth_only":0,
				"isused":	0,
				"desc":"desc1"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/auth/radius/servers/{id}

RADIUS 인증 서버을(를) 수정한다.

Request

URI Parameters

idid: required (integer)

RADIUS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_radius_id
authsvr_radius_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of RADIUSServer)
서버 목록

Items: RADIUSServer
Items

radius_svr_id: (integer)
radius_svr_id
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

auth_port
auth_port: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
인증 포트

accnt_port
accnt_port: required (integer - default: 1813 - minimum: 0 - maximum: 65535)
계정 포트

passwd: (string - maxLength: 500)
passwd
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1)
인증 방식  (PAP:	0,	CHAP:	1)

tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
tm_out
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"name":"RADIUS_server1",
		"servers":[
				{
						"radius_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"auth_port":1812,
						"accnt_port":1813,
						"passwd":"x2xAyWkwMEAh"
				},
				{
						"radius_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"auth_port":1812,
						"accnt_port":1813,
						"passwd":"x2xAyWkwMEAh"
				}
		],
		"auth_type":0,
		"tm_out":30,
		"retry_cnt":3,
		"auth_only":0,
		"desc":"desc1"
}

Response

HTTP status code 200

RADIUS 인증 서버 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

authsvr_radius_id
authsvr_radius_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of RADIUSServer)
서버 목록

Items: RADIUSServer
Items

radius_svr_id: (integer)
radius_svr_id
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

auth_port
auth_port: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
인증 포트

accnt_port
accnt_port: required (integer - default: 1813 - minimum: 0 - maximum: 65535)
계정 포트

passwd: (string - maxLength: 500)
passwd
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1)
인증 방식  (PAP:	0,	CHAP:	1)

tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
tm_out
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_radius_id":1,
				"name":"RADIUS_server1",
				"servers":[
						{
								"radius_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"auth_port":1812,
								"accnt_port":1813
						},
						{
								"radius_svr_id":2,
								"name":"server2",
								"ip":"1.1.1.2",
								"auth_port":1812,
								"accnt_port":1813
						}
				],
				"auth_type":0,
				"tm_out":30,
				"retry_cnt":3,
				"auth_only":0,
				"isused":	0,
				"desc":"desc1"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/auth/radius/servers/{id}

RADIUS 인증 서버을(를) 삭제한다.

Request

URI Parameters

idid: required (integer)
RADIUS 인증 서버 ID

Example:
Example

10

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

RADIUS 인증 서버 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/auth/radius/servers/{id}/reference

RADIUS 인증 서버 참조을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
RADIUS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

RADIUS 인증 서버 참조 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

auth_profile
auth_profile: required (array of items)
인증 프로파일 참조 목록

Items: items
Items

auth_profile_id
auth_profile_id: required (integer)
ID

name: required (string)
name
이름

ip_map
ip_map: required (array of items)
IP 매핑 사용자 참조 목록

Items: items
Items

ip_map_svr_id
ip_map_svr_id: required (integer)
ID

name
name: required (string)
이름

ad_grp
ad_grp: required (array of items)
AD 그룹 매핑 참조 목록

Items: items
Items

ad_grp_map_id
ad_grp_map_id: required (integer)
ID

name
name: required (string)
이름

sslvpn
sslvpn: required (array of items)
SSLVPN 참조 목록

Items: items
Items

sslvpn_grp_id
sslvpn_grp_id: required (integer)
ID

name
name: required (string)
이름

authsvr_radius_id
authsvr_radius_id: required (integer)
ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_radius_id":1,
				"auth_profile":[{"auth_profile_id":1,"name":	"인증	프로파일"}],
				"ip_map":[{"ip_map_svr_id":1,"name":	"IP	매핑	프로파일"}],
				"ad_grp":[{"ad_grp_map_id":1,"name":	"AD	그룹	매핑"}],
				"sslvpn":[{"sslvpn_grp_id":1,"name":	"SSLVPN"}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

RADIUS 인증 서버 적용/취소

PUT

/auth/radius/apply

RADIUS 인증 서버을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

RADIUS 인증 서버 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/auth/radius/cancel

RADIUS 인증 서버을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept

응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

RADIUS 인증 서버 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

TACACS 인증 서버

GET

/auth/tacacs/servers

TACACS 인증 서버 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

TACACS 인증 서버 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of TACACSConfig)
Items: TACACSConfig
Items

authsvr_tacacs_id
authsvr_tacacs_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of TACACSServer)
서버 목록

Items: TACACSServer
Items

tacacs_svr_id
tacacs_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
서버 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1, 2)
인증 방식  (ASCII:	0,	PAP:	1,	CHAP:	2)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused: required (one of 0, 1)
isused
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_tacacs_id":1,
						"name":"TACACS_server1",
						"servers":[
								{
										"tacacs_svr_id":1,
										"name":"server1",
										"ip":"1.1.1.1",
										"port":389
								},
								{
										"tacacs_svr_id":2,
										"name":"server2",
										"ip":"1.1.1.2",
										"port":390
								}
						],
						"auth_type":0,
						"tm_out":30,
						"retry_cnt":3,
						"auth_only":0,
						"isused":	0,
						"desc":"desc1"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/auth/tacacs/servers

TACACS 인증 서버를 추가한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_tacacs_id: (integer)
authsvr_tacacs_id
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of TACACSServer)
서버 목록

Items: TACACSServer
Items

tacacs_svr_id
tacacs_svr_id: (integer)
ID

name: required (string - maxLength: 32)
name
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
서버 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1, 2)
인증 방식  (ASCII:	0,	PAP:	1,	CHAP:	2)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only: required (one of 0, 1)
auth_only
관리자 인증 전용  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"name":"TACACS_server1",
		"servers":[
				{
						"tacacs_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"port":389,
						"passwd":"x2xAyWkwMEAh"
				},
				{
						"tacacs_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"port":390,
						"passwd":"x2xAyWkwMEAh"
				}
		],
		"auth_type":0,
		"tm_out":30,
		"retry_cnt":3,
		"auth_only":0,
		"desc":"desc1"
}

Response

HTTP status code 200

TACACS 인증 서버 추가 성공

Headers

Location: required (string)
Location

Example:
Example

/auth/tacacs/servers/123

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_tacacs_id
authsvr_tacacs_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers: required (array of TACACSServer)
servers
서버 목록

Items: TACACSServer
Items

tacacs_svr_id
tacacs_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
서버 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1, 2)
인증 방식  (ASCII:	0,	PAP:	1,	CHAP:	2)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"authsvr_tacacs_id":1,
		"name":"TACACS_server1",
		"servers":[
				{
								"tacacs_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"port":389
				},
				{
								"tacacs_svr_id":2,
								"name":"server2",
								"ip":"1.1.1.2",
								"port":390
				}
		],
		"auth_type":0,
		"tm_out":30,
		"retry_cnt":3,
		"auth_only":0,
		"isused":	0,
		"desc":"desc1"
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/auth/tacacs/servers

TACACS 인증 서버 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"authsvr_tacacs_id":	1},
				{"authsvr_tacacs_id":	2}
		]
}

Response

HTTP status code 200

TACACS 인증 서버 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/tacacs/servers/names/{auth_only}

TACACS 인증 서버 이름 목록을 조회한다.

Request

URI Parameters

auth_only: required (one of 0, 1)
auth_only
관리자 인증 전용  (OFF:	0,	ON:	1)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

TACACS 인증 서버 이름 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of items)
서버 목록

Items: items
Items

authsvr_tacacs_id
authsvr_tacacs_id: required (integer)
ID

name: required (string)
name
이름

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_tacacs_id":1,
						"name":"TACACS_server1"
				},
				{
						"authsvr_tacacs_id":2,
						"name":"TACACS_server2"
				},
				{
						"authsvr_tacacs_id":3,
						"name":"TACACS_server3"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/tacacs/servers/{id}

TACACS 인증 서버을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
TACACS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

TACACS 인증 서버 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

authsvr_tacacs_id
authsvr_tacacs_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of TACACSServer)
서버 목록

Items: TACACSServer
Items

tacacs_svr_id
tacacs_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
서버 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1, 2)
인증 방식  (ASCII:	0,	PAP:	1,	CHAP:	2)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused: required (one of 0, 1)
isused
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_tacacs_id":1,
				"name":"TACACS_server1",
				"servers":[
						{
								"tacacs_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"port":389
						},
						{
								"tacacs_svr_id":2,
								"name":"server2",
								"ip":"1.1.1.2",
								"port":390
						}
				],
				"auth_type":0,
				"tm_out":30,
				"retry_cnt":3,
				"auth_only":0,
				"isused":	0,
				"desc":"desc1"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/auth/tacacs/servers/{id}

TACACS 인증 서버을(를) 수정한다.

Request

URI Parameters

idid: required (integer)
TACACS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_tacacs_id
authsvr_tacacs_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of TACACSServer)
서버 목록

Items: TACACSServer
Items

tacacs_svr_id
tacacs_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
서버 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type
auth_type: required (one of 0, 1, 2)
인증 방식  (ASCII:	0,	PAP:	1,	CHAP:	2)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only: required (one of 0, 1)
auth_only
관리자 인증 전용  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"name":"TACACS_server1",
		"servers":[
				{
						"tacacs_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"port":389,
						"passwd":"x2xAyWkwMEAh"
				},
				{
						"tacacs_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"port":390,
						"passwd":"x2xAyWkwMEAh"
				}
		],
		"auth_type":0,
		"tm_out":30,
		"retry_cnt":3,
		"auth_only":0,
		"desc":"desc1"
}

Response

HTTP status code 200

TACACS 인증 서버 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

authsvr_tacacs_id
authsvr_tacacs_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers: required (array of TACACSServer)
servers
서버 목록

Items: TACACSServer
Items

tacacs_svr_id
tacacs_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1812 - minimum: 0 - maximum: 65535)
서버 포트

passwd
passwd: (string - maxLength: 500)
비밀번호(Base64 인코딩)

auth_type: required (one of 0, 1, 2)
auth_type
인증 방식  (ASCII:	0,	PAP:	1,	CHAP:	2)

tm_out
tm_out: required (integer - default: 30 - minimum: 1 - maximum: 30)
타임아웃(초)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 5)
재시도 간격(초)

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_tacacs_id":1,
				"name":"TACACS_server1",
				"servers":[
						{
										"tacacs_svr_id":1,
										"name":"server1",
										"ip":"1.1.1.1",
										"port":389
						},
						{
										"tacacs_svr_id":2,
										"name":"server2",
										"ip":"1.1.1.2",
										"port":390
						}
				],
				"auth_type":0,
				"tm_out":30,
				"retry_cnt":3,
				"auth_only":0,
				"isused":	0,
				"desc":"desc1"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/auth/tacacs/servers/{id}

TACACS 인증 서버을(를) 삭제한다.

Request

URI Parameters

idid: required (integer)
TACACS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

TACACS 인증 서버 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/auth/tacacs/servers/{id}/reference

TACACS 인증 서버 참조을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
TACACS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

TACACS 인증 서버 참조 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

auth_profile
auth_profile: required (array of items)
인증 프로파일 참조 목록

Items: items
Items

auth_profile_id
auth_profile_id: required (integer)
ID

name
name: required (string)
이름

ip_map
ip_map: required (array of items)
IP 매핑 사용자 참조 목록

Items: items
Items

ip_map_svr_id
ip_map_svr_id: required (integer)
ID

name: required (string)
name
이름

ad_grp
ad_grp: required (array of items)
AD 그룹 매핑 참조 목록

Items: items
Items

ad_grp_map_id
ad_grp_map_id: required (integer)
ID

name: required (string)
name
이름

sslvpn
sslvpn: required (array of items)
SSLVPN 참조 목록

Items: items
Items

sslvpn_grp_id
sslvpn_grp_id: required (integer)
ID

name
name: required (string)
이름

authsvr_tacacs_id
authsvr_tacacs_id: required (integer)
ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_tacacs_id":1,
				"auth_profile":[{"auth_profile_id":1,"name":	"인증	프로파일"}],
				"ip_map":[{"ip_map_svr_id":1,"name":	"IP	매핑	프로파일"}],
				"ad_grp":[{"ad_grp_map_id":1,"name":	"AD	그룹	매핑"}],
				"sslvpn":[{"sslvpn_grp_id":1,"name":	"SSLVPN"}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

TACACS 인증 서버 적용/취소

PUT

/auth/tacacs/apply

TACACS 인증 서버을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

TACACS 인증 서버 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/auth/tacacs/cancel

TACACS 인증 서버을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

TACACS 인증 서버 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

DBMS 인증 서버

GET

/auth/dbms/servers

DBMS 인증 서버 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

DBMS 인증 서버 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of DBMSConfig)
Items: DBMSConfig
Items

authsvr_extdb_id
authsvr_extdb_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of DMBSServer)
서버 목록

Items: DMBSServer
Items

extdb_svr_id
extdb_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1433 - minimum: 0 - maximum: 65535)
서버 포트

auth_type
auth_type: required (one of 0, 1)
인증 종류  (Plain	Text:	0,	SHA2:	1)

type
type: required (one of 0, 1, 2, 3, 4)
서버 종류  (ORACLE:	0,	MS-SQL:	1,	MYSQL:	2,	PostgreSQL:	3,	내PC지키미:	4)

db_name
db_name: required (string - maxLength: 128)
데이터베이스 이름

db_tbl
db_tbl: required (string - maxLength: 128)
테이블 이름

db_id
db_id: required (string - maxLength: 32)
사용자 ID

db_pw: required (string - maxLength: 128)
db_pw
비밀번호(Base64 인코딩)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_extdb_id":1,
						"name":"ORACLE_server1",
						"auth_type":	0,
						"servers":[
								{
										"extdb_svr_id":1,
										"name":"server1",
										"ip":"1.1.1.1",
										"port":1433
								},
								{
										"extdb_svr_id":2,
										"name":"server2",
										"ip":"1.1.1.2",
										"port":1433
								}
						],
						"type":0,
						"db_name":"auth_db",
						"db_tbl":"user_table",
						"db_id":"admin",
						"db_pw":"x2xAyWkwMEAh",
						"isused":	0,
						"desc":"desc1"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/auth/dbms/servers

DBMS 인증 서버를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language

언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_extdb_id
authsvr_extdb_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of DMBSServer)
서버 목록

Items: DMBSServer
Items

extdb_svr_id: (integer)
extdb_svr_id
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1433 - minimum: 0 - maximum: 65535)
서버 포트

auth_type
auth_type: required (one of 0, 1)
인증 종류  (Plain	Text:	0,	SHA2:	1)

type
type: required (one of 0, 1, 2, 3, 4)
서버 종류  (ORACLE:	0,	MS-SQL:	1,	MYSQL:	2,	PostgreSQL:	3,	내PC지키미:	4)

db_name
db_name: required (string - maxLength: 128)
데이터베이스 이름

db_tbl: required (string - maxLength: 128)
db_tbl
테이블 이름

db_id
db_id: required (string - maxLength: 32)
사용자 ID

db_pw: required (string - maxLength: 128)
db_pw
비밀번호(Base64 인코딩)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"name":"ORACLE_server1",
		"auth_type":	0,
		"servers":[
				{
						"extdb_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"port":1433
				},
				{
						"extdb_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"port":1433
				}
		],
		"type":0,
		"db_name":"auth_db",
		"db_tbl":"user_table",
		"db_id":"admin",
		"db_pw":"x2xAyWkwMEAh",
		"desc":"desc1"
}

Response

HTTP status code 200

DBMS 인증 서버 추가 성공

Headers

Location: required (string)
Location
Example:
Example

/auth/dbms/servers/123

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_extdb_id: (integer)
authsvr_extdb_id
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of DMBSServer)
서버 목록

Items: DMBSServer
Items

extdb_svr_id
extdb_svr_id: (integer)
ID

name: required (string - maxLength: 32)
name
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1433 - minimum: 0 - maximum: 65535)
서버 포트

auth_type
auth_type: required (one of 0, 1)
인증 종류  (Plain	Text:	0,	SHA2:	1)

type
type: required (one of 0, 1, 2, 3, 4)
서버 종류  (ORACLE:	0,	MS-SQL:	1,	MYSQL:	2,	PostgreSQL:	3,	내PC지키미:	4)

db_name
db_name: required (string - maxLength: 128)
데이터베이스 이름

db_tbl
db_tbl: required (string - maxLength: 128)
테이블 이름

db_id: required (string - maxLength: 32)
db_id
사용자 ID

db_pw
db_pw: required (string - maxLength: 128)
비밀번호(Base64 인코딩)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"authsvr_extdb_id":1,
		"name":"ORACLE_server1",
		"auth_type":	0,
		"servers":[
				{
						"extdb_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"port":1433
				},
				{
						"extdb_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"port":1433
				}
		],
		"type":0,
		"db_name":"auth_db",
		"db_tbl":"user_table",
		"db_id":"admin",
		"db_pw":"x2xAyWkwMEAh",
		"isused":	0,
		"desc":"desc1"
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/auth/dbms/servers

DBMS 인증 서버 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of object)
items

삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"authsvr_extdb_id":	1},
				{"authsvr_extdb_id":	2}
		]
}

Response

HTTP status code 200

DBMS 인증 서버 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/dbms/servers/names

DBMS 인증 서버 이름 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

DBMS 인증 서버 이름 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (array of items)
result
서버 목록

Items: items
Items

authsvr_extdb_id
authsvr_extdb_id: required (integer)
ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_extdb_id":1,
						"name":"DBMS_server1"
				},
				{
						"authsvr_extdb_id":2,
						"name":"DBMS_server2"
				},
				{
						"authsvr_radius_id":3,
						"name":"DBMS_server3"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/dbms/servers/{id}

DBMS 인증 서버을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
DBMS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

DBMS 인증 서버 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result
authsvr_extdb_id
authsvr_extdb_id: (integer)
ID

name: required (string - maxLength: 32)
name

이름

servers
servers: required (array of DMBSServer)
서버 목록

Items: DMBSServer
Items

extdb_svr_id
extdb_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1433 - minimum: 0 - maximum: 65535)
서버 포트

auth_type
auth_type: required (one of 0, 1)
인증 종류  (Plain	Text:	0,	SHA2:	1)

type
type: required (one of 0, 1, 2, 3, 4)
서버 종류  (ORACLE:	0,	MS-SQL:	1,	MYSQL:	2,	PostgreSQL:	3,	내PC지키미:	4)

db_name: required (string - maxLength: 128)
db_name
데이터베이스 이름

db_tbl
db_tbl: required (string - maxLength: 128)
테이블 이름

db_id
db_id: required (string - maxLength: 32)
사용자 ID

db_pw
db_pw: required (string - maxLength: 128)
비밀번호(Base64 인코딩)

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_extdb_id":1,
				"name":"ORACLE_server1",
				"auth_type":	0,
				"servers":[
						{
								"extdb_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"port":1433
						},
						{
								"extdb_svr_id":2,
								"name":"server2",
								"ip":"1.1.1.2",
								"port":1433
						}
				],
				"type":0,
				"db_name":"auth_db",
				"db_tbl":"user_table",
				"db_id":"admin",
				"db_pw":"x2xAyWkwMEAh",
				"isused":	0,
				"desc":"desc1"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/auth/dbms/servers/{id}

DBMS 인증 서버을(를) 수정한다.

Request

URI Parameters

idid: required (integer)
DBMS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

authsvr_extdb_id
authsvr_extdb_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of DMBSServer)
서버 목록

Items: DMBSServer
Items

extdb_svr_id
extdb_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1433 - minimum: 0 - maximum: 65535)
서버 포트

auth_type
auth_type: required (one of 0, 1)
인증 종류  (Plain	Text:	0,	SHA2:	1)

type
type: required (one of 0, 1, 2, 3, 4)
서버 종류  (ORACLE:	0,	MS-SQL:	1,	MYSQL:	2,	PostgreSQL:	3,	내PC지키미:	4)

db_name
db_name: required (string - maxLength: 128)
데이터베이스 이름

db_tbl
db_tbl: required (string - maxLength: 128)
테이블 이름

db_id
db_id: required (string - maxLength: 32)
사용자 ID

db_pw
db_pw: required (string - maxLength: 128)
비밀번호(Base64 인코딩)

isused: required (one of 0, 1)
isused
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"name":"ORACLE_server1",
		"auth_type":	0,
		"servers":[
				{
						"extdb_svr_id":1,
						"name":"server1",
						"ip":"1.1.1.1",
						"port":1433
				},
				{
						"extdb_svr_id":2,
						"name":"server2",
						"ip":"1.1.1.2",
						"port":1433
				}
		],
		"type":0,
		"db_name":"auth_db",
		"db_tbl":"user_table",
		"db_id":"admin",
		"db_pw":"x2xAyWkwMEAh",
		"desc":"desc1"
}

Response

HTTP status code 200

DBMS 인증 서버 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
authsvr_extdb_id
authsvr_extdb_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

servers
servers: required (array of DMBSServer)
서버 목록

Items: DMBSServer
Items

extdb_svr_id
extdb_svr_id: (integer)
ID

name
name: required (string - maxLength: 32)
서버 이름

ipip: required (string - maxLength: 255)
서버 주소(IPv4 Address or Domain Name)

portport: required (integer - default: 1433 - minimum: 0 - maximum: 65535)
서버 포트

auth_type
auth_type: required (one of 0, 1)
인증 종류  (Plain	Text:	0,	SHA2:	1)

type
type: required (one of 0, 1, 2, 3, 4)
서버 종류  (ORACLE:	0,	MS-SQL:	1,	MYSQL:	2,	PostgreSQL:	3,	내PC지키미:	4)

db_name
db_name: required (string - maxLength: 128)
데이터베이스 이름

db_tbl
db_tbl: required (string - maxLength: 128)
테이블 이름

db_id
db_id: required (string - maxLength: 32)
사용자 ID

db_pw
db_pw: required (string - maxLength: 128)
비밀번호(Base64 인코딩)

isused: required (one of 0, 1)
isused
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_extdb_id":1,
				"name":"ORACLE_server1",
				"auth_type":	0,
				"servers":[
						{
								"extdb_svr_id":1,
								"name":"server1",
								"ip":"1.1.1.1",
								"port":1433
						},
						{
								"extdb_svr_id":2,
								"name":"server2",
								"ip":"1.1.1.2",
								"port":1433
						}
				],
				"type":0,
				"db_name":"auth_db",
				"db_tbl":"user_table",
				"db_id":"admin",
				"db_pw":"x2xAyWkwMEAh",
				"isused":	0,
				"desc":"desc1"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/auth/dbms/servers/{id}

DBMS 인증 서버을(를) 삭제한다.

Request

URI Parameters

idid: required (integer)
DBMS 인증 서버 ID

Example:
Example

10

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

DBMS 인증 서버 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/auth/dbms/servers/{id}/reference

DBMS 인증 서버 참조을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
DBMS 인증 서버 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

DBMS 인증 서버 참조 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

auth_profile
auth_profile: required (array of items)
인증 프로파일 참조 목록

Items: items
Items

auth_profile_id: required (integer)
auth_profile_id

ID

name
name: required (string)
이름

ip_map
ip_map: required (array of items)
IP 매핑 사용자 참조 목록

Items: items
Items

ip_map_svr_id: required (integer)
ip_map_svr_id
ID

name
name: required (string)
이름

ad_grp: required (array of items)
ad_grp
AD 그룹 매핑 참조 목록

Items: items
Items

ad_grp_map_id
ad_grp_map_id: required (integer)
ID

name: required (string)
name
이름

sslvpn
sslvpn: required (array of items)
SSLVPN 참조 목록

Items: items
Items

sslvpn_grp_id
sslvpn_grp_id: required (integer)
ID

name
name: required (string)
이름

authsvr_extdb_id
authsvr_extdb_id: required (integer)
ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_extdb_id":1,
				"auth_profile":[{"auth_profile_id":1,"name":	"인증	프로파일"}],
				"ip_map":[{"ip_map_svr_id":1,"name":	"IP	매핑	프로파일"}],
				"ad_grp":[{"ad_grp_map_id":1,"name":	"AD	그룹	매핑"}],
				"sslvpn":[{"sslvpn_grp_id":1,"name":	"SSLVPN"}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DBMS 인증 서버 적용/취소

PUT

/auth/dbms/apply

DBMS 인증 서버을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

DBMS 인증 서버 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/auth/dbms/cancel

DBMS 인증 서버을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

DBMS 인증 서버 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

SAML IDP

GET

/auth/saml-idp/servers

SAML IDP 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

SAML IDP 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of SamlIDPConfig)
Items: SamlIDPConfig
Items

entity_id
entity_id: required (string - minLength: 1 - maxLength: 512)
Entity ID

sso_url
sso_url: required (string - minLength: 1 - maxLength: 512)
SSO URL

single_logout_use: required (one of 0, 1)
single_logout_use
Single Logout 사용 여부  (사용:	1,	사용안함:	0)

slo_url
slo_url: required (string - maxLength: 512)
SLO URL

idp_crt
idp_crt: required (string)
인증서 파일명

authsvr_saml_id
authsvr_saml_id: (integer)
ID

name
name: required (string - minLength: 1 - maxLength: 32)
이름

user_id_property: required (string - minLength: 1 - maxLength: 128)
user_id_property
사용자 ID 속성명

setup_type
setup_type: required (one of 0, 1)
Setup Type  (직접	입력:	1,	메타데이터:	0)

descdesc: (string - maxLength: 64)
설명

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

is_crt_upload
is_crt_upload: (one of 0, 1)
직접	입력  시 인증서를 업로드 한 경우 1로 설정
이 값이 1일 경우 HA 멤버 간 인증서가 복사된다.

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"authsvr_saml_id":	1,
						"name":	"SAML	IDP1",
						"entity_id":	"https://192.168.10.10/simplesaml/saml2/idp/metadata.php",
						"sso_url":	"https://192.168.10.10/simplesaml/saml2/idp/SSOService.php",
						"single_logout_use":	1,
						"slo_url":	"https://192.168.10.10/simplesaml/saml2/idp/SingleLogoutService.php",
						"idp_crt":	"idp_metadata.crt",
						"user_id_property":	"guigui",
						"setup_type":	0,
						"desc":	"",
						"isused":	0
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/auth/saml-idp/servers

SAML IDP를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

entity_id
entity_id: required (string - minLength: 1 - maxLength: 512)
Entity ID

sso_url: required (string - minLength: 1 - maxLength: 512)
sso_url
SSO URL

single_logout_use
single_logout_use: required (one of 0, 1)
Single Logout 사용 여부  (사용:	1,	사용안함:	0)

slo_url
slo_url: required (string - maxLength: 512)
SLO URL

idp_crt
idp_crt: required (string)
인증서 파일명

authsvr_saml_id
authsvr_saml_id: (integer)
ID

name
name: required (string - minLength: 1 - maxLength: 32)
이름

user_id_property
user_id_property: required (string - minLength: 1 - maxLength: 128)
사용자 ID 속성명

setup_type: required (one of 0, 1)
setup_type
Setup Type  (직접	입력:	1,	메타데이터:	0)

descdesc: (string - maxLength: 64)
설명

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

is_crt_upload
is_crt_upload: (one of 0, 1)
직접	입력  시 인증서를 업로드 한 경우 1로 설정
이 값이 1일 경우 HA 멤버 간 인증서가 복사된다.

Example:
Example

{
		"name":	"Saml	Idp1",
		"entity_id":	"https://192.168.10.10/simplesaml/saml2/idp/metadata.php",
		"sso_url":	"https://192.168.10.10/simplesaml/saml2/idp/SSOService.php",
		"single_logout_use":	1,
		"slo_url":	"https://192.168.10.10/simplesaml/saml2/idp/SingleLogoutService.php",
		"idp_crt":	"idp_metadata.crt",
		"user_id_property":	"userid",
		"setup_type":	0,
		"desc":	"",
		"is_crt_upload":	0
}

Response

HTTP status code 200

SAML IDP 추가 성공

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/auth/saml-idp/servers

SAML IDP 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"authsvr_saml_id":	1},
				{"authsvr_saml_id":	2}
		]
}

Response

HTTP status code 200

SAML IDP 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/saml-idp/servers/{id}

SAML IDP을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
SAML IDP ID(PK)

Example:
Example

10

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

SAML IDP 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

entity_id
entity_id: required (string - minLength: 1 - maxLength: 512)
Entity ID

sso_url
sso_url: required (string - minLength: 1 - maxLength: 512)
SSO URL

single_logout_use: required (one of 0, 1)
single_logout_use
Single Logout 사용 여부  (사용:	1,	사용안함:	0)

slo_url
slo_url: required (string - maxLength: 512)
SLO URL

idp_crt: required (string)
idp_crt
인증서 파일명

authsvr_saml_id
authsvr_saml_id: (integer)
ID

name
name: required (string - minLength: 1 - maxLength: 32)
이름

user_id_property
user_id_property: required (string - minLength: 1 - maxLength: 128)
사용자 ID 속성명

setup_type
setup_type: required (one of 0, 1)
Setup Type  (직접	입력:	1,	메타데이터:	0)

descdesc: (string - maxLength: 64)
설명

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

is_crt_upload
is_crt_upload: (one of 0, 1)
직접	입력  시 인증서를 업로드 한 경우 1로 설정
이 값이 1일 경우 HA 멤버 간 인증서가 복사된다.

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_saml_id":	1,
				"name":	"Saml	Idp1",
				"entity_id":	"https://192.168.10.10/simplesaml/saml2/idp/metadata.php",
				"sso_url":	"https://192.168.10.10/simplesaml/saml2/idp/SSOService.php",
				"single_logout_use":	1,
				"slo_url":	"https://192.168.10.10/simplesaml/saml2/idp/SingleLogoutService.php",
				"idp_crt":	"idp_metadata.crt",
				"user_id_property":	"userid",
				"setup_type":	0,
				"desc":	"",
				"isused":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/auth/saml-idp/servers/{id}

SAML IDP을(를) 수정한다.

Request

URI Parameters

idid: required (integer)
SAML IDP ID(PK)

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

entity_id
entity_id: required (string - minLength: 1 - maxLength: 512)
Entity ID

sso_url
sso_url: required (string - minLength: 1 - maxLength: 512)
SSO URL

single_logout_use
single_logout_use: required (one of 0, 1)
Single Logout 사용 여부  (사용:	1,	사용안함:	0)

slo_url
slo_url: required (string - maxLength: 512)
SLO URL

idp_crt: required (string)
idp_crt
인증서 파일명

authsvr_saml_id
authsvr_saml_id: (integer)
ID

name
name: required (string - minLength: 1 - maxLength: 32)
이름

user_id_property
user_id_property: required (string - minLength: 1 - maxLength: 128)
사용자 ID 속성명

setup_type
setup_type: required (one of 0, 1)
Setup Type  (직접	입력:	1,	메타데이터:	0)

descdesc: (string - maxLength: 64)
설명

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

is_crt_upload: (one of 0, 1)
is_crt_upload
직접	입력  시 인증서를 업로드 한 경우 1로 설정
이 값이 1일 경우 HA 멤버 간 인증서가 복사된다.

Example:
Example

{
		"name":	"SAML	IDP	1",
		"entity_id":	"https://192.168.10.10/simplesaml/saml2/idp/metadata.php",
		"sso_url":	"https://192.168.10.10/simplesaml/saml2/idp/SSOService.php",
		"single_logout_use":	1,
		"slo_url":	"https://192.168.10.10/simplesaml/saml2/idp/SingleLogoutService.php",
		"idp_crt":	"idp_metadata.crt",
		"user_id_property":	"userid",
		"setup_type":	0,
		"desc":	"",
		"is_crt_upload":	1
}

Response

HTTP status code 200

SAML IDP 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result

entity_id
entity_id: required (string - minLength: 1 - maxLength: 512)
Entity ID

sso_url
sso_url: required (string - minLength: 1 - maxLength: 512)
SSO URL

single_logout_use
single_logout_use: required (one of 0, 1)
Single Logout 사용 여부  (사용:	1,	사용안함:	0)

slo_url: required (string - maxLength: 512)
slo_url
SLO URL

idp_crt
idp_crt: required (string)
인증서 파일명

authsvr_saml_id
authsvr_saml_id: (integer)
ID

name
name: required (string - minLength: 1 - maxLength: 32)
이름

user_id_property
user_id_property: required (string - minLength: 1 - maxLength: 128)
사용자 ID 속성명

setup_type: required (one of 0, 1)
setup_type
Setup Type  (직접	입력:	1,	메타데이터:	0)

descdesc: (string - maxLength: 64)
설명

isused: required (one of 0, 1)
isused
참조 기능 존재 여부  (없음:	0,	참조:	1)

is_crt_upload
is_crt_upload: (one of 0, 1)
직접	입력  시 인증서를 업로드 한 경우 1로 설정
이 값이 1일 경우 HA 멤버 간 인증서가 복사된다.

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_saml_id":	2,
				"name":	"Saml	Idp1",
				"entity_id":	"https://192.168.10.10/simplesaml/saml2/idp/metadata.php",
				"sso_url":	"https://192.168.10.10/simplesaml/saml2/idp/SSOService.php",
				"single_logout_use":	1,
				"slo_url":	"https://192.168.10.10/simplesaml/saml2/idp/SingleLogoutService.php",
				"idp_crt":	"idp_metadata.crt",
				"user_id_property":	"userid",
				"setup_type":	0,
				"desc":	"",
				"isused":	0
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/auth/saml-idp/servers/{id}

SAML IDP을(를) 삭제한다.

Request

URI Parameters

idid: required (integer)
SAML IDP ID(PK)

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

SAML IDP 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/auth/saml-idp/servers/{id}/reference

SAML IDP 참조을(를) 조회한다.

Request

URI Parameters

idid: required (integer)
SAML IDP ID(PK)

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

SAML IDP 참조 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
authsvr_saml_id
authsvr_saml_id: required (integer)
ID

auth_profile
auth_profile: required (array of items)
인증 프로파일 참조 목록

Items: items
Items

auth_profile_id
auth_profile_id: required (integer)
ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"authsvr_saml_id":1,
				"auth_profile":[{"auth_profile_id":1,"name":	"인증	프로파일"}]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

SAML IDP 메타데이터 업로드

POST

/auth/saml-idp/metadata

SAML 메타데이터 파일 업로드을(를) 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: multipart/form-data
Media type

TypeType: object

Properties
Properties

filefile: required (file)
xml 확장자의 메타데이터 파일 업로드

Response

HTTP status code 200

SAML 메타데이터 파일 업로드 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result

entity_id
entity_id: required (string - minLength: 1 - maxLength: 512)
Entity ID

sso_url: required (string - minLength: 1 - maxLength: 512)
sso_url
SSO URL

single_logout_use
single_logout_use: required (one of 0, 1)
Single Logout 사용 여부  (사용:	1,	사용안함:	0)

slo_url
slo_url: required (string - maxLength: 512)
SLO URL

idp_crt
idp_crt: required (string)
인증서 파일명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"entity_id":	"https://192.168.10.10/simplesaml/saml2/idp/metadata.php",
				"sso_url":	"https://192.168.10.10/simplesaml/saml2/idp/SSOService.php",
				"slo_url":	"https://192.168.10.10/simplesaml/saml2/idp/SingleLogoutService.php",
				"single_logout_use":	1,
				"idp_crt":	"idp_metadata.crt"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

SAML IDP 적용/취소

PUT

/auth/saml-idp/apply

SAML IDP을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

SAML IDP 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/auth/saml-idp/cancel

SAML IDP을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

SAML IDP 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

