GET

GET

POST

  DELETE

PUT

  DELETE

GET

GET

PUT

PUT

인증 프로파일 REST API 문서

https://{host}/api/sm

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

인증 프로파일

AD 서버 목록 조회 :  /api/sm/auth/ad/servers/names
LDAP 서버 목록 조회 :  /api/sm/auth/ldap/servers/names/{auth_only}
RADIUS 서버 목록 조회 :  /api/sm/auth/radius/servers/names/{auth_only}
TACACS 서버 목록 조회 :  /api/sm/auth/tacacs/servers/names/{auth_only}
DBMS 서버 목록 조회 :  /api/sm/auth/dbms/servers/names
SAML IDP 서버 목록 조회 :  /api/sm/auth/saml-idp/servers/names
상세 내용은  인증	서버  API 문서를 참고한다.

/auth/profiles

/auth/profiles/{pk}

/auth/profiles/{pk}/reference

/auth/profiles/names/{auth_only}

/auth/profiles/apply

/auth/profiles/cancel

인증 프로파일

GET

/auth/profiles

인증 프로파일 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

auth_only
auth_only: (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

Example:
Example

auth_only?=0

Response

HTTP status code 200

인증 프로파일 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of AuthProfile)
Items: AuthProfile
Items

auth_prod_id
auth_prod_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

auth_only
auth_only: required (string)
관리자 인증 전용  (OFF:	0,	ON:	1)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 30)
인증 시도 횟수

lock_tm
lock_tm: required (integer - default: 3 - minimum: 0 - maximum: 60)
잠김 시간(분)

servers
servers: required (array of items)
Items: items
Items

type
type: required (one of ldap, radius, tacacs, localdb)
서버 종류

idid: required (integer)
서버 ID
type이	localdb	일	경우는	0으로	고정

name
name: required (string)
서버 이름

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"auth_prod_id":	1,
						"name":	"profile1",
						"auth_only":	1,
						"authsvr_saml_id":	null,
						"retry_cnt":	3,
						"lock_tm":	3,
						"servers":[
								{"type":	"ldap",	"id":	1,	"name":	"ldap1"},
								{"type":	"radius",	"id":	1,	"name":	"radius1"},
								{"type":	"tacacs",	"id":	1,	"name":	"tacacs1"},
								{"type":	"localdb",	"id":	0,	"name":	"localdb1"}
						],
						"isused":	0,
						"desc":	"test"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/auth/profiles

인증 프로파일를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

auth_prod_id
auth_prod_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

auth_only
auth_only: required (string)
관리자 인증 전용  (OFF:	0,	ON:	1)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 30)
인증 시도 횟수

lock_tm: required (integer - default: 3 - minimum: 0 - maximum: 60)
lock_tm
잠김 시간(분)

servers
servers: required (array of items)
Items: items
Items

type: required (one of ldap, radius, tacacs, localdb)
type
서버 종류

idid: required (integer)
서버 ID
type이	localdb	일	경우는	0으로	고정

name
name: required (string)
서버 이름

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"name":	"profile1",
		"auth_only":	1,
		"authsvr_saml_id":	null,
		"retry_cnt":	3,
		"lock_tm":	3,
		"servers":[
				{"type":	"ldap",	"id":	1,	"name":	"ldap1"},
				{"type":	"radius",	"id":	1,	"name":	"radius1"},
				{"type":	"tacacs",	"id":	1,	"name":	"tacacs1"},
				{"type":	"localdb",	"id":	0,	"name":	"localdb1"}
		],
		"desc":	"test"
}

Response

HTTP status code 200

인증 프로파일 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

auth_prod_id
auth_prod_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

auth_only: required (string)
auth_only
관리자 인증 전용  (OFF:	0,	ON:	1)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 30)
인증 시도 횟수

lock_tm
lock_tm: required (integer - default: 3 - minimum: 0 - maximum: 60)
잠김 시간(분)

servers
servers: required (array of items)
Items: items
Items

type
type: required (one of ldap, radius, tacacs, localdb)
서버 종류

idid: required (integer)
서버 ID
type이	localdb	일	경우는	0으로	고정

name
name: required (string)
서버 이름

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"auth_prod_id":	1,
		"name":	"profile1",
		"auth_only":	1,
		"authsvr_saml_id":	null,
		"retry_cnt":	3,
		"lock_tm":	3,
		"servers":[
				{"type":	"ldap",	"id":	1,	"name":	"ldap1"},
				{"type":	"radius",	"id":	1,	"name":	"radius1"},
				{"type":	"tacacs",	"id":	1,	"name":	"tacacs1"},
				{"type":	"localdb",	"id":	0,	"name":	"localdb1"}
		],
		"isused":	0,
		"desc":	"test"
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/auth/profiles

인증 프로파일 목록을 (일괄)삭제한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"auth_prod_id":	1},
				{"auth_prod_id":	2}
		]
}

Response

HTTP status code 200

인증 프로파일 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/auth/profiles/{pk}

인증 프로파일을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
인증 프로파일 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증 프로파일 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
auth_prod_id
auth_prod_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

auth_only
auth_only: required (string)
관리자 인증 전용  (OFF:	0,	ON:	1)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 30)
인증 시도 횟수

lock_tm
lock_tm: required (integer - default: 3 - minimum: 0 - maximum: 60)
잠김 시간(분)

servers
servers: required (array of items)
Items: items
Items

type: required (one of ldap, radius, tacacs, localdb)
type
서버 종류

idid: required (integer)
서버 ID
type이	localdb	일	경우는	0으로	고정

name: required (string)
name
서버 이름

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"auth_prod_id":	1,
				"name":	"profile1",
				"auth_only":	1,
				"authsvr_saml_id":	null,
				"retry_cnt":	3,
				"lock_tm":	3,
				"servers":[
						{"type":	"ldap",	"id":	1,	"name":	"ldap1"},
						{"type":	"radius",	"id":	1,	"name":	"radius1"},
						{"type":	"tacacs",	"id":	1,	"name":	"tacacs1"},
						{"type":	"localdb",	"id":	0,	"name":	"localdb1"}
				],
				"isused":	0,
				"desc":	"test"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/auth/profiles/{pk}

인증 프로파일을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
인증 프로파일 ID

Example:
Example

10

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

auth_prod_id: (integer)
auth_prod_id
ID

name
name: required (string - maxLength: 32)
이름

auth_only
auth_only: required (string)
관리자 인증 전용  (OFF:	0,	ON:	1)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 30)
인증 시도 횟수

lock_tm
lock_tm: required (integer - default: 3 - minimum: 0 - maximum: 60)
잠김 시간(분)

servers: required (array of items)
servers
Items: items
Items

type
type: required (one of ldap, radius, tacacs, localdb)
서버 종류

idid: required (integer)
서버 ID
type이	localdb	일	경우는	0으로	고정

name
name: required (string)
서버 이름

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"name":	"profile1",
		"auth_only":	1,
		"authsvr_saml_id":	null,
		"retry_cnt":	3,
		"lock_tm":	3,
		"servers":[
				{"type":	"ldap",	"id":	1,	"name":	"ldap1"},
				{"type":	"radius",	"id":	1,	"name":	"radius1"},
				{"type":	"tacacs",	"id":	1,	"name":	"tacacs1"},
				{"type":	"localdb",	"id":	0,	"name":	"localdb1"}
		],
		"desc":	"test"
}

Response

HTTP status code 200

인증 프로파일 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
auth_prod_id
auth_prod_id: (integer)
ID

name
name: required (string - maxLength: 32)
이름

auth_only
auth_only: required (string)
관리자 인증 전용  (OFF:	0,	ON:	1)

retry_cnt
retry_cnt: required (integer - default: 3 - minimum: 1 - maximum: 30)
인증 시도 횟수

lock_tm: required (integer - default: 3 - minimum: 0 - maximum: 60)
lock_tm
잠김 시간(분)

servers
servers: required (array of items)
Items: items
Items

type: required (one of ldap, radius, tacacs, localdb)
type
서버 종류

idid: required (integer)
서버 ID
type이	localdb	일	경우는	0으로	고정

name
name: required (string)
서버 이름

isused
isused: required (one of 0, 1)
참조 기능 존재 여부  (없음:	0,	참조:	1)

descdesc: (string - maxLength: 64)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
		"auth_prod_id":	1,
		"name":	"profile1",
		"auth_only":	1,
		"authsvr_saml_id":	null,
		"retry_cnt":	3,
		"lock_tm":	3,
		"servers":[
				{"type":	"ldap",	"id":	1,	"name":	"ldap1"},
				{"type":	"radius",	"id":	1,	"name":	"radius1"},
				{"type":	"tacacs",	"id":	1,	"name":	"tacacs1"},
				{"type":	"localdb",	"id":	0,	"name":	"localdb1"}
		],
		"isused":	0,
		"desc":	"test"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/auth/profiles/{pk}

인증 프로파일을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
인증 프로파일 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증 프로파일 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/auth/profiles/{pk}/reference

인증 프로파일 참조을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
인증 프로파일 ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증 프로파일 참조 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

result: required (object)
result

auth_prof_id: required (integer)
auth_prof_id
ID

admin
admin: required (array of items)
관리자 인증 참조 목록

Items: items
Items

admin_id
admin_id: required (integer)
ID

name
name: required (string)
이름

auth_portal
auth_portal: required (array of items)
사용자 인증 포털 참조 목록

Items: items
Items

auth_portal_rule_id
auth_portal_rule_id: required (integer)
ID

name
name: required (string)
이름

auth_option
auth_option: required (array of items)
사용자 인증 설정 참조 목록

Items: items
Items

auth_option_id
auth_option_id: required (integer)
ID

name: required (string)
name
이름

dvc_portal
dvc_portal: required (array of items)
디바이스 포털 참조 목록

Items: items
Items

dvc_portal_conn_info_id
dvc_portal_conn_info_id: required (integer)
ID

name
name: required (string)
이름

vpnvpn: required (array of items)
VPN 인증 참조 목록

Items: items
Items

vpn_center_id: required (integer)
vpn_center_id
ID

name
name: required (string)
이름

dvc_clint: required (array of items)
dvc_clint
통합 클라이언트 설정 참조 목록

Items: items
Items

dvc_clint_cfg_id
dvc_clint_cfg_id: required (integer)
ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"auth_prof_id":	1,
				"admin":	[
						{
								"admin_id":	1,
								"name":	"name"
						}
				],
				"auth_portal":	[
						{
								"auth_portal_rule_id":	1,
								"name":	"name"
						}
				],
				"auth_option":	[
						{
								"auth_option_id":	0,
								"name":	"userauth_common"
						}
				],
				"dvc_portal":	[
						{
								"dvc_portal_conn_info_id":	1,
								"name":	"name"
						}
				],
				"vpn":	[
						{
								"vpn_center_id":	1,
								"name":	"name"
						}
				],
				"dvc_clint":	[
						{
								"dvc_clint_cfg_id":	0,
								"name":	"Integration	CLIENT"
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/auth/profiles/names/{auth_only}

인증 프로파일 이름 목록을 조회한다.

Request

URI Parameters

auth_only
auth_only: required (one of 0, 1)
관리자 인증 전용  (OFF:	0,	ON:	1)

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증 프로파일 이름 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of items)
인증 프로파일 목록

Items: items
Items

auth_prod_id: required (integer)
auth_prod_id
ID

name
name: required (string)
이름

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"auth_prod_id":1,
						"name":"profile1"
				},
				{
						"auth_prod_id":2,
						"name":"profile2"
				},
				{
						"auth_prod_id":3,
						"name":"profile3"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/auth/profiles/apply

인증 프로파일을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

인증 프로파일 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/auth/profiles/cancel

인증 프로파일을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

인증 프로파일 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

