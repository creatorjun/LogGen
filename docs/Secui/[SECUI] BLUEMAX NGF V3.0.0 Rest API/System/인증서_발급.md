GET

PUT

PUT

PUT

GET

POST

  DELETE

GET

  DELETE

GET

GET

POST

  DELETE

GET

GET

GET

인증서 발급 REST API 문서

https://{url}/api/sm

urlurl: required (string)
장비 IP:Port

인증서 발급 설정

/cert-issue

/cert-issue/apply

/cert-issue/cancel

인증서

/cert-issue/certs

/cert-issue/certs/{cert_name}

/cert-issue/certs/{cert_name}/export

SSL 트래픽 검사 인증서

/cert-issue/ssl-certs

/cert-issue/ssl-certs/{ssl_trfc_chk_cert_id}

/cert-issue/ssl-certs/{ssl_trfc_chk_cert_id}/export

SSL 트래픽 검사 신뢰할 수 있는 인증서

/cert-issue/ssl-cert-trusts

인증서 발급 설정

GET

/cert-issue

인증서 발급 설정을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증서 발급 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

ip_ver
ip_ver: required (one of 1, 2)
IP 주소 버전
1:	IPv4,	2:	IPv6

ipip: required (string)
CA 인증 서버 IP 주소
IPv4 Address or IPv6 Address

passwd
passwd: (string - minLength: 9 - maxLength: 32)
인증 비밀번호,Base64로 인코딩하여 전달한다.
9자 이상, 32자 이하의 영문/숫자/특수문자 조합
Well-known Word(admin,administrator,test,qwe,user) 사용 불가

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ip_ver":	1,
				"ip":	"127.0.0.1"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/cert-issue

인증서 발급 설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
ip_ver
ip_ver: required (one of 1, 2)
IP 주소 버전
1:	IPv4,	2:	IPv6

ipip: required (string)
CA 인증 서버 IP 주소
IPv4 Address or IPv6 Address

passwd
passwd: (string - minLength: 9 - maxLength: 32)
인증 비밀번호,Base64로 인코딩하여 전달한다.
9자 이상, 32자 이하의 영문/숫자/특수문자 조합
Well-known Word(admin,administrator,test,qwe,user) 사용 불가

Example:
Example

{
		"ip_ver":	1,
		"ip":	"127.0.0.1",
		"passwd":	"x2xAyWkwMEAh"
}

Response

HTTP status code 200

인증서 발급 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

result: required (object)
result

ip_ver: required (one of 1, 2)
ip_ver
IP 주소 버전
1:	IPv4,	2:	IPv6

ipip: required (string)
CA 인증 서버 IP 주소
IPv4 Address or IPv6 Address

passwd
passwd: (string - minLength: 9 - maxLength: 32)
인증 비밀번호,Base64로 인코딩하여 전달한다.
9자 이상, 32자 이하의 영문/숫자/특수문자 조합
Well-known Word(admin,administrator,test,qwe,user) 사용 불가

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ip_ver":	1,
				"ip":	"127.0.0.1"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/cert-issue/apply

인증서 발급 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

인증서 발급 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/cert-issue/cancel

인증서 발급 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

인증서 발급 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

인증서

GET

/cert-issue/certs

인증서 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증서 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of CertInfo)
Items: CertInfo
Items

cert_name: required (string)
cert_name
인증서 이름

detail_info
detail_info: required (string)
인증서 세부 정보

expire_period
expire_period: required (string)
인증서 만료 기간

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"expire_period":	"June	05	07:39:24	2020	GMT	~	June	05	07:39:24	2021	GMT",
						"cert_name":	"secui",
						"detail_info":	"Issuer:	/C=KR/ST=secui/L=secui/O=secui/OU=secui/CN=secui/emailAddress=example@secui.com\nSubject:	/C=KR/ST=secui/L=secui/O=secui/OU=secui/CN=secui/emailAddress=secui@example.com"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/cert-issue/certs

인증서를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

cert_name
cert_name: required (string - maxLength: 32)
인증서 이름
영문, 숫자, 특수문자( - ,  _ ,  . ,  : )

country
country: required (one of KR, US, JP, CN)
국가 코드
대한민국:	KR,	미국:	US,	일본:	JP,	중국:	CN

state
state: required (string - maxLength: 32)
주/도
영문, 숫자, 특수문자( - ,  _ ,  . )

location: required (string - maxLength: 32)
location
도시
영문, 숫자, 특수문자( - ,  _ ,  . )

organization
organization: required (string - maxLength: 32)
조직
영문, 숫자, 특수문자( - ,  _ ,  . )

organization_unit
organization_unit: required (string - maxLength: 32)
부서
영문, 숫자, 특수문자( - ,  _ ,  . )

common_name: required (string - maxLength: 32)
common_name
별칭
영문, 숫자, 특수문자( - ,  _ ,  . ,  : )

email
email: required (string - maxLength: 32)
메일 주소
영문, 숫자, 특수문자( - ,  _ ,  . )

vpn_ip
vpn_ip: required (string)
VPN 장비 주소
IPv4	또는	IPv6	형식

Example:
Example

{
		"cert_name":	"example",
		"country":	"KR",
		"state":	"example",
		"location":	"example",
		"organization":	"example",
		"organization_unit":	"example",
		"common_name":	"example",
		"email":	"example@example.com",
		"vpn_ip":	"192.168.1.1"
}

Response

HTTP status code 200

인증서 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

cert_name
cert_name: required (string)
인증서 이름

detail_info: required (string)
detail_info
인증서 세부 정보

expire_period
expire_period: required (string)
인증서 만료 기간

Example:
Example

{
		"code":	"ok",
		"result":	{
				"expire_period":	"June	29	06:33:07	2020	GMT	~	June	29	06:33:07	2021	GMT",
				"cert_name":	"example",
				"detail_info":	"Issuer:	/C=KR/ST=secui/L=secui/O=secui/OU=secui/CN=secui/emailAddress=example@secui.com\nSubject:	/C=KR/ST=example/L=example/O=example/OU=example/CN=example/emailAddress=example@example.com"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/cert-issue/certs

인증서 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"cert_name":	"secui1"},
				{"cert_name":	"secui2"}
		]
}

Response

HTTP status code 200

인증서 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/cert-issue/certs/{cert_name}

인증서을(를) 조회한다.

Request

URI Parameters

cert_name: required (string)
cert_name
인증서 ID

Example:
Example

secui1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type

요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증서 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

cert_name
cert_name: required (string)
인증서 이름

detail_info
detail_info: required (string)
인증서 세부 정보

expire_period
expire_period: required (string)
인증서 만료 기간

Example:
Example

{
		"code":	"ok",
		"result":	{
				"expire_period":	"June	29	06:33:07	2020	GMT	~	June	29	06:33:07	2021	GMT",
				"cert_name":	"example",
				"detail_info":	"Issuer:	/C=KR/ST=secui/L=secui/O=secui/OU=secui/CN=secui/emailAddress=example@secui.com\nSubject:	/C=KR/ST=example/L=example/O=example/OU=example/CN=example/emailAddress=example@example.com"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/cert-issue/certs/{cert_name}

인증서을(를) 삭제한다.

Request

URI Parameters

cert_name
cert_name: required (string)
인증서 ID

Example:
Example

secui1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

인증서 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/cert-issue/certs/{cert_name}/export

인증서를 다운로드 한다.

Request

URI Parameters

cert_name
cert_name: required (string)
인증서 ID

Example:
Example

secui1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

qq: (object)
내보내기 파라미터(string)
q=속성 %3D 값 , 속성 %3D 값```

format
format: required (one of 0, 1, 2)
인증서의 저장 형식 (pem:	0,	der:	1,	pfx:	2)

pk_passwd: required (string - maxLength: 32)
pk_passwd
개인키 파일의 비밀번호(Base64 인코딩)

export_key
export_key: required (one of 0, 1)
개인키 파일 내보내기 (OFF:	0,	ON:	1)

Example:
Example

?q=format%3D1,pk_passwd%3Dx2xAyWkwMEAh,export_key%3D1

Response

HTTP status code 200

인증서를 PC로 저장 성공

Body

Media type: application/octet-stream
Media type

TypeType: file

SSL 트래픽 검사 인증서

GET

/cert-issue/ssl-certs

SSL 트래픽 검사 인증서 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

SSL 트래픽 검사 인증서 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of SSLCertInfo)
Items: SSLCertInfo
Items

ssl_trfc_chk_cert_id: required (integer)
ssl_trfc_chk_cert_id
인증서 ID

cert_type
cert_type: required (one of 0, 1, 2)
인증서 타입
0:	CA	인증서,	1:	사설	인증서,	2:	신뢰할	수	있는	루트	인증서

cert_name: required (string)
cert_name
인증서 이름

issuer
issuer: required (string)
인증서 세부 정보( Issuer )

subject
subject: required (string)
인증서 세부 정보( Subject )

expire_period
expire_period: required (string)
인증서 만료 기간

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"ssl_trfc_chk_cert_"ssl_trfc_chk_cert_id":	1,
						"cert_name":	"cacert",
						"cert_type":	0,
						"subject":	"/C=KR/ST=secui/L=secui/O=secui/OU=secui/CN=secui/emailAddress=example@secui.com",
						"issuer":	"/C=KR/ST=secui/L=secui/O=secui/OU=secui/CN=secui/emailAddress=example@secui.com",
						"expire_period":	"May	27	22:52:46	2020	GMT	~	May	27	22:52:46	2021	GMT"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/cert-issue/ssl-certs

SSL 트래픽 검사 인증서를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

cert_name
cert_name: required (string - maxLength: 32)
인증서 이름
영문, 숫자, 특수문자( - ,  _ ,  . )

country: required (one of KR, US, JP, CN)
country
국가 코드
대한민국:	KR,	미국:	US,	일본:	JP,	중국:	CN

state
state: required (string - maxLength: 32)
주/도
영문, 숫자, 특수문자( - ,  _ ,  . )

location
location: required (string - maxLength: 32)
도시
영문, 숫자, 특수문자( - ,  _ ,  . )

organization
organization: required (string - maxLength: 32)
조직
영문, 숫자, 특수문자( - ,  _ ,  . )

organization_unit
organization_unit: required (string - maxLength: 32)
부서
영문, 숫자, 특수문자( - ,  _ ,  . )

common_name: required (string - maxLength: 32)
common_name
별칭
영문, 숫자, 특수문자( - ,  _ ,  . ,  : )

email
email: required (string - maxLength: 32)
메일 주소
이메일 형식

cert_bit
cert_bit: required (one of 1024, 2048 - default: 2048)
인증서 비트수

Example:
Example

{
		"cert_name":	"example",
		"country":	"KR",
		"state":	"example",
		"location":	"example",
		"organization":	"example",
		"organization_unit":	"example",
		"common_name":	"example",
		"email":	"example@example.com",
		"cert_bit":	2048
}

Response

HTTP status code 200

SSL 트래픽 검사 인증서 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
ssl_trfc_chk_cert_id
ssl_trfc_chk_cert_id: required (integer)
인증서 ID

cert_type
cert_type: required (one of 0, 1, 2)
인증서 타입
0:	CA	인증서,	1:	사설	인증서,	2:	신뢰할	수	있는	루트	인증서

cert_name
cert_name: required (string)
인증서 이름

issuer
issuer: required (string)
인증서 세부 정보( Issuer )

subject
subject: required (string)
인증서 세부 정보( Subject )

expire_period
expire_period: required (string)
인증서 만료 기간

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ssl_trfc_chk_cert_id":	2,
				"cert_name":	"example",
				"cert_type":	1,
				"subject":	"/C=US/ST=example/L=example/O=example/OU=example/CN=example/emailAddress=example@example.com",
				"issuer":	"/C=KR/ST=secui/L=secui/O=secui/OU=secui/CN=secui/emailAddress=example@secui.com",
				"expire_period":	"June	29	05:56:31	2020	GMT	~	June	29	05:56:31	2021	GMT"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/cert-issue/ssl-certs

SSL 트래픽 검사 인증서 목록을 (일괄)삭제한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (object)
items
삭제될 인증서 정보

ssl_trfc_chk_cert_id
ssl_trfc_chk_cert_id: required (integer)
인증서 ID(PK)
필수

cert_name: required (string - maxLength: 32)
cert_name
인증서 이름
필수

Example:
Example

{
		"items":	[
				{"ssl_trfc_chk_cert_id":	1,	"cert_name":	"example"}
		]
}

Response

HTTP status code 200

SSL 트래픽 검사 인증서 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result: required (object)
result
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/cert-issue/ssl-certs/{ssl_trfc_chk_cert_id}

SSL 트래픽 검사 인증서을(를) 조회한다.

Request

URI Parameters

ssl_trfc_chk_cert_id: required (integer)
ssl_trfc_chk_cert_id
인증서 ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

qq: (object)
조회 파라미터(string)
q=속성 %3D 값 , 속성 %3D 값```

cert_type
cert_type: required (one of 0, 1, 2)
인증서 종류 (CA	인증서:	0,	사설	인증서:	1,	신뢰할	수	있는	루트	인증서:	2)

Example:
Example

?q=cert_type%3D1

Response

HTTP status code 200

SSL 트래픽 검사 인증서 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
ssl_trfc_chk_cert_id: required (integer)
ssl_trfc_chk_cert_id
인증서 ID

cert_type
cert_type: required (one of 0, 1, 2)
인증서 타입
0:	CA	인증서,	1:	사설	인증서,	2:	신뢰할	수	있는	루트	인증서

cert_name: required (string)
cert_name
인증서 이름

issuer
issuer: required (string)
인증서 세부 정보( Issuer )

subject
subject: required (string)
인증서 세부 정보( Subject )

expire_period
expire_period: required (string)
인증서 만료 기간

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ssl_trfc_chk_cert_id":	2,
				"cert_name":	"example",
				"cert_type":	1,
				"subject":	"/C=US/ST=example/L=example/O=example/OU=example/CN=example/emailAddress=example@example.com",
				"issuer":	"/C=KR/ST=secui/L=secui/O=secui/OU=secui/CN=secui/emailAddress=example@secui.com",
				"expire_period":	"June	29	05:56:31	2020	GMT	~	June	29	05:56:31	2021	GMT"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/cert-issue/ssl-certs/{ssl_trfc_chk_cert_id}/export

SSL 트래픽 검사 인증서를 다운로드 한다.

Request

URI Parameters

ssl_trfc_chk_cert_id: required (integer)
ssl_trfc_chk_cert_id
인증서 ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

qq: (object)
내보내기 파라미터(string)
q=속성 %3D 값 , 속성 %3D 값```

format
format: required (one of 0, 1, 2)
인증서의 저장 형식 (pem:	0,	der:	1,	pfx:	2)

cert_type
cert_type: required (one of 0, 1, 2)
인증서 타입
0:	CA	인증서,	1:	사설	인증서,	2:	신뢰할	수	있는	루트	인증서

pk_passwd
pk_passwd: required (string - maxLength: 32)
개인키 파일의 비밀번호(Base64 인코딩)

export_key
export_key: required (one of 0, 1)
개인키 파일 내보내기 (OFF:	0,	ON:	1)

Example:
Example

?q=format%3D1,pk_passwd%3Dx2xAyWkwMEAh,cert_type%3D1,export_key%3D1

Response

HTTP status code 200

SSL 트래픽 검사 인증서를 PC로 저장 성공

Body

Media type: application/octet-stream
Media type

TypeType: file

SSL 트래픽 검사 신뢰할 수 있는 인증서

GET

/cert-issue/ssl-cert-trusts

SSL 트래픽 검사 신뢰할 수 있는 인증서 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

SSL 트래픽 검사 신뢰할 수 있는 인증서 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of SSLCertInfo)
Items: SSLCertInfo
Items

ssl_trfc_chk_cert_id
ssl_trfc_chk_cert_id: required (integer)
인증서 ID

cert_type
cert_type: required (one of 0, 1, 2)
인증서 타입
0:	CA	인증서,	1:	사설	인증서,	2:	신뢰할	수	있는	루트	인증서

cert_name
cert_name: required (string)
인증서 이름

issuer
issuer: required (string)
인증서 세부 정보( Issuer )

subject
subject: required (string)
인증서 세부 정보( Subject )

expire_period: required (string)
expire_period
인증서 만료 기간

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"ssl_trfc_chk_cert_id":	2,
						"cert_name":	"GeoTrust_Global_CA",
						"cert_type":	2,
						"subject":	"/C=US/O=GeoTrust	Inc./CN=GeoTrust	Global	CA",
						"issuer":	"/C=US/O=GeoTrust	Inc./CN=GeoTrust	Global	CA",
						"expire_period":	"May	21	04:00:00	2002	GMT	~	May	21	04:00:00	2022	GMT"
				},
				{
						"ssl_trfc_chk_cert_id":	3,
						"cert_name":	"yessignTSA_Class_2",
						"cert_type":	2,
						"subject":	"/C=kr/O=yessign/OU=AccreditedCA/CN=yessignTSA	Class	2",
						"issuer":	"/C=KR/O=KISA/OU=Korea	Certification	Authority	Central/CN=KISA	RootCA	4",
						"expire_period":	"October	05	03:46:43	2012	GMT	~	October	05	03:46:43	2022	GMT"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

