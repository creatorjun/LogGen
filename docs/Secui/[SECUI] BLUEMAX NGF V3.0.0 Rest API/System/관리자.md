GET

GET

POST

  DELETE

PUT

  DELETE

GET

POST

PUT

PUT

PUT

GET

PUT

PUT

PUT

관리자 설정 REST API 문서

https://{url}/api/sm

urlurl: required (string)
장비 IP

관리자

/manager/users

/manager/users/{adm_id}

/manager/users/{adm_id}/client

/manager/users/apply

/manager/users/cancel

관리자 북마크

/manager/bookmark/{adm_id}

/manager/bookmark/apply

/manager/bookmark/cancel

관리자

GET

/manager/users

관리자 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

관리자 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of user)
Items: user
Items

adm_id
adm_id: (integer)
ID

type
type: required (one of 0, 1, 2)
관리자 유형 (admin:	0,	vs	admin:	1,	super	admin:	2)
(1,2	유형은	MGMT	호스트만	사용)

login_id
login_id: required (string - maxLength: 32)
로그인 ID

login_pw
login_pw: required (string - minLength: 9 - maxLength: 32)
비밀번호(AES 256 CBC 암호화) (영문,숫자,특수문자	조합)

Example:
Example

[AES	256	암호화	코드	예시(Python)]
import	base64
from	Crypto.Cipher	import	AES
from	Crypto	import	Random

class	AESCipher:

				def	__init__(self,	key):
								self.bs	=	16
								self.key	=	key[:32]

				def	encrypt(self,	raw):
								"""암호화"""
								raw	=	self._pad(raw)
								iv	=	Random.new().read(AES.block_size)
								cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)
								hex_data	=	(iv.hex()	+	cipher.encrypt(raw).hex()).encode('utf-8')
								return	base64.b64encode(hex_data)

				def	decrypt(self,	enc):
								"""복호화"""
								try:
												enc	=	base64.b64decode(enc).decode('utf-8')
												enc	=	bytes(bytearray.fromhex(enc))
												iv	=	enc[:AES.block_size]
												cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)

												return	self._unpad(cipher.decrypt(enc[AES.block_size:]).decode('utf-8'))
								except	binascii.Error:
												raise	DecryptionError('Incorrect	padding')
								except	UnicodeDecodeError:
												raise	DecryptionError('Invalid	key')
								except	Exception	as	e:
												raise	DecryptionError(e)

				def	decrypt_and_b64encode(self,	enc):
								"""복화화된	평문을	base64	인코딩"""
								to_bytes	=	self.decrypt(enc)
								if	isinstance(to_bytes,	str):
												to_bytes	=	to_bytes.encode('utf-8')

								return	base64.b64encode(to_bytes).decode('utf-8')

				def	_pad(self,	s):
								return	s	+	(self.bs	-	len(s)	%	self.bs)	*	chr(self.bs	-	len(s)	%	self.bs)

				@staticmethod
				def	_unpad(s):
								return	s[:-ord(s[len(s)	-	1:])]

name: (string - maxLength: 32)
name
이름

pw_expire_use
pw_expire_use: required (one of 0, 1)
비밀번호 만료 사용 (OFF:	0,	ON:	1)

pw_expire_prd
pw_expire_prd: required (integer - default: 60 - minimum: 1 - maximum: 120)
비밀번호 만료 기간(일)

level
level: required (one of 1, 2, 3)
권한 (Level1:	1,	vs	Level2:	2,	Level3:	3)

depart
depart: (string - maxLength: 32)
부서

phone: (string - maxLength: 16)
phone
연락처

email
email: (string - maxLength: 256)
이메일

email_alarm_use
email_alarm_use: required (one of 0, 1)
경고메일 수신 사용 (OFF:	0,	ON:	1)

id_expire_use
id_expire_use: required (one of 0, 1)
계정 만료 사용 (OFF:	0,	ON:	1)

id_expire_prd
id_expire_prd: (integer - default: 30 - minimum: 1 - maximum: 90)
계정 만료 기간(일)

ext_clnt_use
ext_clnt_use: required (one of 0, 1)
외부 클라이언트 연동 사용 (OFF:	0,	ON:	1)

ext_clnt_id
ext_clnt_id: (string - maxLength: 32)
외부 클라이언트 ID

ext_clnt_secret
ext_clnt_secret: (string - maxLength: 256)
외부 클라이언트 SECRET

otp_use
otp_use: (one of 0, 1)
SECUI OTP 사용 (OFF:	0,	ON:	1)

(지원	버전:	2.1.0)

otp_key: (string - maxLength: 16)
otp_key
SECUI OTP Serial

(지원	버전:	2.1.0)

Example:
Example

OTP	Serial	:	B2457CC51F908DD1

adm_prf
adm_prf: (object)
관리자 권한 프로파일

adm_prf_id
adm_prf_id: required (string)
프로파일 ID

name
name: required (string)
프로파일 이름

adm_ip_use
adm_ip_use: required (one of 0, 1)
관리자 아이피 사용 (OFF:	0,	ON:	1)

adm_ip: (array of items)
adm_ip
관리자 아이피

Items: items
Items

adm_ip_id
adm_ip_id: required (string)
ID

ip_expr
ip_expr: required (string)
IP 주소

adm_vs
adm_vs: (array of items)
관리자 대상 VS (MGMT	호스트만	사용)

Items: items
Items

vs_id
vs_id: required (string)
VS ID

name
name: required (string)
VS 이름

login_dt
login_dt: (string)
마지막 로그인 시간

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"adm_id":	1,
						"type":	0,
						"login_id":	"secui_user",
						"name":	"secui",
						"pw_expire_use":	0,
						"level":	2,
						"depart":	"secui",
						"phone":	"000-0000-0000",
						"email":	"admin@secui.com",
						"email_alarm_use":	1,
						"id_expire_use":	0,
						"ext_clnt_use":	0,
						"otp_use":	0,
						"adm_ip_use":	0,
						"login_dt":	"2018-07-06	23:27:08"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/manager/users

관리자를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

adm_id
adm_id: (integer)
ID

type
type: required (one of 0, 1, 2)
관리자 유형 (admin:	0,	vs	admin:	1,	super	admin:	2)
(1,2	유형은	MGMT	호스트만	사용)

login_id
login_id: required (string - maxLength: 32)
로그인 ID

login_pw
login_pw: required (string - minLength: 9 - maxLength: 32)
비밀번호(AES 256 CBC 암호화) (영문,숫자,특수문자	조합)

Example:
Example

[AES	256	암호화	코드	예시(Python)]
import	base64
from	Crypto.Cipher	import	AES
from	Crypto	import	Random

class	AESCipher:

				def	__init__(self,	key):
								self.bs	=	16
								self.key	=	key[:32]

				def	encrypt(self,	raw):
								"""암호화"""
								raw	=	self._pad(raw)
								iv	=	Random.new().read(AES.block_size)
								cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)
								hex_data	=	(iv.hex()	+	cipher.encrypt(raw).hex()).encode('utf-8')
								return	base64.b64encode(hex_data)

				def	decrypt(self,	enc):
								"""복호화"""
								try:
												enc	=	base64.b64decode(enc).decode('utf-8')
												enc	=	bytes(bytearray.fromhex(enc))
												iv	=	enc[:AES.block_size]
												cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)

												return	self._unpad(cipher.decrypt(enc[AES.block_size:]).decode('utf-8'))
								except	binascii.Error:
												raise	DecryptionError('Incorrect	padding')
								except	UnicodeDecodeError:
												raise	DecryptionError('Invalid	key')
								except	Exception	as	e:
												raise	DecryptionError(e)

				def	decrypt_and_b64encode(self,	enc):
								"""복화화된	평문을	base64	인코딩"""
								to_bytes	=	self.decrypt(enc)
								if	isinstance(to_bytes,	str):
												to_bytes	=	to_bytes.encode('utf-8')

								return	base64.b64encode(to_bytes).decode('utf-8')

				def	_pad(self,	s):
								return	s	+	(self.bs	-	len(s)	%	self.bs)	*	chr(self.bs	-	len(s)	%	self.bs)

				@staticmethod
				def	_unpad(s):
								return	s[:-ord(s[len(s)	-	1:])]

name: (string - maxLength: 32)
name
이름

pw_expire_use
pw_expire_use: required (one of 0, 1)
비밀번호 만료 사용 (OFF:	0,	ON:	1)

pw_expire_prd
pw_expire_prd: required (integer - default: 60 - minimum: 1 - maximum: 120)
비밀번호 만료 기간(일)

level
level: required (one of 1, 2, 3)
권한 (Level1:	1,	vs	Level2:	2,	Level3:	3)

depart
depart: (string - maxLength: 32)
부서

phone
phone: (string - maxLength: 16)
연락처

email
email: (string - maxLength: 256)
이메일

email_alarm_use: required (one of 0, 1)
email_alarm_use
경고메일 수신 사용 (OFF:	0,	ON:	1)

id_expire_use
id_expire_use: required (one of 0, 1)
계정 만료 사용 (OFF:	0,	ON:	1)

id_expire_prd
id_expire_prd: (integer - default: 30 - minimum: 1 - maximum: 90)
계정 만료 기간(일)

ext_clnt_use
ext_clnt_use: required (one of 0, 1)
외부 클라이언트 연동 사용 (OFF:	0,	ON:	1)

ext_clnt_id
ext_clnt_id: (string - maxLength: 32)
외부 클라이언트 ID

ext_clnt_secret: (string - maxLength: 256)
ext_clnt_secret
외부 클라이언트 SECRET

otp_use
otp_use: (one of 0, 1)
SECUI OTP 사용 (OFF:	0,	ON:	1)

(지원	버전:	2.1.0)

otp_key
otp_key: (string - maxLength: 16)
SECUI OTP Serial

(지원	버전:	2.1.0)

Example:
Example

OTP	Serial	:	B2457CC51F908DD1

adm_prf
adm_prf: (object)
관리자 권한 프로파일

adm_prf_id
adm_prf_id: required (string)
프로파일 ID

name
name: required (string)
프로파일 이름

adm_ip_use
adm_ip_use: required (one of 0, 1)
관리자 아이피 사용 (OFF:	0,	ON:	1)

adm_ip
adm_ip: (array of items)
관리자 아이피

Items: items
Items

adm_ip_id
adm_ip_id: required (string)
ID

ip_expr
ip_expr: required (string)
IP 주소

adm_vs
adm_vs: (array of items)
관리자 대상 VS (MGMT	호스트만	사용)

Items: items
Items

vs_id
vs_id: required (string)
VS ID

name
name: required (string)
VS 이름

login_dt
login_dt: (string)
마지막 로그인 시간

Example:
Example

{
		"type":	0,
		"login_id":	"secui_user",
		"login_pw":	"x2xAyWkwMEAh",
		"name":	"secui",
		"pw_expire_use":	0,
		"level":	2,
		"depart":	"secui",
		"phone":	"000-0000-0000",
		"email":	"admin@secui.com",
		"email_alarm_use":	1,
		"id_expire_use":	0,
		"ext_clnt_use":	0,
		"otp_use":	0,
		"adm_ip_use":	0
}

Response

HTTP status code 200

관리자 추가 성공

Headers

Location
Location: required (string)
Example:
Example

/manager/users/1

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

adm_id
adm_id: (integer)
ID

type
type: required (one of 0, 1, 2)
관리자 유형 (admin:	0,	vs	admin:	1,	super	admin:	2)
(1,2	유형은	MGMT	호스트만	사용)

login_id
login_id: required (string - maxLength: 32)
로그인 ID

login_pw
login_pw: required (string - minLength: 9 - maxLength: 32)
비밀번호(AES 256 CBC 암호화) (영문,숫자,특수문자	조합)

Example:
Example

[AES	256	암호화	코드	예시(Python)]
import	base64
from	Crypto.Cipher	import	AES
from	Crypto	import	Random

class	AESCipher:

				def	__init__(self,	key):
								self.bs	=	16
								self.key	=	key[:32]

				def	encrypt(self,	raw):
								"""암호화"""
								raw	=	self._pad(raw)
								iv	=	Random.new().read(AES.block_size)
								cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)
								hex_data	=	(iv.hex()	+	cipher.encrypt(raw).hex()).encode('utf-8')
								return	base64.b64encode(hex_data)

				def	decrypt(self,	enc):
								"""복호화"""
								try:
												enc	=	base64.b64decode(enc).decode('utf-8')
												enc	=	bytes(bytearray.fromhex(enc))
												iv	=	enc[:AES.block_size]
												cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)

												return	self._unpad(cipher.decrypt(enc[AES.block_size:]).decode('utf-8'))
								except	binascii.Error:
												raise	DecryptionError('Incorrect	padding')
								except	UnicodeDecodeError:
												raise	DecryptionError('Invalid	key')
								except	Exception	as	e:
												raise	DecryptionError(e)

				def	decrypt_and_b64encode(self,	enc):
								"""복화화된	평문을	base64	인코딩"""
								to_bytes	=	self.decrypt(enc)
								if	isinstance(to_bytes,	str):
												to_bytes	=	to_bytes.encode('utf-8')

								return	base64.b64encode(to_bytes).decode('utf-8')

				def	_pad(self,	s):
								return	s	+	(self.bs	-	len(s)	%	self.bs)	*	chr(self.bs	-	len(s)	%	self.bs)

				@staticmethod
				def	_unpad(s):
								return	s[:-ord(s[len(s)	-	1:])]

name: (string - maxLength: 32)
name
이름

pw_expire_use
pw_expire_use: required (one of 0, 1)
비밀번호 만료 사용 (OFF:	0,	ON:	1)

pw_expire_prd
pw_expire_prd: required (integer - default: 60 - minimum: 1 - maximum: 120)
비밀번호 만료 기간(일)

level
level: required (one of 1, 2, 3)
권한 (Level1:	1,	vs	Level2:	2,	Level3:	3)

depart
depart: (string - maxLength: 32)
부서

phone
phone: (string - maxLength: 16)
연락처

email
email: (string - maxLength: 256)
이메일

email_alarm_use: required (one of 0, 1)
email_alarm_use
경고메일 수신 사용 (OFF:	0,	ON:	1)

id_expire_use
id_expire_use: required (one of 0, 1)
계정 만료 사용 (OFF:	0,	ON:	1)

id_expire_prd
id_expire_prd: (integer - default: 30 - minimum: 1 - maximum: 90)
계정 만료 기간(일)

ext_clnt_use
ext_clnt_use: required (one of 0, 1)
외부 클라이언트 연동 사용 (OFF:	0,	ON:	1)

ext_clnt_id
ext_clnt_id: (string - maxLength: 32)
외부 클라이언트 ID

ext_clnt_secret: (string - maxLength: 256)
ext_clnt_secret
외부 클라이언트 SECRET

otp_use
otp_use: (one of 0, 1)
SECUI OTP 사용 (OFF:	0,	ON:	1)

(지원	버전:	2.1.0)

otp_key
otp_key: (string - maxLength: 16)
SECUI OTP Serial

(지원	버전:	2.1.0)

Example:
Example

OTP	Serial	:	B2457CC51F908DD1

adm_prf
adm_prf: (object)
관리자 권한 프로파일

adm_prf_id
adm_prf_id: required (string)
프로파일 ID

name
name: required (string)
프로파일 이름

adm_ip_use
adm_ip_use: required (one of 0, 1)
관리자 아이피 사용 (OFF:	0,	ON:	1)

adm_ip
adm_ip: (array of items)
관리자 아이피

Items: items
Items

adm_ip_id
adm_ip_id: required (string)
ID

ip_expr
ip_expr: required (string)
IP 주소

adm_vs
adm_vs: (array of items)
관리자 대상 VS (MGMT	호스트만	사용)

Items: items
Items

vs_id
vs_id: required (string)
VS ID

name
name: required (string)
VS 이름

login_dt
login_dt: (string)
마지막 로그인 시간

Example:
Example

{
		"code":	"ok",
		"result":	{
				"type":	0,
				"login_id":	"secui_user",
				"name":	"secui",
				"pw_expire_use":	0,
				"level":	2,
				"depart":	"secui",
				"phone":	"000-0000-0000",
				"email":	"admin@secui.com",
				"email_alarm_use":	1,
				"id_expire_use":	0,
				"ext_clnt_use":	0,
				"otp_use":	0,
				"adm_ip_use":	0,
				"login_dt":	"2018-07-06	23:27:08"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/manager/users

관리자 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of object)
items
삭제될 정보 목록(최대 100건)

Example:
Example

{
		"items":	[
				{"adm_id":	1},
				{"adm_id":	2}
		]
}

Response

HTTP status code 200

관리자 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

ok:	요청된	모든	항목	정상	처리
pok:	요청	항목	중	일부	항목	오류
nok:	요청된	전체	항목	오류

result
result: required (object)
결과 데이터
code	속성값이	'ok'가	아닐	경우	errors	속성이	포함됨

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/manager/users/{adm_id}

관리자을(를) 조회한다.

Request

URI Parameters

adm_id
adm_id: required (string)
ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

관리자 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result

adm_id
adm_id: (integer)
ID

type: required (one of 0, 1, 2)
type
관리자 유형 (admin:	0,	vs	admin:	1,	super	admin:	2)
(1,2	유형은	MGMT	호스트만	사용)

login_id
login_id: required (string - maxLength: 32)
로그인 ID

login_pw: required (string - minLength: 9 - maxLength: 32)
login_pw
비밀번호(AES 256 CBC 암호화) (영문,숫자,특수문자	조합)

Example:
Example

[AES	256	암호화	코드	예시(Python)]
import	base64
from	Crypto.Cipher	import	AES
from	Crypto	import	Random

class	AESCipher:

				def	__init__(self,	key):
								self.bs	=	16
								self.key	=	key[:32]

				def	encrypt(self,	raw):
								"""암호화"""
								raw	=	self._pad(raw)
								iv	=	Random.new().read(AES.block_size)
								cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)
								hex_data	=	(iv.hex()	+	cipher.encrypt(raw).hex()).encode('utf-8')
								return	base64.b64encode(hex_data)

				def	decrypt(self,	enc):
								"""복호화"""
								try:
												enc	=	base64.b64decode(enc).decode('utf-8')
												enc	=	bytes(bytearray.fromhex(enc))
												iv	=	enc[:AES.block_size]
												cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)

												return	self._unpad(cipher.decrypt(enc[AES.block_size:]).decode('utf-8'))
								except	binascii.Error:
												raise	DecryptionError('Incorrect	padding')
								except	UnicodeDecodeError:
												raise	DecryptionError('Invalid	key')
								except	Exception	as	e:
												raise	DecryptionError(e)

				def	decrypt_and_b64encode(self,	enc):
								"""복화화된	평문을	base64	인코딩"""
								to_bytes	=	self.decrypt(enc)
								if	isinstance(to_bytes,	str):
												to_bytes	=	to_bytes.encode('utf-8')

								return	base64.b64encode(to_bytes).decode('utf-8')

				def	_pad(self,	s):
								return	s	+	(self.bs	-	len(s)	%	self.bs)	*	chr(self.bs	-	len(s)	%	self.bs)

				@staticmethod
				def	_unpad(s):
								return	s[:-ord(s[len(s)	-	1:])]

name
name: (string - maxLength: 32)
이름

pw_expire_use
pw_expire_use: required (one of 0, 1)
비밀번호 만료 사용 (OFF:	0,	ON:	1)

pw_expire_prd
pw_expire_prd: required (integer - default: 60 - minimum: 1 - maximum: 120)
비밀번호 만료 기간(일)

level
level: required (one of 1, 2, 3)
권한 (Level1:	1,	vs	Level2:	2,	Level3:	3)

depart
depart: (string - maxLength: 32)
부서

phone
phone: (string - maxLength: 16)
연락처

email
email: (string - maxLength: 256)
이메일

email_alarm_use
email_alarm_use: required (one of 0, 1)
경고메일 수신 사용 (OFF:	0,	ON:	1)

id_expire_use: required (one of 0, 1)
id_expire_use
계정 만료 사용 (OFF:	0,	ON:	1)

id_expire_prd
id_expire_prd: (integer - default: 30 - minimum: 1 - maximum: 90)
계정 만료 기간(일)

ext_clnt_use
ext_clnt_use: required (one of 0, 1)
외부 클라이언트 연동 사용 (OFF:	0,	ON:	1)

ext_clnt_id
ext_clnt_id: (string - maxLength: 32)
외부 클라이언트 ID

ext_clnt_secret
ext_clnt_secret: (string - maxLength: 256)
외부 클라이언트 SECRET

otp_use: (one of 0, 1)
otp_use
SECUI OTP 사용 (OFF:	0,	ON:	1)

(지원	버전:	2.1.0)

otp_key
otp_key: (string - maxLength: 16)
SECUI OTP Serial

(지원	버전:	2.1.0)

Example:
Example

OTP	Serial	:	B2457CC51F908DD1

adm_prf
adm_prf: (object)
관리자 권한 프로파일

adm_prf_id
adm_prf_id: required (string)
프로파일 ID

name
name: required (string)
프로파일 이름

adm_ip_use
adm_ip_use: required (one of 0, 1)
관리자 아이피 사용 (OFF:	0,	ON:	1)

adm_ip: (array of items)
adm_ip
관리자 아이피

Items: items
Items

adm_ip_id
adm_ip_id: required (string)
ID

ip_expr
ip_expr: required (string)
IP 주소

adm_vs
adm_vs: (array of items)
관리자 대상 VS (MGMT	호스트만	사용)

Items: items
Items

vs_id
vs_id: required (string)
VS ID

name
name: required (string)
VS 이름

login_dt
login_dt: (string)
마지막 로그인 시간

Example:
Example

{
		"code":	"ok",
		"result":	{
				"adm_id":	1,
				"type":	0,
				"login_id":	"secui_user",
				"name":	"secui",
				"pw_expire_use":	0,
				"level":	2,
				"depart":	"secui",
				"phone":	"000-0000-0000",
				"email":	"admin@secui.com",
				"email_alarm_use":	1,
				"id_expire_use":	0,
				"ext_clnt_use":	0,
				"otp_use":	0,
				"adm_ip_use":	0,
				"login_dt":	"2018-07-06	23:27:08"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/manager/users/{adm_id}

관리자을(를) 수정한다.

Request

URI Parameters

adm_id
adm_id: required (string)
ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

adm_id: (integer)
adm_id
ID

type
type: required (one of 0, 1, 2)
관리자 유형 (admin:	0,	vs	admin:	1,	super	admin:	2)
(1,2	유형은	MGMT	호스트만	사용)

login_id: required (string - maxLength: 32)
login_id
로그인 ID

login_pw
login_pw: required (string - minLength: 9 - maxLength: 32)
비밀번호(AES 256 CBC 암호화) (영문,숫자,특수문자	조합)

Example:
Example

[AES	256	암호화	코드	예시(Python)]
import	base64
from	Crypto.Cipher	import	AES
from	Crypto	import	Random

class	AESCipher:

				def	__init__(self,	key):
								self.bs	=	16
								self.key	=	key[:32]

				def	encrypt(self,	raw):
								"""암호화"""
								raw	=	self._pad(raw)
								iv	=	Random.new().read(AES.block_size)
								cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)
								hex_data	=	(iv.hex()	+	cipher.encrypt(raw).hex()).encode('utf-8')
								return	base64.b64encode(hex_data)

				def	decrypt(self,	enc):
								"""복호화"""
								try:
												enc	=	base64.b64decode(enc).decode('utf-8')
												enc	=	bytes(bytearray.fromhex(enc))
												iv	=	enc[:AES.block_size]
												cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)

												return	self._unpad(cipher.decrypt(enc[AES.block_size:]).decode('utf-8'))
								except	binascii.Error:
												raise	DecryptionError('Incorrect	padding')
								except	UnicodeDecodeError:
												raise	DecryptionError('Invalid	key')
								except	Exception	as	e:
												raise	DecryptionError(e)

				def	decrypt_and_b64encode(self,	enc):
								"""복화화된	평문을	base64	인코딩"""
								to_bytes	=	self.decrypt(enc)
								if	isinstance(to_bytes,	str):
												to_bytes	=	to_bytes.encode('utf-8')

								return	base64.b64encode(to_bytes).decode('utf-8')

				def	_pad(self,	s):
								return	s	+	(self.bs	-	len(s)	%	self.bs)	*	chr(self.bs	-	len(s)	%	self.bs)

				@staticmethod
				def	_unpad(s):
								return	s[:-ord(s[len(s)	-	1:])]

name
name: (string - maxLength: 32)
이름

pw_expire_use
pw_expire_use: required (one of 0, 1)
비밀번호 만료 사용 (OFF:	0,	ON:	1)

pw_expire_prd
pw_expire_prd: required (integer - default: 60 - minimum: 1 - maximum: 120)
비밀번호 만료 기간(일)

level: required (one of 1, 2, 3)
level
권한 (Level1:	1,	vs	Level2:	2,	Level3:	3)

depart
depart: (string - maxLength: 32)
부서

phone
phone: (string - maxLength: 16)
연락처

email
email: (string - maxLength: 256)
이메일

email_alarm_use
email_alarm_use: required (one of 0, 1)
경고메일 수신 사용 (OFF:	0,	ON:	1)

id_expire_use
id_expire_use: required (one of 0, 1)
계정 만료 사용 (OFF:	0,	ON:	1)

id_expire_prd
id_expire_prd: (integer - default: 30 - minimum: 1 - maximum: 90)
계정 만료 기간(일)

ext_clnt_use
ext_clnt_use: required (one of 0, 1)
외부 클라이언트 연동 사용 (OFF:	0,	ON:	1)

ext_clnt_id
ext_clnt_id: (string - maxLength: 32)
외부 클라이언트 ID

ext_clnt_secret: (string - maxLength: 256)
ext_clnt_secret
외부 클라이언트 SECRET

otp_use
otp_use: (one of 0, 1)
SECUI OTP 사용 (OFF:	0,	ON:	1)

(지원	버전:	2.1.0)

otp_key
otp_key: (string - maxLength: 16)
SECUI OTP Serial

(지원	버전:	2.1.0)

Example:
Example

OTP	Serial	:	B2457CC51F908DD1

adm_prf
adm_prf: (object)
관리자 권한 프로파일

adm_prf_id: required (string)
adm_prf_id
프로파일 ID

name
name: required (string)
프로파일 이름

adm_ip_use
adm_ip_use: required (one of 0, 1)
관리자 아이피 사용 (OFF:	0,	ON:	1)

adm_ip
adm_ip: (array of items)
관리자 아이피

Items: items
Items

adm_ip_id: required (string)
adm_ip_id
ID

ip_expr
ip_expr: required (string)
IP 주소

adm_vs: (array of items)
adm_vs
관리자 대상 VS (MGMT	호스트만	사용)

Items: items
Items

vs_id
vs_id: required (string)
VS ID

name
name: required (string)
VS 이름

login_dt
login_dt: (string)
마지막 로그인 시간

Example:
Example

{
		"type":	0,
		"login_id":	"secui_user",
		"name":	"secui",
		"pw_expire_use":	0,
		"level":	2,
		"depart":	"secui",
		"phone":	"000-0000-0000",
		"email":	"admin@secui.com",
		"email_alarm_use":	1,
		"id_expire_use":	0,
		"ext_clnt_use":	0,
		"otp_use":	0,
		"adm_ip_use":	0
}

Response

HTTP status code 200

관리자 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code

결과 코드

result: required (object)
result

adm_id
adm_id: (integer)
ID

type
type: required (one of 0, 1, 2)
관리자 유형 (admin:	0,	vs	admin:	1,	super	admin:	2)
(1,2	유형은	MGMT	호스트만	사용)

login_id
login_id: required (string - maxLength: 32)
로그인 ID

login_pw
login_pw: required (string - minLength: 9 - maxLength: 32)
비밀번호(AES 256 CBC 암호화) (영문,숫자,특수문자	조합)

Example:
Example

[AES	256	암호화	코드	예시(Python)]
import	base64
from	Crypto.Cipher	import	AES
from	Crypto	import	Random

class	AESCipher:

				def	__init__(self,	key):
								self.bs	=	16
								self.key	=	key[:32]

				def	encrypt(self,	raw):
								"""암호화"""
								raw	=	self._pad(raw)
								iv	=	Random.new().read(AES.block_size)
								cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)
								hex_data	=	(iv.hex()	+	cipher.encrypt(raw).hex()).encode('utf-8')
								return	base64.b64encode(hex_data)

				def	decrypt(self,	enc):
								"""복호화"""
								try:
												enc	=	base64.b64decode(enc).decode('utf-8')
												enc	=	bytes(bytearray.fromhex(enc))
												iv	=	enc[:AES.block_size]
												cipher	=	AES.new(self.key,	AES.MODE_CBC,	iv)

												return	self._unpad(cipher.decrypt(enc[AES.block_size:]).decode('utf-8'))
								except	binascii.Error:
												raise	DecryptionError('Incorrect	padding')
								except	UnicodeDecodeError:
												raise	DecryptionError('Invalid	key')
								except	Exception	as	e:
												raise	DecryptionError(e)

				def	decrypt_and_b64encode(self,	enc):
								"""복화화된	평문을	base64	인코딩"""
								to_bytes	=	self.decrypt(enc)
								if	isinstance(to_bytes,	str):
												to_bytes	=	to_bytes.encode('utf-8')

								return	base64.b64encode(to_bytes).decode('utf-8')

				def	_pad(self,	s):
								return	s	+	(self.bs	-	len(s)	%	self.bs)	*	chr(self.bs	-	len(s)	%	self.bs)

				@staticmethod
				def	_unpad(s):
								return	s[:-ord(s[len(s)	-	1:])]

name
name: (string - maxLength: 32)
이름

pw_expire_use: required (one of 0, 1)
pw_expire_use
비밀번호 만료 사용 (OFF:	0,	ON:	1)

pw_expire_prd
pw_expire_prd: required (integer - default: 60 - minimum: 1 - maximum: 120)
비밀번호 만료 기간(일)

level
level: required (one of 1, 2, 3)
권한 (Level1:	1,	vs	Level2:	2,	Level3:	3)

depart
depart: (string - maxLength: 32)
부서

phone
phone: (string - maxLength: 16)
연락처

email: (string - maxLength: 256)
email
이메일

email_alarm_use
email_alarm_use: required (one of 0, 1)
경고메일 수신 사용 (OFF:	0,	ON:	1)

id_expire_use: required (one of 0, 1)
id_expire_use
계정 만료 사용 (OFF:	0,	ON:	1)

id_expire_prd
id_expire_prd: (integer - default: 30 - minimum: 1 - maximum: 90)
계정 만료 기간(일)

ext_clnt_use
ext_clnt_use: required (one of 0, 1)
외부 클라이언트 연동 사용 (OFF:	0,	ON:	1)

ext_clnt_id
ext_clnt_id: (string - maxLength: 32)
외부 클라이언트 ID

ext_clnt_secret
ext_clnt_secret: (string - maxLength: 256)
외부 클라이언트 SECRET

otp_use: (one of 0, 1)
otp_use

SECUI OTP 사용 (OFF:	0,	ON:	1)

(지원	버전:	2.1.0)

otp_key: (string - maxLength: 16)
otp_key
SECUI OTP Serial

(지원	버전:	2.1.0)

Example:
Example

OTP	Serial	:	B2457CC51F908DD1

adm_prf
adm_prf: (object)
관리자 권한 프로파일

adm_prf_id
adm_prf_id: required (string)
프로파일 ID

name
name: required (string)
프로파일 이름

adm_ip_use
adm_ip_use: required (one of 0, 1)
관리자 아이피 사용 (OFF:	0,	ON:	1)

adm_ip
adm_ip: (array of items)
관리자 아이피

Items: items
Items

adm_ip_id
adm_ip_id: required (string)
ID

ip_expr
ip_expr: required (string)
IP 주소

adm_vs
adm_vs: (array of items)
관리자 대상 VS (MGMT	호스트만	사용)

Items: items
Items

vs_id
vs_id: required (string)
VS ID

name: required (string)
name
VS 이름

login_dt
login_dt: (string)
마지막 로그인 시간

Example:
Example

{
		"code":	"ok",
		"result":	{
				"adm_id":	1,
				"type":	0,
				"login_id":	"secui_user",
				"name":	"secui",
				"pw_expire_use":	0,
				"level":	2,
				"depart":	"secui",
				"phone":	"000-0000-0000",
				"email":	"admin@secui.com",
				"email_alarm_use":	1,
				"id_expire_use":	0,
				"ext_clnt_use":	0,
				"otp_use":	0,
				"adm_ip_use":	0,
				"login_dt":	"2018-07-06	23:27:08"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/manager/users/{adm_id}

관리자을(를) 삭제한다.

Request

URI Parameters

adm_id
adm_id: required (string)
ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

관리자 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/manager/users/{adm_id}/client

외부 클라이언트 연동 조회

Request

URI Parameters

adm_id
adm_id: required (string)
ID

Example:
Example

10

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

외부 클라이언트 연동 발급 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

ext_clnt_id
ext_clnt_id: required (string)
Client ID

ext_clnt_secret
ext_clnt_secret: required (string)
Client Secret

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ext_clnt_id":	"f2a1ed52710d4533b",
				"ext_clnt_secret":	"FMyNTYifQ"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

POST

/manager/users/{adm_id}/client

외부 클라이언트 연동 발급

Request

URI Parameters

adm_id
adm_id: required (string)
ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

외부 클라이언트 연동 발급 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

ext_clnt_id
ext_clnt_id: required (string)
Client ID

ext_clnt_secret
ext_clnt_secret: required (string)
Client Secret

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ext_clnt_id":	"f2a1ed52710d4533b",
				"ext_clnt_secret":	"FMyNTYifQ"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/manager/users/{adm_id}/client

외부 클라이언트 연동 Secret 재발급

Request

URI Parameters

adm_id
adm_id: required (string)
ID

Example:
Example

10

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

외부 클라이언트 연동 발급 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
ext_clnt_secret: required (string)
ext_clnt_secret
Client Secret

Example:
Example

{
		"code":	"ok",
		"result":	{
				"ext_clnt_secret":	"FMyNTYifQ"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/manager/users/apply

관리자을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

관리자 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/manager/users/cancel

관리자을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

관리자 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

관리자 북마크

GET

/manager/bookmark/{adm_id}

관리자 북마크을(를) 조회한다.

Request

URI Parameters

adm_id
adm_id: required (string)
ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

관리자 북마크 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

bkmk: required (array of )
bkmk

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"bkmk":	["securityPolicy","objectSecProfile"]
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code

결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/manager/bookmark/{adm_id}

관리자 북마크을(를) 수정한다.

Request

URI Parameters

adm_id
adm_id: required (string)
ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
bkmk
bkmk: required (array of )
Example:
Example

{
		"bkmk":	["securityPolicy","objectSecProfile"]
}

Response

HTTP status code 200

관리자 북마크 수정 성공

Headers

Location: required (string)
Location
Example:
Example

/manager/bookmarks/1

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result

bkmk: required (array of )
bkmk

Example:
Example

{
		"code":	"ok",
		"result":	{
				"bkmk":	["securityPolicy","objectSecProfile"]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/manager/bookmark/apply

관리자 북마크을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

관리자 북마크 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/manager/bookmark/cancel

관리자 북마크을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

관리자 북마크 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

