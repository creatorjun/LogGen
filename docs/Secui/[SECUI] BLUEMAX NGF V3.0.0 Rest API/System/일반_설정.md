GET

PUT

PUT

PUT

GET

POST

GET

PUT

GET

PUT

PUT

일반 설정 REST API 문서

https://{url}/api/sm

urlurl: required (string)
장비 IP:Port

일반 설정

인증 프로파일 목록 조회 :  /api/sm/auth/profiles/names/{auth_only}
관리자 인증 전용(auth_only)속성이 ON(1)인 프로파일만 사용한다.
상세 내용은  인증	프로파일  API 문서를 참고한다.

/manager/config

/manager/config/apply

/manager/config/cancel

GUI 서버 인증서

서버 인증서 발급시 웹서버가 재시작됩니다.

/manager/certification

OTP 설정

/manager/otp/config

/manager/otp/types

OTP 설정 적용/취소

/command/otp-config/apply

/command/otp-config/cancel

일반 설정

GET

/manager/config

일반 설정을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

일반 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

gui_idle_use: required (one of 0, 1)
gui_idle_use
유휴 상태 설정 사용 (OFF:	0,	ON:	1)

gui_idle_exit
gui_idle_exit: required (one of 0, 1)
유휴 상태시 GUI 종료 사용 (OFF:	0,	ON:	1)

login_fail_cnt
login_fail_cnt: required (integer - default: 3 - minimum: 3 - maximum: 10)
로그인 최대 실패 횟수

gui_idle_tm
gui_idle_tm: required (integer - default: 30 - minimum: 1 - maximum: 30)
최대 유휴 시간(분)

cli_port
cli_port: required (integer - default: 22 - minimum: 1 - maximum: 65535)
CLI 접속 포트

gui_port
gui_port: required (integer - default: 443 - minimum: 1 - maximum: 65535)
GUI 접속 포트

ext_auth_use
ext_auth_use: required (one of 0, 1)
외부 인증 프로파일 사용 (OFF:	0,	ON:	1)

auth_prof: required (object)
auth_prof

auth_prof_id
auth_prof_id: required (integer)
인증 프로파일 ID

name
name: required (string)
인증 프로파일 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"gui_idle_use":	1,
				"gui_idle_exit":	0,
				"gui_idle_tm":	30,
				"login_fail_cnt":	3,
				"cli_port":	22,
				"gui_port":	443,
				"ext_auth_use":	0,
				"auth_prof":	{
						"auth_prof_id":	1,
						"name":	"인증프로파일"
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/manager/config

일반 설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

gui_idle_use
gui_idle_use: required (one of 0, 1)
유휴 상태 설정 사용 (OFF:	0,	ON:	1)

gui_idle_exit
gui_idle_exit: required (one of 0, 1)
유휴 상태시 GUI 종료 사용 (OFF:	0,	ON:	1)

login_fail_cnt
login_fail_cnt: required (integer - default: 3 - minimum: 3 - maximum: 10)
로그인 최대 실패 횟수

gui_idle_tm
gui_idle_tm: required (integer - default: 30 - minimum: 1 - maximum: 30)
최대 유휴 시간(분)

cli_port
cli_port: required (integer - default: 22 - minimum: 1 - maximum: 65535)
CLI 접속 포트

gui_port
gui_port: required (integer - default: 443 - minimum: 1 - maximum: 65535)
GUI 접속 포트

ext_auth_use
ext_auth_use: required (one of 0, 1)
외부 인증 프로파일 사용 (OFF:	0,	ON:	1)

auth_prof: required (object)
auth_prof

auth_prof_id
auth_prof_id: required (integer)
인증 프로파일 ID

name
name: required (string)
인증 프로파일 이름

Example:
Example

{
		"gui_idle_use":	1,
		"gui_idle_exit":	0,
		"gui_idle_tm":	30,
		"login_fail_cnt":	3,
		"cli_port":	22,
		"gui_port":	443,
		"ext_auth_use":	0,
		"auth_prof":	{
				"auth_prof_id":	1,
				"name":	"인증프로파일"
		}
}

Response

HTTP status code 200

일반 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result

gui_idle_use
gui_idle_use: required (one of 0, 1)
유휴 상태 설정 사용 (OFF:	0,	ON:	1)

gui_idle_exit: required (one of 0, 1)
gui_idle_exit
유휴 상태시 GUI 종료 사용 (OFF:	0,	ON:	1)

login_fail_cnt
login_fail_cnt: required (integer - default: 3 - minimum: 3 - maximum: 10)
로그인 최대 실패 횟수

gui_idle_tm
gui_idle_tm: required (integer - default: 30 - minimum: 1 - maximum: 30)
최대 유휴 시간(분)

cli_port
cli_port: required (integer - default: 22 - minimum: 1 - maximum: 65535)
CLI 접속 포트

gui_port
gui_port: required (integer - default: 443 - minimum: 1 - maximum: 65535)
GUI 접속 포트

ext_auth_use
ext_auth_use: required (one of 0, 1)
외부 인증 프로파일 사용 (OFF:	0,	ON:	1)

auth_prof: required (object)
auth_prof

auth_prof_id
auth_prof_id: required (integer)
인증 프로파일 ID

name
name: required (string)
인증 프로파일 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"gui_idle_use":	1,
				"gui_idle_exit":	0,
				"gui_idle_tm":	30,
				"login_fail_cnt":	3,
				"cli_port":	22,
				"gui_port":	443,
				"ext_auth_use":	0,
				"auth_prof":	{
						"auth_prof_id":	1,
						"name":	"인증프로파일"
				}
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/manager/config/apply

일반 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

change_gui_port
change_gui_port: required (one of 0, 1)
GUI 접속 포트 변경 여부 (NO:	0,	YES:	1)

Example:
Example

{
		"change_gui_port":	0
}

Response

HTTP status code 200

일반 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/manager/config/cancel

일반 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

일반 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

GUI 서버 인증서

GET

/manager/certification

GUI 서버 인증서 조회

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
aignature_alg
aignature_alg: required (string)
서명 알고리즘

issuer
issuer: required (string)
발급자

not_before
not_before: required (string)
유효 기간(시작)

not_after: required (string)
not_after
유효 기간(끝)

subject
subject: required (string)
주제

public_alg
public_alg: required (string)
공개키 알고리즘

Example:
Example

{
		"code":	"ok",
		"result":	{
				"aignature_alg":	"sha256WithRSAEncryption",
				"issuer":	"C=kr,	ST=secui,	L=secui,	O=secui,	OU=secui,	CN=secui/emailAddress=secui@secui.com",
				"not_before":	"Jul	19	07:53:14	2017	GMT",
				"not_after":	"Jul	19	07:53:14	2018	GMT",
				"subject":	"C=kr,	ST=secui,	L=secui,	O=secui,	OU=secui,	CN=secui/emailAddress=secui@secui.com",
				"public_alg":	"rsaEncryption"
		}
}

POST

/manager/certification

GUI 서버 인증서 발급

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

ipip: required (string)
접속 서버 IP 주소(IPv4 Address or IPv6 Address)

daysdays: required (one of 365, 3650)
인증서 발급일수(일)
유효기간 설정에 사용됨

Example:
Example

{
		"ip":	"10.10.10.10",
		"days":	365
}

Response

HTTP status code 200

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
aignature_alg: required (string)
aignature_alg
서명 알고리즘

issuer
issuer: required (string)
발급자

not_before
not_before: required (string)
유효 기간(시작)

not_after
not_after: required (string)
유효 기간(끝)

subject
subject: required (string)
주제

public_alg
public_alg: required (string)
공개키 알고리즘

Example:
Example

{
		"code":	"ok",
		"result":	{
				"aignature_alg":	"sha256WithRSAEncryption",
				"issuer":	"C=kr,	ST=secui,	L=secui,	O=secui,	OU=secui,	CN=secui/emailAddress=secui@secui.com",
				"not_before":	"Jul	19	07:53:14	2017	GMT",
				"not_after":	"Jul	19	07:53:14	2018	GMT",
				"subject":	"C=kr,	ST=secui,	L=secui,	O=secui,	OU=secui,	CN=secui/emailAddress=secui@secui.com",
				"public_alg":	"rsaEncryption"
		}
}

OTP 설정

GET

/manager/otp/config

OTP 설정을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

OTP 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

useuse: required (integer)
OTP 설정 여부
0:	사용	안함,	1:	사용

type: required (integer)
type
OTP 종류
1:	IRUKEY

conf
conf: required (object)
OTP 타입별 설정 정보

11: required (object)
OTP 타입이 1인 (디폴트) 설정

method
method: required (integer)
요청 방식
1:	GET
2:	POST

urlurl: required (string)
URL 주소

params
params: (array of OTP 설정 속성 정보)
속성 목록

Items: OTP 설정 속성 정보
Items

name
name: required (string - minLength: 1 - maxLength: 255)
속성명

영어, 숫자 입력 가능
특수문자 입력 불가능

value
value: required (string - minLength: 1 - maxLength: 255)
속성값
ID
OTP
직접	입력값(영어,	숫자	입력	가능)

id_num_use
id_num_use: required (one of 0, 1 - default: 0)
ID 숫자만 사용여부
0:	사용안함,	1:	사용

Example:
Example

{
		"code":	"ok",
		"result":	{
				"use":	1,
				"type":	1,
				"conf":	{
						"1":	{
								"method":	1,
								"url":	"",
								"params":	[
										{
												"name":	"userId",
												"value":	"ID"
										},
										{
												"name":	"otpNum",
												"value":	"OTP"
										}
								],
								"id_num_use":	1
						}
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/manager/otp/config

OTP 설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (integer)
OTP 설정 여부
0:	사용	안함,	1:	사용

type: required (integer)
type
OTP 종류
1:	IRUKEY

method
method: required (integer)
요청 방식
1:	GET
2:	POST

urlurl: required (string)
URL 주소

params
params: (array of OTP 설정 속성 정보)
속성 목록

Items: OTP 설정 속성 정보
Items

name
name: required (string - minLength: 1 - maxLength: 255)
속성명

영어, 숫자 입력 가능
특수문자 입력 불가능

value
value: required (string - minLength: 1 - maxLength: 255)
속성값
ID
OTP
직접	입력값(영어,	숫자	입력	가능)

id_num_use
id_num_use: required (one of 0, 1 - default: 0)
ID 숫자만 사용여부
0:	사용안함,	1:	사용

Example:
Example

{
		"use":	1,
		"type":	1,
		"method":	2,
		"url":	"https://example.com/otp",
		"params":	[
				{
						"name":	"attr1",
						"value":	"val1"
				}
		],
		"id_num_use":	1
}

Response

HTTP status code 200

OTP 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

useuse: required (integer)
OTP 설정 여부
0:	사용	안함,	1:	사용

type
type: required (integer)
OTP 종류
1:	IRUKEY

conf
conf: required (object)
OTP 타입별 설정 정보

11: required (object)
OTP 타입이 1인 (디폴트) 설정

method
method: required (integer)
요청 방식
1:	GET
2:	POST

urlurl: required (string)
URL 주소

params
params: (array of OTP 설정 속성 정보)
속성 목록

Items: OTP 설정 속성 정보
Items

name
name: required (string - minLength: 1 - maxLength: 255)
속성명

영어, 숫자 입력 가능
특수문자 입력 불가능

value: required (string - minLength: 1 - maxLength: 255)
value
속성값
ID
OTP
직접	입력값(영어,	숫자	입력	가능)

id_num_use: required (one of 0, 1 - default: 0)
id_num_use
ID 숫자만 사용여부
0:	사용안함,	1:	사용

Example:
Example

{
		"code":	"ok",
		"result":	{
				"use":	1,
				"type":	1,
				"conf":	{
						"1":	{
								"method":	1,
								"url":	"",
								"params":	[
										{
												"name":	"userId",
												"value":	"ID"
										},
										{
												"name":	"otpNum",
												"value":	"OTP"
										}
								],
								"id_num_use":	1
						}
				}
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/manager/otp/types

OTP 타입 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

OTP 타입 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of OTP 종류)
Items: OTP 종류
Items

type
type: required (integer)
OTP 타입

name
name: required (string)
OTP 이름

Example:
Example

IRUKEY

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"type":	1,
				"name":	"IRUKEY"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

OTP 설정 적용/취소

PUT

/command/otp-config/apply

OTP 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

OTP 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/otp-config/cancel

OTP 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

OTP 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

