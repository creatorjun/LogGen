GET

GET

PUT

관리 인터페이스 설정 REST API 문서

https://{host}/api/sm

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

관리 인터페이스 설정

/management/interface/enable/services

/management/interface/config

관리 인터페이스 설정

GET

/management/interface/enable/services

설정 가능한 서비스 항목 조회을(를) 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

설정 가능한 서비스 항목 조회 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
admin_service: required (array of 서비스 항목)
admin_service

정책 제어 서비스 목록

Items: 서비스 항목
Items

name: required (string)
name
이름

code
code: required (string)
코드

descdesc: required (string)
설명

network_service
network_service: required (array of 서비스 항목)
네트워크 서비스 목록

Items: 서비스 항목
Items

name
name: required (string)
이름

code
code: required (string)
코드

descdesc: required (string)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"admin_service":	[
						{
								"name":	"snmp",
								"code":	1004,
								"desc":	"SNMP"
						},
						{
								"name":	"nac_agent",
								"code":	1005,
								"desc":	"NAC	Agent"
						}
				],
				"network_service":	[
						{
								"name":	"snmp_trap",
								"code":	2001,
								"desc":	"SNMP	Trap"
						},
						{
								"name":	"syslog",
								"code":	2002,
								"desc":	"Syslog"
						},
						{
								"name":	"tams",
								"code":	2003,
								"desc":	"TAMS"
						},
						{
								"name":	"sw_update",
								"code":	2004,
								"desc":	"SW	UPDATE"
						},
						{
								"name":	"domain_agent",
								"code":	2008,
								"desc":	"Domain	Agent"
						},
						{
								"name":	"cloud_agent",
								"code":	2010,
								"desc":	"Cloud	Agent"
						},
						{
								"name":	"dr",
								"code":	2011,
								"desc":	"DR"
						},
						{
								"name":	"mail_server",
								"code":	2012,
								"desc":	"Mail	Server"
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/management/interface/config

관리 인터페이스 설정을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

관리 인터페이스 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
inf_phy_l3_list: required (array of 인터페이스)
inf_phy_l3_list
관리 인터페이스로 설정 가능한 목록

Items: 인터페이스
Items

idid: required (integer)
인터페이스 ID

name
name: required (string)
인터페이스 이름

ipv4address
ipv4address: required (string)
IPv4 주소

ipv4netmask: required (integer - minimum: 8 - maximum: 32)
ipv4netmask
IPv4 넷마스크

ipv6address
ipv6address: required (string)
IPv6 주소

ipv6prefixlen
ipv6prefixlen: required (integer - minimum: 1 - maximum: 128)
IPv6 넷마스크

inf_phy_mgmt_list
inf_phy_mgmt_list: required (array of 인터페이스)
관리 인터페이스로 설정된 목록

Items: 인터페이스
Items

idid: required (integer)
인터페이스 ID

name
name: required (string)
인터페이스 이름

ipv4address
ipv4address: required (string)
IPv4 주소

ipv4netmask
ipv4netmask: required (integer - minimum: 8 - maximum: 32)
IPv4 넷마스크

ipv6address
ipv6address: required (string)
IPv6 주소

ipv6prefixlen: required (integer - minimum: 1 - maximum: 128)
ipv6prefixlen
IPv6 넷마스크

default_gw_ipv4
default_gw_ipv4: required (string)
IPv4 기본 게이트웨이 정보

default_gw_ipv6: required (string)
default_gw_ipv6
IPv6 기본 게이트웨이 정보

admin_service
admin_service: required (object)
정책 제어 서비스

snmp
snmp: required (one of 0, 1)
SNMP 사용여부
0:	사용안함,	1:	사용

nac_agent
nac_agent: required (one of 0, 1)
NAC Agent 사용여부
0:	사용안함,	1:	사용

network_service: required (object)
network_service
네트워크 서비스

snmp_trap
snmp_trap: required (one of 0, 1)
SNMP Trap 설정 사용 여부
0:	사용안함,	1:	사용

syslog: required (one of 0, 1)
syslog
Syslog 설정 사용 여부
0:	사용안함,	1:	사용

tams
tams: required (one of 0, 1)
TAMS 설정 사용 여부
0:	사용안함,	1:	사용

sw_update
sw_update: required (one of 0, 1)
온라인 업데이트 사용 여부
0:	사용안함,	1:	사용

domain_agent
domain_agent: required (one of 0, 1)
도메인 에이전트 설정 사용 여부
0:	사용안함,	1:	사용

cloud_agent
cloud_agent: required (one of 0, 1)
클라우드 에이전트 설정 사용 여부
0:	사용안함,	1:	사용

drdr: required (one of 0, 1)
DR 설정 사용 여부
0:	사용안함,	1:	사용

mail_server
mail_server: required (one of 0, 1)
메일 서버 설정 사용 여부
0:	사용안함,	1:	사용

contents_update
contents_update: required (one of 0, 1)
컨텐츠 업데이트 사용 여부
0:	사용안함,	1:	사용

device_portal: required (one of 0, 1)
device_portal
연동 디바이스 포털 설정 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

userauth_syslog
userauth_syslog: required (one of 0, 1)
사용자 인증 Syslog 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

userauth_group_agent
userauth_group_agent: required (one of 0, 1)
사용자 인증 그룹 에이전트 간 연동 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

userauth_agent
userauth_agent: required (one of 0, 1)
사용자 인증 에이전트 간 연동 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

Example:
Example

{
		"inf_phy_l3_list":	[
				{
						"id":	3,
						"name":	"eth3",
						"ipv4address":	"192.168.100.20",
						"ipv4netmask":	32,
						"ipv6address":	"",
						"ipv6prefixlen":	null
				}
		],
		"inf_phy_mgmt_list":	[
				{
						"id":	2,
						"name":	"eth2",
						"ipv4address":	"192.168.100.10",
						"ipv4netmask":	32,
						"ipv6address":	"",
						"ipv6prefixlen":	null
				}
		],
		"default_gw_ipv4":	"127.0.0.1",
		"default_gw_ipv6":	"2001:200:3311::1",
		"admin_service":	{
				"snmp":	0,
				"nac_agent":	0
		},
		"network_service":	{
				"snmp_trap":	0,
				"syslog":	0,
				"tams":	0,
				"sw_update":	0,
				"cloud_agent":	0,
				"dr":	0,
				"mail_server":	0,
				"contents_update":	0,
				"device_portal":	0,
				"userauth_syslog":	0,
				"userauth_group_agent":	0,
				"userauth_agent":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/management/interface/config

관리 인터페이스 설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

inf_phy_mgmt_list: required (array of 인터페이스)
inf_phy_mgmt_list
관리 인터페이스로 설정된 목록

Items: 인터페이스
Items

idid: required (integer)
인터페이스 ID

name
name: required (string)
인터페이스 이름

ipv4address
ipv4address: required (string)
IPv4 주소

ipv4netmask
ipv4netmask: required (integer - minimum: 8 - maximum: 32)
IPv4 넷마스크

ipv6address
ipv6address: required (string)
IPv6 주소

ipv6prefixlen
ipv6prefixlen: required (integer - minimum: 1 - maximum: 128)
IPv6 넷마스크

default_gw_ipv4
default_gw_ipv4: required (string)
IPv4 기본 게이트웨이 정보

default_gw_ipv6
default_gw_ipv6: required (string)
IPv6 기본 게이트웨이 정보

admin_service
admin_service: required (object)
정책 제어 서비스

snmp
snmp: required (one of 0, 1)
SNMP 사용여부
0:	사용안함,	1:	사용

nac_agent
nac_agent: required (one of 0, 1)
NAC Agent 사용여부
0:	사용안함,	1:	사용

network_service
network_service: required (object)
네트워크 서비스

snmp_trap
snmp_trap: required (one of 0, 1)
SNMP Trap 설정 사용 여부
0:	사용안함,	1:	사용

syslog
syslog: required (one of 0, 1)
Syslog 설정 사용 여부
0:	사용안함,	1:	사용

tams
tams: required (one of 0, 1)
TAMS 설정 사용 여부
0:	사용안함,	1:	사용

sw_update: required (one of 0, 1)
sw_update
온라인 업데이트 사용 여부
0:	사용안함,	1:	사용

domain_agent
domain_agent: required (one of 0, 1)
도메인 에이전트 설정 사용 여부
0:	사용안함,	1:	사용

cloud_agent
cloud_agent: required (one of 0, 1)
클라우드 에이전트 설정 사용 여부
0:	사용안함,	1:	사용

drdr: required (one of 0, 1)
DR 설정 사용 여부
0:	사용안함,	1:	사용

mail_server
mail_server: required (one of 0, 1)
메일 서버 설정 사용 여부
0:	사용안함,	1:	사용

contents_update: required (one of 0, 1)
contents_update
컨텐츠 업데이트 사용 여부
0:	사용안함,	1:	사용

device_portal
device_portal: required (one of 0, 1)
연동 디바이스 포털 설정 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

userauth_syslog
userauth_syslog: required (one of 0, 1)
사용자 인증 Syslog 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

userauth_group_agent
userauth_group_agent: required (one of 0, 1)
사용자 인증 그룹 에이전트 간 연동 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

userauth_agent: required (one of 0, 1)
userauth_agent
사용자 인증 에이전트 간 연동 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

Example:
Example

{
		"inf_phy_mgmt_list":	[
				{
						"id":	2,
						"name":	"eth2",
						"ipv4address":	"192.168.100.10",
						"ipv4netmask":	32,
						"ipv6address":	"",
						"ipv6prefixlen":	null
				}
		],
		"default_gw_ipv4":	"127.0.0.1",
		"default_gw_ipv6":	"2001:200:3311::1",
		"admin_service":	{
				"ssh":	0,
				"https":	0,
				"icmp":	0,
				"snmp":	0,
				"nac_agent":	0
		},
		"network_service":	{
				"snmp_trap":	0,
				"syslog":	0,
				"tams":	0,
				"sw_update":	0,
				"cloud_agent":	0,
				"dr":	0,
				"mail_server":	0,
				"contents_update:	0,
				"device_portal":	0,
				"userauth_syslog":	0,
				"userauth_group_agent":	0,
				"userauth_agent":	0
		}
}

Response

HTTP status code 200

관리 인터페이스 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
inf_phy_l3_list: required (array of 인터페이스)
inf_phy_l3_list
관리 인터페이스로 설정 가능한 목록

Items: 인터페이스
Items

idid: required (integer)
인터페이스 ID

name: required (string)
name
인터페이스 이름

ipv4address
ipv4address: required (string)
IPv4 주소

ipv4netmask
ipv4netmask: required (integer - minimum: 8 - maximum: 32)
IPv4 넷마스크

ipv6address
ipv6address: required (string)
IPv6 주소

ipv6prefixlen
ipv6prefixlen: required (integer - minimum: 1 - maximum: 128)
IPv6 넷마스크

inf_phy_mgmt_list
inf_phy_mgmt_list: required (array of 인터페이스)
관리 인터페이스로 설정된 목록

Items: 인터페이스
Items

idid: required (integer)
인터페이스 ID

name
name: required (string)
인터페이스 이름

ipv4address
ipv4address: required (string)
IPv4 주소

ipv4netmask
ipv4netmask: required (integer - minimum: 8 - maximum: 32)
IPv4 넷마스크

ipv6address
ipv6address: required (string)
IPv6 주소

ipv6prefixlen: required (integer - minimum: 1 - maximum: 128)
ipv6prefixlen
IPv6 넷마스크

default_gw_ipv4
default_gw_ipv4: required (string)
IPv4 기본 게이트웨이 정보

default_gw_ipv6: required (string)
default_gw_ipv6

IPv6 기본 게이트웨이 정보

admin_service
admin_service: required (object)
정책 제어 서비스

snmp: required (one of 0, 1)
snmp
SNMP 사용여부
0:	사용안함,	1:	사용

nac_agent
nac_agent: required (one of 0, 1)
NAC Agent 사용여부
0:	사용안함,	1:	사용

network_service
network_service: required (object)
네트워크 서비스

snmp_trap
snmp_trap: required (one of 0, 1)
SNMP Trap 설정 사용 여부
0:	사용안함,	1:	사용

syslog
syslog: required (one of 0, 1)
Syslog 설정 사용 여부
0:	사용안함,	1:	사용

tams
tams: required (one of 0, 1)
TAMS 설정 사용 여부
0:	사용안함,	1:	사용

sw_update
sw_update: required (one of 0, 1)
온라인 업데이트 사용 여부
0:	사용안함,	1:	사용

domain_agent: required (one of 0, 1)
domain_agent
도메인 에이전트 설정 사용 여부
0:	사용안함,	1:	사용

cloud_agent
cloud_agent: required (one of 0, 1)
클라우드 에이전트 설정 사용 여부
0:	사용안함,	1:	사용

drdr: required (one of 0, 1)
DR 설정 사용 여부
0:	사용안함,	1:	사용

mail_server
mail_server: required (one of 0, 1)
메일 서버 설정 사용 여부
0:	사용안함,	1:	사용

contents_update
contents_update: required (one of 0, 1)
컨텐츠 업데이트 사용 여부
0:	사용안함,	1:	사용

device_portal: required (one of 0, 1)
device_portal
연동 디바이스 포털 설정 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

userauth_syslog
userauth_syslog: required (one of 0, 1)
사용자 인증 Syslog 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

userauth_group_agent
userauth_group_agent: required (one of 0, 1)
사용자 인증 그룹 에이전트 간 연동 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

userauth_agent
userauth_agent: required (one of 0, 1)
사용자 인증 에이전트 간 연동 사용 여부
POWER 모드에서 지원
0:	사용안함,	1:	사용

Example:
Example

{
		"inf_phy_l3_list":	[
				{
						"id":	3,
						"name":	"eth3",
						"ipv4address":	"192.168.100.20",
						"ipv4netmask":	32,
						"ipv6address":	"",
						"ipv6prefixlen":	null
				}
		],
		"inf_phy_mgmt_list":	[
				{
						"id":	2,
						"name":	"eth2",
						"ipv4address":	"192.168.100.10",
						"ipv4netmask":	32,
						"ipv6address":	"",
						"ipv6prefixlen":	null
				}
		],
		"default_gw_ipv4":	"127.0.0.1",
		"default_gw_ipv6":	"2001:200:3311::1",
		"admin_service":	{
				"ssh":	0,
				"https":	0,
				"icmp":	0,
				"snmp":	0,
				"nac_agent":	0
		},
		"network_service":	{
				"snmp_trap":	0,
				"syslog":	0,
				"tams":	0,
				"sw_update":	0,
				"cloud_agent":	0,
				"dr":	0,
				"mail_server":	0,
				"contents_update":	0,
				"device_portal":	0,
				"userauth_syslog":	0,
				"userauth_group_agent":	0,
				"userauth_agent":	0
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

