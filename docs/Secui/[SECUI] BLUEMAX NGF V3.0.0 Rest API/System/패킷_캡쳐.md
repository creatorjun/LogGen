GET

GET

POST

  DELETE

PUT

  DELETE

GET

GET

GET

GET

PUT

PUT

GET

PUT

PUT

패킷 캡쳐 REST API 문서

https://{url}/api/sm

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/sm

패킷 캡쳐

/packet/captures

/packet/captures/{pk}

/packet/captures/interface-candidates

/packet/captures/capacity

/packet/captures/status

/packet/captures/{pk}/status

/packet/captures/{pk}/start

/packet/captures/{pk}/stop

/packet/captures/{pk}/save

패킷 캡쳐 적용/취소

/command/packet-captures/apply

/command/packet-captures/cancel

패킷 캡쳐

GET

/packet/captures

패킷 캡쳐 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

패킷 캡쳐 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ResPacketCapture)
결과 데이터

Items: ResPacketCapture
Items

ifc_id
ifc_id: required (string)
인터페이스 이름

direction
direction: required (one of 0, 1, 2 - default: 0)
패킷 수집 방향
정방향:	0,	역방향:	1,	양방향:	2

vlan_use
vlan_use: required (one of 0, 1 - default: 0)
VLAN 패킷 수집 사용 유무
사용안함:	0,	사용:	1

vlan_id
vlan_id: required (integer - default: 4096 - minimum: 0 - maximum: 4095)
VLAN 아이디
ANY:	4096

ip_ver
ip_ver: required (one of 1, 2)
아이피 버전
IPv4:	1,	IPv6:	2

dump_src_ip
dump_src_ip: required (string)
수집 출발지 아이피

dump_src_net
dump_src_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 출발지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_src_port
dump_src_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 출발지 포트
ANY:	0

dump_dst_ip
dump_dst_ip: required (string)
수집 목적지 아이피

dump_dst_net
dump_dst_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 목적지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_dst_port
dump_dst_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 목적지 포트
ANY:	0

proto
proto: required (integer - default: 255 - minimum: 0 - maximum: 255)
프로토콜
ANY:	255

pkt_cnt
pkt_cnt: required (integer - default: 100 - minimum: 10 - maximum: 100000)
패킷 수

dump_size
dump_size: required (integer - default: 1 - minimum: 1 - maximum: 100)
수집 크기

dump_tm
dump_tm: required (integer - default: 0 - minimum: 0 - maximum: 3600)
수집 시간
무제한:	0

idid: required (integer)
패킷 수집 아이디

dump_id
dump_id: required (integer)
패킷 수집 상태 아이디

status
status: required (one of 0, 1)
패킷 수집 상태 정보
패킷	수집하지	않는	상태:	0,	패킷	수집하는	상태:	1

filefile: required (one of 0, 1)
패킷 수집된 파일의 존재 여부
파일	없음:	0,	파일	있음:	1

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"id":	1,
				"ifc_id":	"eth0",
				"direction":	0,
				"vlan_use":	0,
				"vlan_id":	4096,
				"ip_ver":	1,
				"dump_src_ip":	"100.0.0.1",
				"dump_src_net":	24,
				"dump_src_port":	1,
				"dump_dst_ip":	"200.0.0.1",
				"dump_dst_net":	24,
				"dump_dst_port":	1,
				"proto":	255,
				"pkt_cnt":	100,
				"dump_size":	1,
				"dump_tm":	0,
				"dump_id":	1,
				"status":	0,
				"file":	0
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/packet/captures

패킷 캡쳐를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
ifc_id
ifc_id: required (string)
인터페이스 이름

direction
direction: required (one of 0, 1, 2 - default: 0)
패킷 수집 방향
정방향:	0,	역방향:	1,	양방향:	2

vlan_use
vlan_use: required (one of 0, 1 - default: 0)
VLAN 패킷 수집 사용 유무
사용안함:	0,	사용:	1

vlan_id: required (integer - default: 4096 - minimum: 0 - maximum: 4095)
vlan_id
VLAN 아이디
ANY:	4096

ip_ver
ip_ver: required (one of 1, 2)
아이피 버전
IPv4:	1,	IPv6:	2

dump_src_ip
dump_src_ip: required (string)
수집 출발지 아이피

dump_src_net
dump_src_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 출발지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_src_port
dump_src_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 출발지 포트
ANY:	0

dump_dst_ip
dump_dst_ip: required (string)
수집 목적지 아이피

dump_dst_net
dump_dst_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 목적지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_dst_port
dump_dst_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 목적지 포트
ANY:	0

proto
proto: required (integer - default: 255 - minimum: 0 - maximum: 255)
프로토콜
ANY:	255

pkt_cnt: required (integer - default: 100 - minimum: 10 - maximum: 100000)
pkt_cnt
패킷 수

dump_size
dump_size: required (integer - default: 1 - minimum: 1 - maximum: 100)
수집 크기

dump_tm
dump_tm: required (integer - default: 0 - minimum: 0 - maximum: 3600)
수집 시간
무제한:	0

Example:
Example

{
		"ifc_id":	"eth0",
		"direction":	0,
		"vlan_use":	0,
		"vlan_id":	4096,
		"ip_ver":	1,
		"dump_src_ip":	"100.0.0.1",
		"dump_src_net":	24,
		"dump_src_port":	1,
		"dump_dst_ip":	"200.0.0.1",
		"dump_dst_net":	24,
		"dump_dst_port":	1,
		"proto":	255,
		"pkt_cnt":	100,
		"dump_size":	1,
		"dump_tm":	0
}

Response

HTTP status code 200

패킷 캡쳐 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

ifc_id
ifc_id: required (string)
인터페이스 이름

direction
direction: required (one of 0, 1, 2 - default: 0)
패킷 수집 방향
정방향:	0,	역방향:	1,	양방향:	2

vlan_use
vlan_use: required (one of 0, 1 - default: 0)
VLAN 패킷 수집 사용 유무
사용안함:	0,	사용:	1

vlan_id
vlan_id: required (integer - default: 4096 - minimum: 0 - maximum: 4095)
VLAN 아이디
ANY:	4096

ip_ver
ip_ver: required (one of 1, 2)
아이피 버전
IPv4:	1,	IPv6:	2

dump_src_ip
dump_src_ip: required (string)
수집 출발지 아이피

dump_src_net
dump_src_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 출발지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_src_port
dump_src_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 출발지 포트
ANY:	0

dump_dst_ip: required (string)
dump_dst_ip
수집 목적지 아이피

dump_dst_net
dump_dst_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 목적지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_dst_port
dump_dst_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 목적지 포트
ANY:	0

proto
proto: required (integer - default: 255 - minimum: 0 - maximum: 255)
프로토콜
ANY:	255

pkt_cnt
pkt_cnt: required (integer - default: 100 - minimum: 10 - maximum: 100000)
패킷 수

dump_size
dump_size: required (integer - default: 1 - minimum: 1 - maximum: 100)
수집 크기

dump_tm
dump_tm: required (integer - default: 0 - minimum: 0 - maximum: 3600)
수집 시간
무제한:	0

idid: required (integer)
패킷 수집 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"id":	1,
				"ifc_id":	"eth0",
				"direction":	0,
				"vlan_use":	0,
				"vlan_id":	4096,
				"ip_ver":	1,
				"dump_src_ip":	"100.0.0.1",
				"dump_src_net":	24,
				"dump_src_port":	1,
				"dump_dst_ip":	"200.0.0.1",
				"dump_dst_net":	24,
				"dump_dst_port":	1,
				"proto":	255,
				"pkt_cnt":	100,
				"dump_size":	1,
				"dump_tm":	0
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

DELETE

/packet/captures

패킷 캡쳐 목록을 (일괄)삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of object)
삭제 아이템 목록

Example:
Example

{
		"items":	[
				{"id":	1},
				{"id":	2}
		]
}

Response

HTTP status code 200

패킷 캡쳐 목록 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
삭제 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/packet/captures/{pk}

패킷 캡쳐을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
패킷 수집 아이디 ( id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

패킷 캡쳐 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

ifc_id: required (string)
ifc_id
인터페이스 이름

direction
direction: required (one of 0, 1, 2 - default: 0)
패킷 수집 방향
정방향:	0,	역방향:	1,	양방향:	2

vlan_use: required (one of 0, 1 - default: 0)
vlan_use
VLAN 패킷 수집 사용 유무
사용안함:	0,	사용:	1

vlan_id
vlan_id: required (integer - default: 4096 - minimum: 0 - maximum: 4095)
VLAN 아이디
ANY:	4096

ip_ver
ip_ver: required (one of 1, 2)
아이피 버전
IPv4:	1,	IPv6:	2

dump_src_ip
dump_src_ip: required (string)
수집 출발지 아이피

dump_src_net
dump_src_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 출발지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_src_port
dump_src_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 출발지 포트
ANY:	0

dump_dst_ip
dump_dst_ip: required (string)
수집 목적지 아이피

dump_dst_net
dump_dst_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 목적지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_dst_port
dump_dst_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 목적지 포트
ANY:	0

proto: required (integer - default: 255 - minimum: 0 - maximum: 255)
proto
프로토콜
ANY:	255

pkt_cnt
pkt_cnt: required (integer - default: 100 - minimum: 10 - maximum: 100000)
패킷 수

dump_size: required (integer - default: 1 - minimum: 1 - maximum: 100)
dump_size
수집 크기

dump_tm
dump_tm: required (integer - default: 0 - minimum: 0 - maximum: 3600)
수집 시간
무제한:	0

idid: required (integer)

패킷 수집 아이디

dump_id
dump_id: required (integer)
패킷 수집 상태 아이디

status: required (one of 0, 1)
status
패킷 수집 상태 정보
패킷	수집하지	않는	상태:	0,	패킷	수집하는	상태:	1

filefile: required (one of 0, 1)
패킷 수집된 파일의 존재 여부
파일	없음:	0,	파일	있음:	1

Example:
Example

{
		"code":	"ok",
		"result":	{
				"id":	1,
				"ifc_id":	"eth0",
				"direction":	0,
				"vlan_use":	0,
				"vlan_id":	4096,
				"ip_ver":	1,
				"dump_src_ip":	"100.0.0.1",
				"dump_src_net":	24,
				"dump_src_port":	1,
				"dump_dst_ip":	"200.0.0.1",
				"dump_dst_net":	24,
				"dump_dst_port":	1,
				"proto":	255,
				"pkt_cnt":	100,
				"dump_size":	1,
				"dump_tm":	0,
				"dump_id":	1,
				"status":	0,
				"file":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/packet/captures/{pk}

패킷 캡쳐을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
패킷 수집 아이디 ( id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
ifc_id: required (string)
ifc_id
인터페이스 이름

direction
direction: required (one of 0, 1, 2 - default: 0)
패킷 수집 방향
정방향:	0,	역방향:	1,	양방향:	2

vlan_use: required (one of 0, 1 - default: 0)
vlan_use
VLAN 패킷 수집 사용 유무
사용안함:	0,	사용:	1

vlan_id
vlan_id: required (integer - default: 4096 - minimum: 0 - maximum: 4095)
VLAN 아이디
ANY:	4096

ip_ver
ip_ver: required (one of 1, 2)
아이피 버전
IPv4:	1,	IPv6:	2

dump_src_ip
dump_src_ip: required (string)
수집 출발지 아이피

dump_src_net
dump_src_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 출발지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_src_port
dump_src_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 출발지 포트
ANY:	0

dump_dst_ip
dump_dst_ip: required (string)
수집 목적지 아이피

dump_dst_net
dump_dst_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 목적지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_dst_port
dump_dst_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 목적지 포트
ANY:	0

proto: required (integer - default: 255 - minimum: 0 - maximum: 255)
proto
프로토콜
ANY:	255

pkt_cnt
pkt_cnt: required (integer - default: 100 - minimum: 10 - maximum: 100000)
패킷 수

dump_size: required (integer - default: 1 - minimum: 1 - maximum: 100)
dump_size

수집 크기

dump_tm
dump_tm: required (integer - default: 0 - minimum: 0 - maximum: 3600)
수집 시간
무제한:	0

Example:
Example

{
		"ifc_id":	"eth1",
		"direction":	2,
		"vlan_use":	1,
		"vlan_id":	1,
		"ip_ver":	1,
		"dump_src_ip":	"100.0.0.2",
		"dump_src_net":	24,
		"dump_src_port":	65535,
		"dump_dst_ip":	"200.0.0.2",
		"dump_dst_net":	24,
		"dump_dst_port":	65535,
		"proto":	0,
		"pkt_cnt":	100000,
		"dump_size":	100,
		"dump_tm":	3600
}

Response

HTTP status code 200

패킷 캡쳐 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

ifc_id
ifc_id: required (string)
인터페이스 이름

direction
direction: required (one of 0, 1, 2 - default: 0)
패킷 수집 방향
정방향:	0,	역방향:	1,	양방향:	2

vlan_use
vlan_use: required (one of 0, 1 - default: 0)
VLAN 패킷 수집 사용 유무
사용안함:	0,	사용:	1

vlan_id
vlan_id: required (integer - default: 4096 - minimum: 0 - maximum: 4095)
VLAN 아이디
ANY:	4096

ip_ver
ip_ver: required (one of 1, 2)
아이피 버전
IPv4:	1,	IPv6:	2

dump_src_ip: required (string)
dump_src_ip
수집 출발지 아이피

dump_src_net
dump_src_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
수집 출발지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_src_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
dump_src_port
수집 출발지 포트
ANY:	0

dump_dst_ip
dump_dst_ip: required (string)
수집 목적지 아이피

dump_dst_net: required (integer - default: 24 - minimum: 0 - maximum: 128)
dump_dst_net
수집 목적지 넷마스크
IPv4:	0~32,	IPv6:	0~128

dump_dst_port
dump_dst_port: required (integer - default: 1 - minimum: 0 - maximum: 65535)
수집 목적지 포트
ANY:	0

proto
proto: required (integer - default: 255 - minimum: 0 - maximum: 255)
프로토콜
ANY:	255

pkt_cnt
pkt_cnt: required (integer - default: 100 - minimum: 10 - maximum: 100000)
패킷 수

dump_size
dump_size: required (integer - default: 1 - minimum: 1 - maximum: 100)
수집 크기

dump_tm
dump_tm: required (integer - default: 0 - minimum: 0 - maximum: 3600)
수집 시간
무제한:	0

idid: required (integer)
패킷 수집 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"id":	1,
				"ifc_id":	"eth1",
				"direction":	2,
				"vlan_use":	1,
				"vlan_id":	1,
				"ip_ver":	1,
				"dump_src_ip":	"100.0.0.2",
				"dump_src_net":	24,
				"dump_src_port":	65535,
				"dump_dst_ip":	"200.0.0.2",
				"dump_dst_net":	24,
				"dump_dst_port":	65535,
				"proto":	0,
				"pkt_cnt":	100000,
				"dump_size":	100,
				"dump_tm":	3600
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/packet/captures/{pk}

패킷 캡쳐을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
패킷 수집 아이디 ( id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

패킷 캡쳐 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/packet/captures/interface-candidates

인터페이스 후보 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

no_off
no_off: (one of 0, 1)
인터페이스 목록에서 사용 상태가 'OFF'인 인터페이스는 제외( OFF:	0,	ON:	1 )

SupportedVersion:	2.0.0

no_on
no_on: (one of 0, 1)
인터페이스 목록에서 사용 상태가 'ON'인 인터페이스는 제외( OFF:	0,	ON:	1 )

SupportedVersion:	2.0.0

Response

HTTP status code 200

인터페이스 후보 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of Interface)
결과 데이터

Items: Interface
Items

idid: required (string)
인터페이스 ID

name
name: required (string)
인터페이스 이름

usage
usage: (one of 1, 2, 3)
용도 (호스트:	1,	HA:	2,	VS:	3)
(MGMT	호스트에서	사용되는	속성)

zone
zone: required (one of 0, 1, 2, 3, 4)
Zone (None:	0,	Internal:	1,	External:	2,	DMZ:	3,	HA:	4)

hwaddr
hwaddr: required (string)
하드웨어 주소

appeared: (one of 0, 1)
appeared
인터페이스 구분 (시스템에	없는	인터페이스:	0,	시스템에	있는	인터페이스:	1)
(MGMT	호스트에서	사용되는	속성)

modemode: required (object)
모드 정보

status: required (object)
status
인터페이스 상태

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"id":	1,
						"name":	"eth0",
						"hwaddr":	"00:0c:29:b2:ac:ec",
						"zone":	"1",
						"mode":	{},
						"status":	{},
						"children":	[]
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/packet/captures/capacity

패킷 캡쳐 용량 정보을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

패킷 캡쳐 용량 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

pkt_dump_min
pkt_dump_min: required (integer)
패킷 수집 덤프 파일 크기 최솟값

pkt_dump_max
pkt_dump_max: required (integer)
패킷 수집 덤프 파일 크기 최댓값

pkt_dump_default
pkt_dump_default: required (integer)
패킷 수집 덤프 파일 크기 기본값

Example:
Example

{
		"code":	"ok",
		"result":	{
				"pkt_dump_min":	1,
				"pkt_dump_max":	100,
				"pkt_dump_default":	1
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/packet/captures/status

패킷 캡쳐 상태 목록을 조회한다.

Request

URI Parameters

pkpk: required (integer)
패킷 수집 아이디 ( dump_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

패킷 캡쳐 상태 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (array of ResPacketCaptureStatus)
result
결과 데이터

Items: ResPacketCaptureStatus
Items

status
status: required (one of 0, 1)
패킷 수집 상태 정보
패킷	수집하지	않는	상태:	0,	패킷	수집하는	상태:	1

filefile: required (one of 0, 1)
패킷 수집된 파일의 존재 여부
파일	없음:	0,	파일	있음:	1

dump_id
dump_id: required (integer)
패킷 수집 아이디

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"dump_id":	1,
				"status":	0,
				"file":	0
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/packet/captures/{pk}/status

패킷 캡쳐 상태을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
패킷 수집 아이디 ( dump_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

패킷 캡쳐 상태 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

status: required (one of 0, 1)
status
패킷 수집 상태 정보
패킷	수집하지	않는	상태:	0,	패킷	수집하는	상태:	1

filefile: required (one of 0, 1)
패킷 수집된 파일의 존재 여부
파일	없음:	0,	파일	있음:	1

dump_id
dump_id: required (integer)
패킷 수집 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"dump_id":	1,
				"status":	0,
				"file":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/packet/captures/{pk}/start

패킷 캡쳐 동작 모드을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
패킷 수집 아이디 ( dump_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

패킷 캡쳐 동작 모드 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

status
status: required (one of 0, 1)
패킷 수집 상태 정보
패킷	수집하지	않는	상태:	0,	패킷	수집하는	상태:	1

filefile: required (one of 0, 1)
패킷 수집된 파일의 존재 여부
파일	없음:	0,	파일	있음:	1

dump_id
dump_id: required (integer)
패킷 수집 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"dump_id":	1,
				"status":	1,
				"file":	1
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/packet/captures/{pk}/stop

패킷 캡쳐 동작 모드을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
패킷 수집 아이디 ( dump_id )

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

패킷 캡쳐 동작 모드 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

status
status: required (one of 0, 1)
패킷 수집 상태 정보
패킷	수집하지	않는	상태:	0,	패킷	수집하는	상태:	1

filefile: required (one of 0, 1)
패킷 수집된 파일의 존재 여부
파일	없음:	0,	파일	있음:	1

dump_id
dump_id: required (integer)
패킷 수집 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"dump_id":	1,
				"status":	1,
				"file":	1
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/packet/captures/{pk}/save

패킷 캡쳐 저장을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
패킷 수집 아이디 ( dump_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

패킷 캡쳐 파일을 PC로 저장 성공

Body

Media type: application/octet-stream
Media type

TypeType: file

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

패킷 캡쳐 적용/취소

PUT

/command/packet-captures/apply

패킷 캡쳐을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

패킷 캡쳐 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/packet-captures/cancel

패킷 캡쳐을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

패킷 캡쳐 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

