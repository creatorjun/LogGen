GET

PUT

GET

POST

GET

PUT

  DELETE

POST

GET

DELETE

DELETE

PUT

PUT

리포트 설정 REST API 문서

https://{url}/api/lr

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/lr

리포트 설정

/report/config

리포트 자동 전송 서버

/report/servers

/report/servers/{pk}

즉석 리포트

/report/start

/report/{request_id}/status

/report/{request_id}/stop

/report/{request_id}/end

리포트 설정 적용/취소

/command/report-config/apply

/command/report-config/cancel

리포트 설정

GET

/report/config

리포트 설정을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

리포트 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

regular_hour
regular_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
정규리포트 생성 시간

regular_min
regular_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
정규리포트 생성 분

d_use
d_use: required (one of 0, 1 - default: 0)
일간 사용여부
사용안함:	0,	사용:	1

w_use
w_use: required (one of 0, 1 - default: 0)
주간 사용여부
사용안함:	0,	사용:	1

w_week
w_week: required (one of 1, 2, 3, 4, 5, 6, 7 - default: 1)
주간 요일
월요일:	1,	화요일:	2,	수요일:	3,	목요일:	4,	금요일:	5,	토요일:	6,	일요일:	7

m_usem_use: required (one of 0, 1 - default: 0)
월간 사용여부
사용안함:	0,	사용:	1

m_daym_day: required (integer - default: 1 - minimum: 1 - maximum: 31)
월간 일

r_use: required (one of 0, 1 - default: 0)
r_use
예약 사용여부 (0:미사용, 1:사용)
사용안함:	0,	사용:	1

r_start_dt
r_start_dt: required (string)
예약 시작 날짜

r_end_dt: required (string)
r_end_dt
예약 종료 날짜

r_dt
r_dt: required (string)
예약 날짜

auto_snd_hour
auto_snd_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
자동 전송 시간

auto_snd_min
auto_snd_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
자동 전송 분

d_report_prf_id
d_report_prf_id: required (integer)
일간 리포트 프로파일 아이디

w_report_prf_id: required (integer)
w_report_prf_id
주간 리포트 프로파일 아이디

m_report_prf_id
m_report_prf_id: required (integer)
월간 리포트 프로파일 아이디

r_report_prf_id
r_report_prf_id: required (integer)
예약 리포트 프로파일 아이디

i_report_prf_id
i_report_prf_id: required (integer)
즉석 리포트 프로파일 아이디

save_dt
save_dt: required (integer - default: 2 - minimum: 2 - maximum: 24)
저장 날짜

lang
lang: required (one of 1, 2, 3 - default: 1)
언어  한국어:	1,	영어:	2,	일본어:	3

d_report_prf_name
d_report_prf_name: required (string)
일간 리포트 프로파일 이름

w_report_prf_name: required (string)
w_report_prf_name
주간 리포트 프로파일 이름

m_report_prf_name
m_report_prf_name: required (string)
월간 리포트 프로파일 이름

r_report_prf_name
r_report_prf_name: required (string)
예약 리포트 프로파일 이름

i_report_prf_name
i_report_prf_name: required (string)
즉석 리포트 프로파일 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"d_report_prf_id":	1,
				"d_report_prf_name":	"default",
				"w_report_prf_id":	1,
				"w_report_prf_name":	"default",
				"m_report_prf_id":	1,
				"m_report_prf_name":	"default",
				"r_report_prf_id":	1,
				"r_report_prf_name":	"default",
				"i_report_prf_id":	1,
				"i_report_prf_name":	"default",
				"save_dt":	2,
				"lang":	1,
				"regular_hour":	0,
				"regular_min":	0,
				"d_use":	0,
				"w_use":	0,
				"w_week":	1,
				"m_use":	0,
				"m_day":	1,
				"r_use":	0,
				"r_start_dt":	"2018-01-01	00:00:00",
				"r_end_dt":	"2018-01-01	01:00:00",
				"r_dt":	"2018-01-01	00:00:00",
				"auto_snd_hour":	0,
				"auto_snd_min":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/report/config

리포트 설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

regular_hour
regular_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
정규리포트 생성 시간

regular_min
regular_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
정규리포트 생성 분

d_use: required (one of 0, 1 - default: 0)
d_use
일간 사용여부
사용안함:	0,	사용:	1

w_use
w_use: required (one of 0, 1 - default: 0)
주간 사용여부
사용안함:	0,	사용:	1

w_week
w_week: required (one of 1, 2, 3, 4, 5, 6, 7 - default: 1)
주간 요일
월요일:	1,	화요일:	2,	수요일:	3,	목요일:	4,	금요일:	5,	토요일:	6,	일요일:	7

m_usem_use: required (one of 0, 1 - default: 0)
월간 사용여부
사용안함:	0,	사용:	1

m_daym_day: required (integer - default: 1 - minimum: 1 - maximum: 31)
월간 일

r_use
r_use: required (one of 0, 1 - default: 0)
예약 사용여부 (0:미사용, 1:사용)
사용안함:	0,	사용:	1

r_start_dt
r_start_dt: required (string)
예약 시작 날짜

r_end_dt
r_end_dt: required (string)
예약 종료 날짜

r_dt
r_dt: required (string)
예약 날짜

auto_snd_hour
auto_snd_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
자동 전송 시간

auto_snd_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
auto_snd_min
자동 전송 분

d_report_prf_id
d_report_prf_id: required (integer)
일간 리포트 프로파일 아이디

w_report_prf_id: required (integer)
w_report_prf_id
주간 리포트 프로파일 아이디

m_report_prf_id
m_report_prf_id: required (integer)
월간 리포트 프로파일 아이디

r_report_prf_id
r_report_prf_id: required (integer)
예약 리포트 프로파일 아이디

i_report_prf_id
i_report_prf_id: required (integer)
즉석 리포트 프로파일 아이디

save_dt
save_dt: required (integer - default: 2 - minimum: 2 - maximum: 24)
저장 날짜

lang: required (one of 1, 2, 3 - default: 1)
lang

언어  한국어:	1,	영어:	2,	일본어:	3

Example:
Example

{
		"d_report_prf_id":	1,
		"w_report_prf_id":	1,
		"m_report_prf_id":	1,
		"r_report_prf_id":	1,
		"i_report_prf_id":	1,
		"save_dt":	24,
		"lang":	2,
		"regular_hour":	23,
		"regular_min":	59,
		"d_use":	1,
		"w_use":	1,
		"w_week":	7,
		"m_use":	1,
		"m_day":	31,
		"r_use":	1,
		"r_start_dt":	"2018-08-31	09:00:00",
		"r_end_dt":	"2018-08-31	14:00:00",
		"r_dt":	"2018-08-31	23:00:00",
		"auto_snd_hour":	23,
		"auto_snd_min":	59
}

Response

HTTP status code 200

리포트 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

regular_hour
regular_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
정규리포트 생성 시간

regular_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
regular_min
정규리포트 생성 분

d_use
d_use: required (one of 0, 1 - default: 0)
일간 사용여부
사용안함:	0,	사용:	1

w_use: required (one of 0, 1 - default: 0)
w_use
주간 사용여부
사용안함:	0,	사용:	1

w_week
w_week: required (one of 1, 2, 3, 4, 5, 6, 7 - default: 1)
주간 요일
월요일:	1,	화요일:	2,	수요일:	3,	목요일:	4,	금요일:	5,	토요일:	6,	일요일:	7

m_usem_use: required (one of 0, 1 - default: 0)
월간 사용여부
사용안함:	0,	사용:	1

m_daym_day: required (integer - default: 1 - minimum: 1 - maximum: 31)
월간 일

r_use
r_use: required (one of 0, 1 - default: 0)
예약 사용여부 (0:미사용, 1:사용)
사용안함:	0,	사용:	1

r_start_dt
r_start_dt: required (string)
예약 시작 날짜

r_end_dt
r_end_dt: required (string)
예약 종료 날짜

r_dt
r_dt: required (string)
예약 날짜

auto_snd_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
auto_snd_hour
자동 전송 시간

auto_snd_min
auto_snd_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
자동 전송 분

d_report_prf_id
d_report_prf_id: required (integer)
일간 리포트 프로파일 아이디

w_report_prf_id
w_report_prf_id: required (integer)
주간 리포트 프로파일 아이디

m_report_prf_id
m_report_prf_id: required (integer)
월간 리포트 프로파일 아이디

r_report_prf_id
r_report_prf_id: required (integer)
예약 리포트 프로파일 아이디

i_report_prf_id
i_report_prf_id: required (integer)
즉석 리포트 프로파일 아이디

save_dt: required (integer - default: 2 - minimum: 2 - maximum: 24)
save_dt

저장 날짜

lang
lang: required (one of 1, 2, 3 - default: 1)
언어  한국어:	1,	영어:	2,	일본어:	3

d_report_prf_name: required (string)
d_report_prf_name
일간 리포트 프로파일 이름

w_report_prf_name
w_report_prf_name: required (string)
주간 리포트 프로파일 이름

m_report_prf_name: required (string)
m_report_prf_name
월간 리포트 프로파일 이름

r_report_prf_name
r_report_prf_name: required (string)
예약 리포트 프로파일 이름

i_report_prf_name
i_report_prf_name: required (string)
즉석 리포트 프로파일 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"d_report_prf_id":	1,
				"d_report_prf_name":	"default",
				"w_report_prf_id":	1,
				"w_report_prf_name":	"default",
				"m_report_prf_id":	1,
				"m_report_prf_name":	"default",
				"r_report_prf_id":	1,
				"r_report_prf_name":	"default",
				"i_report_prf_id":	1,
				"i_report_prf_name":	"default",
				"save_dt":	24,
				"lang":	2,
				"regular_hour":	23,
				"regular_min":	59,
				"d_use":	1,
				"w_use":	1,
				"w_week":	7,
				"m_use":	1,
				"m_day":	31,
				"r_use":	1,
				"r_start_dt":	"2018-08-31	09:00:00",
				"r_end_dt":	"2018-08-31	14:00:00",
				"r_dt":	"2018-08-31	23:00:00",
				"auto_snd_hour":	23,
				"auto_snd_min":	59
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

리포트 자동 전송 서버

GET

/report/servers

리포트 자동 전송 서버 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

리포트 자동 전송 서버 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of ResReportServer)
결과 데이터

Items: ResReportServer
Items

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

file_type
file_type: required (one of 0, 1, 2, 3, 4)
전송 대상

0:	방화벽	정책	설정(excel)
1:	일간	리포트
2:	주간	리포트
3:	월간	리포트
4:	방화벽	정책	설정(json)	-	V3.0에서	추가

proto
proto: required (NaN)
프로토콜
ftp:	0,	sftp:	1,	smtp:	2

addr
addr: required (string)
서버 주소

portport: required (integer - default: 80 - minimum: 0 - maximum: 65535)
포트 번호

enc_use
enc_use: required (one of 0, 1 - default: 0)
암호화 사용여부
사용안함:	0,	사용:	1

auth_use: required (one of 0, 1 - default: 0)
auth_use
인증 사용여부
사용안함:	0,	사용:	1

usr_id
usr_id: required (string)
원격 사용자 아이디

dirdir: required (string)
원격 저장 디렉터리

rcvr_email
rcvr_email: required (string)
수신 메일 주소

sndr_email
sndr_email: required (string)
발신 메일 주소

report_svr_id
report_svr_id: required (integer)
리포트 서버 아이디

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"report_svr_id":	1,
				"use":	0,
				"file_type":	0,
				"proto":	0,
				"addr":	"192.168.0.0",
				"port":	80,
				"enc_use":	0,
				"auth_use":	0,
				"usr_id":	"user1",
				"dir":	"/root",
				"rcvr_email":	"reciver1@secui.com",
				"sndr_email":	"send1@secui.com"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/report/servers

리포트 자동 전송 서버를 추가한다.(최대 5개)

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

file_type
file_type: required (one of 0, 1, 2, 3, 4)
전송 대상

0:	방화벽	정책	설정(excel)
1:	일간	리포트
2:	주간	리포트
3:	월간	리포트
4:	방화벽	정책	설정(json)	-	V3.0에서	추가

proto
proto: required (NaN)
프로토콜
ftp:	0,	sftp:	1,	smtp:	2

addr: required (string)
addr
서버 주소

portport: required (integer - default: 80 - minimum: 0 - maximum: 65535)
포트 번호

enc_use
enc_use: required (one of 0, 1 - default: 0)
암호화 사용여부
사용안함:	0,	사용:	1

auth_use
auth_use: required (one of 0, 1 - default: 0)
인증 사용여부
사용안함:	0,	사용:	1

usr_id: required (string)
usr_id

원격 사용자 아이디

dirdir: required (string)
원격 저장 디렉터리

rcvr_email: required (string)
rcvr_email
수신 메일 주소

sndr_email
sndr_email: required (string)
발신 메일 주소

passwd: required (string)
passwd
원격 비밀번호

Example:
Example

{
		"use":	0,
		"file_type":	0,
		"proto":	0,
		"addr":	"192.168.0.0",
		"port":	80,
		"enc_use":	0,
		"auth_use":	0,
		"usr_id":	"user1",
		"passwd":	"test1234",
		"dir":	"/root",
		"rcvr_email":	"reciver1@secui.com",
		"sndr_email":	"send1@secui.com"
}

Response

HTTP status code 200

리포트 자동 전송 서버 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

file_type
file_type: required (one of 0, 1, 2, 3, 4)
전송 대상

0:	방화벽	정책	설정(excel)
1:	일간	리포트
2:	주간	리포트
3:	월간	리포트
4:	방화벽	정책	설정(json)	-	V3.0에서	추가

proto
proto: required (NaN)
프로토콜
ftp:	0,	sftp:	1,	smtp:	2

addr
addr: required (string)
서버 주소

portport: required (integer - default: 80 - minimum: 0 - maximum: 65535)
포트 번호

enc_use
enc_use: required (one of 0, 1 - default: 0)
암호화 사용여부
사용안함:	0,	사용:	1

auth_use
auth_use: required (one of 0, 1 - default: 0)
인증 사용여부
사용안함:	0,	사용:	1

usr_id
usr_id: required (string)
원격 사용자 아이디

dirdir: required (string)
원격 저장 디렉터리

rcvr_email
rcvr_email: required (string)
수신 메일 주소

sndr_email
sndr_email: required (string)
발신 메일 주소

report_svr_id
report_svr_id: required (integer)
리포트 서버 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"report_svr_id":	1,
				"use":	0,
				"file_type":	0,
				"proto":	0,
				"addr":	"192.168.0.0",
				"port":	80,
				"enc_use":	0,
				"auth_use":	0,
				"usr_id":	"user1",
				"dir":	"/root",
				"rcvr_email":	"reciver1@secui.com",
				"sndr_email":	"send1@secui.com"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/report/servers/{pk}

리포트 자동 전송 서버을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
리포트 서버 아이디 ( report_svr_id )

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

리포트 자동 전송 서버 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

file_type: required (one of 0, 1, 2, 3, 4)
file_type
전송 대상

0:	방화벽	정책	설정(excel)
1:	일간	리포트
2:	주간	리포트
3:	월간	리포트
4:	방화벽	정책	설정(json)	-	V3.0에서	추가

proto
proto: required (NaN)
프로토콜
ftp:	0,	sftp:	1,	smtp:	2

addr
addr: required (string)
서버 주소

portport: required (integer - default: 80 - minimum: 0 - maximum: 65535)
포트 번호

enc_use: required (one of 0, 1 - default: 0)
enc_use
암호화 사용여부
사용안함:	0,	사용:	1

auth_use
auth_use: required (one of 0, 1 - default: 0)
인증 사용여부
사용안함:	0,	사용:	1

usr_id
usr_id: required (string)
원격 사용자 아이디

dirdir: required (string)
원격 저장 디렉터리

rcvr_email: required (string)
rcvr_email
수신 메일 주소

sndr_email
sndr_email: required (string)
발신 메일 주소

report_svr_id
report_svr_id: required (integer)
리포트 서버 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"report_svr_id":	1,
				"use":	0,
				"file_type":	0,
				"proto":	0,
				"addr":	"192.168.0.0",
				"port":	80,
				"enc_use":	0,
				"auth_use":	0,
				"usr_id":	"user1",
				"dir":	"/root",
				"rcvr_email":	"reciver1@secui.com",
				"sndr_email":	"send1@secui.com"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/report/servers/{pk}

리포트 자동 전송 서버을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
리포트 서버 아이디 ( report_svr_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept

응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

file_type
file_type: required (one of 0, 1, 2, 3, 4)
전송 대상

0:	방화벽	정책	설정(excel)
1:	일간	리포트
2:	주간	리포트
3:	월간	리포트
4:	방화벽	정책	설정(json)	-	V3.0에서	추가

proto
proto: required (NaN)
프로토콜
ftp:	0,	sftp:	1,	smtp:	2

addr
addr: required (string)
서버 주소

portport: required (integer - default: 80 - minimum: 0 - maximum: 65535)
포트 번호

enc_use
enc_use: required (one of 0, 1 - default: 0)
암호화 사용여부
사용안함:	0,	사용:	1

auth_use
auth_use: required (one of 0, 1 - default: 0)
인증 사용여부
사용안함:	0,	사용:	1

usr_id
usr_id: required (string)
원격 사용자 아이디

dirdir: required (string)
원격 저장 디렉터리

rcvr_email
rcvr_email: required (string)
수신 메일 주소

sndr_email
sndr_email: required (string)
발신 메일 주소

passwd: required (string)
passwd
원격 비밀번호

Example:
Example

{
		"use":	1,
		"file_type":	3,
		"proto":	2,
		"addr":	"192.168.0.1",
		"port":	8080,
		"enc_use":	1,
		"auth_use":	1,
		"usr_id":	"user2",
		"passwd":	"test4321",
		"dir":	"/root/main",
		"rcvr_email":	"reciver2@secui.com",
		"sndr_email":	"send2@secui.com"
}

Response

HTTP status code 200

리포트 자동 전송 서버 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1)
사용여부
사용안함:	0,	사용:	1

file_type
file_type: required (one of 0, 1, 2, 3, 4)
전송 대상

0:	방화벽	정책	설정(excel)
1:	일간	리포트
2:	주간	리포트
3:	월간	리포트
4:	방화벽	정책	설정(json)	-	V3.0에서	추가

proto
proto: required (NaN)
프로토콜
ftp:	0,	sftp:	1,	smtp:	2

addr
addr: required (string)
서버 주소

portport: required (integer - default: 80 - minimum: 0 - maximum: 65535)
포트 번호

enc_use
enc_use: required (one of 0, 1 - default: 0)
암호화 사용여부
사용안함:	0,	사용:	1

auth_use
auth_use: required (one of 0, 1 - default: 0)
인증 사용여부
사용안함:	0,	사용:	1

usr_id: required (string)
usr_id
원격 사용자 아이디

dirdir: required (string)
원격 저장 디렉터리

rcvr_email
rcvr_email: required (string)
수신 메일 주소

sndr_email
sndr_email: required (string)
발신 메일 주소

report_svr_id
report_svr_id: required (integer)
리포트 서버 아이디

Example:
Example

{
		"code":	"ok",
		"result":	{
				"report_svr_id":	1,
				"use":	1,
				"file_type":	3,
				"proto":	2,
				"addr":	"192.168.0.1",
				"port":	8080,
				"enc_use":	1,
				"auth_use":	1,
				"usr_id":	"user2",
				"dir":	"/root/main",
				"rcvr_email":	"reciver2@secui.com",
				"sndr_email":	"send2@secui.com"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/report/servers/{pk}

리포트 자동 전송 서버을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
리포트 서버 아이디 ( report_svr_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

리포트 자동 전송 서버 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

즉석 리포트

POST

/report/start

리포트 생성 시작

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
s_date
s_date: required (string)
리포트 생성 시작 시간

e_date: required (string)
e_date
리포트 생성 끝 시간

Example:
Example

{
		"s_date":	"2018-08-03	00",
		"e_date":	"2018-08-04	00"
}

Response

HTTP status code 200

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

request_id
request_id: required (string)
리포트 생성 ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"request_id":	"972a1a11f19934401291cc99117ec"
		}
}

GET

/report/{request_id}/status

리포트 생성 진행 상태 조회

Request

URI Parameters

request_id: required (string)
request_id
리포트 생성 ID

Example:
Example

972a1a11f19934401291cc99117ec

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

status
status: required (one of FAIL, IN_PROCESS, END)
리포트 상태
(오류:	FAIL,	생성중:	IN_PROCESS,	완료:	END)

sub_status
sub_status: required (string)
리포트 생성 진행 상태

report_type
report_type: required (one of regular, reservation, instant)
리포트 타입
(정규	리포트:	regular,	예약	리포트:	reservation,	즉석:	instant)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"status":	"END"
				"sub_stauts":	"summary_report	생성	시작
														summary_report	생성	완료
														total_traffic_report	생성	시작,
														total_traffic_report	생성	완료
														END",
				"report_type":	""
		}
}

DELETE

/report/{request_id}/stop

리포트 생성 중지

Request

URI Parameters

request_id
request_id: required (string)
리포트 생성 ID

Example:
Example

972a1a11f19934401291cc99117ec

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

리포트 생성 중지 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

리포트 생성 중지 요청 실패

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/report/{request_id}/end

리포트 생성 종료

Request

URI Parameters

request_id
request_id: required (string)
리포트 생성 ID

Example:
Example

972a1a11f19934401291cc99117ec

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

리포트 생성 종료 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

리포트 생성 종료 요청 실패

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

리포트 설정 적용/취소

PUT

/command/report-config/apply

리포트 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

regular_hour
regular_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
정규리포트 생성 시간

regular_min
regular_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
정규리포트 생성 분

d_use: required (one of 0, 1 - default: 0)
d_use
일간 사용여부
사용안함:	0,	사용:	1

w_use
w_use: required (one of 0, 1 - default: 0)
주간 사용여부
사용안함:	0,	사용:	1

w_week
w_week: required (one of 1, 2, 3, 4, 5, 6, 7 - default: 1)
주간 요일
월요일:	1,	화요일:	2,	수요일:	3,	목요일:	4,	금요일:	5,	토요일:	6,	일요일:	7

m_usem_use: required (one of 0, 1 - default: 0)
월간 사용여부
사용안함:	0,	사용:	1

m_daym_day: required (integer - default: 1 - minimum: 1 - maximum: 31)
월간 일

r_use
r_use: required (one of 0, 1 - default: 0)
예약 사용여부 (0:미사용, 1:사용)
사용안함:	0,	사용:	1

r_start_dt
r_start_dt: required (string)
예약 시작 날짜

r_end_dt
r_end_dt: required (string)
예약 종료 날짜

r_dt
r_dt: required (string)
예약 날짜

auto_snd_hour
auto_snd_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
자동 전송 시간

auto_snd_min
auto_snd_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
자동 전송 분

Example:
Example

{
		"regular_hour":	23,
		"regular_min":	59,
		"d_use":	0,
		"w_use":	1,
		"w_week":	7,
		"m_use":	1,
		"m_day":	31,
		"r_use":	1,
		"r_start_dt":	"2018-08-31	09:00:00",
		"r_end_dt":	"2018-08-31	14:00:00",
		"r_dt":	"2018-08-31	23:00:00",
		"auto_snd_hour":	23,
		"auto_snd_min":	59
}

Response

HTTP status code 200

리포트 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/report-config/cancel

리포트 설정을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

리포트 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

