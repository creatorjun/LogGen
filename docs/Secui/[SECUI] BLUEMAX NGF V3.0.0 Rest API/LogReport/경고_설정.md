GET

PUT

GET

PUT

GET

PUT

PUT

PUT

경고 설정 REST API 문서

https://{url}/api/lr

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/lr

경고 설정

/monitor/setup/alerts

/monitor/setup/alerts/{pk}

/monitor/setup/alerts/processes

경고 설정 적용/취소

/command/setup-alerts/apply

/command/setup-alerts/cancel

경고 설정

GET

/monitor/setup/alerts

경고 설정 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

경고 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ResAlertSetup)
결과 데이터

Items: ResAlertSetup
Items

mod_id
mod_id: required (integer)
경고 모듈 ID

useuse: required (one of 0, 1 - default: 0)
경고 사용 여부  사용안함:	0,	사용:	1

chk_prd
chk_prd: required (integer - default: 0)
검사 주기

limit
limit: required (integer - default: 0)
검사 임계치

level
level: required (one of 2, 3, 4, 5 - default: 2)
경고 발생시 위험 레벨  info:	2,	Minor:	3,	Major:	4,	Critical:	5

mail_use
mail_use: required (one of 0, 1 - default: 0)
경고 발생시 메일 발송 여부  사용안함:	0,	사용:	1

popup_use
popup_use: required (one of 0, 1 - default: 0)
경고 발생시 알람 팝업 발생 여부  사용안함:	0,	사용:	1

sms_use
sms_use: required (one of 0, 1 - default: 0)
경고 발생시 문자 발송 여부  사용안함:	0,	사용:	1

alert_id
alert_id: required (integer)
경고 고유 ID

name: required (string)
name
경고 항목 이름

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"alert_id":	80001,
				"mod_id":	80000,
				"name":	"EVENT_SYSTEM_CPU_TEMPER",
				"use":	0,
				"chk_prd":	0,
				"limit":	0,
				"level":	2,
				"mail_use":	0,
				"popup_use":	0,
				"sms_use":	0
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/monitor/setup/alerts

경고 설정 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of ReqAlertSetup)
수정 아이템 목록

Items: ReqAlertSetup
Items

mod_id
mod_id: required (integer)
경고 모듈 ID

useuse: required (one of 0, 1 - default: 0)
경고 사용 여부  사용안함:	0,	사용:	1

chk_prd
chk_prd: required (integer - default: 0)
검사 주기

limit: required (integer - default: 0)
limit
검사 임계치

level
level: required (one of 2, 3, 4, 5 - default: 2)
경고 발생시 위험 레벨  info:	2,	Minor:	3,	Major:	4,	Critical:	5

mail_use
mail_use: required (one of 0, 1 - default: 0)
경고 발생시 메일 발송 여부  사용안함:	0,	사용:	1

popup_use
popup_use: required (one of 0, 1 - default: 0)
경고 발생시 알람 팝업 발생 여부  사용안함:	0,	사용:	1

sms_use
sms_use: required (one of 0, 1 - default: 0)
경고 발생시 문자 발송 여부  사용안함:	0,	사용:	1

alert_id: required (integer)
alert_id
경고 고유 ID

Example:
Example

{
		"items":	[{
				"alert_id":	80001,
				"mod_id":	80000,
				"use":	1,
				"chk_prd":	23,
				"limit":	100,
				"level":	5,
				"mail_use":	1,
				"popup_use":	1,
				"sms_use":	1
		}]
}

Response

HTTP status code 200

경고 설정 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/monitor/setup/alerts/{pk}

경고 설정을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
경고 고유 ID ( alert_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

경고 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

mod_id
mod_id: required (integer)
경고 모듈 ID

useuse: required (one of 0, 1 - default: 0)
경고 사용 여부  사용안함:	0,	사용:	1

chk_prd
chk_prd: required (integer - default: 0)
검사 주기

limit
limit: required (integer - default: 0)
검사 임계치

level
level: required (one of 2, 3, 4, 5 - default: 2)
경고 발생시 위험 레벨  info:	2,	Minor:	3,	Major:	4,	Critical:	5

mail_use: required (one of 0, 1 - default: 0)
mail_use
경고 발생시 메일 발송 여부  사용안함:	0,	사용:	1

popup_use
popup_use: required (one of 0, 1 - default: 0)
경고 발생시 알람 팝업 발생 여부  사용안함:	0,	사용:	1

sms_use
sms_use: required (one of 0, 1 - default: 0)
경고 발생시 문자 발송 여부  사용안함:	0,	사용:	1

alert_id
alert_id: required (integer)
경고 고유 ID

name
name: required (string)
경고 항목 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"alert_id":	80001,
				"mod_id":	80000,
				"name":	"EVENT_SYSTEM_CPU_TEMPER",
				"use":	0,
				"chk_prd":	0,
				"limit":	0,
				"level":	2,
				"mail_use":	0,
				"popup_use":	0,
				"sms_use":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/monitor/setup/alerts/{pk}

경고 설정을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
경고 고유 ID ( alert_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

mod_id: required (integer)
mod_id

경고 모듈 ID

useuse: required (one of 0, 1 - default: 0)
경고 사용 여부  사용안함:	0,	사용:	1

chk_prd: required (integer - default: 0)
chk_prd
검사 주기

limit
limit: required (integer - default: 0)
검사 임계치

level: required (one of 2, 3, 4, 5 - default: 2)
level
경고 발생시 위험 레벨  info:	2,	Minor:	3,	Major:	4,	Critical:	5

mail_use
mail_use: required (one of 0, 1 - default: 0)
경고 발생시 메일 발송 여부  사용안함:	0,	사용:	1

popup_use
popup_use: required (one of 0, 1 - default: 0)
경고 발생시 알람 팝업 발생 여부  사용안함:	0,	사용:	1

sms_use
sms_use: required (one of 0, 1 - default: 0)
경고 발생시 문자 발송 여부  사용안함:	0,	사용:	1

Example:
Example

{
		"mod_id":	80000,
		"use":	1,
		"chk_prd":	23,
		"limit":	100,
		"level":	5,
		"mail_use":	1,
		"popup_use":	1,
		"sms_use":	1
}

Response

HTTP status code 200

경고 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

mod_id
mod_id: required (integer)
경고 모듈 ID

useuse: required (one of 0, 1 - default: 0)
경고 사용 여부  사용안함:	0,	사용:	1

chk_prd
chk_prd: required (integer - default: 0)
검사 주기

limit
limit: required (integer - default: 0)
검사 임계치

level
level: required (one of 2, 3, 4, 5 - default: 2)
경고 발생시 위험 레벨  info:	2,	Minor:	3,	Major:	4,	Critical:	5

mail_use
mail_use: required (one of 0, 1 - default: 0)
경고 발생시 메일 발송 여부  사용안함:	0,	사용:	1

popup_use: required (one of 0, 1 - default: 0)
popup_use
경고 발생시 알람 팝업 발생 여부  사용안함:	0,	사용:	1

sms_use
sms_use: required (one of 0, 1 - default: 0)
경고 발생시 문자 발송 여부  사용안함:	0,	사용:	1

alert_id
alert_id: required (integer)
경고 고유 ID

name
name: required (string)
경고 항목 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"alert_id":	80001,
				"mod_id":	80000,
				"name":	"EVENT_SYSTEM_CPU_TEMPER",
				"use":	1,
				"chk_prd":	23,
				"limit":	100,
				"level":	5,
				"mail_use":	1,
				"popup_use":	1,
				"sms_use":	1
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/monitor/setup/alerts/processes

경고 프로세스 설정 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

경고 프로세스 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (array of AlertProcess)
result
결과 데이터

Items: AlertProcess
Items

alert_process_id
alert_process_id: required (integer)
경고 프로세스 고유 ID

enable
enable: required (one of 0, 1 - default: 1)
경고 프로세스 사용여부  사용안함:	0,	사용:	1

name
name: required (string)
경고 프로세스 이름

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"alert_process_id":	1,
				"enable":	1,
				"name":	"process1"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/monitor/setup/alerts/processes

경고 프로세스 설정 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of ReqAlertProcessList)
수정 아이템 목록

Items: ReqAlertProcessList
Items

alert_process_id: required (integer)
alert_process_id
경고 프로세스 고유 ID

enable
enable: required (one of 0, 1 - default: 1)
경고 프로세스 사용여부  사용안함:	0,	사용:	1

Example:
Example

{
		"items":	[
				{"alert_process_id":	1,	"enable":	0}
		]
}

Response

HTTP status code 200

경고 프로세스 설정 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

경고 설정 적용/취소

PUT

/command/setup-alerts/apply

경고 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

경고 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/setup-alerts/cancel

경고 설정을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

경고 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

