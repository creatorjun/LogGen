GET

PUT

GET

POST

PUT

GET

PUT

  DELETE

GET

PUT

PUT

로그 관리 설정 REST API 문서

https://{url}/api/lr

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/lr

로그 관리 설정

/monitor/log-manager

/monitor/log-manager/servers

/monitor/log-manager/servers/{pk}

/monitor/log-manager/servers/{pk}/infos

로그 관리 설정 적용/취소

/command/log-manager/apply

/command/log-manager/cancel

로그 관리 설정

GET

/monitor/log-manager

로그 관리 설정을(를) 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

로그 관리 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

backup_start_hour
backup_start_hour: required (integer - default: 1 - minimum: 0 - maximum: 23)
백업 시작 시간 Hour

backup_start_min
backup_start_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
백업 시작 시간 Minute

backup_end_hour: required (integer - default: 5 - minimum: 0 - maximum: 23)
backup_end_hour
백업 종료 시간 Hour

backup_end_min
backup_end_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
백업 종료 시간 Minute

delete_use: required (one of 0, 1 - default: 0)
delete_use
로그 자동 삭제 사용 여부
사용안함:	0,	사용:	1

delete_retention_day
delete_retention_day: required (integer - default: 180 - minimum: 0 - maximum: 9999)
로그 보존 기간

delete_run_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
delete_run_hour
로그 삭제 시작 시간 Hour

delete_run_min
delete_run_min: required (integer - default: 30 - minimum: 0 - maximum: 59)
로그 삭제 시작 시간 Minute

max_disk_usage
max_disk_usage: required (integer - default: 90 - minimum: 10 - maximum: 95)
최대 디스크 사용량

Example:
Example

{
		"code":	"ok",
		"result":	{
				"backup_start_hour":	1,
				"backup_start_min":	0,
				"backup_end_hour":	5,
				"backup_end_min":	0,
				"delete_use":	0,
				"delete_retention_day":	180,
				"delete_run_hour":	0,
				"delete_run_min":	30,
				"max_disk_usage":	90
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/monitor/log-manager

로그 관리 설정을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

backup_start_hour
backup_start_hour: required (integer - default: 1 - minimum: 0 - maximum: 23)
백업 시작 시간 Hour

backup_start_min
backup_start_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
백업 시작 시간 Minute

backup_end_hour
backup_end_hour: required (integer - default: 5 - minimum: 0 - maximum: 23)
백업 종료 시간 Hour

backup_end_min
backup_end_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
백업 종료 시간 Minute

delete_use: required (one of 0, 1 - default: 0)
delete_use
로그 자동 삭제 사용 여부
사용안함:	0,	사용:	1

delete_retention_day
delete_retention_day: required (integer - default: 180 - minimum: 0 - maximum: 9999)
로그 보존 기간

delete_run_hour
delete_run_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
로그 삭제 시작 시간 Hour

delete_run_min
delete_run_min: required (integer - default: 30 - minimum: 0 - maximum: 59)
로그 삭제 시작 시간 Minute

max_disk_usage: required (integer - default: 90 - minimum: 10 - maximum: 95)
max_disk_usage
최대 디스크 사용량

Example:
Example

{
		"backup_start_hour":	23,
		"backup_start_min":	59,
		"backup_end_hour":	23,
		"backup_end_min":	59,
		"delete_use":	1,
		"delete_retention_day":	9999,
		"delete_run_hour":	23,
		"delete_run_min":	59,
		"max_disk_usage":	95
}

Response

HTTP status code 200

로그 관리 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

backup_start_hour
backup_start_hour: required (integer - default: 1 - minimum: 0 - maximum: 23)
백업 시작 시간 Hour

backup_start_min
backup_start_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
백업 시작 시간 Minute

backup_end_hour: required (integer - default: 5 - minimum: 0 - maximum: 23)
backup_end_hour
백업 종료 시간 Hour

backup_end_min
backup_end_min: required (integer - default: 0 - minimum: 0 - maximum: 59)
백업 종료 시간 Minute

delete_use
delete_use: required (one of 0, 1 - default: 0)
로그 자동 삭제 사용 여부
사용안함:	0,	사용:	1

delete_retention_day
delete_retention_day: required (integer - default: 180 - minimum: 0 - maximum: 9999)
로그 보존 기간

delete_run_hour
delete_run_hour: required (integer - default: 0 - minimum: 0 - maximum: 23)
로그 삭제 시작 시간 Hour

delete_run_min
delete_run_min: required (integer - default: 30 - minimum: 0 - maximum: 59)
로그 삭제 시작 시간 Minute

max_disk_usage
max_disk_usage: required (integer - default: 90 - minimum: 10 - maximum: 95)
최대 디스크 사용량

Example:
Example

{
		"code":	"ok",
		"result":	{
				"backup_start_hour":	23,
				"backup_start_min":	59,
				"backup_end_hour":	23,
				"backup_end_min":	59,
				"delete_use":	1,
				"delete_retention_day":	9999,
				"delete_run_hour":	23,
				"delete_run_min":	59,
				"max_disk_usage":	95
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/monitor/log-manager/servers

로그 백업 서버 설정 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

로그 백업 서버 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (array of ResLogManagerServer)
result
결과 데이터

Items: ResLogManagerServer
Items

useuse: required (one of 0, 1 - default: 0)
사용 여부
사용안함:	0,	사용:	1

svr_addr
svr_addr: required (string)
서버 주소

svr_port
svr_port: required (integer - default: 21)
서버 포트

proto
proto: required (string)
프로토콜
FTP,	SFTP

remote_user_id
remote_user_id: required (string - minLength: 1 - maxLength: 32)
원격 사용자 ID

remote_dir: required (string - minLength: 1 - maxLength: 512)
remote_dir
원격 저장 디렉터리

prd_type
prd_type: required (one of 0, 1 - default: 0)
백업 로그 범위
매일:0,	특정	기간:1

prd_day: required (integer - default: 1)
prd_day
매일 백업 대상 전일

prd_start_dt
prd_start_dt: required (string)
특정 기간 백업 시작일

prd_end_dt
prd_end_dt: required (string)
특정 기간 백업 종료일

log_backup_svr_id
log_backup_svr_id: required (integer)
로그 백업 서버 ID

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"log_backup_svr_id":	1,
				"use":	0,
				"svr_addr":	"100.0.0.1",
				"svr_port":	21,
				"proto":	"FTP",
				"remote_user_id":	"test1",
				"remote_dir":	"/log/backup",
				"prd_type":	0,
				"prd_day":	1,
				"prd_start_dt":	"2018-08-18	00:00:00",
				"prd_end_dt":	"2018-08-18	00:00:00"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

POST

/monitor/log-manager/servers

로그 백업 서버 설정를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1 - default: 0)
사용 여부
사용안함:	0,	사용:	1

svr_addr
svr_addr: required (string)
서버 주소

svr_port
svr_port: required (integer - default: 21)
서버 포트

proto
proto: required (string)
프로토콜
FTP,	SFTP

remote_user_id
remote_user_id: required (string - minLength: 1 - maxLength: 32)
원격 사용자 ID

remote_dir
remote_dir: required (string - minLength: 1 - maxLength: 512)
원격 저장 디렉터리

prd_type
prd_type: required (one of 0, 1 - default: 0)
백업 로그 범위
매일:0,	특정	기간:1

prd_day
prd_day: required (integer - default: 1)
매일 백업 대상 전일

prd_start_dt
prd_start_dt: required (string)
특정 기간 백업 시작일

prd_end_dt
prd_end_dt: required (string)
특정 기간 백업 종료일

remote_pw
remote_pw: required (string - minLength: 1 - maxLength: 32)
원격 비밀번호(Base64 인코딩)

Example:
Example

{
		"use":	0,
		"svr_addr":	"100.0.0.1",
		"svr_port":	21,
		"proto":	"FTP",
		"remote_user_id":	"test1",
		"remote_pw":	"x2xAyWkwMEAh",
		"remote_dir":	"/log/backup",
		"prd_type":	0,
		"prd_day":	1,
		"prd_start_dt":	"2018-08-18	00:00:00",
		"prd_end_dt":	"2018-08-18	00:00:00"
}

Response

HTTP status code 200

로그 백업 서버 설정 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ResLogManagerServer)
결과 데이터

Items: ResLogManagerServer
Items

useuse: required (one of 0, 1 - default: 0)
사용 여부
사용안함:	0,	사용:	1

svr_addr: required (string)
svr_addr
서버 주소

svr_port
svr_port: required (integer - default: 21)
서버 포트

proto
proto: required (string)
프로토콜
FTP,	SFTP

remote_user_id
remote_user_id: required (string - minLength: 1 - maxLength: 32)
원격 사용자 ID

remote_dir
remote_dir: required (string - minLength: 1 - maxLength: 512)
원격 저장 디렉터리

prd_type
prd_type: required (one of 0, 1 - default: 0)
백업 로그 범위
매일:0,	특정	기간:1

prd_day
prd_day: required (integer - default: 1)
매일 백업 대상 전일

prd_start_dt: required (string)
prd_start_dt
특정 기간 백업 시작일

prd_end_dt
prd_end_dt: required (string)
특정 기간 백업 종료일

log_backup_svr_id
log_backup_svr_id: required (integer)
로그 백업 서버 ID

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"log_backup_svr_id":	1,
				"use":	0,
				"svr_addr":	"100.0.0.1",
				"svr_port":	21,
				"proto":	"FTP",
				"remote_user_id":	"test1",
				"remote_dir":	"/log/backup",
				"prd_type":	0,
				"prd_day":	1,
				"prd_start_dt":	"2018-08-18	00:00:00",
				"prd_end_dt":	"2018-08-18	00:00:00"
		}]
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/monitor/log-manager/servers

로그 백업 서버 설정 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of ReqLogManagerServerList)
items
수정 아이템 목록

Items: ReqLogManagerServerList
Items

useuse: required (one of 0, 1 - default: 0)
사용 여부
사용안함:	0,	사용:	1

svr_addr
svr_addr: required (string)
서버 주소

svr_port
svr_port: required (integer - default: 21)
서버 포트

proto
proto: required (string)
프로토콜
FTP,	SFTP

remote_user_id
remote_user_id: required (string - minLength: 1 - maxLength: 32)
원격 사용자 ID

remote_dir: required (string - minLength: 1 - maxLength: 512)
remote_dir
원격 저장 디렉터리

prd_type
prd_type: required (one of 0, 1 - default: 0)
백업 로그 범위
매일:0,	특정	기간:1

prd_day: required (integer - default: 1)
prd_day
매일 백업 대상 전일

prd_start_dt
prd_start_dt: required (string)
특정 기간 백업 시작일

prd_end_dt
prd_end_dt: required (string)
특정 기간 백업 종료일

remote_pw
remote_pw: required (string - minLength: 1 - maxLength: 32)
원격 비밀번호(Base64 인코딩)

log_backup_svr_id
log_backup_svr_id: required (integer)
로그 백업 서버 ID

Example:
Example

{
		"items":	[{
				"log_backup_svr_id":	1,
				"use":	1,
				"svr_addr":	"200.0.0.1",
				"svr_port":	210,
				"proto":	"SFTP",
				"remote_user_id":	"test2",
				"remote_pw":	"x2xAyWkwMEAh",
				"remote_dir":	"/log/backup/01",
				"prd_type":	1,
				"prd_day":	30,
				"prd_start_dt":	"2018-08-19	00:00:00",
				"prd_end_dt":	"2018-08-19	00:00:00"
		}]
}

Response

HTTP status code 200

로그 백업 서버 설정 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors: required (array of object)
errors
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/monitor/log-manager/servers/{pk}

로그 백업 서버 설정을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
로그 백업 서버 ID ( log_backup_svr_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

로그 백업 서버 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1 - default: 0)
사용 여부
사용안함:	0,	사용:	1

svr_addr
svr_addr: required (string)
서버 주소

svr_port
svr_port: required (integer - default: 21)
서버 포트

proto
proto: required (string)
프로토콜
FTP,	SFTP

remote_user_id
remote_user_id: required (string - minLength: 1 - maxLength: 32)
원격 사용자 ID

remote_dir
remote_dir: required (string - minLength: 1 - maxLength: 512)
원격 저장 디렉터리

prd_type
prd_type: required (one of 0, 1 - default: 0)
백업 로그 범위
매일:0,	특정	기간:1

prd_day
prd_day: required (integer - default: 1)
매일 백업 대상 전일

prd_start_dt
prd_start_dt: required (string)
특정 기간 백업 시작일

prd_end_dt
prd_end_dt: required (string)
특정 기간 백업 종료일

log_backup_svr_id
log_backup_svr_id: required (integer)
로그 백업 서버 ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"log_backup_svr_id":	1,
				"use":	0,
				"svr_addr":	"100.0.0.1",
				"svr_port":	21,
				"proto":	"FTP",
				"remote_user_id":	"test1",
				"remote_dir":	"/log/backup",
				"prd_type":	0,
				"prd_day":	1,
				"prd_start_dt":	"2018-08-18	00:00:00",
				"prd_end_dt":	"2018-08-18	00:00:00"
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/monitor/log-manager/servers/{pk}

로그 백업 서버 설정을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
로그 백업 서버 ID ( log_backup_svr_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

useuse: required (one of 0, 1 - default: 0)
사용 여부
사용안함:	0,	사용:	1

svr_addr: required (string)
svr_addr
서버 주소

svr_port
svr_port: required (integer - default: 21)
서버 포트

proto
proto: required (string)
프로토콜
FTP,	SFTP

remote_user_id
remote_user_id: required (string - minLength: 1 - maxLength: 32)
원격 사용자 ID

remote_dir: required (string - minLength: 1 - maxLength: 512)
remote_dir
원격 저장 디렉터리

prd_type
prd_type: required (one of 0, 1 - default: 0)
백업 로그 범위
매일:0,	특정	기간:1

prd_day: required (integer - default: 1)
prd_day
매일 백업 대상 전일

prd_start_dt
prd_start_dt: required (string)
특정 기간 백업 시작일

prd_end_dt
prd_end_dt: required (string)
특정 기간 백업 종료일

remote_pw
remote_pw: required (string - minLength: 1 - maxLength: 32)
원격 비밀번호(Base64 인코딩)

Example:
Example

{
		"use":	1,
		"svr_addr":	"200.0.0.1",
		"svr_port":	210,
		"proto":	"SFTP",
		"remote_user_id":	"test2",
		"remote_pw":	"x2xAyWkwMEAh",
		"remote_dir":	"/log/backup/01",
		"prd_type":	1,
		"prd_day":	30,
		"prd_start_dt":	"2018-08-19	00:00:00",
		"prd_end_dt":	"2018-08-19	00:00:00"
}

Response

HTTP status code 200

로그 백업 서버 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

useuse: required (one of 0, 1 - default: 0)
사용 여부
사용안함:	0,	사용:	1

svr_addr
svr_addr: required (string)
서버 주소

svr_port
svr_port: required (integer - default: 21)
서버 포트

proto
proto: required (string)
프로토콜
FTP,	SFTP

remote_user_id
remote_user_id: required (string - minLength: 1 - maxLength: 32)
원격 사용자 ID

remote_dir
remote_dir: required (string - minLength: 1 - maxLength: 512)
원격 저장 디렉터리

prd_type: required (one of 0, 1 - default: 0)
prd_type
백업 로그 범위
매일:0,	특정	기간:1

prd_day
prd_day: required (integer - default: 1)
매일 백업 대상 전일

prd_start_dt: required (string)
prd_start_dt
특정 기간 백업 시작일

prd_end_dt
prd_end_dt: required (string)
특정 기간 백업 종료일

log_backup_svr_id
log_backup_svr_id: required (integer)
로그 백업 서버 ID

Example:
Example

{
		"code":	"ok",
		"result":	{
				"log_backup_svr_id":	1,
				"use":	1,
				"svr_addr":	"200.0.0.1",
				"svr_port":	210,
				"proto":	"SFTP",
				"remote_user_id":	"test2",
				"remote_dir":	"/log/backup/01",
				"prd_type":	1,
				"prd_day":	30,
				"prd_start_dt":	"2018-08-19	00:00:00",
				"prd_end_dt":	"2018-08-19	00:00:00"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/monitor/log-manager/servers/{pk}

로그 백업 서버 설정을(를) 삭제한다.

Request

URI Parameters

pkpk: required (integer)
로그 백업 서버 ID ( log_backup_svr_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

로그 백업 서버 설정 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/monitor/log-manager/servers/{pk}/infos

로그 백업 서버 전송 결과 목록을 조회한다.

Request

URI Parameters

pkpk: required (string)

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

로그 백업 서버 전송 결과 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of LogManagerServerInfo)
결과 데이터

Items: LogManagerServerInfo
Items

datedate: required (string)
날짜

table
table: required (string)
로그 타입

result: required (string)
result
전송 결과
waiting	for	transport:	전송대기,	succeeded:	성공,	failed:	실패

completion_time
completion_time: required (string)
완료 시간

descdesc: required (string)
설명

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"date":	"2018-08-03",
				"table":	"traffic_session",
				"result":	"failed",
				"completion_time":	"2018-08-16	03:12:20",
				"desc":	"접속	실패"
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

로그 관리 설정 적용/취소

PUT

/command/log-manager/apply

로그 관리 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

clear_history
clear_history: required (array of integer)
최근 전송 목록

Example:
Example

{
		"clear_history":	[1,	2,	3]
}

Response

HTTP status code 200

로그 관리 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/log-manager/cancel

로그 관리 설정을 취소합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

로그 관리 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

