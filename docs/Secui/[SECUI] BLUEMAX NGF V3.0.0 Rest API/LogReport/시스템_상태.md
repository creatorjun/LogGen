현재 상태 > 시스템 상태 REST API 문서

https://{url}/api

urlurl: required (string)
장비 IP

현재 상태 검색 시작

주의) 이전에 현재 상태 또는 로그 검색 종료 후 검색 종료 REST API가 호출되지 않았다면 검색 시작 전에  /lr/monitor/current/{request_id}/end  REST API를 요청해 이전 검색 요청에 대한 리소스 정리가 필요
합니다.

POST

GET

GET

DELETE

DELETE

/lr/monitor/current/start

현재 상태 검색 진행 상태 요청

응답 결과 중 status 속성 값이  DONE 일 때까지 반복 요청(폴링)을 해야 합니다.

/lr/monitor/current/{request_id}/status

현재 상태 검색 결과 조회

상태 조회(/{request_id}/status) 응답 결과에서  searched_cnt  속성 값이 0 일 경우 데이터가 없기 때문에 호출 될 필요 없습니다.

/lr/monitor/current/{request_id}/page/{start}/to/{end}

현재 상태 검색 중지

/lr/monitor/current/{request_id}/stop

현재 상태 검색 종료

주의) 재 검색 전, 즉,  /lr/monitor/current/start  REST API 호출 전에 이전 request_id에 해당하는 검색 작업이 종료되지 않았다면 반드시 호출되어야 합니다.

/lr/monitor/current/{request_id}/end

현재 상태 검색 시작

POST

/lr/monitor/current/start

시스템	상태  검색을 시작한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
filters
filters: required (array of 필터)
필터 정보 목록
별첨으로	제공되는	로그	기능	별	컬럼	정의서	참고

Items: 필터
Items

keykey: required (string)
컬럼

value: required (array of String)
value
필터 값 목록
숫자형식: N or N1-N2 or >N, <N, >=N, <=N
문자열

is_not
is_not: required (one of true, false)
NOT 여부

page_rows
page_rows: (integer)
페이지 당 건수

total_rows
total_rows: (integer)
검색 건수

print_object_name
print_object_name: (one of true, false)
객체명 출력 여부

columns
columns: required (array of String)
컬럼 목록
별첨으로	제공되는	로그	기능	별	컬럼	정의서	참고

data_type
data_type: required (string)
현재 상태 검색 타입(기능 별 ID)

system_info : 시스템 정보
process_status : 프로세스 현황
user_info : 현재 접속 중인 관리자
service_status : 서비스 연동 상태

Example:
Example

//	예)	시스템	정보
{
		"filters":	[],
		"data_type":	"system_info",
		"page_rows":	30,
		"total_rows":	1000,
		"print_object_name":	true,
		"columns":	[
				"mem_total",
				"mem_used",
				"mem_used_by_process",
				"mem_used_by_kernel",
				"mem_used_by_ramdisk",
				"mem_reclaim",
				"mem_rate",
				"fs_total",
				"fs_used",
				"fs_used_by_tslog",
				"fs_used_by_seclog",
				"fs_used_by_others",
				"fs_avail",
				"fs_rate",
				"cpu_temperature",
				"fan",
				"dual_psu",
				"psu_status1",
				"psu_status2",
				"mem_free",
				"cpu_cnt",
				"fan_cnt",
				"fan_limit",
				"cpu_limit"
		]
}

Response

HTTP status code 200

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

request_id
request_id: required (string)
요청 ID
검색 라이프사이클 전반에 사용됩니다.

Example:
Example

{
		"code":	"ok",
		"result":	{
				"request_id":	"00001220-005344"
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"50101",
		"dev_t":	"System	API	Error",
		"message":	"Wrong	value.	(columns	:	'pid')"
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

//	로그인	필요
{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

//	(재)로그인	필요
{
		"code":	"94014",
		"dev_t":	"Authentication	Error",
		"message":	"세션이	만료되어	재로그인이	필요합니다."
}

//	권한이	필요
{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t

개발용 메시지

Example:
Example

{
		"code":	"50007",
		"dev_t":	"System	API	Error",
		"message":	"존재하지	않는	리소스입니다."
}

현재 상태 검색 진행 상태 요청

GET

/lr/monitor/current/{request_id}/status

시스템	상태  검색 진행 상태를 조회한다.

Request

URI Parameters

request_id
request_id: required (string)
조회 시작 시 응답에 포함된  요청	ID

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

시스템	상태  검색 진행 상태 요청 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
status
status: required (string)
검색 진행 상태

INIT : 초기 단계
IN_PROGRESS : 검색 진행 단계
DONE : 검색 완료 단계

status가 DONE 일 경우 더 이상 상태 요청 REST API를 호출하지 않아도 됩니다.

progress
progress: required (integer - minimum: 0 - maximum: 100)
검색 진행률(0~100)

searched_cnt
searched_cnt: required (integer)
검색 완료 건수

allow_next
allow_next: required (one of true, false)
다음(next) 검색 가능 여부
true:	'/next'	API	호출	가능,	false:	'/next'	API	호출	불가능

Example:
Example

{
		"code":	"ok",
		"result":	{
				"status":	"DONE",
				"progress":	100,
				"searched_cnt":	576,
				"allow_next":	false
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

//	로그인	필요
{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

//	(재)로그인	필요
{
		"code":	"94014",
		"dev_t":	"Authentication	Error",
		"message":	"세션이	만료되어	재로그인이	필요합니다."
}

//	권한이	필요
{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"50007",
		"dev_t":	"System	API	Error",
		"message":	"존재하지	않는	리소스입니다."
}

현재 상태 검색 결과 조회

GET

/lr/monitor/current/{request_id}/page/{start}/to/{end}

시스템	상태  검색 결과를 조회한다.

Request

URI Parameters

request_id
request_id: required (string)
조회 시작 시 응답에 포함된  요청	ID

start
start: required (integer)
검색 시작 인덱스
결과 데이터에 대한 인덱스를 기준으로 합니다.

endend: required (integer)
검색 종료 인덱스
결과 데이터에 대한 인덱스를 기준으로 합니다.

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

시스템	상태  검색 결과 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
별첨으로	제공되는	로그	기능	별	컬럼	정의서	참고

Example:
Example

//	예)	시스템	상태
{
		"code":	"ok",
		"result":	{
				"log":	[
						{
								"mem_total":	16293588000,
								"mem_used":	3434589000,
								"mem_used_by_process":	2744960000,
								"mem_used_by_kernel":	689240000,
								"mem_used_by_ramdisk":	389000,
								"mem_reclaim":	2112911000,
								"mem_rate":	21.079389572143555,
								"log_fs_total":	18909855744,
								"log_fs_used":	102318080,
								"log_fs_used_by_tslog":	213192,
								"log_fs_used_by_seclog":	0,
								"log_fs_used_by_others":	102104888,
								"log_fs_avail":	18807537664,
								"log_fs_rate":	0.5410833358764648,
								"log_file_total":	1245184,
								"log_file_used":	7674,
								"log_file_avail":	1237510,

								"log_file_rate":	0.6162944436073303,
								"system_fs_total":	10722738176,
								"system_fs_used":	7352500224,
								"system_fs_used_by_var":	479543296,
								"system_fs_used_by_config":	778694656,
								"system_fs_used_by_contents":	866881536,
								"system_fs_used_by_others":	5227380736,
								"system_fs_avail":	3370237952,
								"system_fs_rate":	68.56924438476562,
								"system_file_total":	5240832,
								"system_file_used":	128688,
								"system_file_avail":	5112144,
								"system_file_rate":	2.4554879665374756,
								"system_tmp_fs_total":	2136997888,
								"system_tmp_fs_used":	199491584,
								"system_tmp_fs_used_by_log":	154202112,
								"system_tmp_fs_used_by_logbackup":	3108864,
								"system_tmp_fs_used_by_others":	42180608,
								"system_tmp_fs_avail":	1937506304,
								"system_tmp_fs_rate":	9.335132598876953,
								"system_tmp_file_total":	1048576,
								"system_tmp_file_used":	673,
								"system_tmp_file_avail":	1047903,
								"system_tmp_file_rate":	0.06418228149414062,
								"cpu_temperature":	[
										0,
										0,
										0,
										0,
										0,
										2048,
										0,
										0
								],
								"fan":	[],
								"dual_psu":	"False",
								"psu_status1":	"Fail",
								"psu_status2":	"Fail",
								"mem_free":	10746088000,
								"cpu_cnt":	8,
								"fan_cnt":	0,
								"fan_limit":	0,
								"cpu_limit":	0
						}
				]
		}
}

//	예)	프로세스	현황
{
		"code":	"ok",
		"result":	{
				"log":	[
						{
								"pid":	1111,
								"pname":	"dwatcher",
								"cpu_use":	0.0,
								"vm_use":	92320,
								"rm_use":	3604,
								"flags":	0
						}
				]
		}
}

//	예)	현재	접속	중인	관리자
{
		"code":	"ok",
		"result":	{
				"log":	[
						{
								"admin_id":	"admin",
								"admin_ip":	"192.168.1.1",
								"login_time":	"2020-05-08	08:12:22",
								"level":	"Level	3"
						}
				]
		}
}

//	예)	서비스	연동	상태
{
		"code":	"ok",
		"result":	{
				"log":	[
						{
								"type":	"소프트웨어	패치",
								"location":	"https://192.168.10.10",
								"status":	"주의	-	버전	상이	(현재	버전과	업데이트	버전에	차이가	있음)",
								"current_ver":	"2-48708",
								"latest_ver":	"2-48779",
								"message":	"version	different,	local(2-48708)	vs	remote(2-48779)",
								"status_raw":	301
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

//	로그인	필요
{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

//	(재)로그인	필요
{
		"code":	"94014",
		"dev_t":	"Authentication	Error",
		"message":	"세션이	만료되어	재로그인이	필요합니다."
}

//	권한이	필요
{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"50007",
		"dev_t":	"System	API	Error",
		"message":	"존재하지	않는	리소스입니다."
}

현재 상태 검색 중지

DELETE

/lr/monitor/current/{request_id}/stop

시스템	상태  진행 중인 검색을 중지한다.

Request

URI Parameters

request_id
request_id: required (string)
조회 시작 시 응답에 포함된  요청	ID

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

시스템	상태  검색 중지 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
Example:
Example

{
		"code":	"ok",
		"result":	{
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

//	로그인	필요
{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

//	(재)로그인	필요
{
		"code":	"94014",
		"dev_t":	"Authentication	Error",
		"message":	"세션이	만료되어	재로그인이	필요합니다."
}

//	권한이	필요
{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"50007",
		"dev_t":	"System	API	Error",
		"message":	"존재하지	않는	리소스입니다."
}

현재 상태 검색 종료

DELETE

/lr/monitor/current/{request_id}/end

시스템	상태  검색 종료 후 리소스를 해제한다.

Request

URI Parameters

request_id
request_id: required (string)
조회 시작 시 응답에 포함된  요청	ID

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

시스템	상태  검색 종료 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
Example:
Example

{
		"code":	"ok",
		"result":	{
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

//	로그인	필요
{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

//	(재)로그인	필요
{
		"code":	"94014",
		"dev_t":	"Authentication	Error",
		"message":	"세션이	만료되어	재로그인이	필요합니다."
}

//	권한이	필요
{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"50007",
		"dev_t":	"System	API	Error",
		"message":	"존재하지	않는	리소스입니다."
}

