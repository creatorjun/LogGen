GET

PUT

GET

PUT

GET

PUT

PUT

PUT

로그 설정 REST API 문서

https://{url}/api/lr

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/lr

로그 설정

/monitor/setup/logs

/monitor/setup/logs/{pk}

/monitor/setup/logs/{pk}/options

로그 설정 적용/취소

/command/setup-logs/apply

/command/setup-logs/cancel

로그 설정

GET

/monitor/setup/logs

로그 설정 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

로그 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (array of ResLogSetup)
결과 데이터

Items: ResLogSetup
Items

mod_id
mod_id: required (integer)
설정을 위한 로그 모듈 ID

local_use: required (one of 0, 1 - default: 0)
local_use
로그 저장 여부
사용안함:	0,	사용:	1

syslog1_use
syslog1_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog2_use: required (one of 0, 1 - default: 0)
syslog2_use
syslog 전송 여부
사용안함:	0,	사용:	1

syslog3_use
syslog3_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog4_use
syslog4_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog5_use
syslog5_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

snmptrap1_use
snmptrap1_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap2_use
snmptrap2_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap3_use
snmptrap3_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

auto_backup_use
auto_backup_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

smtp_use
smtp_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

opt_type
opt_type: (integer)
옵션 정보(조회용)
GUI 화면에서 항목을 구분을 위한 옵션값
1:	저장	세부	항목,	2:	전송	세부	항목

log_id
log_id: required (integer)
설정을 위한 로그 고유 ID

name: required (string)
name
로그 이름

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"log_id":	130001,
				"mod_id":	1,
				"name":	"traffic_session",
				"local_use":	0,
				"syslog1_use":	0,
				"syslog2_use":	0,
				"syslog3_use":	0,
				"syslog4_use":	0,
				"syslog5_use":	0,
				"snmptrap1_use":	0,
				"snmptrap2_use":	0,
				"snmptrap3_use":	0,
				"auto_backup_use":	0,
				"smtp_use":	0,
				"opt_type":	1
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/monitor/setup/logs

로그 설정 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of ReqLogSetup)
items
수정 아이템 목록

Items: ReqLogSetup
Items

mod_id
mod_id: required (integer)
설정을 위한 로그 모듈 ID

local_use
local_use: required (one of 0, 1 - default: 0)
로그 저장 여부
사용안함:	0,	사용:	1

syslog1_use
syslog1_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog2_use
syslog2_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog3_use
syslog3_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog4_use
syslog4_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog5_use: required (one of 0, 1 - default: 0)
syslog5_use

syslog 전송 여부
사용안함:	0,	사용:	1

snmptrap1_use
snmptrap1_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap2_use
snmptrap2_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap3_use
snmptrap3_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

auto_backup_use
auto_backup_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

smtp_use: required (one of 0, 1 - default: 0)
smtp_use
snmptrap 전송 여부
사용안함:	0,	사용:	1

opt_type
opt_type: (integer)
옵션 정보(조회용)
GUI 화면에서 항목을 구분을 위한 옵션값
1:	저장	세부	항목,	2:	전송	세부	항목

log_id
log_id: required (integer)
설정을 위한 로그 고유 ID

Example:
Example

{
		"items":	[{
				"log_id":	130001,
				"mod_id":	1,
				"local_use":	1,
				"syslog1_use":	1,
				"syslog2_use":	1,
				"syslog3_use":	1,
				"syslog4_use":	1,
				"syslog5_use":	1,
				"snmptrap1_use":	1,
				"snmptrap2_use":	1,
				"snmptrap3_use":	1,
				"auto_backup_use":	1,
				"smtp_use":	1
		}]
}

Response

HTTP status code 200

로그 설정 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (one of ok, pok, nok)
code
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/monitor/setup/logs/{pk}

로그 설정을(를) 조회한다.

Request

URI Parameters

pkpk: required (integer)
설정을 위한 로그 고유 ID ( log_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

로그 설정 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

mod_id
mod_id: required (integer)
설정을 위한 로그 모듈 ID

local_use: required (one of 0, 1 - default: 0)
local_use
로그 저장 여부
사용안함:	0,	사용:	1

syslog1_use
syslog1_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog2_use
syslog2_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog3_use: required (one of 0, 1 - default: 0)
syslog3_use
syslog 전송 여부
사용안함:	0,	사용:	1

syslog4_use
syslog4_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog5_use
syslog5_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

snmptrap1_use
snmptrap1_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap2_use
snmptrap2_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap3_use: required (one of 0, 1 - default: 0)
snmptrap3_use
snmptrap 전송 여부
사용안함:	0,	사용:	1

auto_backup_use
auto_backup_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

smtp_use
smtp_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

opt_type
opt_type: (integer)
옵션 정보(조회용)
GUI 화면에서 항목을 구분을 위한 옵션값
1:	저장	세부	항목,	2:	전송	세부	항목

log_id
log_id: required (integer)
설정을 위한 로그 고유 ID

name: required (string)
name
로그 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"log_id":	130001,
				"mod_id":	1,
				"name":	"traffic_session",
				"local_use":	0,
				"syslog1_use":	0,
				"syslog2_use":	0,
				"syslog3_use":	0,
				"syslog4_use":	0,
				"syslog5_use":	0,
				"snmptrap1_use":	0,
				"snmptrap2_use":	0,
				"snmptrap3_use":	0,
				"auto_backup_use":	0,
				"smtp_use":	0,
				"opt_type":	1
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/monitor/setup/logs/{pk}

로그 설정을(를) 수정한다.

Request

URI Parameters

pkpk: required (integer)
설정을 위한 로그 고유 ID ( log_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

mod_id
mod_id: required (integer)
설정을 위한 로그 모듈 ID

local_use
local_use: required (one of 0, 1 - default: 0)
로그 저장 여부
사용안함:	0,	사용:	1

syslog1_use
syslog1_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog2_use: required (one of 0, 1 - default: 0)
syslog2_use
syslog 전송 여부
사용안함:	0,	사용:	1

syslog3_use
syslog3_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog4_use
syslog4_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog5_use: required (one of 0, 1 - default: 0)
syslog5_use
syslog 전송 여부
사용안함:	0,	사용:	1

snmptrap1_use
snmptrap1_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap2_use
snmptrap2_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap3_use
snmptrap3_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

auto_backup_use
auto_backup_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

smtp_use: required (one of 0, 1 - default: 0)
smtp_use
snmptrap 전송 여부
사용안함:	0,	사용:	1

opt_type
opt_type: (integer)
옵션 정보(조회용)
GUI 화면에서 항목을 구분을 위한 옵션값
1:	저장	세부	항목,	2:	전송	세부	항목

Example:
Example

{
		"mod_id":	1,
		"local_use":	1,
		"syslog1_use":	1,
		"syslog2_use":	1,
		"syslog3_use":	1,
		"syslog4_use":	1,
		"syslog5_use":	1,
		"snmptrap1_use":	1,
		"snmptrap2_use":	1,
		"snmptrap3_use":	1,
		"auto_backup_use":	1,
		"smtp_use":	1
}

Response

HTTP status code 200

로그 설정 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

mod_id
mod_id: required (integer)
설정을 위한 로그 모듈 ID

local_use
local_use: required (one of 0, 1 - default: 0)
로그 저장 여부
사용안함:	0,	사용:	1

syslog1_use
syslog1_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog2_use
syslog2_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog3_use: required (one of 0, 1 - default: 0)
syslog3_use
syslog 전송 여부
사용안함:	0,	사용:	1

syslog4_use
syslog4_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

syslog5_use
syslog5_use: required (one of 0, 1 - default: 0)
syslog 전송 여부
사용안함:	0,	사용:	1

snmptrap1_use
snmptrap1_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap2_use
snmptrap2_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

snmptrap3_use: required (one of 0, 1 - default: 0)
snmptrap3_use

snmptrap 전송 여부
사용안함:	0,	사용:	1

auto_backup_use
auto_backup_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

smtp_use
smtp_use: required (one of 0, 1 - default: 0)
snmptrap 전송 여부
사용안함:	0,	사용:	1

opt_type: (integer)
opt_type
옵션 정보(조회용)
GUI 화면에서 항목을 구분을 위한 옵션값
1:	저장	세부	항목,	2:	전송	세부	항목

log_id
log_id: required (integer)
설정을 위한 로그 고유 ID

name
name: required (string)
로그 이름

Example:
Example

{
		"code":	"ok",
		"result":	{
				"log_id":	130001,
				"mod_id":	1,
				"name":	"traffic_session",
				"local_use":	1,
				"syslog1_use":	1,
				"syslog2_use":	1,
				"syslog3_use":	1,
				"syslog4_use":	1,
				"syslog5_use":	1,
				"snmptrap1_use":	1,
				"snmptrap2_use":	1,
				"snmptrap3_use":	1,
				"auto_backup_use":	1,
				"smtp_use":	1,
				"opt_type":	1
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/monitor/setup/logs/{pk}/options

로그 옵션 설정 목록을 조회한다.

Request

URI Parameters

pkpk: required (integer)
옵션이 설정되어 있는 로그의 고유 ID ( log_id )

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

로그 옵션 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

result
result: required (array of LogSetupOption)
결과 데이터

Items: LogSetupOption
Items

log_id
log_id: required (integer)
옵션이 설정되어 있는 로그의 고유 ID

optopt: required (string)
옵션 이름
UI	Session,	LocalOut	Session

value1
value1: required (one of 0, 1 - default: 0)
설정 정보
저장	안함:	0,	저장:	1

value2: required (string)
value2
설정 정보

Example:
Example

{
		"code":	"ok",
		"result":	[{
				"log_id":	130001,
				"opt":	"UI	Session",
				"value1":	0,
				"value2":	""
		}]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/monitor/setup/logs/{pk}/options

로그 옵션 설정 목록을 (일괄)수정한다.

Request

URI Parameters

pkpk: required (integer)
옵션이 설정되어 있는 로그의 고유 ID ( log_id )

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items: required (array of LogSetupOption)
items
수정 아이템 목록

Items: LogSetupOption
Items

log_id
log_id: required (integer)
옵션이 설정되어 있는 로그의 고유 ID

optopt: required (string)
옵션 이름
UI	Session,	LocalOut	Session

value1
value1: required (one of 0, 1 - default: 0)
설정 정보
저장	안함:	0,	저장:	1

value2
value2: required (string)
설정 정보

Example:
Example

{
		"items":	[
				{"log_id":	130001,	"opt":	"UI	Session","value1":	0,	"value2":	""},
				{"log_id":	130001,	"opt":	"LocalOut	Session","value1":	1,	"value2":	""}
		]
}

Response

HTTP status code 200

로그 옵션 설정 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors: required (array of object)
errors
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

로그 설정 적용/취소

PUT

/command/setup-logs/apply

로그 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

로그 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/setup-logs/cancel

로그 설정을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

로그 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

