GET

PUT

PUT

PUT

(2.0)현재 상태 전송 설정 REST API 문서

https://{url}/api/lr

urlurl: required (string)
IP:PORT

Example:
Example

https://192.168.100.100/api/lr

현재 상태 전송 설정

/monitor/current-status/senders

현재 상태 전송 설정 적용/취소

/command/current-status-senders/apply

/command/current-status-senders/cancel

현재 상태 전송 설정

GET

/monitor/current-status/senders

현재 상태 전송 설정 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

현재 상태 전송 설정 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (array of 전송 대상 정보)
result
결과 데이터

Items: 전송 대상 정보
Items

send_data_id
send_data_id: required (integer)
전송 목록 ID(PK)

used_flag
used_flag: required (one of 0, 1)
사용 여부
0:	사용안함,	1:	사용

send_hour
send_hour: required (integer - minimum: 0 - maximum: 23)
전송 시간(시)

send_min: required (integer - minimum: 0 - maximum: 59)
send_min
전송 시간(분)

check_value
check_value: required (integer - minimum: 0 - maximum: 157680000)
전송 조건

svr_snd_flag_1
svr_snd_flag_1: required (one of 0, 1)
Syslog 설정 서버1 설정 여부
0:	사용안함,	1:	사용

svr_snd_flag_2
svr_snd_flag_2: required (one of 0, 1)
Syslog 설정 서버2 설정 여부
0:	사용안함,	1:	사용

svr_snd_flag_3: required (one of 0, 1)
svr_snd_flag_3
Syslog 설정 서버3 설정 여부
0:	사용안함,	1:	사용

svr_snd_flag_4
svr_snd_flag_4: required (one of 0, 1)
Syslog 설정 서버4 설정 여부
0:	사용안함,	1:	사용

svr_snd_flag_5: required (one of 0, 1)
svr_snd_flag_5
Syslog 설정 서버5 설정 여부
0:	사용안함,	1:	사용

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"send_data_id":	1,
						"used_flag":	0,
						"send_hour":	0,
						"send_min":	0,
						"check_value":	3600,
						"svr_snd_flag_1":	0,
						"svr_snd_flag_2":	0,
						"svr_snd_flag_3":	0,
						"svr_snd_flag_4":	0,
						"svr_snd_flag_5":	0
				},
				{
						"send_data_id":	2,
						"used_flag":	0,
						"send_hour":	0,
						"send_min":	0,
						"check_value":	3600,
						"svr_snd_flag_1":	0,
						"svr_snd_flag_2":	0,
						"svr_snd_flag_3":	0,
						"svr_snd_flag_4":	0,
						"svr_snd_flag_5":	0
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/monitor/current-status/senders

현재 상태 전송 설정 목록을 (일괄)수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
items
items: required (array of 전송 대상 정보)
수정 아이템 목록

Items: 전송 대상 정보
Items

send_data_id: required (integer)
send_data_id
전송 목록 ID(PK)

used_flag
used_flag: required (one of 0, 1)
사용 여부
0:	사용안함,	1:	사용

send_hour: required (integer - minimum: 0 - maximum: 23)
send_hour
전송 시간(시)

send_min
send_min: required (integer - minimum: 0 - maximum: 59)
전송 시간(분)

check_value
check_value: required (integer - minimum: 0 - maximum: 157680000)
전송 조건

svr_snd_flag_1
svr_snd_flag_1: required (one of 0, 1)
Syslog 설정 서버1 설정 여부
0:	사용안함,	1:	사용

svr_snd_flag_2
svr_snd_flag_2: required (one of 0, 1)
Syslog 설정 서버2 설정 여부
0:	사용안함,	1:	사용

svr_snd_flag_3
svr_snd_flag_3: required (one of 0, 1)
Syslog 설정 서버3 설정 여부
0:	사용안함,	1:	사용

svr_snd_flag_4: required (one of 0, 1)
svr_snd_flag_4

Syslog 설정 서버4 설정 여부
0:	사용안함,	1:	사용

svr_snd_flag_5
svr_snd_flag_5: required (one of 0, 1)
Syslog 설정 서버5 설정 여부
0:	사용안함,	1:	사용

Example:
Example

{
		"items":	[
				{
						"send_data_id":	1,
						"used_flag":	0,
						"send_hour":	0,
						"send_min":	0,
						"check_value":	3600,
						"svr_snd_flag_1":	0,
						"svr_snd_flag_2":	0,
						"svr_snd_flag_3":	0,
						"svr_snd_flag_4":	0,
						"svr_snd_flag_5":	0
				}
		]
}

Response

HTTP status code 200

현재 상태 전송 설정 목록 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (one of ok, pok, nok)
결과 코드

result
result: required (object)
결과 데이터

errors
errors: required (array of object)
수정 실패 시 에러 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"errors":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

현재 상태 전송 설정 적용/취소

PUT

/command/current-status-senders/apply

현재 상태 전송 설정을 적용합니다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

현재 상태 전송 설정 적용 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

PUT

/command/current-status-senders/cancel

현재 상태 전송 설정을 취소합니다.

Request

Headers

Authorization: required (string)
Authorization

API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Example:
Example

{}

Response

HTTP status code 200

현재 상태 전송 설정 취소 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

