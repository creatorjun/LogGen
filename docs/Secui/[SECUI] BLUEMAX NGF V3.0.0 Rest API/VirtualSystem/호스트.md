토폴로지 설정 REST API 문서

https://{host}/api/ho

host
host: required (string)
[IP]:[GUI 접속 PORT]
443	포트는	생략	가능

Example:
Example

192.168.100.100:443

토폴로지 설정

/host/topology

/host/topology/coordinate

/host/topology/layer

/host/topology/interface-candidates

/host/topology/vs-candidates

/host/topology/creation/status

VSI 정보

/host/vsi

/host/vsi/config-info

/host/vsi/{vs_id}

/host/vsi/{vs_id}/status

/host/vsi/{vs_id}/start

/host/vsi/{vs_id}/stop

VRI 정보

/host/vri

/host/vri/{vs_id}

/host/vri/config-info

VWI 정보

/host/vwi

/host/vwi/{vs_id}

/host/vwi/config-info

네트워크 연결 구성

/host/network

VS 정보

/host/vs/info

GB 표현 값으로 실제 lvm이 생성되는 bytes 값

/host/vs/real-size/{si_size}

VS Administrator 관리 VS 설정을 위한 VS 리스트

/host/vs/list-for-administrator

토폴로지 설정

GET

PUT

PUT

GET

GET

GET

POST

GET

GET

PUT

  DELETE

GET

POST

POST

POST

GET

PUT

  DELETE

GET

POST

GET

PUT

  DELETE

GET

POST

PUT

  DELETE

GET

GET

GET

GET

/host/topology

토폴로지을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

토폴로지 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

vsvs: (array of vs_type)
VS 노드 목록

Items: vs_type
Items

vs_id
vs_id: (integer)
VS ID

name
name: required (string - maxLength: 32)
VS 이름 (문자,숫자,'-','_'	조합)

sys_type
sys_type: required (one of PHY, VSI, VRI, VWI)
종류

modelmodel: required (string)
모델

x_crd
x_crd: required (integer - minimum: 0 - maximum: 2560)
X 좌표 값

y_crd: required (integer - minimum: 0 - maximum: 1800)
y_crd
Y 좌표 값

layer
layer: required (integer - minimum: 0 - maximum: 1000)
레이어 값

status
status: required (one of 0, 1)
VS 상태 (OFF:	0,	ON:	1)

phyphy: (array of phys_type)
VS 노드 목록

Items: phys_type
Items

if_name
if_name: required (string)
인터페이스 이름

x_crd
x_crd: (integer - minimum: 0 - maximum: 50)
X 좌표 값

line_crd
line_crd: required (integer - minimum: 0 - maximum: 50)
Y 좌표 값

lineline: (array of line_type)
VS 노드 목록

Items: line_type
Items

line_crd
line_crd: required (integer - minimum: 0 - maximum: 50)
Y 좌표 값

network
network: (array of network_type)
VS 노드 목록

Items: network_type
Items

vs_id
vs_id: required (integer)
VS ID

if_name
if_name: required (string)
인터페이스 이름

vs_if_status
vs_if_status: (array of vs_if_status_type)
VS 노드 목록

Items: vs_if_status_type
Items

vs_id
vs_id: required (integer)
VS ID

if_name: required (string)
if_name
인터페이스 이름

status
status: required (one of 0, 1)
인터페이스 상태 (OFF:	0,	ON:	1)

Example:
Example

{
		"code":	"ok",
		"result":	{
				"vs":	[
						{
								"vs_id":1,
								"name":"",
								"sys_type":"PHY",
								"model":"",
								"x_crd":1,
								"y_crd":1,
								"layer":1,
								"status":1
						}
				],
				"phy":	[
						{
								"if_name":"",
								"x_crd":1,
								"line_crd":1
						}
				],
				"line":	[
						{
								"line_crd":1
						}
				],
				"network":	[
						{
								"vs_id":1,
								"if_name":""
						}
				],
				"vs_if_status":	[
						{
								"vs_id":1,
								"if_name":"",
								"status":1
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/host/topology/coordinate

토폴로지 좌표을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: array of topology_set

Items: topology_set
Items

instance_type
instance_type: required (one of PHY, VS, LINE)
종류

vs_id
vs_id: (integer)
VS ID instance_type	:	VS

line_id
line_id: (integer)
라인 ID instance_type	:	PHY,LINE

if_name
if_name: (string)
인터페이스 이름 instance_type	:	PHY

x_crd: (integer - minimum: 0 - maximum: 50)
x_crd
X 좌표 값 instance_type	:	PHY,VS

y_crd
y_crd: (integer - minimum: 0 - maximum: 1800)
Y 좌표 값 instance_type	:	VS

line_crd
line_crd: (integer - minimum: 70 - maximum: 1730)
라인 좌표 값 instance_type	:	LINE

Example:
Example

[
		{
				"instance_type":"PHY",
				"if_name":"",
				"x_crd":1,
				"line_id":1
		},
		{
				"instance_type":"VS",
				"vs_id":1,
				"x_crd":1,
				"y_crd":1
		},
		{
				"instance_type":"LINE",
				"line_id":1,
				"line_crd":70
		}
]

Response

HTTP status code 200

토폴로지 좌표 수정 성공

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/host/topology/layer

토폴로지 레이어을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
vs_id
vs_id: required (integer)
VS ID

layer: required (integer - minimum: 0 - maximum: 1000)
layer

레이어

Example:
Example

{
		"vs_id":	1,
		"layer":	2
}

Response

HTTP status code 200

토폴로지 레이어 수정 성공

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/host/topology/interface-candidates

연결 가능 인터페이스 후보 목록을 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

qq: required (object)
파라미터(string)
q=속성 %3D 값 , 속성 %3D 값```

vs_id
vs_id: required (integer)
VS ID

sys_type
sys_type: required (one of PHY, VSI, VRI, VWI)
종류

Example:
Example

?q=vs_id%3D1,sys_type%3DPHY

Response

HTTP status code 200

연결 가능 인터페이스 후보 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (array of )
result
인터페이스 이름 목록

Example:
Example

{
		"code":	"ok",
		"result":	[
				"eth0",
				"eth1"
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/host/topology/vs-candidates

연결 가능 VS 후보 목록을 조회한다.

Request

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Query Parameters

qq: required (object)
파라미터(string)
q=속성 %3D 값 , 속성 %3D 값```

vs_id
vs_id: required (integer)
VS ID

Example:
Example

?q=vs_id%3D1

Response

HTTP status code 200

연결 가능 VS 후보 목록 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of VS 정보)
VS 정보 목록

Items: VS 정보
Items

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

name
name: required (string - maxLength: 32)
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

sys_type: required (one of PHY, VSI, VRI, VWI)
sys_type
종류

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"vs_id":	1,
						"sys_type":	"VSI",
						"name":	"secui"
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"dev_t":	"Authentication	Error"
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"dev_t":	"Authentication	Error",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/host/topology/creation/status

VS 생성 현황을 조회한다.

Request

Annotations

Lib.SupportedVersion:
Lib.SupportedVersion

"2.0"

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VS 생성 현황 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
VS 생성 현황
모델	별로	반환되는	VSI	모델이	상이함
NGF	1500:	VSI	100	~	VSI	1500
NGF	2000:	VSI	100	~	VSI	2000
NGF	5000:	VSI	100	~	VSI	5000
NGF	20000:	VSI	100	~	VSI	20000

사용	비율은	소수점	2자리까지	표시

VSIVSI: required (object)
VSI 현황

VSI 100
VSI 100: required (object)
NGF 100 모델

usage_count: required (number)
usage_count
해당 VS 사용 중인 갯수

usage_able_count
usage_able_count: required (number)
해당 VS 사용 가능 갯수

creation_proportion
creation_proportion: required (number)
VS 사용 비율

VSI 200
VSI 200: required (object)
NGF 200 모델

usage_count
usage_count: required (number)
해당 VS 사용 중인 갯수

usage_able_count
usage_able_count: required (number)
해당 VS 사용 가능 갯수

creation_proportion
creation_proportion: required (number)
VS 사용 비율

VSI 300: required (object)
VSI 300
NGF 300 모델

usage_count
usage_count: required (number)
해당 VS 사용 중인 갯수

usage_able_count
usage_able_count: required (number)
해당 VS 사용 가능 갯수

creation_proportion
creation_proportion: required (number)
VS 사용 비율

VSI 500
VSI 500: required (object)
NGF 500 모델

usage_count
usage_count: required (number)
해당 VS 사용 중인 갯수

usage_able_count
usage_able_count: required (number)
해당 VS 사용 가능 갯수

creation_proportion
creation_proportion: required (number)
VS 사용 비율

VSI 1000
VSI 1000: required (object)
NGF 1000 모델

usage_count: required (number)
usage_count
해당 VS 사용 중인 갯수

usage_able_count
usage_able_count: required (number)
해당 VS 사용 가능 갯수

creation_proportion
creation_proportion: required (number)
VS 사용 비율

VSI 1500
VSI 1500: required (object)
NGF 1500 모델

usage_count
usage_count: required (number)
해당 VS 사용 중인 갯수

usage_able_count: required (number)
usage_able_count
해당 VS 사용 가능 갯수

creation_proportion
creation_proportion: required (number)
VS 사용 비율

VRIVRI: required (object)
VRI 현황

usage_count
usage_count: required (number)
해당 VS 사용 중인 갯수

usage_able_count
usage_able_count: required (number)
해당 VS 사용 가능 갯수

creation_proportion
creation_proportion: required (number)
VS 사용 비율

VWIVWI: required (object)
VWI 현황

usage_count: required (number)
usage_count
해당 VS 사용 중인 갯수

usage_able_count
usage_able_count: required (number)
해당 VS 사용 가능 갯수

creation_proportion
creation_proportion: required (number)
VS 사용 비율

total_creation_proportion
total_creation_proportion: required (number)
전체 VS 사용 비율

Example:
Example

{
		"code":	"ok",
		"result":	{
				"VSI":	{
						"VSI	100":	{
								"usage_count":	0,
								"usage_able_count":	4,
								"creation_proportion":	0
						},
						"VSI	200":	{
								"usage_count":	0,
								"usage_able_count":	2,
								"creation_proportion":	0
						},
						"VSI	300":	{
								"usage_count":	0,
								"usage_able_count":	2,
								"creation_proportion":	0
						},
						"VSI	500":	{
								"usage_count":	0,
								"usage_able_count":	1,
								"creation_proportion":	0
						},
						"VSI	1000":	{
								"usage_count":	0,
								"usage_able_count":	1,
								"creation_proportion":	0
						},
						"VSI	1500":	{
								"usage_count":	0,
								"usage_able_count":	0,
								"creation_proportion":	0
						}
				},
				"VRI":	{
						"usage_count":	2,
						"usage_able_count":	4,
						"creation_proportion":	0.25
				},
				"VWI":	{
						"usage_count":	2,
						"usage_able_count":	4,
						"creation_proportion":	0.25
				},
				"total_creation_proportion":	0.5
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

VSI 정보

POST

/host/vsi

VSI 정보을(를) 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

system_info
system_info: required (object)
vs_id
vs_id: (integer)
VS ID

name
name: (string)
VS 이름

modelmodel: (string)
VS 모델

contents_db
contents_db: (one of 0, 1)
호스트 컨텐츠 DB 사용 (OFF:	0,	ON:	1)

boot_autostart: (one of 0, 1)
boot_autostart
부팅시 자동 시작 (OFF:	0,	ON:	1)

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: (integer - minimum: 0 - maximum: 2560)
X 좌표

y_crd
y_crd: (integer - minimum: 0 - maximum: 1800)
Y 좌표

layer
layer: (integer - minimum: 0 - maximum: 1000)
레이어

diffdiff: (array of any)
수정된 키 목록

system_resource
system_resource: required (object)
cpu_limit
cpu_limit: (integer - minimum: 0 - maximum: 100)
CPU 사용률 제한

memory
memory: (string)
메모리

log_disk
log_disk: (string)
로그 저장 공간

able_log_disk
able_log_disk: (string)
total_log_disk
total_log_disk: (string)
system_storage_partial_use
system_storage_partial_use: required (integer)
시스템 공간 사용 용도
0:	모두	사용,	1:	일부	사용

system_storage_size
system_storage_size: required (integer)
사용 시스템 공간
system_storage_partial_use  속성값이 1이면 사용 중인 공간을 반환
system_storage_partial_use  속성값이 0이면 0을 반환

diffdiff: (array of any)
수정된 키 목록

feature: required (object)
feature
modemode: (one of 1, 2)
운영 모드 (Basic:	1,	Power:	2)

default: (object)
default
DEFAULT FEATURE 정보

optional
optional: (object)
OPTIONAL FEATURE 정보

network
network: (array of 네트워크 구성)
Items: 네트워크 구성
Items

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

target_sys_type
target_sys_type: (one of PHY, VSI, VRI, VWI)
대상 종류

target_vs_id
target_vs_id: (integer)
대상 VS ID

target_name
target_name: (string)
대상 VS 이름

target_if_name: (string)
target_if_name
대상 VS 인터페이스 이름

descdesc: (string)
설명

able_value: (object)
able_value

able_sslvpn_user
able_sslvpn_user: (integer)
설정 가능한 동시 접속자 수

total_sslvpn_user
total_sslvpn_user: (integer)
전체 동시 접속자 수

able_tunnel_num: (integer)
able_tunnel_num
설정 가능한 터널 수

total_tunnel_num
total_tunnel_num: (integer)
전체 터널 수

able_device_num
able_device_num: (integer)
설정 가능한 단말 수

total_device_num
total_device_num: (integer)
전체 단말 수

able_log_disk
able_log_disk: (integer)
설정 가능한 로그 디스크 공간

total_log_disk
total_log_disk: (integer)
전체 로그 디스크 공간

no_feature
no_feature: (array of any)
설정 불가 Feature 목록

system_storage: required (object)
system_storage
시스템 스토리지 정보

total_system_storage
total_system_storage: required (integer)
전체 시스템 공간 정수
(소수점 사용 불가)

able_system_storage: required (integer)
able_system_storage
사용 가능한 시스템 공간
(소수점 사용 불가)

기본값  def_system_storage_size  속성값
최소값  min_system_storage_size  속성값

def_system_storage_size
def_system_storage_size: required (integer - default: 3)
시스템 공간 기본값

min_system_storage_size
min_system_storage_size: required (integer - default: 1)
시스템 공간 최솟값

able_model: (object)
able_model
Example:
Example

{
		"VSI	100":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	200":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	300":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	500":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	1000":	{
				"mem_size":	"",
				"log_size_def":	""
		}
}

diffdiff: (array of any)
수정된 키 목록

Example:
Example

{
		"system_info":	{},
		"system_resource":	{
				"system_storage_partial_use":	0,
				"system_storage_size":	1
		},
		"feature":	{}
}

Response

HTTP status code 200

VSI 정보 추가 성공

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/host/vsi/config-info

VSI 설정 가능 정보을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VSI 설정 가능 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

able_sslvpn_user
able_sslvpn_user: (integer)
설정 가능한 동시 접속자 수

total_sslvpn_user: (integer)
total_sslvpn_user
전체 동시 접속자 수

able_tunnel_num
able_tunnel_num: (integer)
설정 가능한 터널 수

total_tunnel_num
total_tunnel_num: (integer)
전체 터널 수

able_device_num
able_device_num: (integer)
설정 가능한 단말 수

total_device_num
total_device_num: (integer)
전체 단말 수

able_log_disk
able_log_disk: (integer)
설정 가능한 로그 디스크 공간

total_log_disk
total_log_disk: (integer)
전체 로그 디스크 공간

no_feature
no_feature: (array of any)
설정 불가 Feature 목록

system_storage
system_storage: required (object)
시스템 스토리지 정보

total_system_storage
total_system_storage: required (integer)
전체 시스템 공간 정수
(소수점 사용 불가)

able_system_storage
able_system_storage: required (integer)
사용 가능한 시스템 공간
(소수점 사용 불가)

기본값  def_system_storage_size  속성값
최소값  min_system_storage_size  속성값

def_system_storage_size
def_system_storage_size: required (integer - default: 3)
시스템 공간 기본값

min_system_storage_size
min_system_storage_size: required (integer - default: 1)
시스템 공간 최솟값

able_model
able_model: (object)
Example:
Example

{
		"VSI	100":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	200":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	300":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	500":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	1000":	{
				"mem_size":	"",
				"log_size_def":	""
		}
}

diffdiff: (array of any)
수정된 키 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"able_log_disk":	25.5,
				"total_log_disk":	51.5,
				"able_sslvpn_max_tunnel_num":	3,
				"total_sslvpn_max_tunnel_num":	3,
				"able_sslvpn_windows_num":	0,
				"total_sslvpn_windows_num":	0,
				"able_sslvpn_etc_num":	3,
				"total_sslvpn_etc_num":	3,
				"able_ipsecvpn_tunnel_num":	1500,
				"total_ipsecvpn_tunnel_num":	1500,
				"able_device_num":	7,
				"total_device_num":	7,
				"system_storage":	{
						"total_system_storage":	11,
						"able_system_storage":	11,
						"def_system_storage_size":	3,
						"min_system_storage_size":	1
				},
				"able_model":	{
						"VSI	100":	{
								"max_cpu_num":	1,
								"def_cpu_num":	1,
								"mem_size":	4,
								"log_space":	{
										"def_size":	0.2,
										"resize":	false
								},
								"max_sslvpn_tunnel_num":	100,
								"max_ipsecvpn_tunnel_num":	1500,
								"max_device_num":	300
						},
						"VSI	200":	{
								"max_cpu_num":	2,
								"def_cpu_num":	2,
								"mem_size":	4,
								"log_space":	{
										"def_size":	0.2,
										"resize":	false
								},
								"max_sslvpn_tunnel_num":	300,
								"max_ipsecvpn_tunnel_num":	2000,
								"max_device_num":	900
						},
						"VSI	300":	{
								"max_cpu_num":	4,
								"def_cpu_num":	4,
								"mem_size":	8,
								"log_space":	{
										"def_size":	100,
										"resize":	true
								},
								"max_sslvpn_tunnel_num":	500,
								"max_ipsecvpn_tunnel_num":	10000,
								"max_device_num":	1500
						},
						"VSI	500":	{
								"max_cpu_num":	6,
								"def_cpu_num":	6,
								"mem_size":	8,
								"log_space":	{
										"def_size":	100,
										"resize":	true
								},
								"max_sslvpn_tunnel_num":	800,
								"max_ipsecvpn_tunnel_num":	20000,
								"max_device_num":	2400
						},
						"VSI	1000":	{
								"max_cpu_num":	8,
								"def_cpu_num":	8,
								"mem_size":	8,
								"log_space":	{
										"def_size":	100,
										"resize":	true
								},
								"max_sslvpn_tunnel_num":	1000,
								"max_ipsecvpn_tunnel_num":	30000,
								"max_device_num":	3000
						}
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/host/vsi/{vs_id}

VSI 정보을(를) 조회한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent

User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VSI 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

system_info: required (object)
system_info
vs_id
vs_id: (integer)
VS ID

name
name: (string)
VS 이름

modelmodel: (string)
VS 모델

contents_db
contents_db: (one of 0, 1)
호스트 컨텐츠 DB 사용 (OFF:	0,	ON:	1)

boot_autostart
boot_autostart: (one of 0, 1)
부팅시 자동 시작 (OFF:	0,	ON:	1)

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: (integer - minimum: 0 - maximum: 2560)
X 좌표

y_crd
y_crd: (integer - minimum: 0 - maximum: 1800)
Y 좌표

layer
layer: (integer - minimum: 0 - maximum: 1000)
레이어

diffdiff: (array of any)
수정된 키 목록

system_resource
system_resource: required (object)
cpu_limit
cpu_limit: (integer - minimum: 0 - maximum: 100)
CPU 사용률 제한

memory: (string)
memory
메모리

log_disk
log_disk: (string)
로그 저장 공간

able_log_disk
able_log_disk: (string)
total_log_disk: (string)
total_log_disk
system_storage_partial_use
system_storage_partial_use: required (integer)
시스템 공간 사용 용도
0:	모두	사용,	1:	일부	사용

system_storage_size
system_storage_size: required (integer)
사용 시스템 공간
system_storage_partial_use  속성값이 1이면 사용 중인 공간을 반환
system_storage_partial_use  속성값이 0이면 0을 반환

diffdiff: (array of any)
수정된 키 목록

feature: required (object)
feature
modemode: (one of 1, 2)
운영 모드 (Basic:	1,	Power:	2)

default
default: (object)
DEFAULT FEATURE 정보

optional: (object)
optional
OPTIONAL FEATURE 정보

network
network: (array of 네트워크 구성)
Items: 네트워크 구성
Items

if_name: (string)
if_name
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

target_sys_type
target_sys_type: (one of PHY, VSI, VRI, VWI)
대상 종류

target_vs_id
target_vs_id: (integer)
대상 VS ID

target_name
target_name: (string)
대상 VS 이름

target_if_name
target_if_name: (string)
대상 VS 인터페이스 이름

descdesc: (string)
설명

able_value: (object)
able_value

able_sslvpn_user
able_sslvpn_user: (integer)
설정 가능한 동시 접속자 수

total_sslvpn_user
total_sslvpn_user: (integer)
전체 동시 접속자 수

able_tunnel_num
able_tunnel_num: (integer)
설정 가능한 터널 수

total_tunnel_num
total_tunnel_num: (integer)
전체 터널 수

able_device_num
able_device_num: (integer)
설정 가능한 단말 수

total_device_num: (integer)
total_device_num
전체 단말 수

able_log_disk
able_log_disk: (integer)
설정 가능한 로그 디스크 공간

total_log_disk
total_log_disk: (integer)
전체 로그 디스크 공간

no_feature
no_feature: (array of any)
설정 불가 Feature 목록

system_storage
system_storage: required (object)
시스템 스토리지 정보

total_system_storage
total_system_storage: required (integer)
전체 시스템 공간 정수
(소수점 사용 불가)

able_system_storage
able_system_storage: required (integer)
사용 가능한 시스템 공간
(소수점 사용 불가)

기본값  def_system_storage_size  속성값
최소값  min_system_storage_size  속성값

def_system_storage_size
def_system_storage_size: required (integer - default: 3)
시스템 공간 기본값

min_system_storage_size
min_system_storage_size: required (integer - default: 1)
시스템 공간 최솟값

able_model
able_model: (object)
Example:
Example

{
		"VSI	100":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	200":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	300":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	500":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	1000":	{
				"mem_size":	"",
				"log_size_def":	""
		}
}

diffdiff: (array of any)
수정된 키 목록

Example:
Example

{
		"code":	"ok",
		"result":	{
				"system_info":	{
						"name":	"VSI100_TEST",
						"model":	"VSI	100",
						"contents_db":	1,
						"boot_autostart":	1,
						"desc":	"",
						"x_crd":	150,
						"y_crd":	240,
						"layer":	0,
						"if_num":	20
				},
				"system_resource":	{
						"cpu_limit":	100,
						"log_disk":	0.2,
						"cpu_partial_use":	0,
						"cpu_partial_num":	0,

						"system_storage_partial_use":	0,
						"system_storage_size":	0,
						"memory":	4,
						"cpu_list":	[]
				},
				"feature":	{
						"LicenseInfo":	{
								"SWType":	"Basic"
						},
						"SWFeature":	{
								"Firewall":	{
										"id":	1,
										"Use":	true
								},
								"SSL	VPN":	{
										"id":	2,
										"Use":	true,
										"Option":	"0,0,0"
								},
								"IPSec	VPN":	{
										"id":	3,
										"Use":	false
								},
								"IPS/DDoS":	{
										"id":	4,
										"Use":	false
								},
								"Anti-virus":	{
										"id":	5,
										"Use":	false
								},
								"Anti-spam":	{
										"id":	6,
										"Use":	false
								},
								"DLP":	{
										"id":	7,
										"Use":	false
								},
								"URL	Filter":	{
										"id":	8,
										"Use":	true
								},
								"Advanced	URL	Filter":	{
										"id":	9,
										"Use":	false
								},
								"OfficeKeeper":	{
										"id":	10,
										"Use":	false
								},
								"App	Control":	{
										"id":	11,
										"Use":	false
								},
								"User	Control":	{
										"id":	12,
										"Use":	false
								},
								"Virtual	System":	{
										"id":	13,
										"Use":	false
								},
								"APT":	{
										"id":	14,
										"Use":	false
								},
								"Device	Control":	{
										"id":	15,
										"Use":	false
								}
						}
				},
				"network":	[],
				"able_value":	{
						"able_log_disk":	25.5,
						"total_log_disk":	51.5,
						"able_sslvpn_max_tunnel_num":	3,
						"total_sslvpn_max_tunnel_num":	3,
						"able_sslvpn_windows_num":	0,
						"total_sslvpn_windows_num":	0,
						"able_sslvpn_etc_num":	3,
						"total_sslvpn_etc_num":	3,
						"able_ipsecvpn_tunnel_num":	1500,
						"total_ipsecvpn_tunnel_num":	1500,
						"able_device_num":	7,
						"total_device_num":	7,
						"system_storage":	{
								"total_system_storage":	11,
								"able_system_storage":	11,
								"def_system_storage_size":	3,
								"min_system_storage_size":	1
						},
						"able_model":	{
								"VSI	100":	{
										"max_cpu_num":	1,
										"def_cpu_num":	1,
										"mem_size":	4,
										"log_space":	{

												"def_size":	0.2,
												"resize":	false
										},
										"max_sslvpn_tunnel_num":	100,
										"max_ipsecvpn_tunnel_num":	1500,
										"max_device_num":	300
								},
								"VSI	200":	{
										"max_cpu_num":	2,
										"def_cpu_num":	2,
										"mem_size":	4,
										"log_space":	{
												"def_size":	0.2,
												"resize":	false
										},
										"max_sslvpn_tunnel_num":	300,
										"max_ipsecvpn_tunnel_num":	2000,
										"max_device_num":	900
								},
								"VSI	300":	{
										"max_cpu_num":	4,
										"def_cpu_num":	4,
										"mem_size":	8,
										"log_space":	{
												"def_size":	100,
												"resize":	true
										},
										"max_sslvpn_tunnel_num":	500,
										"max_ipsecvpn_tunnel_num":	10000,
										"max_device_num":	1500
								},
								"VSI	500":	{
										"max_cpu_num":	6,
										"def_cpu_num":	6,
										"mem_size":	8,
										"log_space":	{
												"def_size":	100,
												"resize":	true
										},
										"max_sslvpn_tunnel_num":	800,
										"max_ipsecvpn_tunnel_num":	20000,
										"max_device_num":	2400
								},
								"VSI	1000":	{
										"max_cpu_num":	8,
										"def_cpu_num":	8,
										"mem_size":	8,
										"log_space":	{
												"def_size":	100,
												"resize":	true
										},
										"max_sslvpn_tunnel_num":	1000,
										"max_ipsecvpn_tunnel_num":	30000,
										"max_device_num":	3000
								}
						}
				},
				"min_reduce_log_size":	0
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/host/vsi/{vs_id}

VSI 정보을(를) 수정한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

system_info
system_info: required (object)
vs_id
vs_id: (integer)
VS ID

name
name: (string)
VS 이름

modelmodel: (string)
VS 모델

contents_db: (one of 0, 1)
contents_db
호스트 컨텐츠 DB 사용 (OFF:	0,	ON:	1)

boot_autostart
boot_autostart: (one of 0, 1)
부팅시 자동 시작 (OFF:	0,	ON:	1)

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: (integer - minimum: 0 - maximum: 2560)
X 좌표

y_crd
y_crd: (integer - minimum: 0 - maximum: 1800)
Y 좌표

layer
layer: (integer - minimum: 0 - maximum: 1000)
레이어

diffdiff: (array of any)
수정된 키 목록

system_resource
system_resource: required (object)
cpu_limit
cpu_limit: (integer - minimum: 0 - maximum: 100)
CPU 사용률 제한

memory
memory: (string)
메모리

log_disk
log_disk: (string)
로그 저장 공간

able_log_disk
able_log_disk: (string)
total_log_disk
total_log_disk: (string)
system_storage_partial_use: required (integer)
system_storage_partial_use
시스템 공간 사용 용도
0:	모두	사용,	1:	일부	사용

system_storage_size
system_storage_size: required (integer)
사용 시스템 공간
system_storage_partial_use  속성값이 1이면 사용 중인 공간을 반환
system_storage_partial_use  속성값이 0이면 0을 반환

diffdiff: (array of any)
수정된 키 목록

feature: required (object)
feature
modemode: (one of 1, 2)
운영 모드 (Basic:	1,	Power:	2)

default
default: (object)
DEFAULT FEATURE 정보

optional
optional: (object)
OPTIONAL FEATURE 정보

network
network: (array of 네트워크 구성)
Items: 네트워크 구성
Items

if_name
if_name: (string)
인터페이스 이름

trunk_if_name: (string)
trunk_if_name
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

target_sys_type
target_sys_type: (one of PHY, VSI, VRI, VWI)
대상 종류

target_vs_id
target_vs_id: (integer)
대상 VS ID

target_name: (string)
target_name
대상 VS 이름

target_if_name
target_if_name: (string)
대상 VS 인터페이스 이름

descdesc: (string)
설명

able_value: (object)
able_value

able_sslvpn_user
able_sslvpn_user: (integer)
설정 가능한 동시 접속자 수

total_sslvpn_user: (integer)
total_sslvpn_user
전체 동시 접속자 수

able_tunnel_num
able_tunnel_num: (integer)
설정 가능한 터널 수

total_tunnel_num
total_tunnel_num: (integer)
전체 터널 수

able_device_num
able_device_num: (integer)
설정 가능한 단말 수

total_device_num
total_device_num: (integer)
전체 단말 수

able_log_disk
able_log_disk: (integer)
설정 가능한 로그 디스크 공간

total_log_disk
total_log_disk: (integer)
전체 로그 디스크 공간

no_feature: (array of any)
no_feature
설정 불가 Feature 목록

system_storage
system_storage: required (object)
시스템 스토리지 정보

total_system_storage
total_system_storage: required (integer)
전체 시스템 공간 정수
(소수점 사용 불가)

able_system_storage
able_system_storage: required (integer)
사용 가능한 시스템 공간
(소수점 사용 불가)

기본값  def_system_storage_size  속성값
최소값  min_system_storage_size  속성값

def_system_storage_size
def_system_storage_size: required (integer - default: 3)
시스템 공간 기본값

min_system_storage_size
min_system_storage_size: required (integer - default: 1)
시스템 공간 최솟값

able_model
able_model: (object)
Example:
Example

{
		"VSI	100":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	200":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	300":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	500":	{
				"mem_size":	"",
				"log_size_def":	""
		},
		"VSI	1000":	{
				"mem_size":	"",
				"log_size_def":	""
		}
}

diffdiff: (array of any)
수정된 키 목록

Example:
Example

{
		"system_info":	{},
		"system_resource":	{
				"system_storage_partial_use":	0,
				"system_storage_size":	1
		},
		"feature":	{},
		"diff":	["key1","key2"]
}

Response

HTTP status code 200

VSI 정보 수정 성공

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/host/vsi/{vs_id}

VSI 정보을(를) 삭제한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VSI 정보 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/host/vsi/{vs_id}/status

VSI 현재 상태을(를) 조회한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VSI 현재 상태 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
name
name: (string)
VS 이름

modelmodel: (string)
??

running_time
running_time: (string)
동작 시간

ha_mode
ha_mode: (one of 1, 2, 3, 4)
HA 동작 모드 (stand-alone:	1,	active-active:	2,	active-standby:	3,	high-level-sync:	4)

ha_status
ha_status: (one of 1, 2)
HA 멤버 상태 (정상:	1,	비정상:	2)

descdesc: (string)
설명

graph_time
graph_time: (string)
자원 사용 현황 및 트래픽 현황 시간

resource_usage: (string)
resource_usage
자원 사용 현황 데이터

traffic_usage
traffic_usage: (string)
트래픽 현황

Example:
Example

{
		"code":	"ok",
		"result":	{
				"name":"",
				"model":"",
				"running_time":"",
				"ha_mode":1,
				"ha_status":1,
				"desc":"",
				"graph_time":"",
				"resource_usage":"",
				"traffic_usage":""
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

POST

/host/vsi/{vs_id}/start

VSI를 시작 한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

POST

/host/vsi/{vs_id}/stop

VSI를 중지 한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VRI 정보

POST

/host/vri

VRI 정보을(를) 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

system_info: required (object)
system_info

name
name: required (string - maxLength: 32)
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: required (integer - minimum: 0 - maximum: 2560)
X 좌표 값

y_crd: required (integer - minimum: 0 - maximum: 1800)
y_crd
Y 좌표 값

layer
layer: required (integer - minimum: 0 - maximum: 1000)
아이콘 레이어 값

system_resource
system_resource: required (object)
cpu_list: (array of CPU)
cpu_list
CPU 정보 목록

Items: CPU
Items

processor_num
processor_num: required (integer)
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num
cpu_num: required (integer)
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type: required (integer - default: 0)
cpu_type
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num
cpu_partial_num: required (integer)
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1
VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use
cpu_partial_use: required (integer - default: 1)
CPU 사용 범위
0:	OFF,	1:	ON

Example:
Example

{
		"system_info":	{
				"name":	"VRI_TEST1",
				"desc":	"VRI	설명",
				"x_crd":	530,
				"y_crd":	160,
				"layer":	0
		},
		"system_resource":	{
				"cpu_list":	[
						{
								"processor_num":	2,
								"cpu_num":	4,
								"cpu_type":	1
						}
				],
				"cpu_partial_num":	4,
				"cpu_partial_use":	1
		}
}

Response

HTTP status code 200

VRI 정보 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
결과 데이터

system_info: required (object)
system_info

name
name: required (string - maxLength: 32)
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: required (integer - minimum: 0 - maximum: 2560)
X 좌표 값

y_crd
y_crd: required (integer - minimum: 0 - maximum: 1800)
Y 좌표 값

layer
layer: required (integer - minimum: 0 - maximum: 1000)
아이콘 레이어 값

system_resource
system_resource: required (object)
cpu_list
cpu_list: (array of CPU)
CPU 정보 목록

Items: CPU
Items

processor_num
processor_num: required (integer)
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num: required (integer)
cpu_num
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type
cpu_type: required (integer - default: 0)
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num
cpu_partial_num: required (integer)
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1
VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use
cpu_partial_use: required (integer - default: 1)
CPU 사용 범위
0:	OFF,	1:	ON

memory
memory: required (integer)
메모리 배정량 값

network
network: required (array of 네트워크 구성)
네트워크 구성 목록

Items: 네트워크 구성
Items

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

target_sys_type
target_sys_type: (one of PHY, VSI, VRI, VWI)
대상 종류

target_vs_id
target_vs_id: (integer)
대상 VS ID

target_name
target_name: (string)
대상 VS 이름

target_if_name
target_if_name: (string)
대상 VS 인터페이스 이름

descdesc: (string)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"system_info":	{
						"name":	"VRI_TEST1",
						"desc":	"VRI	설명",
						"x_crd":	530,
						"y_crd":	160,
						"layer":	0
				},
				"system_resource":	{
						"cpu_list":	[
								{
										"processor_num":	2,
										"cpu_num":	4,
										"cpu_type":	1
								}
						],
						"cpu_partial_num":	4,
						"cpu_partial_use":	1,
						"memory":	4
				},
				"network":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/host/vri/{vs_id}

VRI 정보을(를) 조회한다.

Request

URI Parameters

vs_id: required (integer)
vs_id
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VRI 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
VRI 설정 정보

system_info: required (object)
system_info

name: required (string - maxLength: 32)
name
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: required (integer - minimum: 0 - maximum: 2560)
X 좌표 값

y_crd
y_crd: required (integer - minimum: 0 - maximum: 1800)
Y 좌표 값

layer: required (integer - minimum: 0 - maximum: 1000)
layer
아이콘 레이어 값

system_resource
system_resource: required (object)
cpu_list
cpu_list: (array of CPU)
CPU 정보 목록

Items: CPU
Items

processor_num
processor_num: required (integer)
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num
cpu_num: required (integer)
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type
cpu_type: required (integer - default: 0)
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num
cpu_partial_num: required (integer)
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1
VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use: required (integer - default: 1)
cpu_partial_use
CPU 사용 범위
0:	OFF,	1:	ON

memory
memory: required (integer)
메모리 배정량 값

network: required (array of 네트워크 구성)
network
네트워크 구성 목록

Items: 네트워크 구성
Items

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid: (integer - minimum: 1 - maximum: 4094)
vlanid
VLAN ID

target_sys_type
target_sys_type: (one of PHY, VSI, VRI, VWI)
대상 종류

target_vs_id
target_vs_id: (integer)
대상 VS ID

target_name
target_name: (string)
대상 VS 이름

target_if_name
target_if_name: (string)
대상 VS 인터페이스 이름

descdesc: (string)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"system_info":	{
						"name":	"VRI_TEST1",
						"desc":	"VRI	설명",
						"x_crd":	530,
						"y_crd":	160,
						"layer":	0
				},
				"system_resource":	{
						"cpu_list":	[
								{
										"processor_num":	2,
										"cpu_num":	4,
										"cpu_type":	1
								}
						],
						"cpu_partial_num":	4,
						"cpu_partial_use":	1,
						"memory":	4
				},
				"network":	[
						{
								"if_name":	"eth0",
								"trunk_if_name":	"",
								"vlan_type":	1,
								"vlanid":	1,
								"target_sys_type":	"PHY",
								"target_vs_id":	1,
								"target_name":	"Physical",
								"target_if_name":	"eth1",
								"desc":	""
						}
				]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/host/vri/{vs_id}

VRI 정보을(를) 수정한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

descdesc: required (string - maxLength: 256)
설명

system_resource: required (object)
system_resource
시스템 자원

cpu_list
cpu_list: (array of CPU)
CPU 정보 목록

Items: CPU
Items

processor_num
processor_num: required (integer)
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num
cpu_num: required (integer)
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type
cpu_type: required (integer - default: 0)
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num
cpu_partial_num: required (integer)
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1

VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use
cpu_partial_use: required (integer - default: 1)
CPU 사용 범위
0:	OFF,	1:	ON

diffdiff: required (array of string)
변경된 속성 이름 목록

Example:
Example

["cpu_list",	"cpu_partial_use"]

diffdiff: required (array of string)
변경된 속성 이름 목록

Example:
Example

["desc",	"system_resource"]

Example:
Example

{
		"desc":	"설명",
		"system_resource":	{
				"cpu_list":	[
						{
								"processor_num":	2,
								"cpu_num":	4,
								"cpu_type":	1
						}
				],
				"cpu_partial_num":	4,
				"cpu_partial_use":	1,
				"diff":	["cpu_list"]
		},
		"diff":	["desc",	"system_resource"]
}

Response

HTTP status code 200

VRI 정보 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
VRI 설정 정보

system_info: required (object)
system_info

name: required (string - maxLength: 32)
name
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: required (integer - minimum: 0 - maximum: 2560)
X 좌표 값

y_crd
y_crd: required (integer - minimum: 0 - maximum: 1800)
Y 좌표 값

layer: required (integer - minimum: 0 - maximum: 1000)
layer
아이콘 레이어 값

system_resource
system_resource: required (object)
cpu_list
cpu_list: (array of CPU)
CPU 정보 목록

Items: CPU
Items

processor_num
processor_num: required (integer)
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num
cpu_num: required (integer)
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type
cpu_type: required (integer - default: 0)
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num
cpu_partial_num: required (integer)
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1

VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use
cpu_partial_use: required (integer - default: 1)
CPU 사용 범위
0:	OFF,	1:	ON

memory
memory: required (integer)
메모리 배정량 값

network
network: required (array of 네트워크 구성)
네트워크 구성 목록

Items: 네트워크 구성
Items

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

target_sys_type
target_sys_type: (one of PHY, VSI, VRI, VWI)
대상 종류

target_vs_id
target_vs_id: (integer)
대상 VS ID

target_name
target_name: (string)
대상 VS 이름

target_if_name
target_if_name: (string)
대상 VS 인터페이스 이름

descdesc: (string)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"system_info":	{
						"name":	"VRI_TEST1",
						"desc":	"VRI	설명",
						"x_crd":	530,
						"y_crd":	160,
						"layer":	0
				},
				"system_resource":	{
						"cpu_list":	[
								{
										"processor_num":	2,
										"cpu_num":	4,
										"cpu_type":	1
								}
						],
						"cpu_partial_num":	4,
						"cpu_partial_use":	1,
						"memory":	4
				},
				"network":	[
						{
								"if_name":	"eth0",
								"trunk_if_name":	"",
								"vlan_type":	1,
								"vlanid":	1,
								"target_sys_type":	"PHY",
								"target_vs_id":	1,
								"target_name":	"Physical",
								"target_if_name":	"eth1",
								"desc":	""
						}
				]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/host/vri/{vs_id}

VRI 정보을(를) 삭제한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VRI 정보 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/host/vri/config-info

VRI 설정 가능 정보을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VRI 설정 가능 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
max_cpu_num: required (integer)
max_cpu_num
모델 별 최대 CPU 수

def_cpu_num
def_cpu_num: required (integer)
모델 별 기본 CPU 갯수

mem_size
mem_size: required (integer)
모델 별 메모리 배정량

Example:
Example

{
		"code":	"ok",
		"result":	{
				"max_cpu_num":	16,
				"def_cpu_num":	1,
				"mem_size":	4
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

VWI 정보

POST

/host/vwi

VWI 정보를 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent: required (string)
User-Agent
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

system_info: required (object)
system_info

name
name: required (string - maxLength: 32)
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: required (integer - minimum: 0 - maximum: 2560)
X 좌표 값

y_crd
y_crd: required (integer - minimum: 0 - maximum: 1800)
Y 좌표 값

layer
layer: required (integer - minimum: 0 - maximum: 1000)
아이콘 레이어 값

modelmodel: required (one of Basic Switch, L3 Link Switch, VLAN Switch - default: Basic Switch)
VWI의 모델 구분
Basic	Switch:	기본	스위
L3	Link	Switch:	L3	연결	전용	스위치
VLAN	Switch:	VLAN	전용	스위치

system_resource: required (object)
system_resource
cpu_list
cpu_list: (array of CPU)
CPU 정보 목록

Items: CPU
Items

processor_num
processor_num: required (integer)
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num
cpu_num: required (integer)
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type
cpu_type: required (integer - default: 0)
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num
cpu_partial_num: required (integer)
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1
VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use
cpu_partial_use: required (integer - default: 1)
CPU 사용 범위
0:	OFF,	1:	ON

Example:
Example

{
		"system_info":	{
				"name":	"VWI_TEST1",
				"model":	"Basic	Switch",
				"desc":	"",
				"x_crd":	570,
				"y_crd":	180,
				"layer":	0
		},
		"system_resource":	{
				"cpu_list":	[
						{
								"processor_num":	1,
								"cpu_num":	4,
								"cpu_type":	1
						}
				],
				"cpu_partial_num":	1,
				"cpu_partial_use":	0
		}
}

Response

HTTP status code 200

VWI 정보 추가 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

system_info: required (object)
system_info

name
name: required (string - maxLength: 32)
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: required (integer - minimum: 0 - maximum: 2560)
X 좌표 값

y_crd
y_crd: required (integer - minimum: 0 - maximum: 1800)
Y 좌표 값

layer
layer: required (integer - minimum: 0 - maximum: 1000)
아이콘 레이어 값

modelmodel: required (one of Basic Switch, L3 Link Switch, VLAN Switch - default: Basic Switch)
VWI의 모델 구분
Basic	Switch:	기본	스위
L3	Link	Switch:	L3	연결	전용	스위치
VLAN	Switch:	VLAN	전용	스위치

system_resource
system_resource: required (object)
cpu_list
cpu_list: (array of CPU)
CPU 정보 목록

Items: CPU
Items

processor_num
processor_num: required (integer)
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num
cpu_num: required (integer)
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type
cpu_type: required (integer - default: 0)
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num
cpu_partial_num: required (integer)
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1
VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use
cpu_partial_use: required (integer - default: 1)
CPU 사용 범위
0:	OFF,	1:	ON

memory
memory: required (integer)
메모리 배정량 값

network
network: required (array of 네트워크 구성)
네트워크 구성 목록

Items: 네트워크 구성
Items

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type: (one of 1, 2)
vlan_type
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

target_sys_type
target_sys_type: (one of PHY, VSI, VRI, VWI)
대상 종류

target_vs_id
target_vs_id: (integer)
대상 VS ID

target_name
target_name: (string)
대상 VS 이름

target_if_name
target_if_name: (string)
대상 VS 인터페이스 이름

descdesc: (string)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"system_info":	{
						"name":	"VWI_TEST1",
						"model":	"Basic	Switch",
						"desc":	"",
						"x_crd":	570,
						"y_crd":	180,
						"layer":	0
				},
				"system_resource":	{
								"cpu_partial_use":	0,
								"cpu_partial_num":	1,
								"memory":	4
				},
				"network":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

GET

/host/vwi/{vs_id}

VWI 정보을(를) 조회한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization: required (string)
Authorization
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VWI 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

system_info: required (object)
system_info

name
name: required (string - maxLength: 32)
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

descdesc: (string - maxLength: 256)

설명

x_crd
x_crd: required (integer - minimum: 0 - maximum: 2560)
X 좌표 값

y_crd: required (integer - minimum: 0 - maximum: 1800)
y_crd
Y 좌표 값

layer
layer: required (integer - minimum: 0 - maximum: 1000)
아이콘 레이어 값

modelmodel: required (one of Basic Switch, L3 Link Switch, VLAN Switch - default: Basic Switch)
VWI의 모델 구분
Basic	Switch:	기본	스위
L3	Link	Switch:	L3	연결	전용	스위치
VLAN	Switch:	VLAN	전용	스위치

system_resource
system_resource: required (object)
cpu_list: (array of CPU)
cpu_list
CPU 정보 목록

Items: CPU
Items

processor_num
processor_num: required (integer)
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num
cpu_num: required (integer)
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type: required (integer - default: 0)
cpu_type
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num
cpu_partial_num: required (integer)
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1
VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use
cpu_partial_use: required (integer - default: 1)
CPU 사용 범위
0:	OFF,	1:	ON

memory: required (integer)
memory
메모리 배정량 값

network
network: required (array of 네트워크 구성)
네트워크 구성 목록

Items: 네트워크 구성
Items

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

target_sys_type
target_sys_type: (one of PHY, VSI, VRI, VWI)
대상 종류

target_vs_id
target_vs_id: (integer)
대상 VS ID

target_name
target_name: (string)
대상 VS 이름

target_if_name
target_if_name: (string)
대상 VS 인터페이스 이름

descdesc: (string)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"system_info":	{
						"name":	"VWI_TEST1",
						"model":	"Basic	Switch",
						"desc":	"",
						"x_crd":	570,
						"y_crd":	180,
						"layer":	0
				},
				"system_resource":	{
						"cpu_partial_use":	0,
						"cpu_partial_num":	1,
						"memory":	4
				},
				"network":	[]
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

PUT

/host/vwi/{vs_id}

VWI 정보을(를) 수정한다.

Request

URI Parameters

vs_id: required (integer)
vs_id
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

descdesc: required (string - maxLength: 256)
설명

system_resource
system_resource: required (object)
시스템 자원

cpu_list
cpu_list: (array of CPU)
CPU 정보 목록

Items: CPU
Items

processor_num: required (integer)
processor_num
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num
cpu_num: required (integer)
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type
cpu_type: required (integer - default: 0)
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num: required (integer)
cpu_partial_num
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1
VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use
cpu_partial_use: required (integer - default: 1)
CPU 사용 범위
0:	OFF,	1:	ON

diffdiff: required (array of string)
변경된 속성 이름 목록

Example:
Example

["cpu_list",	"cpu_partial_use"]

diffdiff: required (array of string)
변경된 속성 이름 목록

Example:
Example

["desc",	"system_resource"]

Example:
Example

{
		"desc":	"설명",
		"system_resource":	{
				"cpu_list":	[
						{
								"processor_num":	2,
								"cpu_num":	4,
								"cpu_type":	1
						}
				],
				"cpu_partial_num":	4,
				"cpu_partial_use":	1,
				"diff":	["cpu_list"]
		},
		"diff":	["desc",	"system_resource"]
}

Response

HTTP status code 200

VWI 정보 수정 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

system_info: required (object)
system_info

name
name: required (string - maxLength: 32)
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

descdesc: (string - maxLength: 256)
설명

x_crd
x_crd: required (integer - minimum: 0 - maximum: 2560)
X 좌표 값

y_crd: required (integer - minimum: 0 - maximum: 1800)
y_crd
Y 좌표 값

layer
layer: required (integer - minimum: 0 - maximum: 1000)
아이콘 레이어 값

modelmodel: required (one of Basic Switch, L3 Link Switch, VLAN Switch - default: Basic Switch)
VWI의 모델 구분
Basic	Switch:	기본	스위
L3	Link	Switch:	L3	연결	전용	스위치
VLAN	Switch:	VLAN	전용	스위치

system_resource
system_resource: required (object)
cpu_list
cpu_list: (array of CPU)
CPU 정보 목록

Items: CPU
Items

processor_num
processor_num: required (integer)
프로세서 번호(모델 별 상이)
NGF	2000:	2
NGF	5000:	2
NGF	20000:	2

cpu_num
cpu_num: required (integer)
CPU 번호(모델 별 CPU 갯수 내 선택)

cpu_type
cpu_type: required (integer - default: 0)
CPU 타입 (물리/논리)
0:	물리,	1:	논리

cpu_partial_num
cpu_partial_num: required (integer)
CPU 사용 개수(기본값: 모델 별 최댓값)
최소	개수:	1
최대	개수:	모델	별	상이함
VSI	100:	1
VSI	200:	2
VSI	300:	4
VSI	500:	6
VSI	1000:	8

cpu_partial_use
cpu_partial_use: required (integer - default: 1)
CPU 사용 범위
0:	OFF,	1:	ON

memory
memory: required (integer)
메모리 배정량 값

network
network: required (array of 네트워크 구성)
네트워크 구성 목록

Items: 네트워크 구성
Items

if_name: (string)
if_name
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

target_sys_type
target_sys_type: (one of PHY, VSI, VRI, VWI)
대상 종류

target_vs_id: (integer)
target_vs_id
대상 VS ID

target_name
target_name: (string)
대상 VS 이름

target_if_name
target_if_name: (string)
대상 VS 인터페이스 이름

descdesc: (string)
설명

Example:
Example

{
		"code":	"ok",
		"result":	{
				"system_info":	{
						"name":	"VWI_TEST1",
						"model":	"Basic	Switch",
						"desc":	"",
						"x_crd":	570,
						"y_crd":	180,
						"layer":	0
				},
				"system_resource":	{
						"cpu_partial_use":	0,
						"cpu_partial_num":	1,
						"memory":	4
				},
				"network":	[]
		}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/host/vwi/{vs_id}

VWI 정보을(를) 삭제한다.

Request

URI Parameters

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VWI 정보 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GET

/host/vwi/config-info

VWI 설정 가능 정보을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VWI 설정 가능 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result
max_cpu_num
max_cpu_num: required (integer)
모델 별 최대 CPU 수

def_cpu_num
def_cpu_num: required (integer)
모델 별 기본 CPU 갯수

mem_size: required (integer)
mem_size
모델 별 메모리 배정량

Example:
Example

{
		"code":	"ok",
		"result":	{
				"max_cpu_num":	16,
				"def_cpu_num":	1,
				"mem_size":	4
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

네트워크 연결 구성

POST

/host/network

네트워크 연결 구성을(를) 추가한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

if_info1: required (object)
if_info1

sys_type
sys_type: (one of PHY, VSI, VRI, VWI)
종류

vs_id
vs_id: (integer)
VS ID

if_name
if_name: (string)
인터페이스 이름

trunk_if_name: (string)
trunk_if_name
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

diff_dic: (string)
diff_dic
if_info2: required (object)
if_info2

sys_type
sys_type: (one of PHY, VSI, VRI, VWI)
종류

vs_id: (integer)
vs_id
VS ID

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

diff_dic: (string)
diff_dic

descdesc: required (string)

Example:
Example

{
		"if_info1":	{
				"sys_type":	"",
				"vs_id":	"",
				"if_name":	"",
				"trunk_if_name":	"",
				"vlan_type":	"",
				"vlanid":	""
		},
		"if_info2":	{
				"sys_type":	"",
				"vs_id":	"",
				"if_name":	"",
				"trunk_if_name":	"",
				"vlan_type":	"",
				"vlanid":	""
		},
		"desc":	""
}

Response

HTTP status code 200

네트워크 연결 구성 추가 성공

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"11000",
		"message":	"중복되는	항목이	존재합니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

PUT

/host/network

네트워크 연결 구성을(를) 수정한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

if_info1: required (object)
if_info1

sys_type
sys_type: (one of PHY, VSI, VRI, VWI)
종류

vs_id
vs_id: (integer)
VS ID

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type: (one of 1, 2)
vlan_type
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

diff_dic: (string)
diff_dic

if_info2: required (object)
if_info2

sys_type
sys_type: (one of PHY, VSI, VRI, VWI)
종류

vs_id: (integer)
vs_id
VS ID

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

diff_dic: (string)
diff_dic

descdesc: required (string)
diffdiff: required (array of )

Example:
Example

{
		"if_info1":	{
				"sys_type":	"",
				"vs_id":	"",
				"if_name":	"",
				"trunk_if_name":	"",
				"vlan_type":	"",
				"vlanid":	"",
				"diff_dict":	{}
		},
		"if_info2":	{
				"sys_type":	"",
				"vs_id":	"",
				"if_name":	"",
				"trunk_if_name":	"",
				"vlan_type":	"",
				"vlanid":	"",
				"diff_dict":	{}
		},
		"desc":	"",
		"diff":[]
}

Response

HTTP status code 200

네트워크 연결 구성 수정 성공

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"잘못된	값이	입력되었습니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

DELETE

/host/network

네트워크 연결 구성을(를) 삭제한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties

if_info1: required (object)
if_info1

sys_type
sys_type: (one of PHY, VSI, VRI, VWI)
종류

vs_id
vs_id: (integer)
VS ID

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

diff_dic: (string)
diff_dic
if_info2: required (object)
if_info2

sys_type
sys_type: (one of PHY, VSI, VRI, VWI)
종류

vs_id
vs_id: (integer)
VS ID

if_name
if_name: (string)
인터페이스 이름

trunk_if_name
trunk_if_name: (string)
트렁크 인터페이스 이름

vlan_type
vlan_type: (one of 1, 2)
VLAN 타입 (VLAN	트렁크:	1,	VLAN	ID:	2)

vlanid
vlanid: (integer - minimum: 1 - maximum: 4094)
VLAN ID

diff_dic: (string)
diff_dic

Example:
Example

{
		"if_info1":	{
				"sys_type":	"",
				"vs_id":	"",
				"if_name":	"",
				"trunk_if_name":	"",
				"vlan_type":	"",
				"vlanid":	""
		},
		"if_info2":	{
				"sys_type":	"",
				"vs_id":	"",
				"if_name":	"",
				"trunk_if_name":	"",
				"vlan_type":	"",
				"vlanid":	""
		}
}

Response

HTTP status code 200

네트워크 연결 구성 삭제 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result
result: required (object)
결과 데이터

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"ok",
		"result":	{}
}

HTTP status code 400

잘못된 요청 (BAD	REQUEST)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12004",
		"message":	"다른	기능에서	사용	중입니다."
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t: (string)
dev_t
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

VS 정보

GET

/host/vs/info

VS 정보을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept: required (string)
Accept
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language
Accept-Language: (one of ko, en, ja - default: ko)
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VS 정보 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

result: required (object)
result

vs_use
vs_use: required (one of on, off)
VS 기능 사용

vs_info: required (object)
vs_info
idid: required (integer)
VS ID

type
type: required (one of HOST, VSI)
VS 타입

contents_mgmt
contents_mgmt: required (one of self, host)
컨텐츠 DB 관리 방식

Example:
Example

{
		"code":	"ok",
		"result":	{
				"vs_use":	1,
				"vs_info":	{
						"id":	1,
						"type":	"VSI",
						"contents_mgmt":	"self",
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

GB 표현 값으로 실제 lvm이 생성되는 bytes 값

GET

/host/vs/real-size/{si_size}

GB 표현 값으로 실제 lvm이 생성되는 bytes 값을(를) 조회한다.

Request

URI Parameters

si_size: required (number)
si_size
VSI 편집 시 설정한 log disk(GB 단위)

Example:
Example

1

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type
Content-Type: required (string)
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

GB 표현 값으로 실제 lvm이 생성되는 bytes 값 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result: required (object)
result
real_lvm_size
real_lvm_size: required (number)
GUI에 설정된 SI 표현 단위로 실제 생성될 lvm real size

Example:
Example

{
		"code":	"ok",
		"result":	{
				"real_lvm_size":	100663296
				}
		}
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

VS Administrator 관리 VS 설정을 위한 VS 리스트

GET

/host/vs/list-for-administrator

VS Administrator 관리 VS 설정을 위한 VS 리스트을(를) 조회한다.

Request

Headers

Authorization
Authorization: required (string)
API Token
로그인 후 응답에 포함된  api_token 을 설정

Example:
Example

fd9b3a9e18ac461cb036a8ea9053985e

Accept
Accept: required (string)
응답 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Content-Type: required (string)
Content-Type
요청 시 사용되는 MIME 타입은 JSON 형식이다.

Example:
Example

application/json

Accept-Language: (one of ko, en, ja - default: ko)
Accept-Language
언어 설정

필수가 아니며 요청 시 설정되지 않으면 로그인 시 사용된 언어로 설정됩니다.
한국어:	ko,	영어:	en,	일어:	ja

Example:
Example

ko

User-Agent
User-Agent: required (string)
User-Agent  HTTP 헤더
웹 브라우저에서는 자동으로 설정되지만 서버 프로그램에서는 필수로 입력되어야 한다.
인증 후 사용되는 모든 REST API를 호출할 때 필수 입력 사항이다.

Example:
Example

Mozilla/5.0	(Windows	NT	10.0;	Win64;	x64)	AppleWebKit/537.36	(KHTML,	like	Gecko)	Chrome/84.0.4147.105	Safari/537.36

Response

HTTP status code 200

VS Administrator 관리 VS 설정을 위한 VS 리스트 조회 성공

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code: required (string)
code
결과 코드

result
result: required (array of VS 정보)
VS 정보 목록

Items: VS 정보
Items

vs_id
vs_id: required (integer)
VS ID

Example:
Example

1

name
name: required (string - maxLength: 32)
VS 이름
영문,	숫자로	시작
영문,	숫자,	특수문자(-,	_)만	입력	가능   예약어(base_vsi,	base_vri_vwi,	vsi0,	physical,	ha)	사용	불가

Example:
Example

{
		"code":	"ok",
		"result":	[
				{
						"name":	"test1",
						"vs_id":	1
				},
				{
						"name":	"test2",
						"vs_id":	2
				}
		]
}

HTTP status code 401

인증 또는 인가되지 않음 (UNAUTHORIZED)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message: required (string)
message

결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"94011",
		"message":	"인증되지	않은	상태에서	API가	요청되었습니다."
}

{
		"code":	"94019",
		"message":	"인가되지	않은	API	요청입니다."
}

HTTP status code 404

리소스를 찾을 수 없음 (NOT	FOUND)

Body

Media type: application/json
Media type

TypeType: object

Properties
Properties
code
code: required (string)
결과 코드

message
message: required (string)
결과 메시지

dev_t
dev_t: (string)
개발용 메시지

Example:
Example

{
		"code":	"12001",
		"message":	"존재하지	않는	항목입니다."
}

